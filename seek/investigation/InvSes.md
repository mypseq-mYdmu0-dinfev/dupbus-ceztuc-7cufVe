[+working directories: `dupbus-ceztuc-7cufVe/` & `investigation/`]
Stop all tasks and heartbeat; retire SA —— this session ended
Address below in order (NOT parallel; don't stop until completion):
01. IMPORTANT: Before anything else (incl. reading ANYTHING; to preserve current context), scan your context window and identify all issues; generate an audit report as an `audit_` file
02. Enter investigation mode; read both root & `investigation/` CLAUDE.md + Unconditionals
03. Re-read all `seek` mandatory files; re-scan context window again (might be compacted after the reads) & re-identify issues; read & analyse this session's `rlog_` file; update the created `audit_`, simultaneously re-format it for compliance (e.g. numbered.md)
04. Read previous files in `investigation/` as needed (whenever needed from now on)
05. Read my observations & requests (if any; after `investigate session` OR Line 1 of msg)
06. Respond in `response_` for all above
07. If no critical qb, #replace in `changes_`
08. Independently review the created `changes_` & challenge yourself (e.g. Are the issues new/old? If old, was a fix attempted before? If yes, why didn't it work? How could you be confident this time it'll work?) —— Soft Suggestion: Spawn an SA for multi-perspective or if it helps
09. Debate w/ yourself (or the SA); don't stop until attaining highly compelling, rigorous, viable solutions for all issues w/o gaps
10. Update the created `changes_` as needed (MUST be 100% what you're about to execute)
11. Directly execute the changes (override `investigation/CLAUDE.md` "approval first")
12. Update the created `response_` to concisely persuade me the issues were permanently fixed

---

Note:
- Reminder: Ensure declarations are 100% correct & all protocols are 100% complied
- IMPORTANT: If compaction occurred during this turn, immediately re-read all mandatory files & any files created in this turn to ensure context fully recovered
- If any critical qb (from "07", etc.; if necessary), stop & create a random `Schedule` (which should prompt a notification via OTGC) to alert me; I'll then simply deny that and answer you
- FYI: This session is likely a fork of an AJAP session, where the AJAP could be still running