# Changes —— `changes_202606271704`
*Harden S3.2: make LinkedIn (S3.2.2) & Glassdoor (S3.2.4) MANDATORY whenever S3.2 runs. UPDATED per `query_202606271721` (Change 01–04 feedback): the mandatory declaration + invalid-site rule moved OUT of the S3.2 header (kept short) into S3.2 Notes as concise points; per-line "mandatory" folded into the EXISTING trailing bracket (no new bracket); the login-out clause is a SUB-bullet (not a dash-extension). Baseline (Replace) = the `changes_202606271643` state. `#replace`, verbatim snippets. Gate preserved ("mandatory" = whenever S3.2 executes).*

*Note: the S3.2 HEADER got no net change (the long paragraph I first added was reverted to the original one-line header) —— so it no longer appears as a change here.*

---

## Change 01 —— `seek/context/ajap.md` S3.2.2 (LinkedIn —— fold "mandatory" into existing bracket; login → Notes)
**Replace:**
```
- S3.2.2. LinkedIn —— in Tab 4+: a logged-in account is available (credentials in `seek/.claude/aux_login.local.md`).
```
**With:**
```
- S3.2.2. LinkedIn —— in Tab 4+: a logged-in account is available (credentials in `seek/.claude/aux_login.local.md`). `navigate` to `https://www.google.com/search?q=[company_name]+site%3Alinkedin.com%2Fcompany` → `find` the LinkedIn company page result → `click` it → if the page loads logged-in, read it; if a login wall/overlay appears (logged out), re-login per S3.2 Notes, then proceed. (Mandatory source —— never skip; old Google-referrer bypass retired, you have an account now.)
```

## Change 02 —— `seek/context/ajap.md` S3.2.4 (Glassdoor —— same treatment)
**Replace:**
```
- S3.2.4. Glassdoor —— in Tab 4+: same logged-in account as LinkedIn (credentials in `seek/.claude/aux_login.local.md`).
```
**With:**
```
- S3.2.4. Glassdoor —— in Tab 4+: same logged-in account as LinkedIn (credentials in `seek/.claude/aux_login.local.md`). `navigate` to `https://www.google.com/search?q=[company_name]+site%3Aglassdoor.com.au` → `find` the Glassdoor result → `click` it → if a login wall appears (logged out), re-login per S3.2 Notes; for a human-verification/captcha blocker, handle per S3.2 Notes. (Mandatory source —— never skip; old Google-referrer bypass retired.)
```

## Change 03 —— `seek/context/ajap.md` S3.2 Notes (mandatory + invalid-site points; login-out as sub-bullet)
**Replace:**
```
S3.2 Notes:
- For blockers, attempt up to twice before moving on
  - e.g. Glassdoor shows "Humans only" → screenshot → click the square box (next to "Verify you are human" below "...for troubleshooting steps.")
  - Logic: me = human; you = my representative; hence, you = human in this situation
- If < 5 useful sources, expand within the 10-site cap per job
- Close all research tabs before S4
```
**With:**
```
S3.2 Notes:
- Mandatory sources: S3.2.1 (Official), S3.2.2 (LinkedIn), and S3.2.4 (Glassdoor) MUST all be visited whenever S3.2 runs; never skip. S3.2.3/S3.2.5 stay conditional.
- A site attempted but genuinely invalid/empty/non-existent for this employer (no official site, no LinkedIn company page, or no Glassdoor page) does NOT count toward the max-10 cap —— note its absence in the AR.
- For blockers, attempt up to twice before moving on
  - A logged-out state is NEVER a blocker for the mandatory LinkedIn/Glassdoor sources: re-login (`seek/.claude/aux_login.local.md`) and continue; never "move on" for a login reason. Only an unresolvable captcha after two attempts permits moving on, and it MUST be noted in the AR.
  - e.g. Glassdoor shows "Humans only" → screenshot → click the square box (next to "Verify you are human" below "...for troubleshooting steps.")
  - Logic: me = human; you = my representative; hence, you = human in this situation
- If < 5 useful sources, expand within the 10-site cap per job
- Close all research tabs before S4
```

---

## Self-review
1. Header kept short (reverted the paragraph); the two new rules (mandatory set + invalid-site-doesn't-count) live as concise Notes points, per Change 01.
2. Per-line marker folded into the EXISTING trailing bracket on S3.2.2/S3.2.4 —— no extra bracket (Change 02/03).
3. Login-out clause is now a sub-bullet under "For blockers…", not a dash-extension (Change 04).
4. Invalid-site rule added per request (attempted-but-empty sites don't count toward the max-10).
5. ⚠️ Open blocker (B1, see `response_202606271721`): S3.2.2/S3.2.4 still imply AJAP re-logs-in by typing credentials, which conflicts with `universal/cic_bot.md § ladder step 5` ("CC must NOT type a password —— harness-prohibited even for a throwaway/authorised account"). The login text now points to "S3.2 Notes" rather than spelling out the typing —— pending your ruling on whether Sonnet AJAP may actually type.
6. Revertable: 3 verbatim snippets; swap each **With** → **Replace**.
