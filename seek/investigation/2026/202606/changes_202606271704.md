# Changes —— `changes_202606271704`
*Harden S3.2: make LinkedIn (S3.2.2) & Glassdoor (S3.2.4) MANDATORY research sources whenever S3.2 runs (the login obstacle that previously excused skipping is gone). Needed edit —— previously they were listed but skippable ("attempt up to twice before moving on" + "if <5 sources, expand"). `#replace` format, verbatim snippets. The S3.2 Research Gate (only runs for ceiling ≥50 / score ≥70 jobs) is preserved —— "mandatory" means "whenever S3.2 executes".*

---

## Change 01 —— `seek/context/ajap.md` S3.2 intro (declare the mandatory set)
**Replace:**
```
S3.2. CIC site visits (Tab 4⁺ ONLY; NEVER use Tab 3 for research; read then close each; max 10 **websites** in total per job, NOT webpages):
```
**With:**
```
S3.2. CIC site visits (Tab 4⁺ ONLY; NEVER use Tab 3 for research; read then close each; max 10 **websites** in total per job, NOT webpages). **MANDATORY for every job that reaches S3.2: S3.2.1 (Official), S3.2.2 (LinkedIn), and S3.2.4 (Glassdoor) —— all three MUST be visited and read; they are NOT skippable (the login obstacle that previously excused skipping LinkedIn/Glassdoor is removed —— see S3.2.2/S3.2.4). S3.2.3/S3.2.5 remain conditional.** If a mandatory source genuinely has no page for this employer (e.g. no LinkedIn company page exists), note that in the AR rather than silently omitting it.
```

## Change 02 —— `seek/context/ajap.md` S3.2.2 (LinkedIn → MANDATORY)
**Replace:**
```
- S3.2.2. LinkedIn —— in Tab 4+: a logged-in account is available (credentials in `seek/.claude/aux_login.local.md`).
```
**With:**
```
- S3.2.2. LinkedIn (**MANDATORY —— never skip; being logged out is NOT a reason to skip: re-login and continue**) —— in Tab 4+: a logged-in account is available (credentials in `seek/.claude/aux_login.local.md`).
```

## Change 03 —— `seek/context/ajap.md` S3.2.4 (Glassdoor → MANDATORY)
**Replace:**
```
- S3.2.4. Glassdoor —— in Tab 4+: same logged-in account as LinkedIn (credentials in `seek/.claude/aux_login.local.md`).
```
**With:**
```
- S3.2.4. Glassdoor (**MANDATORY —— never skip; being logged out is NOT a reason to skip: re-login and continue**) —— in Tab 4+: same logged-in account as LinkedIn (credentials in `seek/.claude/aux_login.local.md`).
```

## Change 04 —— `seek/context/ajap.md` S3.2 Notes (login-out ≠ blocker)
**Replace:**
```
S3.2 Notes:
- For blockers, attempt up to twice before moving on
  - e.g. Glassdoor shows "Humans only" → screenshot → click the square box (next to "Verify you are human" below "...for troubleshooting steps.")
```
**With:**
```
S3.2 Notes:
- For blockers, attempt up to twice before moving on —— BUT a logged-out state is NEVER a blocker for the mandatory LinkedIn/Glassdoor sources: re-login (credentials in `seek/.claude/aux_login.local.md`) and continue; never "move on" from them for a login reason. Only a genuine unresolvable captcha after two attempts permits moving on, and it MUST be noted in the AR.
  - e.g. Glassdoor shows "Humans only" → screenshot → click the square box (next to "Verify you are human" below "...for troubleshooting steps.")
```

---

## Self-review
1. Was hardening needed? Yes —— previously LinkedIn/Glassdoor were listed sources but the Notes explicitly allowed skipping ("attempt up to twice before moving on"; "if <5 useful sources, expand"), so neither was guaranteed. Now both are explicitly MANDATORY.
2. Gate preserved: "mandatory" is scoped to "every job that reaches S3.2". Low-score jobs (ceiling <50) and M7=0 external early-exits still skip S3.2 entirely —— unchanged, so no wasted research on jobs that will be skipped anyway.
3. Login handling: logged-out is explicitly NOT a skip reason (re-login, per the filled `aux_login.local.md`); only an unresolvable captcha after 2 tries permits moving on, and must be logged in the AR (so a genuine miss is visible, never silent).
4. Edge case covered: if an employer truly has no LinkedIn/Glassdoor page, note it in the AR (don't silently omit) —— keeps "mandatory" honest without forcing a non-existent page.
5. Revertable: 4 verbatim snippets in `ajap.md`; swap each **With** → **Replace**.
