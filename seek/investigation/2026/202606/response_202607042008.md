# Response to query_202607042008.md
*Root cause found & fixed —— you did NOT break anything; detail in `changes_202607042016`*

## Verdict

1. Two DISTINCT causes, not one —— and neither was your last edit.
2. Of the 2 alarming sessions, ONE had ZERO real violations (pure false positives); the other had ONE real slip.

## Cause 1 —— false positives (the "3 violations over 3 files" session)

3. `HendersonAdvocacy` and `LeisuresShoeBoutique` CLs are CLEAN —— correct P.S., no dashes.
4. The flag was a latent bug in `cl_check.py` (and `ajap_logs.py`): both scan from the `## 6.` marker to END OF FILE.
- 4.1. But applied ARs legitimately have a `## 7. Employer Questions` section AFTER the CL.
- 4.2. So the "file must end with the exact P.S. line" check hit a Q&A answer as the last line → false P.S. violation; a dash in a Q&A answer would false-flag too.
5. It surfaced NOW (not your edit) because the recent runs applied to question-heavy design/marketing roles (Loom-video etc.) that carry a `## 7`. The bug has sat latent since `cl_check.py` was created (~2026-06-09); my recent edits (C2 fix, rlog cuts, the CL-gate dedup) never touched the P.S./scan logic.

## Cause 2 —— a REAL slip (the "3 violations over 1 file" session)

6. `PendulumCommunications` is genuinely compromised: em-dash x2 in the CL, a wrong custom P.S. ("...portfolio available at..."), and a rogue opening (neither template).
7. The rlog (SA-scanned) shows the CL gate NEVER RAN for it: no `cl_check.py exit 0` line; the em-dash was noticed only POST-submission ("already submitted; no action possible"); the SA was at its 1hr hard-retirement at that moment.
8. So Pendulum is a gate-SKIP —— the degrading/retiring SA submitted without running `cl_check.py`, and MA did not gate it. The rlog shows the same pattern elsewhere (AlderRoad submitted without the approval gate).

## The fix (executed + tested)

9. Bounded the CL scan to its SECTION only (`## 6.` marker → next `## ` heading / EOF) in BOTH `cl_check.py` (the live gate) and `ajap_logs.py` (your analytics).
10. Result on the recent run: 3 flags → 1. Henderson & Leisures now pass; Pendulum stays `❌` for its real `## 6` issues. No real violation is missed (a dash / bad P.S. inside the CL is still caught).

## Why this also helps Cause 2

11. A gate that cries wolf gets ignored. The P.S. false-positive trained the agents to distrust `cl_check.py`, which plausibly fed the real gate-skips. A trustworthy gate is the first step.
12. The systemic fix for gate-skips is the one from last turn: MA detects SA1 compaction/degradation signs → RUTHLESSLY retire before it can submit; and never approve a submit without a fresh `cl_check.py` pass. I recommend we make that the next change (it's a `main_ajap` edit, separate from this code fix).

## Note

13. I also fixed `ajap_logs.py`, so your future violation counts stop inflating —— re-run it and Session A's "3 violations" should drop to 0.
