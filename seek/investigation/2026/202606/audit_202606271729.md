# Audit —— AJAP context trim opportunities (`audit_202606271729`)
*Per `query_202606271721 Task 2`. ANALYSIS ONLY —— nothing changed, no `changes_` created. Token baseline (via `token-count`): `ajap.md` 10,696 · `main_ajap.md` 6,855 · `MA_hb.md` 2,518 · (`gcl.md` 2,083 · `SA2_hb.md` 1,067). The 3 flagged files ≈ 20.1k tokens.*

---

## 1. The key lever
1.1. `MA_hb.md` is the best ROI: it is re-read on EVERY heartbeat AND at every card's S0.1, so its tokens are paid *repeatedly* —— trimming it is the only change that reduces *recurring* churn (plausibly the main driver of the frequent-compaction you reported), not just the one-time session-start load.
1.2. A Sonnet SA does NOT read `main_ajap.md` (spawn rule: "items 3–8 only"). So for the SA the heavy files are `ajap.md` + `MA_hb.md`; `main_ajap.md` weighs only on the MA.

## 2. Cross-file duplication (same rule written in full in 2–3 files)
2.1. **Heartbeat mechanism** (300s-fixed primary, transient 60s, never-churn, finite-loop-banned) is stated nearly verbatim in `main_ajap.md` AND `MA_hb.md` (§ Fast-confirm overlay). Canonical → `main_ajap.md`; `MA_hb.md` keeps only the per-wake checklist + a 1-line pointer. **~450–550 tok (recurring).**
2.2. **Silent-compaction / `ma_full_reread_marker` rationale** is spelled out ~5× (`SA2_hb.md`, `main_ajap.md` ×3, `MA_hb.md` ×2). State once in `SA2_hb.md`; elsewhere "→ see `SA2_hb.md`". **~200–300 tok.**
2.3. **MA Post-Compaction Recovery** written in full 3× (`main_ajap.md`, `MA_hb.md`, `seek/CLAUDE.md`). Shrink `MA_hb.md`'s every-wake copy to trigger + pointer. **~250–350 tok (recurring).** (Verify the triple-statement isn't deliberate compaction-survival redundancy —— but the every-wake copy is the safe one to shrink, since `seek/CLAUDE.md` already loads a compaction-first copy.)
2.4. **Watchdog reactions** (4 alert types) listed in full 3× (`SA2_hb.md`, `main_ajap.md`, `MA_hb.md`). Canonical → `SA2_hb.md`. **~200–260 tok.**
2.5. **Broad file check + batch tripwire** near-verbatim in `MA_hb.md` + `main_ajap.md` ×2. Canonical → `MA_hb.md`. **~150–200 tok.**
2.6. **C2 "this-session-only, never cumulative, number-emojis"** restated 6×. One full statement per agent's file suffices. **~100–150 tok.**

## 3. Within-file repetition
3.1. `ajap.md` **External-Portal-Exit / "Quick-apply never skipped for low score"** stated 4× (L211 tail, scoring-bands exception, S4 prose). Keep one. **~120–160 tok.**
3.2. `ajap.md § S3.2` LinkedIn/Glassdoor "mandatory + re-login + credentials-path" repetition —— the `aux_login.local.md` path alone appeared 4×. (Partly REDUCED this turn by the Change 01–04 revision; further consolidation possible.) **~120–180 tok remaining.**
3.3. `ajap.md` **`⏳_` lifecycle** (Applying→Applied, remove prefix on success) stated 4× (Filename, S5 CRITICAL, S6.4.4, Outcome line). Keep the CRITICAL block. **~100–140 tok.**
3.4. `main_ajap.md` **CL-check recipe** (`cl_check.py` then `grep -n "^## 6\." → Read offset limit 80`) stated 3× (Between-Loop, Heartbeat 4.1, Session-Start 8.1). Define once. **~140–180 tok.**
3.5. `main_ajap.md` **"run_in_background=True"** warning 4×. One full warning, rest inline. **~50–70 tok.**

## 4. Low-value content (cut candidates, ~zero behavioural risk)
4.1. **Inline `rlog 202606…` citations** (≥12 across `ajap.md` + `main_ajap.md`) —— historical provenance, not runtime instructions; the rules stand without them. **~150–200 tok.**
4.2. **"Formerly …" / file-rename breadcrumbs** (`MA_hb.md` L1, `SA2_hb.md`, `ajap.md` L3). **~25–40 tok.**
4.3. `ajap.md` S3.2 Notes **"Logic: me = human…" justification** (the captcha *action* stays; the philosophy goes). **~30–40 tok.**
4.4. `main_ajap.md § Opus-Mode Fallback` rationale prose → shrink to one clause (rules stay). **~70–100 tok.**

## 5. ⚠️ Not a trim —— a CONTRADICTION to fix
5.1. The SA poll-retry count differs between files: `ajap.md` says **60×/900s**, `main_ajap.md § SA Spawn Instruction` says **20×/300s**. The SA follows the spawn prompt at spawn but `ajap.md` during the loop —— an active inconsistency. Reconcile to one value (this also unlocks deduping the polling protocol, which is currently embedded in both).

## 6. Estimate & priority
| File | tok | trimmable | % |
|---|---|---|---|
| `ajap.md` | 10,696 | ~900–1,200 | ~9–11% |
| `main_ajap.md` | 6,855 | ~1,000–1,400 | ~15–20% |
| `MA_hb.md` | 2,518 | ~700–900 | ~28–36% |
| **Total** | **20,069** | **~2,600–3,500** | **~13–17%** |

6.1. Top 5 (high value, low risk): (a) collapse `MA_hb.md` heartbeat-mechanism → pointer [2.1]; (b) state silent-compaction rationale once [2.2]; (c) shrink `MA_hb.md` recovery copy [2.3]; (d) delete inline rlog citations [4.1]; (e) consolidate the `⏳_` lifecycle [3.3]. The `MA_hb.md` items (a/c) matter most —— recurring cost.

## 7. Guardrails (do NOT cut —— load-bearing)
7.1. The canonical Monitor bash commands (`main_ajap.md`, `SA2_hb.md`), the Pre-Flight state table + K1–K6, the Q&A lookup table, and `gcl.md` templates/openings. These are runtime data or exact-spawn text, not redundancy.

## 8. Status
8.1. Per your instruction, NOTHING was changed and no `changes_` created. When you want to proceed, the safe order is: fix the §5 contradiction first, then apply 2.x dedups (canonical + pointers) one file at a time with a re-read sanity check, leaving §7 untouched. A trimming pass of ~2.6–3.5k tokens (~13–17%) is realistic without losing any operative rule.
