# Query 202606262114

the two `archive/` subfolders in `applied/` and `skipped/` are getting too many files
opening slows my mac considerably
so i wanna sort them by myself into months, just like the `sessions/`
i.e. `202606/`, `202605/`, etc. in each `archive/`
as said, i'll sort these archived ARs by myself
but if this means anything needa be changed (e.g. ajap.md? not sure) in order to work normally, do it for me, then summarise what changed
QMM
