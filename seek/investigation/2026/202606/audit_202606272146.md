# Audit —— risky/judgment AJAP trims (`audit_202606272146`)
*Replaces the voided `❌_audit_202606272057`. These are the items that genuinely HOLD RISK and need your second thoughts —— NOT executed. The practically-risk-free trims were already done (`changes_202606272057` + `changes_202606272146`). References `audit_202606271729` for per-item detail. Per `numbered.md`.*

## The core trade-off (read first)

1. The biggest RECURRING-cost lever is `MA_hb.md` —— it is re-read IN FULL on every wake, so its tokens are paid over and over (the real driver of mid-run compaction).
2. But `MA_hb.md` is also the every-wake SOURCE OF TRUTH —— MA acts from it each wake.
3. So trimming `MA_hb.md` by pointing its content to another file trades recurring savings for a RECALL risk: between compactions, the pointed-to file (e.g. `main_ajap.md`) may drift out of attention whilst `MA_hb.md` stays fresh.
4. Net: the cheap-to-trim files (`main_ajap.md`, read once at start) barely help the recurring cost; the file that WOULD help (`MA_hb.md`) is the one trimming endangers. That tension is why these are judgment calls, not free cuts.

## Item A —— main_ajap "On every wake" ↔ MA_hb overlap (`audit pt 3/7`)

5. `main_ajap.md § Heartbeat` "On every wake" largely duplicates `MA_hb.md`'s checklist (~250 tok).
6. Two ways, both flawed:
- 6.1. Pointer `main_ajap → MA_hb`: loses main_ajap's UNIQUE bits (read `ma_state` for `sa_id`; the 4.3 "submit-then-proceed across multiple heartbeats → close Tab 3").
- 6.2. Merge those bits INTO `MA_hb.md` first: raises the RECURRING (every-wake) cost —— counterproductive.
7. My recommendation: LEAVE it (neither option is a clean win).

## Item B —— Watchdog reactions stated 3× (`audit pt 6`)

8. The 4 reactions live in `SA2_hb.md` (canonical), `MA_hb.md` L66, and `main_ajap.md` L155 (~200 tok).
9. `MA_hb.md`'s copy is every-wake-critical (keep). `main_ajap.md`'s (read once) could become a pointer to `SA2_hb.md`.
10. My recommendation: EXECUTABLE on greenlight —— trim ONLY `main_ajap.md`'s copy to a pointer (~80 tok off the start-load; `MA_hb.md` untouched). Low risk.

## Item C —— Defensive repetitions that GUARD known bugs (`audit pt 9/11/13`)

11. These LOOK trimmable but each restatement defends a logged failure —— trimming them risks MORE bugs (against your "fewer bugs" goal).
- 11.1. `run_in_background=True` ×4 —— "the single most dangerous misconfiguration".
- 11.2. External-portal-exit / Quick-apply-never-skipped ×4 —— a "recurring SA error".
- 11.3. `⏳_` lifecycle ×4 —— Applying→Applied; a miss = mis-routed/duplicate AR.
12. My recommendation: KEEP all —— the tokens buy bug-prevention. Trim only if you judge the saving worth the regression risk.

## Item D —— Post-Compaction Recovery stated 3× (`audit pt 5`)

13. `seek/CLAUDE.md` (loads first) + `main_ajap.md` (full, 8 steps) + `MA_hb.md` (condensed, every-wake).
14. `MA_hb.md`'s copy is the EVERY-WAKE compaction-catch —— the thing many recent turns hardened.
15. My recommendation: KEEP —— pointer-izing it risks the silent-compaction catch we built `ma_full_reread_marker` for.

## Item E —— C2-line format INCONSISTENCY (a bug, not a trim)

16. The C2 line is written 3× and they DISAGREE:
- 16.1. `MA_hb.md` L28 (re-read trigger): `**[current_TS]** 🎯[N] job(s) processed.`
- 16.2. `MA_hb.md` L67 + `main_ajap.md` L80: `[current_TS] 🎯[N] **job(s) processed so far.**`
17. Actual runs use the L67 form (`[current_TS] 🎯[N] **job(s) processed so far.**`).
18. Risk: L28's wrong template could mis-fire the "last C2 didn't match → re-read ajap.md" trigger.
19. My recommendation: FIX L28 to the L67 canonical (confirm the canonical first) —— I can do it on your word.

## Item F —— Heartbeat mechanism intra-`MA_hb` (`audit pt 3`)

20. `MA_hb.md` L57 (mechanism summary) overlaps its own L70–75 (Fast-confirm overlay) —— ~50 RECURRING tok.
21. My recommendation: BORDERLINE —— trimming L57 to a 1-line pointer saves recurring cost but removes the quick-reference; do only if you want maximum leanness in the every-wake file.

## Summary

22. Safe to execute on your greenlight (low risk): Item B (~80 tok), Item E (a fix, fewer bugs), Item F (~50 recurring tok).
23. Recommend KEEP (token saving not worth the bug/recall risk): Items A, C, D.
24. Remaining theoretically-trimmable ≈ 1.5–2k tok, but almost all of it sits in Items A/C/D —— i.e. it is NOT practically risk-free, which is why it is here and not already done.
