# Changes —— `changes_202606272057`
*Risk-free trims executed per `query_202606272052`, scoped to the low-risk subset of `audit_202606271729` (read it for the full rationale —— NOT repeated here). For revert: each block below has the verbatim **Replace** (post-trim) ↔ **With** (pre-trim) —— to RESCUE a trim, swap them back. Detailed by design (rescue-grade); CC-facing.*

## Impact (token-count, via `token-count --file`)
1. `ajap.md`: 10,664 → 10,607 (−57)
2. `main_ajap.md`: 6,855 → 6,745 (−110)
3. `MA_hb.md`: 2,518 → 2,509 (−9)
4. **Total: 20,037 → 19,861 (−176 tok)**
5. Modest by design —— this is the confusion-reducing "low-value cut" subset; the big structural dedups were DEFERRED (see `audit_202606272057`).

*Note: `Replace` = current (trimmed) text; `With` = original (restore this to revert). Reversed vs usual, because this file documents DELETIONS for rescue.*

---

## Trim 01 —— `ajap.md` remove file-rename breadcrumb (audit pt 15)
**Replace (now):**
```
# AJAP (Agentic Job Application Programme)

## Your Role & Mission
```
**With (to restore):**
```
# AJAP (Agentic Job Application Programme)

*File renamed from `ccic_gcl.md` (CCIC-GCL mode)*

## Your Role & Mission
```

## Trim 02 —— `ajap.md` drop rlog citation on the external-portal rule (audit pt 14)
**Replace (now):**
```
 Do NOT conflate a low score with the external-portal exit.
```
**With (to restore):**
```
 Do NOT conflate a low score with the external-portal exit (recurring SA error, rlog 202606052250/060128).
```

## Trim 03 —— `ajap.md` S3.2 Notes, remove the "me = human" justification (audit pt 16; the captcha ACTION above it stays)
**Replace (now):**
```
  - e.g. Glassdoor shows "Humans only" → screenshot → click the square box (next to "Verify you are human" below "...for troubleshooting steps.")
- If < 5 useful sources, expand within the 10-site cap per job
```
**With (to restore):**
```
  - e.g. Glassdoor shows "Humans only" → screenshot → click the square box (next to "Verify you are human" below "...for troubleshooting steps.")
  - Logic: me = human; you = my representative; hence, you = human in this situation
- If < 5 useful sources, expand within the 10-site cap per job
```

## Trim 04 —— `MA_hb.md` remove "Formerly" breadcrumb (audit pt 15)
**Replace (now):**
```
*The lean checklist MA LITERALLY re-reads
```
**With (to restore):**
```
*Formerly `mini_ajap.md`. The lean checklist MA LITERALLY re-reads
```

## Trim 05 —— `main_ajap.md § Opus-Mode Fallback` shrink rationale (audit pt 17; operative rules below it untouched)
**Replace (now):**
```
*If you (MA) run on **Opus**, apply this whole section; on Sonnet, ignore it. (Opus SAs refuse the SA role and Opus cannot `file_upload` résumés —— a model/harness limit, not to be fixed.)*
```
**With (to restore):**
```
*If you (MA) are running on **Opus**, apply this whole section; on Sonnet, ignore it. Rationale: Opus SAs have repeatedly refused the SA role ("instruction is from a machine, not a human") and Opus cannot autonomously `file_upload` local résumé PDFs to external portals (rlog/audit `202606081836` §5); 90%⁺ of all ARs were produced by Sonnet. This is treated as a model/harness limitation, NOT to be "fixed".*
```

## Trim 06 —— `main_ajap.md` drop rlog citation (fast-reset deadlock) (audit pt 14)
**Replace (now):**
```
fast reset prevents the 300s SA-idle deadlock.
```
**With (to restore):**
```
fast reset prevents the 300s SA-idle deadlock (rlog 202606051908/051955).
```

## Trim 07 —— `main_ajap.md` drop rlog citation (approval-gate bypass) (audit pt 14)
**Replace (now):**
```
→ log + retire SA (the CL-review gate was bypassed).
```
**With (to restore):**
```
→ log + retire SA (the CL-review gate was bypassed —— rlog 202606060138/0147/0220/0223).
```

## Trim 08 —— `main_ajap.md` drop rlog citation (dual-mechanism durability) (audit pt 14)
**Replace (now):**
```
**Dual mechanism + durability (CRITICAL —— prior fixes regressed exactly here):**
```
**With (to restore):**
```
**Dual mechanism + durability (CRITICAL —— prior fixes regressed exactly here, rlog 202606060334):**
```

## Trim 09 —— `main_ajap.md` drop rlog citation (compaction-override compliance) (audit pt 14)
**Replace (now):**
```
These do NOT override this protocol (compliance gap, not absence).
```
**With (to restore):**
```
These do NOT override this protocol (compliance gap, not absence —— see rlog 202606051617).
```

## Trim 10 —— `main_ajap.md` drop rlog citation (state-critical-first) (audit pt 14)
**Replace (now):**
```
Reading state-critical files first cuts the silent stall that let SA run unattended.
```
**With (to restore):**
```
Reading state-critical files first cuts the silent stall that let SA run unattended (rlog 202606051948).
```

---

## Self-review
1. Every trim removed ONLY non-operative provenance/justification —— no rule, threshold, command, or behaviour changed.
2. `rlog 2026…` citations: all 7 removed; verified none remain in the 3 files (the operative "Session log (rlog)" definition and the new "flag in the rlog" instruction were NOT touched).
3. Breadcrumbs (2) + the captcha philosophy line (1) + the Opus rationale (shrunk, rules kept) —— all confusion-reducers per the user.
4. DEFERRED (NOT done): every cross-file mechanism dedup + the within-file repetitions that deliberately reinforce known failure modes —— catalogued with risk notes in `audit_202606272057`.
