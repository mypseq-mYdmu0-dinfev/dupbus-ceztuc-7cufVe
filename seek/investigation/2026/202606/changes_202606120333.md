# Changes —— `changes_202606120333`
*Fix: `cl_check.py` was created to serve SA1/SA3/MA but last turn only got wired into the Opus-solo path. This wires it into the EXISTING (Sonnet) live flow —— SA self-gate + all three MA Submit-gates. Executed per explicit "otherwise, fix it". SA3 hooks deferred (SA3 does not exist yet —— lands with the §5–§7 redesign).*

---

## Change 01 —— `ajap.md` S4.7 (SA deterministic self-gate)
Added a final bullet after "Do NOT proceed to S5…": after the AR exists (S5), before reporting `Applying` (S6.4.2.5), SA runs `python3 /seek/context/cl_check.py [AR_PATH]`; HARD violation (exit 1) → fix in-place + re-run until exit 0; soft `⚠️` advisory. Backstop to the existing manual S4.7 checks, not a replacement.

## Change 02 —— `main_ajap.md § Between-Loop Audit`, Applying step 1 (MA primary gate)
Prepended a deterministic first pass: `python3 /seek/context/cl_check.py [AR_PATH]` (exit 1 → compromised → step 3). The manual `## 6.` read is retained but re-scoped to the SUBJECTIVE checks the script cannot make (banned words, false/overclaimed content).

## Change 03 —— `main_ajap.md § Session Start` 8.1 (MA mid-application resume gate)
Same pattern: run `cl_check.py` first (exit 1 → 8.1.2 void path), then read `## 6.` for subjective content only.

## Change 04 —— `main_ajap.md § Heartbeat` 4.1 (MA heartbeat approval gate)
Same pattern: `cl_check.py` + subjective read; approve only if exit 0 AND read clean; compromised if exit 1 OR read flags.

---

## Self-review
1. Coverage —— the tool now fires at every Submit chokepoint that exists today: SA before declaring `Applying` (Change 01), and all three MA approval points (Changes 02–04: between-loop, pre-flight resume, heartbeat). No live Submit path now reaches "approve" without a deterministic pass.
- 1.1. SA3 (auditor) would be the natural fourth caller, but it does not exist yet —— wiring it now would be dead reference. It lands with the §5–§7 redesign (`changes_202606091942` "NOT executed" note).
2. Non-destructive —— every edit ADDS the script as a first/extra pass and KEEPS the manual read for subjective judgement (false claims, salary/notice, overclaim). No check was removed, so nothing the manual flow caught is lost.
3. Sequencing —— the SA gate is correctly placed AFTER S5 (the AR file must exist for a file-based linter); the forward-reference to S6.4.2.5 is explicit.
4. Soft vs hard —— only HARD (exit 1) blocks; soft `⚠️` (colon-pillar, slop-words that may be company names) is advisory, so a firm named "Synergy…" will not wrongly block a Submit.
5. Opus path —— already references the script (`§ Opus-Mode Fallback`); unchanged.
