# Response to query_202606050715.md
*Qi order changed to all-n-then-all-p (executed)*

1. Done —— new order `01n → 02n → 03n → 01p → 02p → 03p` (all New-to-you passes first, then all plain passes), replacing the old `01n → 01p → 02n …` (n-then-p per Qi).
- 1.1. Two files edited; no `changes_` gate used since you said execute directly.

2. `queue.md` (core logic) —— two edits:
- 2.1. `## order:` note: now states the sequence applies within each of two passes (all `n` first, then all `p`), with the explicit `01n → 02n → 03n → 01p → 02p → 03p` example.
- 2.2. Processing Rules pt 1: rewritten as TWO ROUNDS —— Round 1 = every `[Qi]n` in `order:` sequence (e.g. `01n → 02n → 03n`); Round 2 = every `[Qi]p` in `order:` sequence (e.g. `01p → 02p → 03p`); rule 1.3 now advances within a round by `order:`, crossing to Round 2 only after all `n` passes are exhausted.

3. `context/ajap.md` (references) —— two edits:
- 3.1. A6.1: rewritten to the two-round description with the `01n → 02n → 03n → 01p → 02p → 03p` example.
- 3.2. S7.2.2: "`n` then `p` per Qi" → "ALL `n` passes then ALL `p` passes".

4. Unaffected (checked, no change needed):
- 4.1. `main_ajap.md` and `mini_ajap.md` reference only `queue.md` rule 2 (New-to-you enforcement), not the n/p ordering.
- 4.2. `queue.md` rule 2, rule 3, and the All-Qi-Exhausted Edge Case are order-agnostic; A6.3's "every Qi (n+p) exhausted" still holds.

5. Note: the `order:` line itself (`order: 01, 02, 03`) is unchanged —— you still reprioritise Qi by editing only that line; the round structure applies on top of whatever sequence it holds.
