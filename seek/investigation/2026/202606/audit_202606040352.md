# Audit Report —— AJAP Long Session (202605310518 `~`202606040352)
*First pass: context-window scan. Second pass (post re-read) appended at §8.*

## 1. Scope & Method

- 1.1. Subject: the long AJAP session spanning multiple continued runs from session-start `202605310518` through the final monitoring ticks before `investigation mode`.
- 1.2. Method (first pass): scan of the current context window (no `rchat_` available, per user) + spot-check of `gcl/` ARs not in `applied_archive/` or `skipped_archive/`.
- 1.3. Compaction status: no `🚨` post-compact sentinel observed in the retained context; the session appears un-compacted at scan time, but §8 re-verifies after a mandatory re-read.
- 1.4. Severity key: 🔴 high (truthfulness / submitted-and-wrong) · 🟠 medium (compliance / discipline) · 🟡 low (doc/process).

## 2. 🔴 Cover-Letter Compliance Deviations

- 2.1. **Systemic colon-led pillar format.** The CLs used colon-headed pillar paragraphs ("Strategic Transformation:", "Value Engineering:", "Stakeholder Management:" / Template [1] "Problem Solving:" etc.), introducing colons beyond the 6 sanctioned template colons that `gcl.md` + `cc_writing.md` permit. The template names the three strengths INLINE in one sentence, not as headers.
- 2.2. **Omitted template blocks.** The same CLs dropped the standard "Throughout my 16⁺ years… [B2B/C, NPO, Govt; org scales; clients by country; industries + Projects URL]" breadth block and the standard "Thank you for considering… I look forward to discussing…" closing.
- 2.3. **Confirmed scope:** the 5 late applications —— Peninsula (1813), AAEH (1858), CityRecitalHall (1916), ENA (1918), HungryPanda (1922).
- 2.4. **⚠️ Unverified scope:** whether the earlier `~`50 applications (TS `202606010007`–`202606011758`) share the same deviation is NOT established from context; requires spot-check (flagged in §7).
- 2.5. **AAEH faculty-name slip.** CL referenced "Human Computer Interaction" though the IxD resume was used; `gcl.md` directs IxD/Marketing-resume CLs to say "Interaction Design".
- 2.6. **HungryPanda tool overclaim.** CL named "Canva and CapCut" specifically —— not in `pro_profile.md` (Adobe CC / DaVinci / Figma are); softened by "such as" and trivially learnable, but not profile-grounded.

## 3. 🔴 Pre-Defined Q&A Errors (AAEH, submitted, unfixable)

- 3.1. **Salary**: answered `$100k`. Rule: suitability 59 (<85) → ~$75,000 target → nearest available ≤ target = **$70k**, never above. The $100k answer overrode the deliberate modest-ask rule.
- 3.2. **Notice period**: answered `2 weeks`. Rule: **"None, I'm ready to go now."**
- 3.3. Both were corrected for the later Carma/IQVIA preparations, but the AAEH submission is final.

## 4. 🟠 Environment / Capability Limit

- 4.1. **External-portal résumé upload is blocked in-session.** CIC `file_upload` rejects any local deliverable PDF ("only files the user has shared with this session can be uploaded"); the shadow-DOM proxy workaround depends on the same call, so it fails identically.
- 4.2. **Consequence:** every external "Apply" role (M7=0) can at best become **Pending** (CL prepared, user submits manually) —— it can never be auto-submitted. Carma Senior UX Designer was correctly routed to Pending on this basis.
- 4.3. This limit was not known at session start; it was discovered at the Carma attempt and logged to `rlog`.

## 5. 🟠 Skip-Rate / Scoring Discipline

- 5.1. **Observation:** the two post-`202606011000` runs show an 80%⁺ skip rate (user-flagged).
- 5.2. **Largely justified.** The clear majority of skips are correct hard-skill-gate exits —— software/backend/cyber/network/data engineers, Salesforce/.NET developers, architects, interior/graphic designers, garment technicians, geophysicists —— all out-of-scope roles surfaced by the broad `business` / `business-analyst` / `ui-ux` queries. These SHOULD be skipped.
- 5.3. **Genuine concern —— a borderline-external over-skip pattern.** A subset of skips used "External Portal Early-Exit / S4.4" to exit roles the profile could actually perform, conflating "external (cannot auto-submit)" with "not worth applying":
  - 5.3.1. Crown Equipment IT Asset Administrator —— a genuine NO-gate entry-level admin fit, skipped as "entry-level + external <70".
  - 5.3.2. Allens Technical Business Analyst —— 6 yrs BA experience; skipped on an accredited-BA-qual gap + external.
  - 5.3.3. Uber Partner Manager (Advertising) & Martian Logic Growth Marketing —— adjacent marketing/analytics fits, skipped as external + sub-70.
- 5.4. **Root cause:** treating external-portal roles as near-automatic sub-70 skips (App Efficiency 0/5 dragging borderline scores under threshold) rather than recording the viable ones as Pending. User has directed voiding all post-1000 skips (actioned in `response_`).

## 6. 🟡 Process / Documentation

- 6.1. **Single-agent operation.** The session ran as one unified agent (no MA/SA spawn) per a user standing directive —— authorised, not a defect; but `main_ajap.md` still documents the MA/SA architecture (doc/impl mismatch worth reconciling).
- 6.2. **display_N counter.** C2 🎯 count tracked session_N only (per memory `feedback_ajap_display_n`) —— consistent; no error spotted.
- 6.3. **Audit trail.** Mid-session audit + capability-limit incidents were appended to `rlog_202605311407.md`; correction notes were appended to the 5 affected applied ARs (this-session files, editable). Good traceability.

## 7. Follow-Ups / Open Items

- 7.1. Spot-check a sample of the earlier `~`50 applied CLs (pre-1813) to determine whether the colon-pillar deviation is session-wide or only late-run (§2.4).
- 7.2. Decide a standing rule for external-portal roles: viable (≥ threshold) → Pending with CL prepared; below → skip. Stop letting M7=0 alone sink borderline-viable roles to skip.
- 7.3. Reconcile `main_ajap.md` MA/SA wording with the single-agent reality (or restore MA/SA).
- 7.4. Consider an `ajap.md` note recording the external-portal upload limit so future runs classify external roles as Pending-at-best up front.

## 8. Second-Pass Update (post mandatory re-read)
*Re-read: `gcl.md`, `ajap.md` (full) + spot-check of 3 earlier applied CLs. No `🚨` compaction sentinel; context coherent. The re-read materially revises §2.4 and §5 below.*

- 8.1. **No compaction loss detected.** Protocol understanding intact against `gcl.md`/`ajap.md`; no missing-context symptoms.

- 8.2. **🔴 ROOT CAUSE of the colon-pillar deviation —— a gap in the S4.7 self-review checklist.** `gcl.md:91` bans colons in the CL body, yet the mandatory pre-AR self-review (`ajap.md:283-290`) checks for dashes, bare `+`, `16⁺` frequency, template choice, and 10 banned GenAI words —— but has **NO colon check**. So colon-headed pillars passed self-review undetected. This is the single highest-value fix.

- 8.3. **🟢 §2.4 RESOLVED —— the colon-pillar + Q&A errors are LATE-RUN ONLY, not session-wide.** Spot-check of earlier applied CLs:
  - 8.3.1. Kinokuniya (00:35): correct INLINE strengths ("With my proven capabilities in Problem Solving, Quality Assurance, and Resource Management, I bring 8 years…"), full breadth block, standard closing. ✅
  - 8.3.2. TwoProvidores (10:45): correct inline strengths + breadth block + closing; one minor stray inline colon ("strategy: coordinating…") and dropped "16⁺" from the breadth line. 🟡 lesser.
  - 8.3.3. AlphaCarHire (13:37): correct inline strengths; salary answered **$70,000** (CORRECT); breadth block omitted (shorter CL). 🟡 lesser.
  - 8.3.4. Conclusion: early/mid runs were compliant on structure and Q&A; the colon-pillar format, the $100k salary, the "2 weeks" notice, and the "HCI"-vs-IxD slip all cluster in the LATE run (post-`~`1800: Peninsula, AAEH, CityRecitalHall, ENA, HungryPanda). **Pattern = long-session late-run quality drift** —— the model improvised away from the memorised template as the session aged.

- 8.4. **🟢 §5 CORRECTED —— the external skips were RULE-COMPLIANT, not execution errors.** `ajap.md:203` (External Portal Early-Exit: `M7=0 AND ceiling<70 → skip, no research/CL`) and `ajap.md:279` (S4.4 exception: `final<70 AND external → skip`) explicitly mandate skipping sub-70 external roles. Crown IT Asset Admin, Allens BA, Uber, Martian Logic were all scored <70 + external → the protocol REQUIRES skip. Therefore:
  - 8.4.1. The 80%⁺ skip rate is a product of (a) broad `business`/`business-analyst`/`ui-ux` queries surfacing many out-of-scope technical/design roles (correct hard-gate skips) and (b) the explicit external-and-sub-70-skip rule —— NOT of judgement error.
  - 8.4.2. The improvement is therefore a PROTOCOL question for the user, not a correction of my behaviour: should genuinely-performable external roles (e.g. the Crown entry-level admin) be routed to **Pending** (CL prepped, user submits) instead of skipped? Given §4's finding that external roles can never be auto-submitted anyway, "Pending-not-skip for viable external roles" would preserve them without changing the submit reality.

- 8.5. **🟠 §4 reinforced.** Even the documented shadow-DOM upload workaround (`ajap.md:470`) cannot beat the session sandbox's `file_upload` block —— so the External Portal Instructions are, in this environment, unreachable past the upload step. Worth a standing note in `ajap.md`.

- 8.6. **Revised recommendations (supersede §7):**
  - 8.6.1. Add a colon check to the S4.7 self-review list ("no `:` in CL body beyond the template's inherent colons; strengths INLINE, never colon-headed"). Highest priority.
  - 8.6.2. Add a salary/notice re-confirm to S4.7 or S6.2 (cross-check against the Pre-Defined Q&A table) to catch late-run drift like the AAEH $100k / "2 weeks".
  - 8.6.3. Consider periodic mid-session re-read of the `gcl.md` CL template (e.g. every N cards or on continued-run resume) to arrest late-session drift.
  - 8.6.4. Decide the external-viable-role policy (§8.4.2): Pending vs skip.
  - 8.6.5. Record the external-portal upload sandbox limit in `ajap.md` so external roles are classified Pending-at-best up front.
  - 8.6.6. Reconcile `main_ajap.md` MA/SA wording with single-agent reality.

*These are findings only —— no protocol files edited (per investigation CP, edits require a `changes_` for approval).*
