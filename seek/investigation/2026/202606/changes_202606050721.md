# Changes —— Qi Processing Order (all-n-then-all-p)
*Executed 05/06/2026; ref query_202606050715.md. Records edits already applied to live AJAP files so future sessions know.*

1. What changed (intent)
- 1.1. Old order: `01n → 01p → 02n → 02p → 03n → 03p` —— per Qi, the New-to-you (`n`) pass then the plain (`p`) pass, before advancing to the next Qi.
- 1.2. New order: `01n → 02n → 03n → 01p → 02p → 03p` —— TWO ROUNDS: Round 1 runs every Qi's `n` pass in `order:` sequence; Round 2 then runs every Qi's `p` pass in `order:` sequence.
- 1.3. Rationale (user directive): surface all freshly-posted "New to you" cards across every Qi first, before spending time on the heavier plain (all-jobs) passes.

2. Files edited
- 2.1. `/seek/queue.md` —— core processing logic (mutable run-data, not a `context/` protocol file).
- 2.2. `/seek/context/ajap.md` —— the two references that described the old order.
- 2.3. Unaffected (verified): `main_ajap.md` and `mini_ajap.md` reference only `queue.md` rule 2 (New-to-you enforcement), not the n/p ordering, so neither needed editing.

3. `queue.md` —— edit 3.1 (`## order:` note)
- 3.1. Now reads: "AJAP processes Qi strictly in the sequence on this line (left → right) within EACH of the two passes —— ALL `n` (New-to-you) passes first across the whole sequence, THEN ALL `p` (plain) passes (i.e. `01n → 02n → 03n → 01p → 02p → 03p`), NOT by row order in the table. To reprioritise, the user edits ONLY this line (e.g. `order: 02, 01, 03`)."

4. `queue.md` —— edit 4.1 (Processing Rules pt 1, rewritten as two rounds)
- 4.1. Lead-in: "AJAP derives TWO passes per Qi automatically; process them in TWO ROUNDS across the whole `order:` line —— ALL `n` passes first, THEN ALL `p` passes —— do NOT add separate `n`/`p` rows".
- 4.2. Sub-rule 1.1: "Round 1 —— ALL `[Qi]n` (new) passes FIRST, in `order:` sequence." For each Qi left → right, navigate to its table URL unchanged and process all "New to you" cards across all pages (e.g. `01n → 02n → 03n`).
- 4.3. Sub-rule 1.2: "Round 2 —— ALL `[Qi]p` (plain) passes SECOND, in `order:` sequence." For each Qi left → right, strip the trailing `&tags=new` and process the default (all) view (e.g. `01p → 02p → 03p`).
- 4.4. Sub-rule 1.3: "Advance within a round by the `order:` line; move from Round 1 to Round 2 only after EVERY Qi's `n` pass is exhausted, and conclude only after every Qi's `p` pass is exhausted."

5. `context/ajap.md` —— edit 5.1 (A6.1)
- 5.1. Now reads: "Read `/seek/queue.md`; process per that file's Processing Rules in TWO ROUNDS over the `order:` line sequence —— Round 1: every `[Qi]n` New-to-you pass (table URL unchanged) in `order:` sequence; Round 2: every `[Qi]p` plain pass (`&tags=new` stripped) in `order:` sequence (i.e. `01n → 02n → 03n → 01p → 02p → 03p`)."

6. `context/ajap.md` —— edit 6.1 (S7.2.2)
- 6.1. Changed the parenthetical from "(in `order:` sequence, `n` then `p` per Qi)" to "(in `order:` sequence, ALL `n` passes then ALL `p` passes)".

7. Invariants preserved (NOT changed)
- 7.1. The `order:` line value itself (`order: 01, 02, 03`) is untouched —— Qi priority is still reprioritised by editing only that line; the two-round structure rides on top of whatever sequence it holds.
- 7.2. `queue.md` rule 2 (New-to-you enforcement), rule 3 (URL-construction restriction), and the All-Qi-Exhausted Edge Case are order-agnostic and unchanged; A6.3's "every Qi (n+p) exhausted" still holds.
- 7.3. The `&tags=new` strip-not-append mechanic, and A7's no-hand-built-URL rule, are unchanged.

8. Note for future sessions
- 8.1. Separately, the user later retitled `queue.md` from "AJAP Fallback Queue (`queue.md`)" to "AJAP Queue" —— unrelated to this change; do not revert.
