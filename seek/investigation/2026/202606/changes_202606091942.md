# Changes —— `changes_202606091942`
*Three groups: (A) DOCUMENTATION of edits an Opus AJAP run already made under the user's "override" permission; (B) NEW deterministic CL-linter tool; (C) Opus-mode fallback note. Groups B & C executed this turn per explicit instruction (query_202606091942 §7.2 + §Fresh Run). Group A is a record only —— those edits pre-exist. The large 4-agent redesign (§3–§7) is NOT executed —— it is gated on the SDK decision (see response §25).*

---

## A. Edits already performed during an AJAP run by Opus (under user "override")

*Captured from `git diff` of `seek/context/` since last investigation turn. Not authored this session; documented here for the trail, reviewed in response §14.*

### A1. `ajap.md` —— K1 silent-skip keywords extended
**Was:** `K1. Title contains `Director`/`Full Stack`/`Master``
**Now:** `K1. Title contains `Director`/`Full Stack`/`Master`/`Architect`/`Interior`/`Surveyor``

### A2. `main_ajap.md` —— SA spawn records spawn time
**Added to the "After spawning" line:** ``Also append `sa_spawn_TS: [current_TS]` (via `TZ='Australia/Sydney' date +"%Y%m%d%H%M"`) to record spawn time for the 1hr retirement check.``

### A3. `main_ajap.md` —— SA 1hr hard-retire tripwire (under § SA Deterioration → Retire immediately)
**Added:** ``SA has been running for 1hr⁺ from spawn (`sa_spawn_TS` … compare) —— regardless of current activity; write "STOP" to `ma_msg.md`, wait 30s, reset to "Continue", retire, spawn fresh SA; log to rlog.``

### A4. `MA_hb.md` —— SA 1hr hard-limit check on every wake
**Added (after "Actively check SA1"):** ``**SA 1hr hard limit:** check `sa_spawn_TS` in `ma_state.md`; if current time ≥ `sa_spawn_TS` + 60 min → write "STOP" … retire SA immediately, spawn fresh SA, log to rlog. No exceptions…``

*Review/agreement: response §14.*

---

## B. NEW FILE —— `seek/context/cl_check.py` (executed)

Deterministic CL compliance linter (logic extracted/adapted from `gscpt/ajap_logs.py` `scan_applied_violations`, extended with the `audit_202606040352` colon-pillar finding and the `16⁺`-once rule). For SA1 (before declaring Applying), SA3/MA (before approving Submit), and MA's half-hourly cold audit.

- **HARD checks** (fail, exit 1): CL section heading ≠ `## 6.`; dash/plus symbols `— – +` in CL; `16⁺` more than once; file does not end with the exact P.S. line.
- **SOFT checks** (advisory, exit 0): colon-pillar headers; AI-slop/banned words (may match a company name → agent judges).
- Usage: `python3 /seek/context/cl_check.py <AR_path> [<AR_path> …]`. Exit 0 clean / 1 hard violation / 2 error.
- **Validated this turn:** smoke-tested over 40 archived applied ARs → 32 clean, 8 hard violations (real legacy dash/P.S. breaches), no crashes; both code paths exercised; soft-warning false-positive path confirmed on a company literally named "Synergy…".
- **Limit (documented in the file header):** format linting only —— cannot catch false claims, wrong salary/notice, or wrong résumé variant; LLM gates (SA1/SA3) stay. `BANNED_WORDS` seeded; extend from `gcl.md`/`cc_writing.md`.

---

## C. `main_ajap.md` —— new `## Opus-Mode Fallback` section (executed)

Inserted after § SA Spawn Instruction. Encodes query §Fresh Run: on Opus, do not spawn SA1 → run solo (with per-card `S0.1` re-read + `cl_check.py` self-gate restored, since the SA safeguard is absent —— closes `audit_202606081836` §3 "solo mode uncovered"); may spawn helpers only for non-`context/` tasks; heartbeat + SA2 still enforced; silently skip (no AR) all M7=0 external-portal jobs. Self-challenge: scoped strictly to Opus (Sonnet path untouched), additive, no behavioural change for the 90%⁺ Sonnet runs.

---

## NOT executed this turn (gated —— see response §25 for why)
- §3 heartbeat structural fix (couple marker-to-action + `latest_TS`-staleness watchdog).
- §4 `msg_board.md` bus; §5 SA2-as-LLM; §6 SA3 + MA-as-PD restructure; §7 half-hourly cold audit wiring.
- Reason: all of the above is the *native shape of the Agent SDK*; building it by hand in the CC-REPL is likely throwaway if we adopt the SDK. One go/no-go decision (response §25 QB) sequences everything.
