# Audit —— deferred AJAP trims (`audit_202606272057`)
*Companion to `audit_202606271729` (the full catalogue —— read it for per-item detail, NOT repeated here). This file records ONLY what was NOT trimmed in `changes_202606272057`, with the reason, so a future careful pass can finish the job safely. Per `numbered.md` (headings = labels; numbers in body; bite-size).*

## Executed vs deferred

1. Executed this turn (risk-free, confusion-reducing): `audit_202606271729` pts 14 (rlog citations), 15 (breadcrumbs), 16 ("me = human" line), 17 (Opus-Mode rationale shrink) —— see `changes_202606272057`. Net −176 tok.
2. Everything below is DEFERRED —— each needs a read-after-trim sanity check before it can be trusted, so it was NOT done blind.

## Deferred —— cross-file mechanism dedups (biggest savings, highest care)

3. Heartbeat mechanism (`audit pt 3`, ~450–550 tok).
- 3.1. Collapse `MA_hb.md`'s copy to a pointer at `main_ajap.md § Heartbeat`.
- 3.2. Deferred: `MA_hb.md` is the every-wake re-read; removing the mechanism inline trusts MA to recall `main_ajap.md` —— must verify MA still rebuilds a lost Monitor correctly before relying on a pointer.
4. Post-Compaction Recovery (`audit pt 5`, ~250–350 tok).
- 4.1. Shrink `MA_hb.md`'s copy to trigger + pointer.
- 4.2. Deferred: this is the most safety-critical path; the triple-statement (also in `seek/CLAUDE.md`) may be deliberate compaction-survival redundancy.
5. Watchdog reactions (`audit pt 6`, ~200–260 tok) + broad-file-check (`audit pt 7`, ~150–200 tok).
- 5.1. Make `SA2_hb.md` / `MA_hb.md` canonical; point the others.
- 5.2. Deferred: the reactions are operative on every wake —— a pointer risks MA acting on a half-remembered rule.
6. Silent-compaction rationale (`audit pt 4`, ~200–300 tok).
- 6.1. State the WHY once (in `SA2_hb.md`); delete the ~5 repeats.
- 6.2. Deferred: recent + safety-critical; the repetition currently reinforces the new `ma_full_reread_marker` fix —— trim only after a stable run confirms it.

## Deferred —— within-file repetition that GUARDS known bugs

7. These look redundant but each restatement defends a logged recurring failure —— trimming them is NOT risk-free.
- 7.1. External-portal-exit / "Quick-apply never skipped" 4× (`audit pt 9`) —— guards a "recurring SA error".
- 7.2. `⏳_` lifecycle 4× (`audit pt 11`) —— the Applying→Applied mechanic; a miss = a mis-routed/duplicate AR.
- 7.3. `run_in_background=True` 4× (`audit pt 13`) —— guards "the single most dangerous misconfiguration".
8. Verdict: keep as deliberate reinforcement unless a future run proves the SA no longer needs it.

## Deferred —— medium-risk, the best next candidates

9. CL-check recipe 3× in `main_ajap.md` (`audit pt 12`, ~140–180 tok).
- 9.1. `grep -n "^## 6\." → Read offset limit 80` is restated at Between-Loop, Heartbeat 4.1, and Session-Start 8.1.
- 9.2. MA-only file, mechanical recipe —— the safest structural dedup; do this FIRST in the careful pass.
10. C2 "never cumulative / number-emojis" 6× (`audit pt 8`, ~100–150 tok).
- 10.1. Trim to one statement per agent's file.
- 10.2. Lower-risk than the mechanism dedups but still operative —— verify each agent retains one copy.
11. S3.2 LinkedIn/Glassdoor residue (`audit pt 10`) —— mostly already reduced across recent turns; little left, low priority.

## New observations (beyond `audit_202606271729`)

12. None material —— the original audit was thorough; this turn's executed cuts confirmed its "low-value" category was accurate (no rule depended on the removed provenance).
13. Process note for the careful pass: do ONE dedup → re-read the affected file end-to-end → confirm the operative rule survives in its canonical home → only then the next. Never batch the mechanism dedups.

## Estimate remaining

14. Deferred savings still on the table ≈ 1,700–2,500 tok (the bulk of the audit's ~2.6–3.5k), almost all in the cross-file mechanism dedups (pts 3–6) and the MA-only recipe (pt 9).
