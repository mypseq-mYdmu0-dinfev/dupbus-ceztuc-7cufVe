# Changes —— `changes_202606271833`
*AJAP-file edits from `query_202606271829`: (3.4) reconcile SA poll-retry to the larger value; (4.1b) rename the AJAP login file `aux_login.local.md` → `ajap_login.local.md` + update its 3 references; (5.3.1) make re-login explicit (Sonnet types directly) with an rlog-and-continue fallback. `#replace`, verbatim snippets. (cic_bot.md / root `.claude/` moves are OUTSIDE AJAP —— documented in `response_202606271829`, no `changes_`.)*

---

## Change 01 —— `seek/context/main_ajap.md` § SA Spawn Instruction (retry 20→60 / 300→900s)
*Reconciles the contradiction (`audit_202606271729 pt 18`): `ajap.md` already says 60×/900s; the spawn prompt lagged at 20×/300s. Larger value = SA waits longer before self-retiring (cheaper than premature respawn).*
**Replace:**
```
if anything else, follow the instruction, then wait 15s and re-check, up to 20 times (300s total); if still not "Continue" after 20 checks, stop (become idle).
```
**With:**
```
if anything else, follow the instruction, then wait 15s and re-check, up to 60 times (900s total); if still not "Continue" after 60 checks, stop (become idle).
```

## Change 02 —— `seek/context/ajap.md` credential-file rename (3 references) + the file itself
*File renamed on disk: `seek/.claude/aux_login.local.md` → `seek/.claude/ajap_login.local.md` (git-ignored; `mv` preserves content). Then replace_all the path in `ajap.md` (S3.2.2, S3.2.4, and the S3.2 Notes re-login bullet —— the last is re-stated in full in Change 03).*
**Replace (each occurrence):**
```
seek/.claude/aux_login.local.md
```
**With:**
```
seek/.claude/ajap_login.local.md
```

## Change 03 —— `seek/context/ajap.md` S3.2 Notes re-login sub-bullet (direct-type + rlog-fallback)
*Per 5.3.1: Sonnet inputs credentials directly (confirmed working); if entry ever fails, rlog + CONTINUE, never stop. Replace = the pre-this-turn state (still `aux_login`).*
**Replace:**
```
  - A logged-out state is NEVER a blocker for the mandatory LinkedIn/Glassdoor sources: re-login (`seek/.claude/aux_login.local.md`) and continue; never "move on" for a login reason. Only an unresolvable captcha after two attempts permits moving on, and it MUST be noted in the AR.
```
**With:**
```
  - A logged-out state is NEVER a blocker for the mandatory LinkedIn/Glassdoor sources: re-login by inputting the credentials from `seek/.claude/ajap_login.local.md` DIRECTLY (Sonnet can type them), then continue; never "move on" for a login reason. If credential entry ever fails (e.g. a harness change that blocks Sonnet too), flag it in the rlog (so a future investigation session updates this) and CONTINUE anyway —— never stop. Only an unresolvable captcha after two attempts permits moving on, and it MUST be noted in the AR.
```

---

## Self-review
1. Retry reconciled to 60×/900s in both files (`ajap.md` already had it; `main_ajap.md` now matches).
2. Rename done cleanly: file `mv`'d (content preserved), all 3 `ajap.md` references updated, `.gitignore` updated to `seek/.claude/ajap_login.local.md` (verified ignored).
3. Re-login: explicit Sonnet-types-directly + the "rlog and continue, never stop" fallback you mandated —— so even a future harness change cannot stall an unattended run.
4. B1 from last turn is now RESOLVED (Sonnet types; the cic_bot.md "ban" reframed as Opus-only, outside AJAP).
5. Revertable: Change 01/03 swap With→Replace; Change 02 replace_all back + `mv` the file back.
