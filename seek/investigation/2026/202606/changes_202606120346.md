# Changes (PROVISIONAL) —— `changes_202606120346`
*Provisional / revert-candidate (the user will tag it red manually; was named with a `*`, renamed to drop the unreadable glob char). To be re-scrutinised when the user formally replies `response_202606091942.md`; stands only until then (or the SDK overhaul). Rewritten to `#replace` format (`replace.md`) with verbatim pre-/post-fix snippets for clean revert.*

---

## ⚠️ SCOPE ASSUMPTION (read first —— object if wrong)
The user asked for a "quick fix (SA3, etc.)" so AJAP can run THIS week to use quota, with assumptions allowed. I deliberately did NOT build a standalone SA3 agent, the `msg_board.md` bus, an LLM SA2, or the MA→PD restructure —— that unproven multi-file re-architecture is the biggest risk to an UNATTENDED week-long run (the exact quota-waste to avoid), and `response_202606091942 §25` defers it to the SDK. Instead I shipped the two contained, high-value, low-risk fixes that make the EXISTING proven Sonnet MA/SA1/SA2 flow reliable, plus last turn's `cl_check.py` wiring. The SA3 *audit function* this week is served by MA + `cl_check.py`. If you wanted the full build now, say so and I'll do it.

---

## Change 01 —— Heartbeat: marker CERTIFIES the re-read (fixes hollow heartbeat)
*Root failure (`audit_202606081836` §2, recurred N=10→N=92): MA touched the marker FIRST then skipped the actual Read; the watchdog saw it "alive" over a hollow wake. Fix: Read first, touch last as proof. 5 snippets across 2 files.*

### 01a —— `seek/context/MA_hb.md` (header note)
**Replace:**
```
*Formerly `mini_ajap.md`. The lean checklist MA LITERALLY re-reads (not from memory) at the TOP of every wake AND at S0.1 of every card. First action each wake: `touch /seek/.claude/tmp/ma_hb_reread_marker`, then re-read this file. Detailed mechanics live in `main_ajap.md` (read at start); watchdog spec in `SA2_hb.md`. If unsure of any item, re-read `ajap.md` & `main_ajap.md`.*
```
**With:**
```
*Formerly `mini_ajap.md`. The lean checklist MA LITERALLY re-reads (not from memory) at the TOP of every wake AND at S0.1 of every card. Each wake: re-read this file IN FULL with the Read tool and emit `✅ context/MA_hb.md`, run the checks, THEN as the FINAL step `touch /seek/.claude/tmp/ma_hb_reread_marker` —— the marker CERTIFIES the re-read happened, so it MUST come after the read, never before (touching first is exactly what produced the hollow-heartbeat failure). Detailed mechanics live in `main_ajap.md` (read at start); watchdog spec in `SA2_hb.md`. If unsure of any item, re-read `ajap.md` & `main_ajap.md`.*
```

### 01b —— `seek/context/MA_hb.md` (on-wake first bullet)
**Replace:**
```
On EVERY wake (primary `heartbeat`/`newfile`/`sa2-alert` tick, OR an `SA2_hb.md` watchdog line):
- FIRST `touch /seek/.claude/tmp/ma_hb_reread_marker`, then run the Active Check + Rogue tripwire above.
```
**With:**
```
On EVERY wake (primary `heartbeat`/`newfile`/`sa2-alert` tick, OR an `SA2_hb.md` watchdog line):
- FIRST re-read THIS file (`MA_hb.md`) in full with the Read tool and emit `✅ context/MA_hb.md`; then run the Active Check + Rogue tripwire above. (The `ma_hb_reread_marker` touch is the LAST action of the wake —— see the final bullet —— never the first.)
```

### 01c —— `seek/context/MA_hb.md` (new proof-of-read final bullet)
**Replace:**
```
- Output C2 when a new legitimate AR appeared: `[current_TS] 🎯[N] **job(s) processed so far.**` (N = number emojis = this-session count only); then `touch /seek/.claude/tmp/ma_c2_marker` (the watchdog measures this).
```
**With:**
```
- Output C2 when a new legitimate AR appeared: `[current_TS] 🎯[N] **job(s) processed so far.**` (N = number emojis = this-session count only); then `touch /seek/.claude/tmp/ma_c2_marker` (the watchdog measures this).
- **Proof-of-read (FINAL action of EVERY wake):** `touch /seek/.claude/tmp/ma_hb_reread_marker` —— ONLY now, after the genuine re-read + `✅` + Active Check above are actually done. This is the signal the SA2 watchdog trusts; touching it at the top of the wake, or without a real re-read, is a contract breach and is the root of the hollow-heartbeat failure.
```

### 01d —— `seek/context/main_ajap.md` (§ Heartbeat stamps line)
**Replace:**
```
- Stamps (watchdog measurement surface): `touch /seek/.claude/tmp/ma_hb_reread_marker` at the top of every wake; `touch /seek/.claude/tmp/ma_c2_marker` on every `🎯[N]`.
```
**With:**
```
- Stamps (watchdog measurement surface): `touch /seek/.claude/tmp/ma_hb_reread_marker` as the FINAL step of every wake —— ONLY after a genuine re-read of `MA_hb.md` + `✅ context/MA_hb.md`, NEVER at the top (a top-touch certifies a re-read that did not happen → hollow heartbeat); `touch /seek/.claude/tmp/ma_c2_marker` on every `🎯[N]`.
```

### 01e —— `seek/context/main_ajap.md` (§ Heartbeat step 1)
**Replace:**
```
1. `touch /seek/.claude/tmp/ma_hb_reread_marker`; re-read `MA_hb.md`; run its Active Check + Rogue tripwire. On `WATCHDOG-MA-STALL` → rebuild/repair the primary Monitor (likely died/orphaned).
```
**With:**
```
1. Re-read `MA_hb.md` in full (Read tool) + emit `✅ context/MA_hb.md`; run its Active Check + Rogue tripwire; `touch /seek/.claude/tmp/ma_hb_reread_marker` ONLY as the final step, after that re-read (proof-of-read; never before). On `WATCHDOG-MA-STALL` → rebuild/repair the primary Monitor (likely died/orphaned).
```

---

## Change 02 —— Submit approval SINGLE-USE + 3-min expiry (fixes runaway multi-submit)
*Bug (user §6 / `audit_202606081836` §7): one "Submit then proceed" approval let SA1 keep submitting without re-waking MA. SA-side only (no MA approval-string change → no deadlock risk). The precise "Submit [AR_TS]" token across all ~8 sites is the deferred, scrutinised version.*

### 02a —— `seek/context/ajap.md` (S6.4.2.5)
**Replace:**
```
S6.4.2.5. **[SA mode only]** Report to MA via chat: `[AR_PATH] | Applying | [N] | [FLAGS or "none"]` — then read `/seek/.claude/tmp/ma_msg.md` and wait (per MA-SA communication rules above) until it reads "Submit then proceed to next card"; if it reads an instruction to void, void the AR (per Void Rule: rename `⏳_` prefix to `❌_`) then close Tabs 3 & 2 and return to Tab 1; do NOT follow S6.2.3.2
```
**With:**
```
S6.4.2.5. **[SA mode only]** Report to MA via chat: `[AR_PATH] | Applying | [N] | [FLAGS or "none"]` — then read `/seek/.claude/tmp/ma_msg.md` and wait (per MA-SA communication rules above) until it reads "Submit then proceed to next card". **This approval is SINGLE-USE and EXPIRES 3 minutes after you first read it:** consume it for THIS ONE submit only; then immediately return to waiting and re-report `Applying` for the NEXT applying card to obtain a FRESH approval before any further submit —— NEVER carry one approval across cards (this is what prevents runaway multi-submits that never re-wake MA). If more than 3 min have passed since you read the approval and you have not yet submitted, discard it and re-report `Applying` to request a new one. If it reads an instruction to void, void the AR (per Void Rule: rename `⏳_` prefix to `❌_`) then close Tabs 3 & 2 and return to Tab 1; do NOT follow S6.2.3.2
```

### 02b —— `seek/context/main_ajap.md` (§ SA Spawn Instruction prompt)
**Replace:**
```
For applying jobs: at S6.4.2.5 before Submit, await explicit approval via `/seek/.claude/tmp/ma_msg.md`. Do not output the C2 count to user.
```
**With:**
```
For applying jobs: at S6.4.2.5 before Submit, await explicit approval via `/seek/.claude/tmp/ma_msg.md` — each approval is SINGLE-USE and expires after 3 min; submit ONLY the current card, then await a fresh approval before any next submit (never carry one forward). Do not output the C2 count to user.
```

---

## Self-review / assumptions
1. Reliability-first: both changes only TIGHTEN safety of the proven flow (more re-reads, fewer blind submits); neither adds an unproven agent.
2. No removals: every prior check retained; additive only.
3. Revertable: 7 snippets across `MA_hb.md`, `main_ajap.md`, `ajap.md` —— all verbatim above; revert = swap each **With** back to its **Replace**.
4. Assumption (per "make assumptions unless extremely critical"): read "quick fix (SA3, etc.)" as "the safe subset that lets AJAP run reliably", NOT "build the full SA3 restructure now". Flagged above to override immediately.
5. Untouched on purpose: `SA2_hb.md` (bash watchdog now meaningful since the marker is honest); the `latest_TS`-staleness watchdog (`response §13.3`) NOT added (needs fragile BSD-`date` epoch math in SA2's bash).
