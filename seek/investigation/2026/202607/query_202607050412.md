# AJAP Overhaul —— Kickoff (for the Fable session)
*Drafted by the prior (Opus) investigation session on the user's behalf. This is your 1st message. The prior session's full wrap is `dupbus-ceztuc-7cufVe/seek/investigation/2026/202606/close_202607050412.md` —— read that first.*

## 0. Mission
- 0.1. Rebuild AJAP (an autonomous SEEK job-application multi-agent system) PROPERLY on the **Anthropic Agent SDK** (or **Managed Agents** —— you decide which fits), replacing ~a month of REPL quick-fixes with a deterministic, code-driven design.
- 0.2. You are asked to JUDGE feasibility yourself (the prior session deliberately did not) —— including whether the SDK inherently dissolves the recurring problems (compaction, heartbeat, MA-idling).
- 0.3. Dispatch SA(s) to read previous comms files if helpful.

## 1. Session setup —— READ CAREFULLY (a correction to the user's assumption)
- 1.1. The user said "start from dir: root + investigation/". Correction: you need TWO working directories —— (a) `dupbus-ceztuc-7cufVe/` (root, to READ the current AJAP files + this query; operate in investigation mode, NOT AJAP mode) AND (b) the sibling `AJAP_repo/` (to BUILD the new system). Confirm both are added before starting.
- 1.2. Do NOT add `seek/` as a working directory —— that risks triggering AJAP mode / reading `seek/CLAUDE.md`.
- 1.3. 🔴 HARD CONSTRAINT: NEVER edit, create, move, or delete anything under `dupbus-ceztuc-7cufVe/seek/` —— AJAP is STILL RUNNING there to use the weekly quota. You may READ it freely. All new work is dup-edited or built from scratch in `AJAP_repo/`.
- 1.4. `AJAP_repo/` is a SEPARATE private git repo (sibling of `dupbus-ceztuc-7cufVe/`). Its root `CLAUDE.md` is intentionally near-empty so the default repo's CC can browse `AJAP_repo/gcl|ccl` without accidentally reading the working `AJAP_dir/CLAUDE.md` and starting AJAP. Judge whether that empty-root design is apt.

## 2. AJAP_repo/ target structure (already partly created: `AJAP_dir/`, `AJAP_inv/`, empty `CLAUDE.md`, `.gitignore`)
- 2.1. `AJAP_dir/` —— holds the SOLE working `CLAUDE.md` for a live AJAP session; nothing else.
- 2.2. `AJAP_protocols/` —— like the current `seek/context/` but ONLY AJAP protocol files (e.g. `ajap.md`). Non-AJAP context (e.g. `gcl.md`) will later migrate to `career/` → eventually `dupbus-ceztuc-7cufVe/cp/career/`. For now those stay in `seek/context/` so AJAP keeps running.
- 2.3. `queue.md`, `README.md`, `gcl/`, `ccl/` —— mirror current; the new build SCANS/CREATES into the CURRENT `seek/gcl|ccl` until fully built (so AJAP keeps running). `queue.md` may be restructured if the new design needs it.
- 2.4. `AJAP_inv/` —— the investigation workspace (renamed from `investigation/`). Once you create `AJAP_inv/CLAUDE.md`, move this session's comms into `AJAP_inv/2026/202607/`.
- 2.5. `interviews/` stays in `seek/` for now (later → `career/`).

## 3. Curated reading list (the user's original list REVIEWED for repetition —— read THESE, skip the rest; preferably by SA to save context)
- 3.1. The ~18 quick-fix comms turns the user listed are HISTORY —— each is already folded into (a) the CURRENT file state and (b) the close's unresolved-issues section. Do NOT read them individually.
- 3.2. READ, in order:
  - 3.2.1. `close_202607050412.md` (the wrap —— coverage, decisions, all unresolved issues).
  - 3.2.2. The CURRENT AJAP files (the actual state to reverse-engineer): `seek/context/{main_ajap.md, ajap.md, MA_hb.md, SA2_hb.md, gcl.md, cl_check.py}`, `seek/CLAUDE.md`, `seek/queue.md`. Side request: if `main_ajap.md` still needed after overhaul, cleanly rename it as `MA_ajap.md` for consistency.
  - 3.2.3. `response_202606091942.md` —— the STRATEGIC design discussion (the PD/PM/PE/auditor 4-agent model, msg_board bus, SA2-as-LLM time-keeper, SA3 auditor, the Agent-SDK §25 analysis incl. the auth/billing caveat).
  - 3.2.4. `MA_hb_LEAN_PROPOSAL_202606272232.md` + `audit_202606272146.md` —— the lean-file philosophy + the risky trim backlog.
  - 3.2.5. `backup/backup_Claude/backup_Claude_AJAP_1.0` & `2.0` —— the last cleanly-reviewed versions (READ-ONLY; NO edits in `backup/`).
- 3.3. Avoid reading any `rchat_*.txt` (too large). If necessary, dispatch SA(s) to do that. The rlog issues are pre-themed in §5.

## 4. What the current system is (to reverse-engineer, then improve)
- 4.1. Roles: MA (main agent, orchestrator) spawns one SA1 (worker) per card; SA2 (bash watchdog) catches stalls; a persistent Monitor heartbeat wakes MA.
- 4.2. The user's intended future model (`response_202606091942`): MA=PD (agent-management + reporting only), SA1=PE (worker), SA2=PM-on-time (LLM time-keeper), SA3=PM-on-scope (auditor); a `msg_board.md` bus; all-or-nothing compaction handling.
- 4.3. `MA_hb.md` is meant to be a LEAN "password-hint" answer-sheet (any miss → re-read ALL mandatory files), NOT a rule store —— it bloated to 9KB; a ~3KB rewrite is drafted but UNAPPLIED.
- 4.4. The core pain the SDK should dissolve: MA compaction (context bloat, ~172KB context/, ~hourly compaction), heartbeat reliability, and MA idling/burning-tokens-doing-nothing.

## 5. Known runtime issues (themed from rlogs `202606202330`, `202606272329`, `202607042122`)
- 5.1. Theme clusters (each recurred across runs):
  - 5.1.1. 🔴 Post-compaction: recovery NOT triggered despite injection phrases (MA ran Heartbeat not Recovery, didn't emit 🚨, didn't halt running SAs); Monitors die at compaction; approvals race the compaction window.
  - 5.1.2. 🔴 MA idling / burning tokens doing nothing (the priority): ~100 min idle from misreading single-Qi exhaustion as full-queue; ~35 min from a SYNCHRONOUS foreground SA spawn blocking MA; MA re-asking the user to "click Submit" every 60s heartbeat for ~12 min.
  - 5.1.3. 🔴 Autonomy breaches: MA repeatedly requested USER manual browser actions (submit/apply) —— no "Submit-blocked → exhaust CIC methods → pending → continue" fallback existed.
  - 5.1.4. 🔴 Approval-gate: racy handshake (⏳_ → CL audit → write "Submit then proceed" → SA polls) caused bypasses (SA submitted with no MA approval), expiry races, 900s timeouts, stale-"Continue" consumption.
  - 5.1.5. 🔴 Dual-SA race + duplicate K3 processing: old SA not retired when new spawned (context summary didn't reflect it) → two SAs on one card.
  - 5.1.6. 🔴 Scoring misroute: SA fabricates a "<50/<70" threshold and skips Quick-apply (M7=5) jobs scoring 35–69 that should be APPLIED —— recurs across many cards; MA sometimes mis-applied the 70-gate too.
  - 5.1.7. 🔴 CL fabrication: fictitious references (UNSW not UTS), wrong employer ("Kungfu Empire" not Karma Effect), em-dash slipping through —— `cl_check.py` is STRUCTURAL only, no factual/semantic check.
  - 5.1.8. 🔴 Fabricated `🕰️` timestamps declared without running Bash → risks early/late SA retirement.
  - 5.1.9. 🟡 Watchdog false positives (can't tell intentional SA-absence from a stall); SA liveness inferred from file mtimes/tab-state not task-notifications; 7hr SA overrun invisible ("Task not found"); hard-limit retire/respawn churn dominates.
  - 5.1.10. 🟡 External-portal `file_upload` blocked (CIC only shares session files); LinkedIn credential auto-login now BLOCKED by the harness classifier (so §S3.2.2's "Sonnet types directly" assumption is already void); 529/rate-limit crashes.
  - 5.1.11. 🔴 MA navigated Tab 1 (SA's SEEK results tab), resetting `&tags=new` and losing queue position.
- 5.2. TOP-5 SYSTEMIC ROOT CAUSES (the case for the SDK —— each is inherently dissolved by a deterministic code loop):
  - 5.2.1. Non-deterministic MA orchestration via file-markers + heartbeats + async approval —— a racy timing-dependent handshake produces EVERY high-severity failure. An SDK loop where MA directly `await`s the SA's structured result then acts removes this whole class.
  - 5.2.2. Compaction destroys MA state/control —— recovery depends on MA self-detecting injection phrases (repeatedly failed); Monitors die at compaction. A persisted-state SDK design with SA results returned to a durable parent removes compaction as a failure vector.
  - 5.2.3. No autonomous browser-failure fallback → MA defaults to waiting for the user (structural autonomy breach).
  - 5.2.4. SA has no reliable liveness/lifecycle signal to MA (infers from mtimes/tab-state/stale summaries) → dual-SA races, overruns, premature retirement, duplicate processing.
  - 5.2.5. Scoring/CL correctness lives in fallible SA prose, not deterministic code —— routing bands + factual claims re-derived each time; a deterministic scoring/routing function + a factual-linter would kill the most-repeated violations.

## 6. Critical to verify / decide (before committing to a build)
- 6.1. 🔴 Agent-SDK AUTH & BILLING: the user is on Claude **Max** (not API). Prior research suggests the SDK needs an API key + a monthly credit-pool, NOT the flat subscription —— this changes the cost basis. CONFIRM on the official docs before committing (open via CIC).
- 6.2. Agent SDK vs Managed Agents: which gives deterministic multi-agent orchestration + browser/computer-use + file upload (needed for external portals) on the user's plan.
- 6.3. Whether the new system still launches via a CC session in the app, or runs as a standalone process (CLI/launchd) —— the user asked this explicitly.

## 7. Suggested first steps
- 7.1. Confirm the two working dirs (§1); create `AJAP_repo/AJAP_inv/CLAUDE.md` + `AJAP_repo/README.md`; move this query's session comms into `AJAP_inv/2026/202607/`.
- 7.2. Resolve §6.1 (auth/billing) —— it gates everything.
- 7.3. Produce a DevPlan (read `universal/plan.md`) for the overhaul (phased), then build incrementally in `AJAP_repo/` whilst AJAP keeps running in `seek/`.
- 7.4. Re-derive the two un-scrutinised quick fixes (`changes_202606121035`, `changes_202606120346`) rather than porting them blindly.
