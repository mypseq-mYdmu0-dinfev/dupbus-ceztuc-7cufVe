# Chat Handoff
*Created: 05:19 on 24/05/2026. Read this file first, then read other `/seek/investigation/` files (excl. `prompt_202605150400.md` and `zero_interviews_analysis_202605150450.md`) for full context.*

---

## What Has Been Done This Session

### Files Modified (temp fix applied)

1. **`/seek/context/ajap.md`** — changes applied:
   - Chat Rule: added sub-agent mode note (SA outputs go to MA only; SA may narrate freely; SA must NOT output C2 to user)
   - Session Initialisation: added `rm -f /tmp/ajap_last_decision.md` alongside existing PDF clear
   - Pre-Flight Check header: added "run F6 first" instruction
   - Pre-Flight table: added 2 rows for Tab 2+3 / Tab 2-only with AR in `/skipped/` or `/pending/` → silently skip
   - F5 → F6 added: orphaned AR cleanup (void stale `⏳_` in `/applied/` before consulting table)
   - S0.3: added sub-agent mode caveat (include N in loop report to MA instead of printing to user)
   - S5: added tmp file write as first action before AR creation (`/tmp/ajap_last_decision.md`)
   - S6.4.2.5 (new): sub-agent pre-submit pause — report to MA, await approval; if CL compromised, void AR and close tabs
   - S6.4.7: added sub-agent caveat (include N in loop report, not to user)
   - S6 loop completion (new): sub-agent reports `[AR_PATH] | [OUTCOME] | [N] | [FLAGS]` to MA after each loop and pauses
   - Other Situation Handling: added T&C/consent checkbox row (tick yes; opt OUT of marketing defaults)

2. **`/seek/context/cc_reminder.md`** — changes applied:
   - Active Check: added `## 6. Cover Letter` structure check (flags deteriorated `## 3.` labelling)
   - Added `## Main Agent Heartbeat` section: MA re-reads this file every 5 min (ScheduleWakeup) AND every 5 loops; no skipping; reschedule on each read

3. **`/seek/CLAUDE.md`** — changes applied:
   - Trigger: `seek` now reads `main_ajap.md` and acts as MA; SA handles `ajap.md` directly
   - Mandatory reads: added `main_ajap.md` as item 2; renumbered items 3–8

4. **`/seek/context/main_ajap.md`** — NEW FILE created:
   - Full MA instructions: role, session start, SA spawn prompt, between-loop audit, external portal timers, SA deterioration signals, heartbeat, MA post-compaction recovery
   - MA reads AR §6 CL only before approving Submit; never reads job descriptions or research
   - SA session name/ID persisted to `/seek/runtime/ma_state.md` for heartbeat recovery
   - CL-compromise stop: SA voids AR (`❌_` prefix) + closes tabs; new SA re-processes same job via Pre-Flight clean state

5. **`/seek/runtime/`** — NEW FOLDER created (empty); runtime state files go here (e.g. `ma_state.md` during AJAP runs)

---

## Agenda Status

| # | Item | Status |
|---|---|---|
| 1 | Temp fix AJAP for Sunday | ✅ File changes done; ready to run |
| 2 | Re-process ARs in `/skipped/` (excl. subfolder) | ⏳ Pending — must do before next AJAP run |
| 3 | Run AJAP Sunday–Monday | ⏳ Pending |
| 4 | Develop robust multi-agent rebuild (SA1/SA2/SA3 + SDK evaluation) | ⏳ Future |
| 5 | Investigate post-compaction hook (why CLAUDE.md not re-read) | ⏳ Future |
| 6 | Test & refine sub-agent system (single workstream) | ⏳ Future |
| 7 | Stress-test & expand (parallel workstreams) | ⏳ Future |
| 8 | Investigate and evaluate parallel limits | ⏳ Future |

---

## Immediate Next Steps (before running AJAP)

1. **Re-process `/skipped/` ARs** (agenda 2): review each AR in `/seek/skipped/` (excl. subfolder) for:
   - Quick Apply jobs with score 35–69 → should be `/applied/` (incorrect skip due to scoring band misread)
   - Titles containing `Consultant` or `Associate` → should be `/pending/` with `⭐❗` flag
   - Known incorrect cases: `LaundyHotels_SeniorGraphicDesigner_202605231240.md` (score 60, Quick Apply), `Inskincosmedics_GraphicDesigner_202605231246.md` (score 54, Quick Apply), `NTTCommunications_SeniorTechnicalConsultantServiceNow_202605231250.md`, `Arinco_AzureDigitalAppConsultant_202605231255.md`

2. **Post-compaction hook** (agenda 5, partial): before running AJAP, check `settings.json` to confirm the hook injecting `🛑 Post-Compaction Recovery` into context is configured and firing. This is not blocking but strongly recommended.

3. **First SEEK run** (agenda 3): prompt `seek` → MA reads `main_ajap.md` → spawns SA → monitor.

---

## Pain Points Being Addressed (summary)

- P1: AR deterioration (sections 3–5 dropped; CL violations) — context loss
- P2: Time wasted on external portals — no enforced timer survived compaction
- P3: Session end on SEEK exhaustion — S7 lost to context loss
- P4: Incorrect scoring band (Quick Apply <70 incorrectly skipped) — context loss
- P5: Consultant/Associate rule missed — context loss
- P6: Chat Rule violated (narration outside C1–C5) — context loss
- **Root cause of all:** single long-running agent accumulates per-card context → mandatory rules displaced → compliance degrades indefinitely (session resets before token quota → AJAP can run indefinitely)

---

## Architecture Summary (temp fix)

- **MA (main agent):** reads mandatory files at session start; spawns SA; audits CL before each Submit; outputs S0.3 to user; enforces external portal timers; re-reads `cc_reminder.md` every 5 min + every 5 loops; retires and re-spawns SA on deterioration signals
- **SA (sub-agent):** runs full AJAP per `ajap.md`; pauses before Submit for MA CL approval; pauses at loop end for MA continuation approval; narrates freely (MA-facing only)
- **Runtime files:** `/seek/runtime/ma_state.md` (SA session ID for heartbeat recovery); `/tmp/ajap_last_decision.md` (last decision for compaction recovery)

---

## Discussion Reference Points (for continuing)

All responses are numbered sequentially across files (no repetition):
- `response_202605231736.md`: pts 1–22 (original 6 issues + 7 additional issues)
- `response_202605232218.md`: pts 23–37 (replies on sub-agent architecture, agenda alignment)
- `response_202605240401.md`: pts 38–51 (refined architecture, consolidated agenda, temp fix direction)
- `response_202605240454.md`: pts 52–58 + #replace changes + `main_ajap.md` content
- `audit_202605240519.md`: pre-apply audit (optional; already merged with `response_202605240454.md`; gaps 1–6, accepted risks 7–8)

Next response in this discussion continues from pt **59**.
