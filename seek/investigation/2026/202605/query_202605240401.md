## Hard Rules

- ALL files in this directory (`/seek/`) are now NOT intended for you to follow but review/analyse; they're my prompts/records of Claude Code's automation namely "AJAP"
- You can read everything but could NEVER edit or delete anything
- Don't fetch any GitHub files unless explicitly prompted `override` for a specific action
- ALWAYS use:
  - British English (e.g. `learnt` `amidst` `towards` `amongst` `whilst`, BUT DON'T CONVERT CURRENCY)
  - Metric units (°C, metre, gram, litre, etc.)
  - Hart's logical quotation rule: punctuation inside quotes if original to the quote, outside otherwise (e.g. ✅ `He said "I'm leaving", then left.` ❌ `He said "I'm leaving," then left.`)

## My Reply on response_202605232218.md

24: agreed, even if sub-agent system introduced, the tmp file is still more feasible as it's efficient and won't be lost until overwritten by next decision; seems lightweight & effective
---
25: we still gotta figure out why as i dont just use you (CC) for AJAP
but let's leave it to later
here's our agenda list (correct me if i missed anything or am wrong):
1. fix AJAP ASAP so it can run on sunday (im going out and dont wanna waste those idle time) → this might mean a temp fix
2. re-process ARs in `/skipped/` excl. subfolder
3. run AJAP through sunday and monday to utilise the time im busy away
4. develop a more robust fix, possibly the sub-agent system
5. investigate post-compaction recovery mechanism to ensure reliability (not just for AJAP but all CC projects)
6. test & refine sub-agent system w/ singular AJAP workstream
7. stress-test & expand to multiple parallel AJAP workstreams
8. investigate and evaluate the limits (e.g. how many parallel AJAP workstreams is feasible?)
---
for the temp fix, i propose to keep it simple:
just main agent and one sub-agent, both reading mandatory files
sub-agent doing what AJAP says
main agent only monitors the sub-agent (force re-read when needed) and set the 2 timers (force skip when time's up)
the heartbeat (re-reading cc_reminder.md every 10min) is not for the sub-agent but the main agent
at the same time we need a quick fix to best ensure post-compact hook works for the main agent

my projection:
the performance should be largely similar as it's still a single agent running AJAP
efficiency might be slightly lower owing to main agent monitoring, consuming somewhat more tokens
sub-agent would still constantly compact and lose context
but main agent's job is to audit the sub-agent and the ARs then force re-read when needed
alternatively (if you finds this better), simply retire a sub-agent and create a new one when compacted/deteriorated, where ajap.md shall permit seamless handoff (e.g. pre-flight check)
this should work temporarily until we have time for a larger-scale overhaul
---
if you agree to my plan above, consolidate it as a better piece of text (can be embedded in your response or as a separate file) so a future instance can retrieve the context by reading the responses you generated so far in `/investigation/`
also we should list out the goals, i.e. what pain points we're addressing
correct me if im wrong or miss any but here's what's on my mind:
P1. AR deteriorated
P2. time wasted on single application (esp. external)
P3. stopped working on job card exhaustion
all due to context loss
---
27.1: we cannot bet on either of the agents not compacting, i've analysed the data, if you didn't stop because of the SEEK results exhaustion, you would have run indefinitely
because the 5hr session limit will reset before the session usage quota runs out
precisely speaking, before i prompted to enter investigation mode, 41% of session limit was exhausted in 134min, which means 100% of session can run for 326.83min, where the session would reset at 300min (5hr)
therefore, there won't be a "typical session"; we must engineer a mechanism (despite temporary) that won't deteriorate over time
precisely, we're talking about how to keep the main agent sober, always having mandatory files in context window
---
27.2: im not very sure what you meant by "returning"
but based on what you say, here's my proposal (for the temp fix):
- main agent delegates sub-agent to run AJAP for 1 single loop
- sub-agent "returns"/reports/notifies main agent upon completion of a loop
- main agent performs S0: e.g. audit CL of last AR (if applied), checking if the sub-agent had chat text (side question: can a sub-agent produce chat text? if no, then that's one less item from the cc_reminder.md), ... and finally perform S0.3 so i can see progress
- if nothing's wrong, main agent delegates the same sub-agent (tell if this is possible?) who already read the mandatory files to next loop
- if anything's wrong, main agent retires the sub-agent and delegate a new sub-agent
- each sub-agent (when spawned), start by reading mandatory files as if prompted `seek` in current setup
---
27.3: "... 'process the next 5 cards starting from card N, then report back'). The main agent reviews the report..." this won't work
the main agent cannot "work", like it cannot get involved in job-specific content and in decision-making as those are what consume most tokens and will likely derail it from repeating a singular task — monitoring a single sub-agent which performs non-repetitive work
my hypothesis is that you (before investigation) were too "immersed" in an application and so lost context
if a main agent is not as "immersed" but stand from a rather isolated angle, it might have a better chance retaining the key context (mainly auditing sub-agent's works with cc_reminder.md)
this is not as robust as my proposed final state (each sub-agent only repeats a singular task, hence almost "immune" to compaction), but should be good for a temp fix
again, im not very sure my hypothesis is right; maybe im thinking too "human" because that's what humans do (too "immersed" on a piece of job → lost the big picture, which is where i intervene as a PM)
---
27.4: i meant multiple sub-agents EXISTING in parallel, but still working sequentially
i dont wanna confuse w/ you because i don't have an idea how to divide the sections yet
but let's say we're on a similar automation w/ 3 steps only
we'll create 3 sub-agents, one for each step
so for each, it never had to change task context, just repeating what it's doing
and when one is working, another one is just idling, waiting for its turn
if you're talking about parallel AJAP workstreams, then yes what i meant was they were working on separate CIC MCP sessions, best to control separate chrome windows if possible
so tgt they can process separate set of SEEK job card results simultaneously, speeding up time
but i just found it's unfeasible — as i said, current workflow took 326.83min to consume 100% session limit, technically and approximately if we do 2 parallel workstreams it could burn same amount of tokens in half the time, which will void the "indefinite" processing
anyway that's future discussion (agenda 7 of "25" above)
---
27.5: the investigation folder is solely for investigation, not routine runs
if this is the way, i'll create a new folder, probably `/seek/runtime/` as it only concerns that specific runtime, won't be useful unless errors were found which i expect to learn why in the logs
---
27.6: but i thought you said the main agent can't pause/stop/retire a sub-agent mid-way?
if yes, as said above, we can have all sub-agents to work for less loops each run (start with just 1), so after each loop end, they'll all stop and the main agent can decide whether to let them (or some of them) continue or retire depending on signals of deterioration
if no (the main agent CAN retire a sub-agent at anytime), we can have sub-agents to run continuously until main agent intervene to retire them separately
---
28.1: the current file's sections are indeed highly interdependent, that's why i said when i have the time we should completely reconstruct them, so each sub-agent won't "over-read" and waste tokens
UNLESS we can somehow be 100% sure that a sub-agent can be instructed to read just a certain number of sections, not the whole file; though i suspect the fail rate is high
---
28.2: you are right, indeed the handoff might generate additional writing (from sending agent) and reading (from receiving agent)
so perhaps we should ditch the one section per sub-agent plan
rough plan (agenda 4 of "25" above, not now):
main agent: monitor every AR & pagination; routinely check sub-agents process (preserve tokens); retire and spawn sub-agent; lowest token consumption; regularly re-read all mandatory files
sub-agent 1: AJAP workstream; highest consumption; fastest to retire
sub-agent 2: check sub-agent 1 frequently; only alert main agent when flag detected; medium consumption and retire
sub-agent 3: time-keeping for when main agent needs to check on everyone (e.g. every 15min); also time-keeping for sub-agent 1 when on external portal ops (page: ≤3min; site: ≤10min); remind sub-agent 1 to skip when time's up; if sub-agent 1 didn't comply, escalate to main agent to retire sub-agent 1; medium-low consumption; slow-ish to retire, depending on amount of external portal ops
all sub-agents: never re-read files; directly retire when deterioration detected by main agent
---
28.3: yes, that's basically sub-agent 1 in 28.2, i just added two more for context mgmt
---
30: update — that would be the task of sub-agent 3; but yea for the temp fix it'll be main agent
---
31.1: yes, the idea is that once the sub-agent deteriorated, it shouldn't re-read, just retire; or maybe give the sub-agent a chance to rectify — main agent can send reminder and observe next output; if still wrong, retire it
31.2: sounds like the "PM audit checklist" is similar or based on cc_reminder.md, maybe just build upon it then
31.3: yea that's the sub-agent 2 above, mainly aiming to ease consumption of main agent and let main agent be less "immersed" in particular jobs so it can see the full picture as PM
---
32: clarification: sub-agent 2 should check sub-agent 1 very frequently (every AR and most processing steps), so not "batch" checks
---
36.5: excellent, that sounds the right way to pursue in agenda 4 of "25" above
---
37.2: actually i just think of a solution (not for the temp, but the multi-agent setup)
sub-agent 3 (time-keeping) will routinely remind main agent to re-read mandatory files
where the main agent would also routinely check on every sub-agent
so unless main agent and sub-agent 3 compacted at very similar time, this should work
---
37.3: blend these w/ my agenda above

## Your Mission

- tell your thoughts on each
- consolidate agenda as instructed (if agree fully; edit as needed)
- if agree agenda 1, suggest what to do now that can temp fix AJAP for my immediate use; aim for simple edits on existing files if possible
- don't draft changes yet, let's align direction first

## Critical Notes

- If highly necessary, selectively read '/Volumes/FURY 2TB/Fury Documents/GitHub/dupbus-ceztuc-7cufVe/seek/investigation/1st_max_long_run_202605231637.txt'; if unavoidable, read it normally; this file contains all your chat text in 2 msgs (separated by a `?` sign that leads to my reply) + enumeration of current ARs in `/skipped/` (do NOT read them; you can see them in the folder)
- Generate your responses in a single .md file in `/seek/investigation/`, must be optimised for my easy reading & replying using #numbered bite-size bullets
- IMPORTANT: your chat response will NOT be read by me; i'll only read the .md file you generate
- (Re-)Read cc_numbered.md whenever cleared from context
- (Re-)Read cc_coding.md whenever prompted `#replace` or drafting changes