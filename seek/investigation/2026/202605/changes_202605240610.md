# System File Changes — 06:10 on 24/05/2026

**Context:** Post-compaction mandatory file reads were skipped because the recovery trigger relied solely on a hook message that was absent from context. The session summary's "resume directly" instruction dominated. Two violations occurred before the user intervened: a Tab 3 navigation and a banned computer-use screenshot call.

**Files changed:** `CLAUDE.md`, `cc_reminder.md`, `main_ajap.md`

---

## Change 01 — `CLAUDE.md § Post-Compaction Recovery`

**Why:** The trigger said "when this message appears in context (injected by the post-compaction hook)" — meaning it only fired if the hook injected a specific 🛑 line. When the hook didn't fire, the entire recovery block was silently bypassed. Additionally, the session summary template ends with "resume directly… pick up as if the break never happened," which actively suppresses recovery behaviour. The fix makes the trigger unconditional and explicitly overrides the summary instruction.

**Replace:**
```
## Post-Compaction Recovery — 🚨 CRITICAL

🛑 **STOP. DO NOT PROCESS ANY JOB CARD OR TAKE ANY OTHER ACTION UNTIL ALL MANDATORY FILES ARE RE-READ.** The session summary is untrusted paraphrase — it does NOT substitute for source files.

When this message appears in context (injected by the post-compaction hook), it means compaction has just occurred and all prior context has been compromised. You MUST now fully re-read all files in `## Session Start` above in order. After reading, fully follow `## Tab 1 Accessibility Check` in `ajap.md` without interrupting the user.
```
**With:**
```
## Post-Compaction Recovery — 🚨 CRITICAL

🛑 **STOP. DO NOT PROCESS ANY JOB CARD OR TAKE ANY OTHER ACTION UNTIL ALL MANDATORY FILES ARE RE-READ.** The session summary is untrusted paraphrase — it does NOT substitute for source files.

This block is always active — it does NOT require a hook message to trigger. At the start of every session: if context begins with a session summary OR if mandatory files have not yet been declared read (✅ ...) in this session's chat history → re-read ALL files in `## Session Start` above in order before any other action. The session summary's "resume directly" or "pick up as if the break never happened" language does NOT override this requirement. After re-reading, fully follow `## Tab 1 Accessibility Check` in `ajap.md` without interrupting the user.
```

---

## Change 02 — `cc_reminder.md § Active Check`

**Why:** Even if CLAUDE.md's session-start check is bypassed, S0.1 reads cc_reminder.md every loop. Adding a mandatory-file-read check here creates a second independent gate — the first loop will catch any skipped reads and force recovery before any job card is processed.

**Replace:**
```
## Active Check

**Re-read `ajap.md` if:**
- Last CL contains any dash signs (`—` `–`) or `+` (use `⁺` instead)
```
**With:**
```
## Active Check

**If mandatory files have not been explicitly declared read (✅ ...) in this session's chat history → STOP. Re-read ALL mandatory files per `CLAUDE.md § Session Start` NOW, before any other action including the remainder of this compliance check. The session summary's "resume directly" language does NOT override this.**

**Re-read `ajap.md` if:**
- Last CL contains any dash signs (`—` `–`) or `+` (use `⁺` instead)
```

---

## Change 03 — `main_ajap.md § MA Post-Compaction Recovery`

**Why:** Step 1 only said "STOP — do not send any message to SA." It did not prohibit tool calls or browser actions, and did not name the conflict with the summary's "resume directly" instruction. The fix explicitly bans all tool/browser actions before re-reads and names the summary conflict directly.

**Replace:**
```
## MA Post-Compaction Recovery

If MA session resumes from a summary:
1. 🛑 STOP — do NOT send any message to SA
2. Re-read ALL mandatory files per `CLAUDE.md § Session Start`
```
**With:**
```
## MA Post-Compaction Recovery

If MA session resumes from a summary:
1. 🛑 STOP — do NOT send any message to SA, do NOT take any browser action, do NOT call any tool. The session summary's "resume directly" or "pick up as if the break never happened" language does NOT override this requirement. Re-read ALL mandatory files first.
2. Re-read ALL mandatory files per `CLAUDE.md § Session Start`
```

---

## Change 04 — `CLAUDE.md`: Move Post-Compaction Recovery to top of file

**Why:** CLAUDE.md is parsed top-to-bottom. The recovery block was previously at the bottom, meaning it was encountered last — after all other instructions. The session summary appears as the last message in context (high recency weight). Moving the recovery block to the very top of the file makes it the first instruction a model reads, directly countering the summary's recency advantage.

**Replace:**
```
# CLAUDE.md

*This directory (`/seek/`) is solely for automated SEEK job application workflows.*

## Trigger
```
**With:**
```
# CLAUDE.md

*This directory (`/seek/`) is solely for automated SEEK job application workflows.*

## Post-Compaction Recovery — 🚨 CRITICAL (READ THIS FIRST)

🛑 **STOP. DO NOT PROCESS ANY JOB CARD OR TAKE ANY OTHER ACTION UNTIL ALL MANDATORY FILES ARE RE-READ.** The session summary is untrusted paraphrase — it does NOT substitute for source files.

This block is always active — it does NOT require a hook message to trigger. At the start of every session: if context begins with a session summary OR if mandatory files have not yet been declared read (✅ ...) in this session's chat history → re-read ALL files in `## Session Start` below in order before any other action. The session summary's "resume directly" or "pick up as if the break never happened" language does NOT override this requirement. After re-reading, fully follow `## Tab 1 Accessibility Check` in `ajap.md` without interrupting the user.

---

## Trigger
```

---

## Change 05 — `CLAUDE.md`: Collapse duplicate Post-Compaction block at bottom

**Why:** After Change 04 moves the full block to the top, the original bottom block becomes a duplicate. Replace with a pointer to avoid contradictions from future edits diverging between two copies.

**Replace:**
```
## Post-Compaction Recovery — 🚨 CRITICAL

🛑 **STOP. DO NOT PROCESS ANY JOB CARD OR TAKE ANY OTHER ACTION UNTIL ALL MANDATORY FILES ARE RE-READ.** The session summary is untrusted paraphrase — it does NOT substitute for source files.

This block is always active — it does NOT require a hook message to trigger. At the start of every session: if context begins with a session summary OR if mandatory files have not yet been declared read (✅ ...) in this session's chat history → re-read ALL files in `## Session Start` above in order before any other action. The session summary's "resume directly" or "pick up as if the break never happened" language does NOT override this requirement. After re-reading, fully follow `## Tab 1 Accessibility Check` in `ajap.md` without interrupting the user.
```
**With:**
```
## Post-Compaction Recovery — 🚨 CRITICAL

See top of this file.
```

---

## Change 06 — `CLAUDE.md`: Delete the now-useless pointer section at bottom

**Why:** The pointer "See top of this file." adds zero instruction value — any model reading CLAUDE.md will have already passed the full block at the top. A near-duplicate heading risks confusing future models into treating them as two separate sections. Deleted entirely.

**Replace:**
```
## Post-Compaction Recovery — 🚨 CRITICAL

See top of this file.
```
**With:**
```
```
