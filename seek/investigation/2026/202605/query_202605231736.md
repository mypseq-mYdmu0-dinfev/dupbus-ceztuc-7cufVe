## Hard Rules

- ALL files in this directory (`/seek/`) are now NOT intended for you to follow but review/analyse; they're my prompts/records of Claude Code's automation namely "AJAP"
- You can read everything but could NEVER edit or delete anything
- Don't fetch any GitHub files unless explicitly prompted `override` for a specific action
- ALWAYS use:
  - British English (e.g. `learnt` `amidst` `towards` `amongst` `whilst`, BUT DON'T CONVERT CURRENCY)
  - Metric units (°C, metre, gram, litre, etc.)
  - Hart's logical quotation rule: punctuation inside quotes if original to the quote, outside otherwise (e.g. ✅ `He said "I'm leaving", then left.` ❌ `He said "I'm leaving," then left.`)

## Issues

since you now have the files re-read
based on the rules you understand now, spot out issues again from your context window which could be less than before the files re-reading
---
1st issue is that you prompted:
`"No" is selected ✅. The Terms and Conditions checkbox needs to be accepted before clicking Next — may I check that box to proceed with submitting the application?`
this caused you to stop and await my reply which is a hard violation (fully autonomous)
we should have mandated what to do in such scenario so you can handle yourself
---
2nd issue is you spent too much time on the ~2nd job card's external application portal which caused separate problems:
2.1. you first decided to skip due to score below 70 but external portal, but after you announced that in chat (which shouldn't exist either, but not a major problem/cost), you seem to have compacted which i dont quite understand as it's quite "early" in the session
you said:
```
Re-estimate: 61 < 70, M7=0 → Exception → skip. Getting timestamp and creating AR:

✅ `cc_reminder.md`

✅ `ajap.md`

🎯2️⃣ **job(s) processed so far.**

✅ `CLAUDE.md`, `ajap.md`, `gcl.md`, `pro_profile.md`, `culous_yu_resume_ats.md`, `cc_writing.md`, `cc_reminder.md`

✅ `cc_reminder.md`

🎯0️⃣ **job(s) processed so far.**

⚠️ Paramount SAP SuccessFactors portal requires account login; password entry prohibited — marking Pending for user to complete.
```
i dont understand why you have re-read cc_reminder.md when creating AR (again, not a major problem, just dont get it)
but then you re-read ajap.md and then all mandatory files so you seemed to have compacted
2.2. similar thing happened to a later job card that you decided to skip, possibly have created the concise AR for a skip job, then you closed Tab 4⁺ and Tab 3, but stopped at Tab 2
then you compacted, rear to that, possibly owing to pre-flight check, you saw Tab 2 open but the AR is possibly still in `/applied/`
2.2.1. for skip/pending, we have to mandate to move (per Move Rule) to target folder before editing the AR, which can prevent re-processing a job already decided to skip/save (pending) in case you got compacted after the decision made but before the file moved
2.2.2. pre-flight check table seems only concern `/applied/` ARs, should add line(s) for situations where ARs are found in either `/skip/` `/pending/` which should immediately go directly to K6; i propose to simply add these 2 lines (improve if needed): `| Tab 2+3 | ✅ (in `/skipped/` or `/pending/`) | — | — | Skipped/saved | Silently skip (K6-equiv.) |` `| Tab 2 only | ✅ (in `/skipped/` or `/pending/`) | — | — | Skipped/saved | Silently skip (K6-equiv.) |`
2.3. back to the "spent too much time" issue, i highly suspect you compacted AND did NOT re-read CLAUDE.md & mandatory files (i dont see declarations) AND somehow lost the context to re-read cc_reminder.md for every loop start
we need a safer mechanism to enforce the time limit
i propose to create a sub-agent running in parallel:
Sub-agent 1 (runtime): when session initialises (add to § Session Initialisation if you see fit), create this sub-agent to start a stop watch → every 10 min (i.e. when stop watch shows multiples of 10 min, so no memory needed), this sub-agent will force you (main agent) to pause immediately → re-read cc_reminder.md → follow it (e.g. re-read other files) → continue where you left off
this ensures cc_reminder.md is re-read frequently enough, even if a loop stalls for a long time
2.4. we previously introduced the time limit in § External Portal Instructions, but you apparently failed to honour it, possibly due to compaction (voiding the timer)
i propose to create two more sub-agents:
2.4.1. Sub-agent 2 (entire external portal ops): get current job employer + role → get time indicated in § External Portal Instructions (≤10 min for now; don't hard-code; might vary) → start timer → when time's up, prompt you (main agent) to immediately stop if still on that job employer + role → treat as "struggling" per § External Portal Instructions
2.4.2. Sub-agent 3 (external portal page): get current job employer + role → get current external portal page URL + title → get time indicated in § External Portal Instructions (≤3 min for now; don't hard-code; might vary) → start timer → when time's up, prompt you (main agent) to immediately stop if still on that page (check URL + title) → treat as "struggling" per § External Portal Instructions; if no longer on that page but still same job application, loop again with new timer from "get current external portal page URL + title" for the new page
for 2.3 & 2.4: if sub-agents are inefficient (background running consume large amount tokens?) feel free to suggest alternative solutions
2.5: if sub-agent is adopted for 2.3, we needa review cc_reminder.md and update if needed as it's now solely for re-reading during S0; though it still has to remain lightweight as it's now even more frequently re-read (S0+every 10min)
---
3rd issue is rather minor as it might be fixed by above measures
as you compacted but didn't re-read mandatory files
ARs have deteriorated, evidenced in NuEraDesignStudio_PracticeManager_202605231232.md and Expertech_BusinessAnalystCMS_202605231250.md where the AR structure is all gone and the CL isn't even in §6
i propose to add in cc_reminder.md to check if last applied AR had this text: `## 6. Cover Letter`
---
4th issue is that the CLs still have rule violation, mostly `—`
i saw you chat text (again, shouldn't exist in the first place but minor issue):
`Now applying via Quick apply. Need to check for CL violation first — P3 contains an em dash ("—"). Let me fix that in the CL before submitting.`
despite that, that file (Expertech) still has a dash, as well as CGCRecruitment_DesignCoordinatorSeniorDocumentationLead_202605231202.md where the AR didn't even deteriorate (context likely not as compromised)
i propose a more technical approach to scan for `–` `—` `+`
read '/Volumes/FURY 2TB/Fury Documents/VS Code/Culous Codes/ajap_logs_creator.py' which is a .py script i built to create logs and simultaneously check for rule violation
you may adopt the .py approach to have cc_reminder.md to mechanically search for their existence in last AR below the line `## [no.] Cover Letter` (since it might not be `6.` if you deteriorated)
if helpful, save it as a .py script in `/.claude/` or wherever appropriate so the script can be directly run w/o having to bake into ajap.md
---
5th issue is that apparently you stopped after job card exhaustion, but i suspect this won't happen if cc_reminder.md is frequently enforced as you would have re-read ajap.md and know what to do (S7)
---
6th issue is some jobs are incorrectly skipped
e.g. `/skipped/LaundyHotels_SeniorGraphicDesigner_202605231240.md` scored 60 but NOT external portal (SEEK "Quick apply") and should have been applied
but again i suspect this won't happen if above updates were applied and you didn't lose context of ajap.md
---
7th issue (new; added after first run)
you compacted after first read (w/o this issue) but before that you said:
```
Reviewing context-window issues first, then reading mandatory files.

**Issues identified from this context window:**

1. **Post-compaction mandatory file re-reads NOT declared** — The session summary notes compaction occurred. CLAUDE.md requires all 7 mandatory files to be re-read before any action. In this context window I went straight to submitting NuEra without declaring any re-reads.

2. **gcl.md not re-read before Expertech CL** — No declaration of re-reading gcl.md before drafting the Expertech BA CMS cover letter.

3. **Expertech AR file still contains em dash** — I fixed one em dash before textarea submission but the AR file at `applied/Expertech_BusinessAnalystCMS_202605231250.md` still has `— capabilities that translate` in Section 3.

...
```
if this is true, this is more disastrous because if you have truly re-read the mandatory files and just didn't declare, i couldn't understand why these errors took place

## Your Mission

- Critically reflect then concisely tell your thoughts on above but dont edit any files
- Raise any other issues you found

## Critical Notes

- If highly necessary, selectively read '/Volumes/FURY 2TB/Fury Documents/GitHub/dupbus-ceztuc-7cufVe/seek/investigation/1st_max_long_run_202605231637.txt'; if unavoidable, read it normally; this file contains all your chat text in 2 msgs (separated by a `?` sign that leads to my reply) + enumeration of current ARs in `/skipped/` (do NOT read them; you can see them in the folder)
- Generate your responses in a single .md file in `/seek/investigation/`, must be optimised for my easy reading & replying using #numbered bite-size bullets
- IMPORTANT: your chat response will NOT be read by me; i'll only read the .md file you generate

---

# Numbered Lists (`#numbered`)

## Format

Number every single item as below especially but not limited to when prompted `#numbered`. As shown, sub-items (2nd level onwards; e.g. `- 1.1. xxx...`) MUST follow bullets to prevent line break failure (displaying as single line; `1. xxx... 1.1. xxx...`).

## Example List

e.g. 4-level indentation (build up to 5 levels but only if necessary):

```
1. xxx...
- 1.1. xxx...
- 1.2. xxx...
  - 1.2.1. xxx...
    - 1.2.1.1. xxx...
    - 1.2.1.2. xxx...
  - 1.2.2. xxx...
2. xxx...
```

## Notes

- Each level MUST have more than one item
  - If a sub-item would be alone, don't build that level —— include it in the parent instead
  - e.g. In above example, if 1.2.1.2 is not necessary (leaving 1.2.1.1 alone), don't build the 4th level and just include 1.2.1.1 in 1.2.1
  - If a sub-item level (e.g. 4th level: 1.2.1.1 & 1.2.1.2) is established, ensure its parent (e.g. 3rd level: 1.2.1) exists
- Must use hardcoded manual numbering, never markdown auto-numbering
- Strictly ensure NO sentence/bullet unnumbered e.g. `- xxx...`
- Each number must have a dot `.` between it and the text e.g. `1.1. xxx` instead of `1.1 xxx`
- The dot is not needed when referring to it e.g. "Item 1.1 detailed xxx..."

## Reply

- On long responses above 100 words (even not prompted `#numbered`):
  - Follow above format (rather than unnumbered bullets) for **easy reply**
  - Instead of `re "xxx..."`, I can refer by `re 1.2.1` or just `1.2.1:`
- Avoid number repetition throughout a discussion issue/topic
  - e.g. You responded w/ above example list as 5th artefact (#05), I replied `1.2.1: xxx...`
    - You must NOT respond as `1.2.1. xxx...` again as that clashes w/ the original pt in #05, i.e. two counts of `1.2.1` on same issue
    - Instead start a new pt w/ continuing number (n+1), where n = last pt in #05
    - Example list above ends w/ pt 2, so next response (if on same issue) starts w/ pt 3:

```
3. Re 1.2.1, xxx...
- 3.1. xxx... (sub-items if needed)
- 3.2. xxx...
...
```