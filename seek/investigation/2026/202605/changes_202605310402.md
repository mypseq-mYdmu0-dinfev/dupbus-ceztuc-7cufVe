# AJAP Changes — `changes_202605310402.md`
*Executes all decisions from `query_202605310340.md` (reply to `response_202605302236.md`). All items below were EXECUTED this turn unless tagged 🚫NOT-DONE.*

---

## Thoughts (mission item 1)

The run's core failure was not the SA going rogue (post-compaction drift) but **MA being structurally blind to it** — it only ran `find /applied -name "⏳_*"`, so 195 rogue/`skipped` files and a false "all exhausted / no deterioration" claim went unnoticed. The highest-value change here is therefore the **broad `latest_TS` file-window check + batch-name rogue tripwire** (Issue 2), now baked into both `main_ajap.md` and `mini_ajap.md`. Everything else (gcl move, queue.md, renames, heartbeat reconciliation, file-watch wake) is sound and validated; I implemented all of it. The only thing I did NOT do is hard-create an *active* auto-resume Routine (see C9 + close_ — it cannot message a live session; I configured it PAUSED with a guard-and-resume design for you to review).

---

## A. Renames (filesystem; clean `git mv`, NOT Void-Rule `❌_`, per your "cleanly rename")

- A1. `seek/runtime/runtime_*.md` → `rlog_*.md` (9 files, incl. 2 post-22:32 parallel-run logs `…302239`, `…302302`).
- A2. `seek/investigation/2026/202605/session_*.txt` (4) → moved to `seek/runtime/rchat_*.txt`. (`runtime_chat_202605302038.txt` → `rchat_202605302038.txt` you had already done.)

## B. New file

- B1. `seek/queue.md` — Qi fallback table (you then refined the param column to "view-only"). Order `01,02,03` = business-analyst, ui/ux, business (your BA↔business swap). Each URL carries `&tags=new`; AJAP runs `[Qi]n` (URL as-is) then `[Qi]p` (strip `&tags=new`). Includes New-to-you enforcement (rule 2) and the gated All-Qi-Exhausted edge case (`Active: false`).

## C. Governing-file edits (executed via Edit/Write tool)

- C1. **Folder → `gcl/`** in `seek/CLAUDE.md`, `seek/context/ajap.md`, `seek/context/main_ajap.md`, `seek/context/mini_ajap.md`, `career/CP_notes.md`: every `/seek/{applied,pending,skipped}/` (and bare `/applied/…`) → `/seek/gcl/…`. `/seek/ccl/` left intact. Verified: 0 non-gcl leftovers.
- C2. **`cc_reminder.md` → `mini_ajap.md`**: `git mv` of the file + full content rewrite (see C6); all live references updated in `seek/CLAUDE.md` (8th mandatory file), `ajap.md` (S0.1), `main_ajap.md`. A single "Formerly `cc_reminder.md`" breadcrumb is intentionally retained in the file header.
- C3. **`ajap.md` A6** rewritten to consume `/seek/queue.md` (removed the 3 hard-coded fallback URLs); **S7.2.2** updated to the queue; **C2 spec + S0.3** updated to `[current_TS] 🎯[N]` with "N = this session only".
- C4. **`runtime_` → `rlog_`** in `main_ajap.md` (session-log creation + incident-append paths).
- C5. **`main_ajap.md` — FULL REWRITE** (Large-Proportion edit per `coding.md`). Key changes vs prior:
  - Heartbeat reconciled to **Monitor + `sleep` + file-watch** (removed all `ScheduleWakeup` *usage*; kept one "is NOT used" note). Canonical Monitor command embedded (20s file-watch poll on `/seek/gcl` + `INT` tick; `INT=300` default, `60` when approval pending).
  - **`session_start_TS` + `latest_TS`** stamped to `ma_state.md` at Session Start; **broad file check** over `/seek/gcl/{applied,pending,skipped}` since `latest_TS` on every wake; **rogue tripwire** (batch-name / multi-job AR → instant retire).
  - **C2 = session-only**, derived from files (`count of non-❌_ ARs since session_start_TS`), prefixed with `current_TS`.
  - SA spawn prompt: added "NEVER batch-process" + `newtoyou=[n]` in the loop-report format.
  - Deterioration signals: added batch-rogue + fabricated-external-reason.
  - Post-Compaction Recovery: re-spawn Monitor (not ScheduleWakeup); recover state from `ma_state.md`.
  - Session-Start step 2: purge stale Monitor + note legacy `ma-heartbeat-cc-reminder` retired.
- C6. **`mini_ajap.md` — FULL REWRITE** (renamed from cc_reminder): lean checklist with Active Check, rogue tripwire, Monitor+file-watch heartbeat, broad `latest_TS` check, dynamic 60/300, session-only C2.
- C7. **`seek/investigation/CLAUDE.md`**: naming convention updated — chat history `session_*` → `rchat_*` in `/seek/runtime/`; runtime logs `rlog_*`.

## D. `gcl/skipped` cleanup (your 8.2)

- D1. Voided (`❌_` prefix, reversible, NOT deleted) **195** non-archive `skipped/` files with TS < `202605302232` — the rogue batch files + unverified legit skips (you opted to let them re-process). **28** files ≥ 22:32 (parallel run) LEFT untouched. `skipped_archive/` untouched.

## E. Scheduled tasks (Routines)

- E1. Legacy `ma-heartbeat-cc-reminder`: set to DEPRECATED + disabled + prompt neutered (no delete API — you delete it in the app).
- E2. 🟡 Created `ajap-auto-resume` (hourly `0 * * * *`) but **PAUSED (disabled)** with a guard-and-resume prompt. See close_ caveat — it CANNOT message a live session; it can only resume a dead one as a fresh MA.

---

## Self-Challenge (mission: independently review/assess/challenge)

- X1. **gcl move is a hard dependency I did NOT perform** (you do it in Finder). Until then, every updated path + the Monitor `find /seek/gcl` points at a non-existent folder → **next AJAP run breaks / heartbeat file-watch silently no-ops.** 🔴 Mitigation: prominent close_ blocker. The Monitor `find` is `2>/dev/null` so it degrades to tick-only (no crash), but the broad-check `ls` and routing writes WOULD fail. **Do the Finder move before any next run.**
- X2. **File-watch Monitor is unproven** (the `sleep`-only tick is proven; the combined `find -newer` loop is new). Risk: if `date`/`find` behave oddly in the Monitor shell, ticks could stall. Low risk (plain bash), but flagged. The proven `sleep` tick is preserved inside it.
- X3. **Broad check cost**: listing+validating all new files each wake costs more tokens than the old `⏳_`-only check. Acceptable trade for catching rogues; bounded by `latest_TS` window so it stays small in steady state.
- X4. **C2 from files**: deriving N by counting files each loop is reliable but assumes the gcl folders exist (X1) and that filename TS is parseable (12-digit). Robust for current naming.
- X5. **Auto-resume guard** could misfire: if a live AJAP goes >20 min without updating `latest_TS` (e.g. SA on long external-portal work while MA idle), the guard might wrongly judge it "dead" and spawn a 2nd MA. The heartbeat updates `latest_TS` each clean wake (≤300s), so 20 min is a safe margin — but it's PAUSED pending your review regardless.
- X6. **Historical comms NOT edited**: prior `investigation/` `audit_/changes_/close_/query_/response_` and `rchat_`/`rlog_` files still say `cc_reminder.md`, `/seek/applied/`, `runtime_`. Deliberate — they are dated records; editing them would falsify history. Only LIVE governing files were changed.
- X7. **Memory store NOT edited** (root §8.6 forbids it). I checked: the display_N memory note doesn't reference folder paths, so no stale path there. If any memory does, you must update it manually.
