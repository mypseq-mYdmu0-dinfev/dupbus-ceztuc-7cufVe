# Probe One-Pager

This file is a throwaway liveness probe for the deliverable gate in
`cscpt/dlint_quick.py`. It carries enough ordinary prose to clear the substance
floor, sits outside every protocol tree, and is named like something a third
party might read, so the classifier should record it as a deliverable that owes
a FULL dlint run. It is deleted the moment the probe finishes.
