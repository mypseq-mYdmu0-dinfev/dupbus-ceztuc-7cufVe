8. This is prose with the americanism color in it.
- 8.1. A proper numbered sub-item, fine.
- a bad bullet that breaks #numbered
Inline code `"color": "colour"` and `behavior` should NOT flag.
```
def f():
    color = "behavior"  # code block, must be ignored
    x = a - b
```
9. They said "done," which is a Hart red.
