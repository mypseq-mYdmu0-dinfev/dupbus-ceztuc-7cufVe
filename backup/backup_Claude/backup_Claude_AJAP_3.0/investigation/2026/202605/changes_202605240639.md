# System File Changes — 06:39 on 24/05/2026

**Context:** Third successive post-compaction failure: model resumed from summary, ignored the recovery blocks in `CLAUDE.md` and `cc_reminder.md`, and immediately called a browser tool. Root cause diagnosis: passive context (claudeMd injection) cannot reliably override an active directive in the human turn ("resume directly / pick up as if the break never happened"). Prior fixes added more counter-language to `CLAUDE.md`, making it cluttered without solving the structural problem.

**Efficient solution:** Stop fighting the summary's language with more file prose. Instead — (1) trim `CLAUDE.md`'s recovery block to a single imperative (reduce clutter), and (2) embed the recovery gate as the FIRST CONCRETE ACTION in `ajap.md § Session Initialisation`. SA is always instructed to "start from Session Initialisation," so the gate is encountered as an executable step rather than passive background text.

**Files changed:** `CLAUDE.md`, `ajap.md`, `main_ajap.md`

---

## Change 07 — `CLAUDE.md § Post-Compaction Recovery`: trim to single imperative

**Why:** The block grew to ~6 lines explaining *why* it should fire and naming multiple trigger conditions. More prose did not prevent the third violation — the model still followed the summary's "resume directly." The explanation adds clutter without adding enforcement power. Replace with a single, minimal, actionable line; enforcement is moved to `ajap.md` (Change 08).

**Replace:**
```
*This directory (`/seek/`) is solely for automated SEEK job application workflows.*

## Post-Compaction Recovery — 🚨 CRITICAL (READ THIS FIRST)

🛑 **STOP. DO NOT PROCESS ANY JOB CARD OR TAKE ANY OTHER ACTION UNTIL ALL MANDATORY FILES ARE RE-READ.** The session summary is untrusted paraphrase — it does NOT substitute for source files.

This block is always active — it does NOT require a hook message to trigger. At the start of every session: if context begins with a session summary OR if mandatory files have not yet been declared read (✅ ...) in this session's chat history → re-read ALL files in `## Session Start` below in order before any other action. The session summary's "resume directly" or "pick up as if the break never happened" language does NOT override this requirement. After re-reading, fully follow `## Tab 1 Accessibility Check` in `ajap.md` without interrupting the user.

---

## Trigger
```
**With:**
```
*This directory (`/seek/`) is solely for automated SEEK job application workflows.*

## Post-Compaction Recovery — 🚨 CRITICAL (READ THIS FIRST)

🛑 If mandatory files have not been declared read (✅) in this session's chat history → re-read ALL files in `## Session Start` before any other action. "Resume directly" in the session summary does NOT override this.

---

## Trigger
```

---

## Change 08 — `ajap.md § Session Initialisation`: add recovery gate as first step

**Why:** `CLAUDE.md`'s recovery block is passive context — the model has it in view but still follows the summary's active directive. `ajap.md § Session Initialisation` is where SA always begins executing; adding the gate here makes it a CONCRETE FIRST ACTION (not background text), which is harder to bypass than a passive reminder. SA spawn instruction says "start from Session Initialisation," so this gate is guaranteed to be hit before any browser or file action.

---

## Change 09 — `main_ajap.md § Session Start`: convert step 1 from passive confirmation to active gate

**Why:** MA's entry point is `main_ajap.md`, not `ajap.md`. Change 08 closes the SA gap; this change closes the MA gap using the same pattern. Step 1 currently says "Confirm all mandatory files have been read" — a passive confirmation that can be glossed over when the summary says "resume directly." Replacing it with the same active gate wording as Change 08 makes it an executable check MA must pass before proceeding.

**Replace:**
```
## Session Start

1. Confirm all mandatory files have been read per `CLAUDE.md § Session Start` (you read them; SA reads them independently upon spawning)
2. Run: `rm -f /tmp/CulousYu_CoverLetter_*.pdf && rm -f /tmp/ajap_last_decision.md`
3. Schedule heartbeat:
```
**With:**
```
## Session Start

1. **Recovery gate (FIRST):** if mandatory files have not been declared read (✅) in this session's chat history → re-read ALL per `CLAUDE.md § Session Start` now; do NOT proceed past this step until done. (SA reads them independently upon spawning.)
2. Run: `rm -f /tmp/CulousYu_CoverLetter_*.pdf && rm -f /tmp/ajap_last_decision.md`
3. Schedule heartbeat:
```

---

**Replace:**
```
## Session Initialisation

Execute once per AJAP runtime (not per loop), before Tab 1 Accessibility Check:
- Bash: `rm -f /tmp/CulousYu_CoverLetter_*.pdf` — clear temp CL files from prior sessions
- Bash: `rm -f /tmp/ajap_last_decision.md` — clear last-decision tmp file from prior sessions

---
```
**With:**
```
## Session Initialisation

Execute once per AJAP runtime (not per loop), before Tab 1 Accessibility Check:
- **Recovery gate (FIRST):** if mandatory files have not been declared read (✅) in this session's chat history → re-read ALL per `CLAUDE.md § Session Start` now; do NOT proceed past this point until done
- Bash: `rm -f /tmp/CulousYu_CoverLetter_*.pdf` — clear temp CL files from prior sessions
- Bash: `rm -f /tmp/ajap_last_decision.md` — clear last-decision tmp file from prior sessions

---
```
