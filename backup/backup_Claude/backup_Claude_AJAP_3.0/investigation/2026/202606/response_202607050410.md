# Response to query_202607050410.md
*Session wrapped for the Fable overhaul. Continues from response_202606272232 (last pt 29 → start 30). Kept short —— my context is near-full.*

## Delivered

30. `#close` done: `investigation/2026/202606/close_202607050412.md` (start-month = June) —— coverage, decisions, and EXTRA-detailed unresolved issues incl. what no `changes_` captured (the un-applied MA_hb lean rewrite, the two un-scrutinised quick fixes, the SDK auth gate).
31. Fable kickoff query drafted: `investigation/2026/202607/query_202607050412.md` (July folder, per your note).

## Corrections / flags (mission: my thoughts)

32. re your session-setup —— CORRECTION: "root + investigation/" is not enough. Fable needs BOTH `dupbus-ceztuc-7cufVe/` (read current files + this query) AND the sibling `AJAP_repo/` (to build). Do NOT add `seek/` (would risk triggering AJAP).
33. re your reading-list —— I cut it hard: the ~18 quick-fix comms turns are HISTORY, already folded into the current file state + the close. Fable reads the close + current files + 3 strategic artefacts only (`response_202606091942`, the MA_hb lean proposal, `audit_202606272146`). Big token saving, nothing lost.
34. 🔴 Live finding from the rlogs that changes things: LinkedIn credential auto-login is NOW BLOCKED by the harness classifier (`rlog_202607042122`) —— so our recent "Sonnet types credentials directly" fix (S3.2.2) is already VOID in practice. I did NOT touch it (seek/ is off-limits, AJAP running); flagged for Fable to solve in the rebuild.
35. 🔴 The strongest rebuild signal: the sub-agent's Top-5 root causes (in the query §5.2) show the SDK inherently dissolves the whole racy-async-approval + compaction + no-liveness-signal failure class —— i.e. this is worth doing, but Fable must FIRST verify the SDK's auth/billing on your Max plan (it may need an API key + credit pool, not the flat subscription).

## Note

36. I did not touch anything in `seek/` context this turn (only wrote comms in `investigation/`). AJAP keeps running untouched.
