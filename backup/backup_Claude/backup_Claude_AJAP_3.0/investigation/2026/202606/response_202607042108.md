# Response to query_202607042108.md
*Continues from response_202607042104 (last pt 31 → start 32). The gate-enforcement fix is done.*

## re Preamble —— does your hardening suffice?

32. Necessary, but NOT sufficient on its own.
- 32.1. Your bolded "**MUST** run `cl_check`" (in `ajap.md` + `main_ajap` Opus-mode) strengthens the SA-side gate —— good.
- 32.2. But a COMPACTED/degraded SA is exactly the one that ignores a "MUST". An instruction cannot enforce itself on an agent that has lost the instruction.
- 32.3. So it needed an MA-side backstop that does NOT depend on SA discipline —— which I've now added.

## re 30/31 —— done (`changes_202607042112`)

33. MA now runs `cl_check.py` on EVERY new `Outcome: Applied` AR in its broad-check.
34. Any HARD failure = the SA submitted a compromised CL → the SA either skipped its gate (30.1) or has compacted (30.2) → RETIRE it at once + rlog + `⚠️`-flag the AR.
35. This is a new "Retire immediately" trigger —— it would have caught Pendulum (MA would cl_check the Applied AR, see the em-dash/P.S./opening failures, and retire the SA that turn).

## The defence is now layered

36. Four independent layers, so a slip must defeat ALL of them:
- 36.1. SA runs `cl_check` before declaring Applying (`ajap.md` gate).
- 36.2. SA waits for MA approval → MA runs `cl_check` before approving (pre-submit).
- 36.3. SA submits without approval → approval-gate-bypass retire.
- 36.4. NEW: MA `cl_check`s every Applied AR → HARD fail = retire (catches whatever slips 36.1–36.3).

## One honest limit

37. A CL that defeats all four layers is ALREADY submitted —— it cannot be un-sent. MA retires the SA to stop FURTHER bad submits and flags the AR; that is the best achievable short of the SDK rebuild (which gates in code, deterministically).
38. Net: the gate-skip is now enforced as far as an instruction-driven system can. AJAP is meaningfully safer to run than before this turn.
