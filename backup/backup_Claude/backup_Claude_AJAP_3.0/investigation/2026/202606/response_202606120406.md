# Response to query_202606120406.md
*Renamed the `*` file; reformatted this session's `changes_` files to #replace*

1. Rename —— done. `changes_202606120346*.md` → `changes_202606120346.md` (the unreadable glob char is gone; tag it red as you like). Its header note was updated to drop the `*`.

2. Read `replace.md` —— noted the format: each edit is a `## Change` block with a verbatim **Replace:** (pre-fix) and **With:** (post-fix) code-fenced snippet, carrying ≥3 surrounding lines so the location is unambiguous and the change is cleanly revertible (swap **With** back to **Replace**).

3. Reformatted all four `changes_` files CREATED THIS SESSION to that format (filenames unchanged):
- 3.1. `changes_202606080108.md` —— already had a Replace/With; widened its snippet with surrounding context for a unique anchor.
- 3.2. `changes_202606091942.md` —— groups A (Opus edits)/C (Opus-mode note) now have verbatim Replace/With; group B (`cl_check.py`) is a NEW file (no pre-fix content → revert = delete the file), stated explicitly.
- 3.3. `changes_202606120333.md` —— all 4 `cl_check.py`-wiring edits now Replace/With.
- 3.4. `changes_202606120346.md` —— all 7 snippets (heartbeat marker-couple ×5, submit-expiry ×2) now Replace/With; scope-assumption header retained.

4. Left untouched (NOT created this session, per your "this session" scope): `changes_202606050721.md`, `changes_202606060503.md`. Say the word if you want those reformatted too.

5. Revert is now mechanical for every change this session: in each file, replace each **With** block back with its **Replace** block. Cross-edit note recorded where one edit's anchor contains another's added text (`changes_202606091942` C-before-A2).
