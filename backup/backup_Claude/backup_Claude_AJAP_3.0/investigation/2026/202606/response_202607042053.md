# Response to query_202607042053.md
*Continues from response_202607042008 (last pt 13 → start 14). You were right; I was wrong —— reverted.*

## re 4 —— my diagnosis was wrong

14. Conceded. `ajap.md` L349 confirms the CL (§6) is the LAST AR section, and S6.2.4 says answered questions APPEND to §3 —— so Henderson/Leisures' `## 7. Employer Questions` is the REAL violation, not a legitimate section.
15. Why I edited the scripts (your "unclear"): I wrongly assumed the `## 7` was legitimate, so I "fixed" cl_check to skip past it. That was the mistake —— it MASKED a real violation. Reverted both `cl_check.py` and `ajap_logs.py` to their original logic.

## re 11 —— never distrust cl_check.py

16. Agreed, and I retract the "cried wolf / false positive" framing entirely.
17. cl_check did its job correctly: the P.S. was NOT the file's final line because a `## 7` wrongly followed the CL —— a true HARD violation.

## What I changed this turn (`changes_202607042058`)

18. Reverted `cl_check.py` + `ajap_logs.py` DETECTION to original (P.S. must be the file's final line). Verified: Henderson flags again; a clean AR still passes.
19. Per your update, improved ONLY cl_check.py's terminal MESSAGE (no mechanical change): the P.S. violation now reminds the agent that the P.S. must be last AND that employer questions APPEND to the END of §3 (never a new section, never replace §3).
20. Strengthened `ajap.md` S6.2.4 (the real culprit): explicitly BANS creating a new post-CL section (e.g. `## 7`), and ties it to the cl_check invariant (§6 stays last so its P.S. is the final line).

## The corrected root cause

21. Two behaviours, both agent-side (not the script):
- 21.1. Henderson/Leisures: the SA appended answered questions as a NEW `## 7` instead of into §3 —— cl_check correctly flags it; the S6.2.4 reinforcement + the guiding message target it.
- 21.2. Pendulum: a genuine gate-SKIP —— the SA submitted without running cl_check at all (near its 1hr retirement). That remains a discipline/compaction issue; the systemic fix is the stringent SA1-compaction-detection + ruthless-retire we discussed, plus never approving a submit without a fresh cl_check pass.

22. So: the script stays trusted and unchanged in behaviour; the fix is in ajap.md (where questions go) + a more helpful message when it does fire.
