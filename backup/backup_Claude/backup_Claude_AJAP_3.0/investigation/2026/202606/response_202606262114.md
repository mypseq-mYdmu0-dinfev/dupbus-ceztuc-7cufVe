# Response to query_202606262114.md
*Month-foldering the archives is safe; 3 wording clarifications applied*

1. Good news: your plan is safe. AJAP already treats the archives correctly for month-nesting —— I only had to make three wording clarifications (`changes_202606262114`) so nothing is ambiguous after you reorganise. Go ahead and sort `applied_archive/` and `skipped_archive/` into `[YYYYMM]/` folders.

2. Why almost nothing needed changing (I checked every dependency).
- 2.1. Duplicate-detection (`ajap.md` K6) already searches "incl. sub-folders", so it will still find an archived AR whether it sits in `*_archive/` or `*_archive/202606/`. Cross-session de-dup keeps working (and K6 only looks back 30 days anyway).
- 2.2. Session counts (the `🎯N` tally, the heartbeat broad-check, S0.1.2) only ever look at THIS-session files (by filename timestamp) or explicitly skip `*_archive` —— old archived files are excluded regardless of how they are nested.
- 2.3. `ajap_logs.py` only ever scanned the files directly inside `applied/skipped/pending/`, never the archive —— so your analytics are unaffected.
- 2.4. `cl_check.py` takes explicit file paths —— unaffected.

3. What I changed (3 small wording edits, all revertible).
- 3.1. `ajap.md` S0.1.2 —— "excl. `_archive` sub-folders" → "excl. anything under any `*_archive/` folder, including its `[YYYYMM]/` month sub-folders".
- 3.2. `queue.md` Step A —— "(NOT `skipped_archive`)" → "(NOT anything under `skipped_archive/`, including its `[YYYYMM]/` month sub-folders)".
- 3.3. `README.md` —— documented the new `*_archive/[YYYYMM]/` structure.

4. Two operational tips for your manual sort.
- 4.1. Use **Move** (`mv` / Finder drag within the same drive), not copy —— move preserves each file's timestamps, so the live Monitor's `newfile` watch will NOT mistake them for new ARs. (A copy would reset timestamps and could spam wakes.)
- 4.2. Keep the folder names EXACTLY `applied_archive/` and `skipped_archive/`; only ADD `[YYYYMM]/` inside them. Ideally pause AJAP during the bulk move (tidier, though `mv` is safe even if it's running).
