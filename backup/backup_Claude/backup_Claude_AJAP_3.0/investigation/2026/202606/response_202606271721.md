# Response to query_202606271721.md
*Change 01–04 revised, Task 1 & 3 done, Task 2 audited (no changes) —— one real blocker (B1)*

1. Change 01–04 —— all revised as asked (`changes_202606271704` UPDATED, not new).
- 1.1. 01: S3.2 header reverted to the short one-line form; the mandatory declaration + the new "invalid/empty site doesn't count towards the max-10" rule now sit as two concise points in S3.2 Notes.
- 1.2. 02/03: removed the extra `(**MANDATORY…**)` bracket; folded "Mandatory source —— never skip" into each line's EXISTING trailing bracket.
- 1.3. 04: the logged-out clause is now a sub-bullet under "For blockers…", not a dash-extension of it.

2. Task 1 —— `changes_202606271704.md` updated in place (3 snippets now; the header change dropped since it nets to zero). Did not create a new one.

3. Task 2 —— audited, nothing changed (`audit_202606271729`; no `changes_`, per your instruction).
- 3.1. Token baseline: `ajap.md` 10.7k · `main_ajap.md` 6.9k · `MA_hb.md` 2.5k (≈20k of your 74.5k).
- 3.2. Your suspicion is correct —— there is real redundancy. Realistic lean: **~2.6–3.5k tokens (~13–17%)** without losing any operative rule.
- 3.3. My thoughts on priority: the biggest win is `MA_hb.md`, because it is re-read on EVERY heartbeat and every card —— its tokens are paid over and over, so trimming it cuts *recurring* churn, which is the part that actually drives mid-run compaction (more than the one-time session-start load). The top targets are the heartbeat-mechanism and post-compaction blocks that are written in full in BOTH `MA_hb.md` and `main_ajap.md`; `MA_hb.md` only needs the checklist + a pointer.
- 3.4. ⚠️ The audit also caught a real CONTRADICTION (not redundancy): the SA poll-retry count is `60×/900s` in `ajap.md` but `20×/300s` in `main_ajap.md`'s spawn prompt. Worth fixing regardless of trimming.
- 3.5. I did NOT touch anything —— say the word and I'll do a careful leaning pass (fix the contradiction first, then dedup-to-pointers one file at a time with a re-read check).

4. Task 3 —— cic_bot.md credentials detached (done; no `changes_`, outside AJAP).
- 4.1. Created `universal/cic_login.local.md` (git-ignored —— verified) holding the credentials; `cic_bot.md § Logins` now points to it and keeps the Opus-safe behaviour: CC READS it and PRINTS the email/password in chat for you to paste (never types them).
- 4.2. ⚠️ Security flag: those credentials were already committed in `cic_bot.md` (git history commit `890be8e`) and #synced to GitHub, so they are already exposed in history. Detaching stops FUTURE commits, but does not scrub the past. Since it is a deliberately risk-free throwaway (no PII, Apple Hide-My-Email, MFA elsewhere) the stakes are low —— but I'd still rotate that password when convenient (and history-scrub if you care). Also: the git-ignored file is NOT backed up by #sync, so keep your own copy.

5. B1 —— the blocker this surfaced (needs your ruling).
- 5.1. `cic_bot.md` ladder step 5 is explicit —— CC must NOT type a password to authenticate into any field, "harness-prohibited even for a throwaway account the user supplies/authorises". That is a blanket CC rule, not Opus-only.
- 5.2. But my earlier AJAP edit (`changes_202606271643`) instructs AJAP (Sonnet) to "enter the email & password… submit" at a LinkedIn/Glassdoor login wall —— i.e. to type credentials. If the harness rule in 5.1 applies to Sonnet too, that instruction simply won't execute, and "mandatory LinkedIn/Glassdoor + auto re-login" cannot actually hold when logged out.
- 5.3. You believe Sonnet AJAP CAN type credentials (you have the runtime experience; I don't). So I have NOT ripped anything out —— I only softened the S3.2.2/S3.2.4 wording to point at "S3.2 Notes" rather than spell out the typing. Please confirm which is true:
  - 5.3.1. Sonnet AJAP genuinely CAN type the login (you've seen it work) → I keep the type-and-continue path; cic_bot.md step 5 is then Opus-only in practice.
  - 5.3.2. It cannot (harness blocks it) → then for a logged-out wall AJAP should NOT type; instead note the source as "logged-out, skipped" in the AR and proceed (autonomy preserved, "mandatory" relaxes to "attempt; note if logged out"). Summoning you would break the unattended run, so I'd avoid it.
- 5.4. My lean: confirm 5.3.1 empirically on the next run (does AJAP actually log in?). If it ever stalls at a login wall, we adopt 5.3.2.

6. Everything reversible: Change 01–04 (3 snippets in `changes_202606271704`); Task 3 (delete `cic_login.local.md` + the two pointer edits). Task 2 made no changes at all.
