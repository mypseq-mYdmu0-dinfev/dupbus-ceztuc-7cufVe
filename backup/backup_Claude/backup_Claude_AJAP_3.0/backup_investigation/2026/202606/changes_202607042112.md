# Changes —— `changes_202607042112`
*Systemic gate-enforcement fix (30.1 + 30.2), greenlit in `query_202607042108`. The SA-side "MUST run `cl_check`" (your manual hardening) is necessary but a degraded/compacted SA can ignore it —— so this adds the MA-side BACKSTOP: MA runs `cl_check.py` on EVERY new `Outcome: Applied` AR; any HARD failure = the SA submitted a compromised CL (gate skipped or compacted) → RETIRE at once. This catches the exact Pendulum-type slip. Standard `replace.md` direction.*

---

## Change 01 —— `main_ajap.md § Heartbeat` broad-check: cl_check every new Applied AR
**Replace:**
```
3. **Broad file check:** list every non-`❌_` file in `/seek/gcl/applied|pending|skipped` with filename-TS ≥ `latest_TS`. Validate each (single-job AR, correct folder, CL `## 6.` if applied, no rogue name). Any rogue → retire SA per tripwire. **Actively check SA1 (do NOT wait for a loop report):**
```
**With:**
```
3. **Broad file check:** list every non-`❌_` file in `/seek/gcl/applied|pending|skipped` with filename-TS ≥ `latest_TS`. Validate each (single-job AR, correct folder, CL `## 6.` if applied, no rogue name). Any rogue → retire SA per tripwire. **CL backstop (30.1/30.2 —— run on EVERY new `Outcome: Applied` AR in the window):** `Bash: python3 /seek/context/cl_check.py [AR_PATH]` —— exit 1 (HARD) = the SA submitted a compromised CL (it skipped its own gate OR has compacted): RETIRE the SA at once (no correction), rlog the violation + AR path, and append a `⚠️` note to the AR (already submitted → cannot void). This is the enforcement the SA-side `cl_check` mandate cannot guarantee. **Actively check SA1 (do NOT wait for a loop report):**
```

## Change 02 —— `main_ajap.md § SA Deterioration Signals` add the retire-immediately trigger
**Replace:**
```
- SA submitted an application (AR reached `Outcome: Applied`) with NO MA "Submit then proceed to next card" approval recorded for it —— the `⏳_` CL-review gate was bypassed.
- SA has been running for 1hr⁺ from spawn
```
**With:**
```
- SA submitted an application (AR reached `Outcome: Applied`) with NO MA "Submit then proceed to next card" approval recorded for it —— the `⏳_` CL-review gate was bypassed.
- A NEW `Outcome: Applied` AR (filename-TS ≥ `latest_TS`) FAILS `cl_check.py` (HARD) —— the SA submitted a compromised CL (gate skipped or SA compacted); already submitted (cannot void) → retire at once, rlog, and `⚠️`-note the AR. Caught by the § Heartbeat broad-check CL backstop.
- SA has been running for 1hr⁺ from spawn
```

## Change 03 —— `MA_hb.md` broad-check: brief pointer to the backstop
**Replace:**
```
- **Broad file check (NOT just `⏳_`):** list every non-`❌_` file in `/seek/gcl/applied`, `/gcl/pending`, `/gcl/skipped` with filename-TS ≥ `latest_TS`. Validate each: single-job AR, correct routing folder, CL `## 6.` if applied, no rogue name (per tripwire). Enumerate ONLY by filename-TS —— NEVER by `find -newer hb_marker`.
```
**With:**
```
- **Broad file check (NOT just `⏳_`):** list every non-`❌_` file in `/seek/gcl/applied`, `/gcl/pending`, `/gcl/skipped` with filename-TS ≥ `latest_TS`. Validate each: single-job AR, correct routing folder, CL `## 6.` if applied, no rogue name (per tripwire). **Run `cl_check.py` on each NEW `Outcome: Applied` AR —— a HARD fail means the SA submitted a broken CL → RETIRE it immediately (per `main_ajap` § Heartbeat CL backstop / § SA Deterioration).** Enumerate ONLY by filename-TS —— NEVER by `find -newer hb_marker`.
```

---

## Self-review
1. Layered defence now: (a) SA runs `cl_check` before Applying (`ajap.md` S4.7/gate); (b) SA reports Applying → MA runs `cl_check` before approving (`main_ajap` Between-Loop); (c) approval bypass → retire; (d) NEW: MA `cl_check`s every Applied AR in the broad-check → HARD fail = retire. Layer (d) catches anything that slips (a)–(c).
2. Irreducible limit (stated honestly in `response`): the ONE already-submitted bad CL cannot be un-sent —— MA retires the SA to stop FURTHER damage and flags the AR. That is the best achievable; the true zero-slip fix is the SDK rebuild.
3. Efficiency: only NEW Applied ARs are checked (filename-TS ≥ `latest_TS`), each once.
4. Within MA's remit: running `cl_check` reads the `## 6.` CL for violation-checking only (allowed per § Role & Constraints); it returns pass/fail, not CL content into MA's reasoning.
5. Revert = swap each With→Replace.
