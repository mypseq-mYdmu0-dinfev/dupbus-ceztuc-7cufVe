# Session Closing (202606060448–202606060640)
*AJAP heartbeat durability, SA2 watchdog, compaction, 6-Qi*

## 1. Coverage
- 1.1. Forked-session investigation of AJAP run `202606050729` (rlog-anchored; my MA context was a stale slice at N≈202 whilst the real run reached N=317).
- 1.2. Audited recurring failures: heartbeat regressions, compaction-recovery breaches, concurrent/orphaned Monitors, `hb_marker` race, `⏳_` self-submit, slow `ma_msg` reset, External-Portal-Exit misread, rlog contamination.
- 1.3. Implemented the user's directives: robust persistent heartbeat, independent SA2 watchdog (900s), compaction "resume directly" override, rlog hygiene, active SA1 checking, and the 6-Qi flatten (removing `n`/`p`).

## 2. Decisions
- 2.1. Durability thesis (root cause of every regression): a fix survives compaction ONLY if embedded in a mandatory-on-every-wake file (`MA_hb.md`) or an always-present file (`seek/CLAUDE.md`). Chat/rlog-only fixes regressed; file-embedded ones held.
- 2.2. SA2 watchdog = deterministic bash `Monitor`, NOT an LLM —— it cannot compact (dissolving the user's Critical Loophole), reads marker mtimes (not chat text), wakes MA via `tmp/sa2_alert.md` file-write AND direct echo. Open for user veto → LLM SA2.
- 2.3. `git mv mini_ajap.md → MA_hb.md`; new `SA2_hb.md`; mandatory reads now 9.
- 2.4. Compaction order fixed to `🚨` → re-read all mandatory (state files first) → broad check + `🎯[N]` → respawn both Monitors (removed a duplicate-`🚨` typo).
- 2.5. Qi flattened to Qi01–06 (Qi01–03 = `&tags=new`; Qi04–06 = plain, with new-twin mapping); New-to-you now OPENS the new-twin Qi instead of clicking/append (prevents the non-existent-new-page crash).

## 3. Issues
- 3.1. 🟡 Open decision (user's call): bash watchdog (recommended) vs a true LLM SA2 —— file structure supports either.
- 3.2. 🟡 Parallel AJAP session is running OLD code; user will have its MA re-read all mandatory files (not restart) to load the new design + spawn the watchdog.
- 3.3. 🟢 `F1.3` still clicks "New to you" on the user's own Tab 1 in normal `seek` mode (outside the queue system) —— left as-is, flagged in `response_202606060625.md §15.4`.
- 3.4. 🟢 Runtime verification pending: watchdog/heartbeat/active-SA1 behaviour is only observable on the next real session start.

## 4. Session Files (6)
- 4.1. `202606/query_202606060448.md` —— user observations (heartbeat, SA2, compaction, rlog)
- 4.2. `202606/audit_202606060452.md` —— rlog-anchored audit of session 202606050729
- 4.3. `202606/response_202606060448.md` —— analysis, plan, execution confirmation
- 4.4. `202606/changes_202606060503.md` —— full edit record (waves 1 + 2)
- 4.5. `202606/query_202606060625.md` —— user reply (SA2 form, order, leanness, Qi)
- 4.6. `202606/response_202606060625.md` —— reply: bash watchdog, order fix, 6-Qi

## 5. Other Files (7)
- 5.1. Created:
  - 5.1.1. `context/SA2_hb.md` —— watchdog contract (bash Monitor spec)
- 5.2. Modified:
  - 5.2.1. `context/main_ajap.md` —— heartbeat/watchdog, compaction order, rlog hygiene, gate hardening, active SA1, `sa2_alert` watch
  - 5.2.2. `context/ajap.md` —— External-Portal-Exit clarity, Qi01–06 (A6/S7.2), `MA_hb` refs
  - 5.2.3. `context/MA_hb.md` —— edited (compaction override, stamps, watchdog, active SA1, trims)
  - 5.2.4. `seek/CLAUDE.md` —— mandatory list (9; +`SA2_hb`), compaction override
  - 5.2.5. `seek/queue.md` —— 6-Qi flatten, new-twin, open-right-Qi
- 5.3. Moved/Voided:
  - 5.3.1. `git mv context/mini_ajap.md → context/MA_hb.md`

## 6. Remarks
- 6.1. To activate everything in the running parallel session: have its MA re-read ALL mandatory files (loads `MA_hb.md` + `SA2_hb.md` + 6-Qi `queue.md`, spawns the watchdog). A fresh `seek` session activates it automatically.
- 6.2. All changes grep-verified (zero `n`/`p` remnants; `sa2_alert` wired in 3 files; 9-item mandatory list). Behavioural proof requires a live session.
