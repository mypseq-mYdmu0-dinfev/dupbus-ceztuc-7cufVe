# Session Closing (202606062031–202607050412)
*AJAP Investigation —— many quick-fixes; real Agent-SDK overhaul now handed to a Fable session*

## 1. Coverage
- 1.1. Post-compaction: repaired invalid `seek/.claude/settings.json` (trailing comma → hook never registered); added `ma_full_reread_marker` + SA2 `WATCHDOG-FULLREAD-STALL` (>75 min) as the hook-INDEPENDENT silent-compaction backstop.
- 1.2. Heartbeat: hollow-heartbeat fix (marker touched ONLY after a genuine re-read); PRIMARY 300s Monitor never churned + a SEPARATE transient 60s Monitor (`hb60_task`) spawned per approval, killed on submit.
- 1.3. Created `seek/context/cl_check.py` (deterministic CL linter: section≠6, dashes/`+`, `16⁺`>1, P.S. tail, template [1]/[2] neither/both, seniority soft-check) and wired it into SA1 + every MA Submit-gate.
- 1.4. Submit approval made SINGLE-USE + 3-min expiry (stops runaway multi-submits); SA 1hr hard-retire; SA poll-retry reconciled to 60×/900s.
- 1.5. `ajap.md`: K1 +Architect/Interior/Surveyor; queue re-verification sweep (re-check earlier Qi before advancing); LinkedIn+Glassdoor made MANDATORY research with account re-login (`seek/.claude/ajap_login.local.md`); Opus-Mode solo fallback.
- 1.6. Archives month-foldered (`*_archive/[YYYY]/[YYYYMM]/`) —— K6 already recurses, so safe.
- 1.7. CIC login overhaul (non-AJAP) —— closed separately in `sessions/2026/202606/close_202606272027.md` (`cic.md`/`cic_bot.md`/`.claude/cic_login.local.md`; Opus spawns a Sonnet SA to log in).
- 1.8. Trims: removed rlog citations, file-rename breadcrumbs, the captcha "logic" line, shrank the Opus rationale, deduped the CL-gate recipe (−213 tok across `ajap.md`/`main_ajap.md`/`MA_hb.md`).
- 1.9. Fixed the C2-line inconsistency —— all templates now `**[current_TS]** 🎯[N] job(s) processed.`.

## 2. Decisions
- 2.1. Never-churn-300s + conditional-60s heartbeat is the ROOT fix for MA oversleep (once the 300s Monitor is up it never fades).
- 2.2. `MA_hb.md` is meant to be a LEAN "password-hint" answer-sheet (all-or-nothing: any miss → re-read ALL mandatory files), NOT a rule store. It bloated to 9KB; a lean ~3KB rewrite is drafted (see 3.1).
- 2.3. Keep SA2 (bash watchdog) connected —— it is the external compaction-detector; it should become an LLM time-keeper only in the overhaul.
- 2.4. Credentials detached + git-ignored (`ajap_login.local.md` for AJAP; `.claude/cic_login.local.md` for CIC). Sonnet types directly; Opus spawns a Sonnet SA.
- 2.5. `changes_` convention: `Replace` = before, `With` = after (per `replace.md`); each `changes_`'s `Replace` should equal the prior `changes_`'s `With` on the same line, so the chain is git-free traceable.

## 3. Issues (unresolved —— EXTRA detail; NOT reflected by any `changes_`)
- 3.1. 🔴 `MA_hb.md` LEAN REWRITE is only PROPOSED (draft `MA_hb_LEAN_PROPOSAL_202606272232.md`), NOT executed —— it is the most-critical file, awaiting the user's approval. It pairs with: (a) a `main_ajap § Heartbeat` "wake-work" enumeration so nothing moved-out is lost; (b) collapsing the C1–C5 table so it stops re-stating the S0.3 C2 format; (c) an SA1-compaction-detection + ruthless-retire rule to REPLACE defensive repetition.
- 3.2. 🔴 Context bloat: `context/` ≈172KB, ~83% over backup 1.0's 94KB → ~hourly compactions. `MA_hb.md` is the suspected main culprit (re-read every wake). `audit_202606272146.md` holds the genuinely-risky trim items (Items A–F) awaiting second thoughts.
- 3.3. 🟡 `changes_202606121035.md` (silent-compaction backstop) & `changes_202606120346.md` (heartbeat marker-couple + submit expiry) were QUICK FIXES without the user's full scrutiny —— candidates to revert/re-derive in the overhaul.
- 3.4. 🟡 Defensive repetitions (`run_in_background` ×4, external-portal ×4, `⏳_` lifecycle ×4) still repeated —— agreed to replace with detection+retire, NOT yet done.
- 3.5. 🟡 Root `CLAUDE.md` is unavoidably read despite `seek/` working dir —— a real problem the new `AJAP_repo/` (empty `CLAUDE.md`) is designed to solve.
- 3.6. 🟡 Agent-SDK auth/billing UNVERIFIED: earlier research (`response_202606091942 §25`) suggests the SDK needs an API key + a Max credit-pool, NOT the flat subscription —— Fable MUST confirm before committing (it changes the cost basis).
- 3.7. 🟡 rlog-surfaced runtime issues (esp. `rlog_202606202330` —— MA waited HOURS for manual action, burning tokens re-reading `MA_hb.md` doing nothing) —— themed by a sub-agent this turn; folded into the Fable handoff query.

## 4. Comms Files (investigation session; all in `seek/investigation/2026/202606/` unless noted)
- 4.1. `202606062031`: investigation kickoff (heartbeat/post-compact/SA problems)
- 4.2. `202606080108`: settings.json JSON repair (`changes_`)
- 4.3. `202606091942`: big response (SDK, msg_board, SA2/SA3, cl_check, Opus note) + `changes_`
- 4.4. `202606120333`: cl_check wiring (`changes_`)
- 4.5. `202606120346`: heartbeat marker-couple + submit expiry (`changes_`; QUICK FIX)
- 4.6. `202606120406`: rename `*` file + reformat `changes_`
- 4.7. `202606121035`: silent-compaction backstop (`changes_`; QUICK FIX)
- 4.8. `202606121128`: queue re-verification sweep (`changes_`)
- 4.9. `202606262114`: archive month-foldering (`changes_`)
- 4.10. `202606271632`: heartbeat 60/300, cl_check templates, LinkedIn/Glassdoor login (`changes_202606271643`)
- 4.11. `202606271704`: S3.2 mandatory-sources (`changes_`; Replace-chain later fixed)
- 4.12. `202606271721`: revisions + cic-credential detach + `audit_202606271729` + B1 (typing)
- 4.13. `202606271829`: cic reframe + retry-reconcile + login-file renames (`changes_202606271833`)
- 4.14. `202606271906`: changes-chain fix + Opus-spawns-Sonnet-SA login
- 4.15. `202606271954`: unified spawn-SA login flow; leaned cic files
- 4.16. `202606272023`: CIC review + special `sessions/2026/202606/close_202606272027.md`
- 4.17. `202606272052`: trim execution (`changes_202606272057`)
- 4.18. `202606272139`: trim-direction fix + `audit_202606272146` (voided `❌_audit_202606272057`) + `changes_202606272146`
- 4.19. `202606272232`: MA_hb lean PROPOSAL + C2 fix (`changes_202606272232`)
- 4.20. `202607050412`: this close + the Fable handoff query (`202607/query_202607050412.md`)

## 5. Non-Comms Files
- 5.1. Created: `seek/context/cl_check.py`; `seek/.claude/ajap_login.local.md` (git-ignored); `.claude/cic_login.local.md` (git-ignored); `MA_hb_LEAN_PROPOSAL_202606272232.md` (draft, unapplied).
- 5.2. Modified: `seek/context/{ajap.md, main_ajap.md, MA_hb.md, SA2_hb.md}`; `seek/queue.md`; `seek/README.md`; `seek/.claude/settings.json`; `universal/{cic.md, cic_bot.md}` (cic mostly user-leaned); `dupbus-ceztuc-7cufVe/.gitignore`.
- 5.3. Moved/Voided: `aux_login.local.md`→`ajap_login.local.md`; `universal/cic_login.local.md`→`.claude/cic_login.local.md`; `❌_audit_202606272057.md` (voided).

## 6. Remarks (for the Fable overhaul session)
- 6.1. The Fable session's kickoff query is `investigation/2026/202607/query_202607050412.md` —— it carries the overhaul spec, the curated reading list, the AJAP_repo plan, and the rlog themes.
- 6.2. HARD constraint for Fable: NEVER edit anything in `seek/` (AJAP still runs to use quota); build only in the sibling `AJAP_repo/`.
- 6.3. `backup/` is read-only reference (1.0 = last full review; 2.0 = last semi-review) —— NO edits there.
- 6.4. Two files this session touched but which STILL need re-derivation with scrutiny: `changes_202606121035` and `changes_202606120346` (see 3.3).
