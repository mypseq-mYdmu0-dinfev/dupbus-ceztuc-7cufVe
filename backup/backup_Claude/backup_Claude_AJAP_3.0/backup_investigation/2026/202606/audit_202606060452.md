# Audit — AJAP Session 202606050729

*InvSes step 01 draft (context-window scan) now updated at step 03 with the authoritative `rlog_202606050729.md` and the 8 mandatory files. Formatted per `numbered.md`.*

---

1. Scope, Method & a Critical Caveat
- 1.1. This MA context is a FORK slice (InvSes Line 23): it reflects only loops N≈197→202 (5 Jun ~21:42–22:17), where every CL was clean and operations looked healthy.
- 1.2. The `rlog_202606050729.md` is authoritative and shows the true session ran 5 Jun 07:45 → 6 Jun 04:52, N=1 → 317, across many SAs (… → SA15 → SA16 → SA19 → SA20 → SA21 → SA22), riddled with recurring incidents my slice never witnessed.
- 1.3. Lesson 0 (meta): a forked/compacted MA context can look perfectly healthy whilst the real session is failing. Audits MUST be rlog-anchored, not context-anchored. My step-01 findings were directionally right but under-counted severity.
- 1.4. Old-vs-new verdict (InvSes step 08): the dominant issues are OLD and RECURRING, several with prior fix attempts that REGRESSED. Root regressor = compaction wiping the correct approach and MA reconstructing a wrong one from memory.

2. CRITICAL — Compaction-induced regression spiral (META root cause)
- 2.1. Evidence: rlog 202606051617 (MA resumed from summary, skipped mandatory re-reads, SA self-submitted MarlinHumanCapital unapproved); 202606051948 (MA compacted again, stuck in 10⁺ blocking recovery reads whilst SA ran unattended); 202606060334 (heartbeat fix "first applied at N=182 … session later regressed to finite loops … post-compaction recovery reconstructing the wrong approach from memory").
- 2.2. Mechanism: on compaction the MA loses the corrected procedure, then rebuilds an inferior one (finite-loop heartbeat, skipped resets, no TaskStop-before-respawn). Every prior point-fix is fragile to the next compaction.
- 2.3. Prior fix attempted? Yes — a "rapid resume" path and batched mandatory reads were PROPOSED at 202606051948 but never durably implemented; regressions continued through 04:52.
- 2.4. Why it failed: the fix lived only in chat/rlog, not in the always-re-read files (`seek/CLAUDE.md`, `mini_ajap.md`). Anything not in a mandatory-on-every-wake file does not survive compaction.
- 2.5. Severity: CRITICAL — this is the multiplier behind 2-? below.

3. CRITICAL — Heartbeat mechanism: fixed, regressed, re-fixed (recurring)
- 3.1. Evidence: 202606052043 (switched Bash run_in_background → Monitor); 202606060330 (dual-Monitor experiment); 202606060334-335 (root cause: finite `run_in_background` loops only notify on COMPLETION, so INT=60 was inert and a failed completion notification = indefinite sleep; re-fixed to persistent canonical Monitor `b9tvmqdys`).
- 3.2. My slice corroborates the survivor design (persistent Monitor, INT 60/300 dynamic) working correctly — i.e. the CORRECT approach is known; it just does not persist across compaction (see 2).
- 3.3. Prior fix attempted? Yes, ≥3 times (N=182, dual-monitor, N≈?). Regressed twice.
- 3.4. Why it failed: same as 2.4 — the canonical Monitor command lives in `main_ajap.md` (read at session start only), not enforced on every post-compaction wake.
- 3.5. Severity: CRITICAL (no heartbeat = SA runs blind/unattended).

4. CRITICAL — Concurrent/orphaned Monitors (TaskStop-before-respawn not enforced)
- 4.1. Evidence: rlog 202606060222 item 1 ("multiple concurrent Monitor tasks likely ran simultaneously … behwrswrs spawned without stopping predecessor"); 202606060330 dual-monitor; my slice's unexplained task `b155vgofu` (a wake from a Monitor I have no record of spawning) — independent corroboration.
- 4.2. Consequence: redundant wakes, doubled `hb_marker` touches (compounds 5), ambiguous TaskStop targets (a stale ID can leave a live Monitor orphaned).
- 4.3. Prior fix attempted? Stated as enforced "going forward" at 202606060222, but my later slice still shows an untracked task → not reliably holding.
- 4.4. Severity: CRITICAL (compounds heartbeat correctness).

5. HIGH — `hb_marker` broad-check races the Monitor's own touch (missed-file bug)
- 5.1. The Monitor does `echo newfile; touch "$M"` BEFORE MA wakes; an MA broad check of `find … -newer "$M"` then returns empty and can drop the very file that triggered the wake.
- 5.2. Direct evidence in my slice: a `-newer hb_marker` check returned nothing whilst filename-TS ≥ `latest_TS` surfaced `WoolworthsGroup_…_202606052204.md` (a real skipped AR that would have been undercounted).
- 5.3. Fix direction: MA enumeration MUST key SOLELY on filename-TS ≥ `latest_TS`; `hb_marker` is the Monitor's private wake trigger only.
- 5.4. Severity: HIGH (silent undercount + skipped audits).

6. CRITICAL — SA self-submits without the `⏳_` MA-approval gate (control-loop bypass)
- 6.1. Evidence: rlog 202606060138/0147/0220/0223/0225, and 202606060222 item 4 ("SA19 submitted without `⏳_` approval step on ≥4 occasions"). CLs happened to be clean, so "no harm" — but the CL-review gate was bypassed, which is exactly when a bad CL would slip through.
- 6.2. Root: SA runs background; MA's "Submit then proceed" gate depends on SA pausing at S6.4.2.5 and on MA seeing the report. When MA is absent (compaction/heartbeat down) SA either deadlocks (7) or proceeds unsupervised.
- 6.3. Prior fix attempted? Not structurally; handled case-by-case via retirement (SA19 retired 202606060225).
- 6.4. Severity: CRITICAL (quality gate is the whole point of MA).

7. HIGH — MA resets `ma_msg.md` to "Continue" too slowly → SA idle deadlock (recurring)
- 7.1. Evidence: 202606050819, 202606051908 (N=179), 202606051955 (N=182, "MA should reset … FASTER"), 202606060222 item 3 (stale "Submit then proceed" persisted after LivingMyWay).
- 7.2. Mechanism: SA polls `ma_msg.md` 20×15s (300s) then goes idle; if MA does not reset within that window the SA is lost and must be respawned (costly).
- 7.3. Fix direction: tie reset-to-"Continue" to a single deterministic post-submission step; make corrections one-liners actioned fast; assert ma_msg-vs-⏳_ consistency each wake.
- 7.4. Severity: HIGH (recurring SA loss).

8. HIGH — "New to you" prioritisation unenforceable (newtoyou never reaches MA)
- 8.1. Evidence: 202606051828 (MA never instructed the click; "MA relies on newtoyou field in SA loop reports but those were not visible in new context window"); proposed permanent check at 202606051837/0839 (newtoyou_miss_count in `ma_state.md`); still a carry-forward item.
- 8.2. Confirms my step-01 §1.3.1: background SA reports are invisible to MA, so any MA logic keyed on the loop report (newtoyou, FLAGS, OUTCOME) is dead; only file-watch survives.
- 8.3. Prior fix attempted? Proposed only; not durably implemented (again, lived outside mandatory-read files → lost to compaction, cf. 2.4).
- 8.4. Severity: HIGH (coverage gap: stale, already-seen cards re-scanned; new cards missed).

9. HIGH — SA-side logic errors (recurring, SA prompt/`ajap.md` clarity)
- 9.1. External Portal Early-Exit misapplied to Quick-apply jobs: 202606052250 (AlltechAV/BespokeCareers/InLoop, M7=5, score 35–49 wrongly skipped) and 202606060128 (Deputy/LegalVision). The rule is "M7=0 AND ceiling <70 → skip"; SAs keep applying the `<70` clause to M7=5 jobs that belong in the 35–49 APPLY band. `ajap.md § S3 / § S4.4 Exception` wording invites this misread.
- 9.2. S1 routing misses (Consultant/Associate/Google/Director → pending, not skipped/applied): 202606051451, 202606060430, 202606060435; K1 Director-title ARs at 202606051839/0908.
- 9.3. CL-quality drift: em dash (0752/1038/060104/060142), `16⁺` twice (060028), `## 3.`/`## 4.` instead of `## 6.` (051908/060225/060437), "Yours sincerely" + missing P.S. (060437).
- 9.4. Duplicate AR / K6 misses early (050813–0908, multiple retirements).
- 9.5. Severity: HIGH in aggregate (each triggers a correction cycle → SA idle risk per 7, and retirements).

10. MEDIUM — "⏳_ gone" treated as success without disambiguation
- 10.1. `⏳_` can vanish via void (`❌_`) or error, not only successful submission; MA inferred "applied" purely from disappearance.
- 10.2. Fix direction: after `⏳_` disappears, assert a same-stem non-`⏳_`/non-`❌_` AR exists in `/gcl/applied/` with `Outcome: Applied` before confirming.
- 10.3. Severity: MEDIUM.

11. MEDIUM — C2 (🎯N) emission drift; N magnitude
- 11.1. Evidence: 202606060222 item 2 ("MA failed to output C2 on every new AR — omitted entirely since session start this context"). My fork DID emit C2 — another illustration that different post-compaction contexts drift differently (cf. 2).
- 11.2. N reached 317 in one ~21h session — consistent with the rlog and with MEMORY `feedback_ajap_display_n` (session-only count; no offset double-count observed). Not an error, but confirms scale → context pressure.
- 11.3. Severity: MEDIUM.

12. LOW — `latest_TS` churn
- 12.1. Advancing `latest_TS` to wall-clock "now" each wake can leapfrog an SA-stamped filename whose `date` lagged, risking a skipped new AR.
- 12.2. Fix direction: advance `latest_TS` only to the max validated filename-TS this window.
- 12.3. Severity: LOW (latent; mitigated whilst the filename-TS method is used).

13. Prioritisation for `changes_`
- 13.1. Tier 1 (durability across compaction — fixes the multiplier): 2, 3, 4 — make the canonical persistent-Monitor + TaskStop-before-respawn + post-compaction "rapid resume" live INSIDE mandatory-on-every-wake files (`mini_ajap.md`, and `seek/CLAUDE.md` recovery gate), since only those survive compaction (2.4).
- 13.2. Tier 2 (correctness of the control loop): 5 (filename-TS-only enumeration), 6 (⏳_ gate hardening), 7 (fast ma_msg reset), 10 (submission disambiguation).
- 13.3. Tier 3 (coverage + SA logic): 8 (newtoyou via file/Tab-1 not via report), 9 (ajap.md clarifications: Early-Exit band, S1 routing, CL self-review), 12 (latest_TS).
- 13.4. Constraint: every fix MUST be encoded where it is re-read after compaction; chat/rlog-only fixes are why 2/3/8 regressed.

14. Verified against prior investigation files (step 04 —— RESOLVED)
- 14.1. `changes_202606050721` only changed the Qi n-then-p processing order —— it touched NONE of the heartbeat/compaction/control-loop issues; it HELD because it was written into `ajap.md`/`queue.md` (file-embedded).
- 14.2. `audit_202606040352 § 9` RECOMMENDED fixes (S4.7 colon check, salary/notice re-confirm, mid-session template re-read) but they were never turned into a `changes_` —— and CL drift duly recurred this session (em dash, `16⁺`-twice, `## 3.` sections, "Yours sincerely"). The 202606051837/0839 newtoyou proposal was likewise never embedded in `ajap.md`/`MA_hb.md`.
- 14.3. Conclusion (confirms 2.4): file-embedded fixes held; recommendation-only and chat/rlog-only fixes regressed or never landed. This is the ENTIRE rationale for embedding the present fixes in mandatory-read + always-present files.
