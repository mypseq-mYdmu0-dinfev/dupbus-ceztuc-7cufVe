# Response to query_202606120346.md
*Provisional quick fix applied so AJAP can run this week —— scope assumption flagged*

1. Done —— AJAP is runnable now. Provisional edits in `changes_202606120346*.md` (`*` = revert-candidate, per your instruction).

2. One scope call I made (please sanity-check —— it is the only "extremely critical" judgement here).
- 2.1. I did NOT hand-build a standalone SA3 / `msg_board.md` / LLM-SA2 / MA-as-PD this turn. Building that unproven multi-agent re-architecture in a rush is the single biggest risk to an UNATTENDED week-long run —— i.e. the exact quota-waste you are trying to avoid.
- 2.2. Instead I shipped the two contained fixes that make the PROVEN Sonnet MA/SA1/SA2 flow reliable now, on top of last turn's `cl_check.py` wiring. The SA3 *audit function* this week is covered by MA running `cl_check.py` at every Submit-gate (already live).
- 2.3. If you actually wanted the full SA3 build now, one word and I'll do it (still `*`-flagged) —— but my recommendation for a hands-off week is what I shipped.

3. What I changed (both tighten safety of the existing flow; neither adds a new agent).
- 3.1. **Heartbeat fix (the real one):** the `ma_hb_reread_marker` is now touched as the LAST step of a wake, ONLY after MA actually re-reads `MA_hb.md` + emits `✅`. Previously it was touched FIRST, so the watchdog saw "alive" while MA skipped the read —— the hollow heartbeat that recurred N=10→N=92 (`audit_202606081836`). Now a fresh marker genuinely certifies a re-read; no watchdog change needed.
- 3.2. **Runaway-submit fix:** a Submit approval is now SINGLE-USE and expires after 3 min. SA1 submits only the current card, then must re-report `Applying` for the next card to earn a fresh approval —— it can no longer keep submitting without re-waking MA (your §6 concern). Implemented SA-side only, so there is no MA/SA string-mismatch deadlock risk.

4. What I deliberately deferred (documented in the `*` file, all for next-week scrutiny).
- 4.1. The precise "Submit [AR_TS] then proceed" token across all ~8 approval sites —— deferred because a partial rollout could deadlock an unattended run; the single-use+expiry rule gives most of the safety now.
- 4.2. The `latest_TS`-staleness watchdog (`response_202606091942 §13.3`) —— needs BSD-`date` epoch math inside SA2's bash; too fragile to add un-scrutinised to a week-long run. The marker-couple fix (3.1) already addresses the same failure from the other side.
- 4.3. SA3 / `msg_board.md` / LLM-SA2 / MA-as-PD —— the full redesign, gated on the SDK decision.

5. To run: start AJAP as usual (`seek`). It will use `cl_check.py` at every CL gate, re-read `MA_hb.md` properly on each wake, and re-gate every single submit. Everything this turn is revertible next week when you formally reply to `response_202606091942.md`.
