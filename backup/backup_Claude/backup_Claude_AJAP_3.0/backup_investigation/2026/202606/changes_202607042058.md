# Changes —— `changes_202607042058`
*REVERTS `changes_202607042016` (based on a wrong diagnosis) and applies the correct fix. Correct root cause (per `query_202607042053` + `ajap.md` L349/S6.2.4): the CL (§6) is the LAST AR section, so its P.S. MUST be the file's final line —— cl_check.py was RIGHT to flag Henderson/Leisures; their `## 7. Employer Questions` is the real violation (answered questions must APPEND to §3, per S6.2.4). So: revert cl_check.py + ajap_logs.py logic to original; improve ONLY cl_check.py's terminal message (per your update); strengthen ajap.md S6.2.4. Standard `replace.md` direction.*

---

## Change 01 —— `seek/context/cl_check.py` revert CL-boundary to original (scan to EOF)
**Replace:**
```
    # Bound the CL to its OWN section: from the `## 6.` marker to the NEXT
    # `## ` heading (e.g. `## 7. Employer Questions`) or EOF. Checks must NOT
    # bleed into later AR sections —— the CL's P.S. is the last line of THIS
    # section, not the file, and a Q&A answer's dash is not a CL violation.
    rest = content[m.end():]
    nxt = re.search(r"^##\s", rest, re.MULTILINE)
    cl = content[m.start(): m.end() + nxt.start()] if nxt else content[m.start():]

    # HARD: dash/plus symbols
```
**With:**
```
    cl = content[m.start():]

    # HARD: dash/plus symbols
```

## Change 02 —— `seek/context/cl_check.py` revert P.S. LOGIC to original; improve MESSAGE only
**Replace:**
```
    # HARD: P.S. must be the LAST content line of the CL SECTION (not the file
    # —— later AR sections like `## 7. Employer Questions` legitimately follow).
    # Drop trailing blank lines and a trailing `---` separator, then compare.
    cl_lines = cl.splitlines()
    while cl_lines and (not cl_lines[-1].strip() or set(cl_lines[-1].strip()) <= {"-"}):
        cl_lines.pop()
    last_line = cl_lines[-1].strip() if cl_lines else ""
    if last_line != PS_REQUIRED_TAIL:
        hard.append("CL does NOT end with the exact required P.S. line")
```
**With:**
```
    # HARD: the P.S. tail must be the file's FINAL content line —— the CL (§6)
    # is the LAST AR section, so nothing may follow the P.S. (logic unchanged;
    # only the message guides the agent to the correct fix).
    stripped = content.rstrip()
    last_line = stripped.splitlines()[-1] if stripped else ""
    if last_line != PS_REQUIRED_TAIL:
        hard.append(
            "AR does NOT end with the exact P.S. line —— the CL (§6) must be the "
            "LAST section, so the P.S. must be the file's final line. If employer "
            "questions were answered, APPEND them to the END of §3 Application "
            "Tailoring (S6.2.4); NEVER put them in a new section after the CL, and "
            "never replace/overwrite §3."
        )
```

## Change 03 —— `gscpt/ajap_logs.py` revert `scan_applied_violations` to original
**Replace:**
```
        # --- Bound the CL to its OWN section: `## N. Cover Letter` marker to the
        # NEXT `## ` heading (e.g. `## 7. Employer Questions`) or EOF. Later AR
        # sections are NOT part of the CL and must not be scanned (their dashes
        # are not CL violations, and the P.S. is the last line of THIS section,
        # not of the file). ---
        marker_match = re.search(
            r"##\s+\d+\.\s+Cover Letter",
            content,
        )

        if marker_match:
            rest = content[marker_match.end():]
            nxt = re.search(r"^##\s", rest, re.MULTILINE)
            cl = (
                content[marker_match.start(): marker_match.end() + nxt.start()]
                if nxt else content[marker_match.start():]
            )

            # Type 1: rule-violation symbols within the CL section.
            total += sum(cl.count(symbol) for symbol in RULE_VIOLATION_SYMBOLS)

            # Type 2: P.S. must be the LAST content line of the CL section
            # (drop trailing blank lines and a trailing `---` separator).
            cl_lines = cl.splitlines()
            while cl_lines and (
                not cl_lines[-1].strip() or set(cl_lines[-1].strip()) <= {"-"}
            ):
                cl_lines.pop()
            if (cl_lines[-1].strip() if cl_lines else "") != PS_REQUIRED_TAIL:
                total += 1
```
**With:**
```
        # --- Type 1: rule-violation symbols after the Cover Letter marker ---
        marker_match = re.search(
            r"##\s+\d+\.\s+Cover Letter",
            content,
        )

        if marker_match:
            scan_content = content[marker_match.start():]
            total += sum(
                scan_content.count(symbol)
                for symbol in RULE_VIOLATION_SYMBOLS
            )

        # --- Type 2: file must END with exactly PS_REQUIRED_TAIL ---
        # rstrip() tolerates an invisible trailing newline / spaces after the
        # period, but any real text after the line fails the check.
        stripped = content.rstrip()
        last_line = stripped.splitlines()[-1] if stripped else ""
        if last_line != PS_REQUIRED_TAIL:
            total += 1
```

## Change 04 —— `gscpt/ajap_logs.py` revert docstring
**Replace:**
```
      1. Each occurrence of a RULE_VIOLATION_SYMBOLS symbol within the CL
         SECTION only (`## N. Cover Letter` marker → next `## ` heading / EOF;
         skipped if the file has no marker) —— NOT later AR sections.
      2. A single P.S.-ending violation (+1) if the CL section's last content
         line (ignoring trailing blanks / a `---` separator) is not exactly
         PS_REQUIRED_TAIL.
```
**With:**
```
      1. Each occurrence of a RULE_VIOLATION_SYMBOLS symbol appearing AFTER the
         Cover Letter marker (skipped if the file has no marker).
      2. A single P.S.-ending violation (+1) if the file does NOT end with
         exactly PS_REQUIRED_TAIL —— i.e. its final line is not that exact line,
         or there is any text after it.
```

## Change 05 —— `seek/context/ajap.md` S6.2.4 reinforce §3 placement (the real culprit)
**Replace:**
```
S6.2.4. If answered any questions, MUST **append** to end of "3. Application Tailoring" in AR (DON'T replace/overwrite entire section)
```
**With:**
```
S6.2.4. If answered any questions, MUST **append** them to the END of "3. Application Tailoring" in the AR (DON'T replace/overwrite the section). NEVER create a new section (e.g. `## 7. Employer Questions`) —— the CL (§6) MUST stay the LAST AR section so its P.S. is the file's final line (exactly what `cl_check.py` enforces; ANY section after §6 is a HARD violation)
```

---

## Self-review
1. cl_check.py + ajap_logs.py DETECTION logic is now identical to the original (P.S. = file's final line) —— they correctly flag the `## 7` violation again. Verified: Henderson now `❌` (with the new guiding message); a clean AR still passes.
2. Only cl_check.py's terminal MESSAGE was enhanced (per your update) —— no mechanical change.
3. ajap.md S6.2.4 now bans a new post-CL section and ties it to the cl_check invariant —— addresses the true cause (SA misplaced Q&A into a `## 7`).
4. `changes_202607042016` is superseded/reverted by this file (kept as the record of the wrong attempt).
5. Revert this turn = swap each With→Replace.
