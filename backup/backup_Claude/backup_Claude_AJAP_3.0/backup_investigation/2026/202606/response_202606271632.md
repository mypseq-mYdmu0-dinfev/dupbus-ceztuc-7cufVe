# Response to query_202606271632.md
*All three done & verified —— heartbeat, cl_check.py templates, account login*

1. Heartbeat —— the 300s never gets touched now (`changes_202606271643` Task 1).
- 1.1. The PRIMARY 300s Monitor is spawned ONCE and is never TaskStop'd/re-spawned in normal operation —— it runs untouched the whole session, so liveness no longer depends on a successful respawn.
- 1.2. When a 60s cadence is needed (approval pending), MA spawns a SEPARATE transient Monitor (`hb60_task` = `while true; do sleep 60; echo "heartbeat"; done`); on submission-confirm it `TaskStop`s ONLY that `hb60_task`. Exactly your design.
- 1.3. One deliberate detail: the 60s is a pure timer (no file-watch) so it cannot race the primary's `newfile` marker. Post-compaction recovery also clears any orphaned `hb60_task`.

2. CL template check —— `cl_check.py` now enforces Opening [1]/[2] (Task 2; tested on your 3 examples).
- 2.1. HARD fail (exit 1) when the opening matches **neither** [1] nor [2] —— this caught both rogues: `ArchieRose…SeniorPackagingDesigner` and `AlluraPartners_SeniorInsightsAnalyst` (both used a made-up triad). It also catches "edited what cannot be edited" —— any tampering with the fixed opening makes it match neither.
- 2.2. HARD fail when **both** [1] and [2] are present.
- 2.3. SOFT flag for the wrong-seniority case: `Access4_PartnerSuccessManager` used [1] though the title says "Manager" → `⚠️ likely should be [2]`. (Soft, because picking [1] vs [2] by seniority is a judgement the script shouldn't hard-block —— but SA1/SA3 now get the nudge.)
- 2.4. No false positives: valid [1] and [2] CLs pass clean. Detection uses the verbatim fixed skill-triads from `gcl.md § Opening 1/2`.

3. LinkedIn/Glassdoor —— account login replaces the workaround (Task 3).
- 3.1. `ajap.md` S3.2.2/S3.2.4 no longer rely on the Google-referrer bypass; AJAP now reads the page and, if it hits a login wall, signs in with your account, then continues. Same credentials for both sites.
- 3.2. Placeholder created at `seek/.claude/aux_login.local.md` —— fill in `email:` and `password:`.
- 3.3. ⚠️ Security: this repo auto-#syncs to GitHub and `seek/.claude/` IS tracked, so I git-ignored the credentials file (verified —— it will NOT be committed/pushed). Trade-off: because it is ignored, the #sync does NOT back it up, so keep your own copy of the login. If you'd rather not store the password in plaintext at all, tell me and we can have AJAP prompt-and-pause instead.

4. Note on your archive sort: I see you used `applied_archive/2026/202606/` (YYYY/YYYYMM, like `sessions/`). That is fine —— K6's "incl. sub-folders" recurses to any depth, so dedup still works.

5. All revertable (12 verbatim snippets/files in the `changes_`).
