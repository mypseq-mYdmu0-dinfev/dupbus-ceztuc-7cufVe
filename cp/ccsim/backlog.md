# CCSIM Backlog

*Append-only system-improvement log. Entry format + rules: see `cp/ccsim/CLAUDE.md` §3. NEVER edit/delete an entry —— resolve by appending `→ ✅ RESOLVED [TS] (ref …)` beneath it.*

---

## `close_202606262244.md` —— SUMMON-on-failed-#sprint rule never written
Problem: This session flagged that `sprint.md` needed a "SUMMON the user on a failed/unfinished `#sprint`" rule (so failure is never silent, after a prior silent-sprint-failure disappointment), but the edit that turn was scoped to `cic.md`/`cic_bot.md` only, and the rule was never added to `sprint.md`. It predates the backlog-wiring mechanism (built 23 Jul), so it sat unlogged until this sweep caught it.
Suggestion: Add a short clause to `universal/sprint.md` (Sprint Report or Push-Through Mechanism section): if a `#sprint` ends with an unresolved blocker and no path forward, write the report then SUMMON the user via push notification, mirroring the existing Critical/Untracked-Task Caveat language.
Ref: `wrap_202607232332.md` §3.9

## `wrap_202607232332.md` —— Recurring stray-space-before-TS filename defect
Problem: At least 4 comms filenames across 2 months carry an accidental space between the prefix and the TS digits (`close_ 202605310448.md` [since renamed by the user], `close_ 202606142239.md`, `career_close_ 202606162244.md`, `dissertation_close_ 202607151919.md`). Each instance was noticed ad hoc and left for a manual rename; none was ever root-caused as a recurring pattern.
Suggestion: Add a lightweight filename-format check to the existing pre-commit pairing-lint hook (`.githooks/pre-commit`) —— a regex per root CLAUDE.md §3.3.8's naming convention —— flagging (not necessarily blocking) any comms filename with an embedded space before the TS digits.
Ref: `wrap_202607232332.md` §3.14
→ 🟡 PARTIALLY RESOLVED 202607292245 (Ref: `career_query_202607292238.md`) —— `career_close_ 202606162244.md` renamed to `career_close_202606162244.md`, cross-refs in `CP_notes.md`, the AlltechAV AR, `int.md`, `wrap_202607232332.md` §4.10, and a user memory fixed to match; historical session comms files left untouched on purpose (they accurately recorded the filename as it stood at the time). `close_ 202606142239.md` and `dissertation_close_ 202607151919.md` remain open.
→ ✅ RESOLVED 202608012345 (Ref: `cscpt/flint.py`, `cscpt/flint_hook.sh`, `cscpt/tlint.py`, `.githooks/pre-commit`) —— made mechanical on three layers, because the suggestion as logged (a pre-commit regex) could only ever catch the defect after it existed. (1) PREVENTION: new `flint.py`, a PreToolUse gate that BLOCKS any Write/Edit/MultiEdit/NotebookEdit whose target basename carries the defect and names the correct filename in the refusal —— PreToolUse and not PostToolUse because a PostToolUse hook cannot undo a write. Repo-scoped, per `hook_guide.md` §4.7's rule that a blocking lint must be; out of scope it downgrades to an advisory rather than going quiet. (2) ALERT ON ENCOUNTER: `tlint.py` now sweeps the folder listing it was ALREADY taking for its TS-clash check, so an existing offender surfaces as a by-product of a write CC was making anyway —— no extra I/O, no hunt —— and reports it as model-visible `additionalContext` so CC raises a `⚠️` to the user. (3) The suggested pre-commit regex was still built, but re-scoped to what the gate cannot reach: files created by Bash or in Finder never meet a PreToolUse hook, so `.githooks/pre-commit` BLOCKS a staged ADD of an offending path and warns on a MODIFY. Detection rule: `^\S*_\s+(?=20\d{10}(?!\d))` —— calibrated against all 5331 TS-bearing basenames in this repo; the obvious broader rule ("a TS-bearing name containing whitespace") would have blocked 5 legitimate files (`MGTK746 … _ <TS>.txt` and `AJAP Logs <TS>.csv`), whose style spaces names throughout. Pinned by `cp/ccsim/sandbox/flint_filename_gate_regression_test.py` (60/60), which re-derives that calibration from the live repo on every run. Also fixed in passing, same defect class: both `.githooks/pre-commit` loops used `for f in $staged`, and git does not quote a path merely for containing a space, so a stray-space path was torn in two —— the `#sync` allowlist arm refused an allowlisted file and printed half a filename. `flint.py` still needs its `~/.claude/settings.json` PreToolUse registration to go live; layers 2 and 3 are live and verified.

## `wrap_202607232332.md` —— No documented method for editing Automator .app bundles
Problem: CC has now edited/rebuilt 3⁺ separate Automator `.app` bundles (`SortSS.app` via `#replace` instructions, `PDF Conversion.app` via the Option C rebuild + adhoc re-sign, `Continue.app`/`SimpleContinue.app`/`CCC.app` rework) —— each time apparently re-deriving the extract-`document.wflow`/edit/re-sign technique from scratch rather than following a written method.
Suggestion: Once the current `CCC.app` work settles, distil the proven technique (locate `document.wflow` inside `Contents/`, edit, `codesign --force --deep -s -` to re-sign adhoc) into a short reference file (e.g. a new `automator/README.md`), so future `.app` edits don't re-derive it.
Ref: `wrap_202607232332.md` §1.2 / §5.1

## `wrap_202607232332.md` —— `queued_queries/` has no forcing function to get picked up
Problem: 3 items currently sit in `sessions/queued_queries/` (`ccsim_query_202607060438.md` since 6 Jul, `citi_query_202607162351.md` and `ajap_query_202607162356.md` since 16 Jul) awaiting the user to manually send each to a dedicated new session; nothing currently surfaces their age or count except an ad hoc check like this sweep's.
Suggestion: Have `universal/wrap.md`'s template (or the CCSIM §2 Session-Start check) glob + list `sessions/queued_queries/` open items (excluding `README.md`/`.DS_Store`) with their queued-since date, so ageing items surface automatically at every `#wrap` rather than needing a manual find.
Ref: `wrap_202607232332.md` §3.1 / §3.4 / §3.5

## `ccsim_response_202607232338.md` —— Pairing-lint hook has no exception for root CLAUDE.md §3.6.1's short-message case
Problem: Root CLAUDE.md §3.6.1 allows a `response_` to a non-`query_` message ≤30w to skip creating a sibling `query_` file (quote the message in Line 1 instead), but `.githooks/pre-commit`'s pairing lint (built this same month, per `ccsim_close_202607232054.md` §1.7) enforces the sibling unconditionally, blocking the commit until a `query_` is created anyway. Discovered live this turn.
Suggestion: Either drop §3.6.1's skip-if-≤30w exception (always create the `query_`, simplest fix) or teach the hook to allow an orphaned `response_` whose Line 1 matches the `# Response to "..."` quoted-message pattern. Recommend the former —— the hook's invariant (every `response_` has a traceable `query_`) is valuable for `DATS.py`/`#wrap` funnel-reading and a quoted-message `query_` costs nothing to create.
Ref: `ccsim_response_202607232338.md` §3
→ ✅ RESOLVED 202607240238 (ref `ccsim_response_202607240227.md` §1) —— user chose a THIRD option over both proposals: keep the check but demote it from a hard block (`exit 1`) to a non-blocking yellow-flag reminder (prints, then `exit 0`), mirroring dlint's yellow-vs-red distinction. `.githooks/pre-commit` edited + tested end-to-end (orphan→reminder+exit 0; paired→silent; `#sync` marker→still blocks). A valid orphan `response_` (short chat msg quoted inline per root §3.6.1) now commits freely whilst still surfacing a push-the-query reminder.

## `universal/glossary.md` vs `universal/sprint.md` —— FOF's all-at-once override is declared on ONE side only
Problem: The two files give opposite instructions on the same decision, and only one of them admits it. `glossary.md` (§ Terms, `FOF` entry) defines FOF as "use Workflow tool w/ all-at-once fleet", then adds its own carve-out —— "This overrides `sprint.md`'s 'NEVER one all-at-once fleet...'" —— justified by "Usually means ses% = 0, hence sesL is unlikely to kill SAs mid-flight". `sprint.md` (§ `#sprint` —— Time Abundant, the Workflow bullet) states the reverse as an absolute: "MANDATE wave-staging INSIDE the script —— bounded sequential batches, NEVER one all-at-once fleet", citing root `CLAUDE.md` §9.02.4 (a sesL kills them together; partials are compromised). `sprint.md` never mentions FOF, the glossary, or any exemption —— yet the same file DOES spell out a different override elsewhere ("A sprint also overrides the 'request/await confirmation' steps of OTHER protocols"), so the silence is an inconsistency rather than house style. Consequence: an agent mid-`#sprint` reads `sprint.md` and sees an unconditional ban, whilst an agent that happened to look FOF up sees a licence to fan out all at once; which rule governs is settled by whichever file that agent happened to open, i.e. arbitrarily at runtime (`universal/coding.md` § Prompted Components —— an unstated conflict is a coin-flip). The glossary's trigger is also soft ("Usually" ses% = 0), so it leaves the case where that is false undecided.
Suggestion: Owner's call —— both files sit outside CCSIM's default ownership here and the correct answer depends on which behaviour is actually wanted, so this is logged rather than fixed. Two candidates: (a) KEEP the override and back-reference it from `sprint.md`'s Workflow bullet in one clause, so the exemption is visible to the agent who only reads `sprint.md`, and replace "usually ses% = 0" with a condition that can actually be checked before fanning out; or (b) DROP the override, letting wave-staging govern FOF too, and delete the glossary's carve-out line. (a) is the cheaper edit and preserves the stated intent; either way both files must end up stating the same rule.
Ref: `universal/glossary.md` § Terms (FOF entry); `universal/sprint.md` § `#sprint` —— Time Abundant (Workflow bullet); root `CLAUDE.md` §9.02.4
→ ✅ RESOLVED 202607290427 (ref `universal/glossary.md` § Terms, FOF entry) —— NOT A DEFECT; owner's ruling, logged in error. The premise "only one of them admits it" is wrong: the glossary's FOF entry ALREADY carries the override as an explicit sub-item ("This overrides 'NEVER one all-at-once...' of `sprint.md`"), and it sits on the EXEMPTING side, which is where an exception belongs —— the general rule states the norm, the exception declares itself. Nor is it a runtime coin-flip: FOF is a term, so an agent invoking it has necessarily resolved it through the glossary and therefore has the override in hand, whilst an agent reading only `sprint.md` is not invoking FOF at all and correctly wave-stages. A back-reference in `sprint.md` would buy nothing and add a second place for the same rule to drift. Both files stand as written; entry retained (not deleted) per §3.3, so this same non-conflict is not re-logged by the next sweep that reads the two files side by side.

## `ccsim_response_202607291950.md` —— Declaration emitted twice in one turn
- Problem: when a turn has nothing to DO but wait, the harness still requires visible output, so CC fills the slot with a partial declaration batch —— producing two batches in one turn. Same reflex previously produced progress notes wearing the blocker glyph.
- Suggestion: discipline first (whilst waiting, make a TOOL CALL and emit no chat text); if it recurs, add a clint check for more than one declaration batch after the last genuine user message.
- Ref: `ccsim_response_202607291950.md` §93

## `ccsim_close_202607291954.md` —— Regression suites assert the owner's half of the Void Rule
- Problem: three suites failed this session not from regression but from asserting a voided `❌_` file still exists. The Void Rule is two-party (CC renames, owner deletes), so that assertion breaks whenever the owner does his half.
- Suggestion: standing rule for future suites —— assert only that no LIVE copy exists; never assert the presence of a voided file.
- Ref: `ccsim_close_202607291954.md` §8.4

## `cscpt/clint.py` —— Reader's sanctioned `.` reply is blocked by clint
- Problem: `universal/glossary.md` and the `GitHub/` CLAUDE.md both mandate that when the user's message is a lone `.`, the agent replies with `.` only in chat. But clint's READER mode blocks EVERY non-blank line (only `override` exempt), so a Reader session following its own documented convention gets Stop-blocked doing so.
- Suggestion: decide whether that dot-echo convention should be clint-visible at all, or was always meant to rest on instruction-following like Reader's other rules. If it should be enforced, exempt a lone `.` in READER too —— but note this deliberately blurs it with the REPO deadlock-escape, which was kept OFF for Reader on purpose.
- Ref: `ccsim_response_202607301737.md` §118
→ ✅ RESOLVED 202607302010 (Ref: `ccsim_response_202607301941.md` §122 —— the lone-`.` exemption now covers READER mode, tagged `clean:dot_reader`, so the Reader's sanctioned dot-echo is no longer flagged.)

---

*Below appended by the 202607 Backlog Sweep (202608012026), per `cp/ccsim/CLAUDE.md` §6.*

## Root `CLAUDE.md` —— § citations left dangling by repeated renumbering
- Problem: root `CLAUDE.md` carried two internal §9 citations and NEITHER resolved. §3.1.1 read "unless override (§9.1)" and §5.8 read "NOT a full listing per §9.2", whilst the live headings run §9.01–§9.06 —— by the file's own 2-digit convention §9.2 ≠ §9.02, and §9.02 is SA-spawning, not the never-list rule (§9.01) plainly intended. §9 was restructured three times in July by three different sessions, each correcting its own edit, none sweeping the citations pointing INTO it. The rot is invisible per-session and shows only with a month laid side by side. `cscpt/nlint.py` enforces numbering CONTINUITY but never checks that a `§n.n` token resolves to anything, so the repo's most-read file mis-pointed with no lint firing.
- Suggestion: add CHECK D to `cscpt/nlint.py` —— for any file carrying its own `- n.nn.` numbering, resolve every `§n(.n)*` token against that same file's item list and ADVISE (exit 0, `additionalContext`) on each unresolved one. Keep v1 to SELF-citations; cross-file refs (e.g. `sprint.md`'s "root `CLAUDE.md` §9.02.4") need a resolver that knows which file "root" means, which is a second problem. Renumbering will recur, and "remember to grep afterwards" is the failure mode, not the fix (§8.1).
- Ref: root `CLAUDE.md` §3.1.1 & §5.8; `close_202607130651.md` §1.1; `close_202607141621.md` §1.4; `close_202607231053.md` §1.1
→ 🟡 PARTIALLY RESOLVED 202608012026 (Ref: `ccsim_response_202608011950.md`) —— both live dangles fixed after git archaeology recovered their original targets: §9.2 → `§9.01` (commit `902f9ec` shows old §9.2 WAS "NEVER list all files"), and §9.1's target was the `override` definition (commit `46d3723`), since migrated to `glossary.md`, so §3.1.1 now points there. The nlint CHECK D half remains OPEN —— without it the next renumber re-creates the defect.

## Sweep 202607 —— voided `❌_` files age unnoticed because §8.2.4's reminder cannot fire
- Problem: root `CLAUDE.md` §8.2.4 mandates reminding the user of any voided file ≥7 days old, then disarms itself in the same line ("don't actively search"). On disk at the sweep: 10 `❌_` files repo-wide, 9 of them ≥7 days, four dating to 25 June (37 days). No comms file in `202607/` mentions any of them. Six of the month's twelve closes logged their OWN voided files, every one as a 🟢 —— so each session sees only what it voided that day, none sees the queue, and the accumulation is invisible by construction. This is the NOT-NOTICED class (`cp/ccsim/CLAUDE.md` §8.7): the prose exists and was skipped every time, so rewording changes nothing.
- Suggestion: one read-only script, `cscpt/pending.py`, printing both halves of the user's outstanding queue with days-since-mtime —— (a) repo-wide `❌_*`, flagging ≥7 days per §8.2.4; (b) `sessions/queued_queries/*` less `README.md`/`.DS_Store`/`❌_*`. Call it from `cp/ccsim/CLAUDE.md` §2 (CCSIM session start) and from `universal/wrap.md` (monthly). It must NEVER delete —— it prints, the user acts. Merge the separate `queued_queries` entry above into this one script: same failure shape (a two-party queue where CC's half is done and the user's half is invisible), and two mechanisms for one problem is exactly the drift to avoid.
- Ref: root `CLAUDE.md` §8.2.4; `close_202607141307.md` §3; `ccsim_close_202607291954.md` §3; the `queued_queries/` entry above

## `cp/ccsim/CLAUDE.md` §6 —— the sweep's "already self-registered" premise did not hold for 202607
- Problem: §6 directs the sweeper to hunt patterns rather than un-logged flags, "per-session items already self-registered by each `close_`". That wiring (`universal/close.md` § Preamble's CCSIM-backlog clause) landed in commit `8df3237` at 202607232125. TEN of the month's twelve closes were written BEFORE it; only `ccsim_close_202607250023.md` and `ccsim_close_202607291954.md` had it. So for 202607 the premise is false —— whatever those ten flagged as CCSIM-worthy was never appended anywhere, and a sweep that by mandate skips un-logged per-session flags is structurally guaranteed to miss them. The same window covers all of 202605 and 202606.
- Suggestion: (a) one-off —— read the § Issues section of the ten pre-wiring 202607 closes and append anything CCSIM-owned still live; scope to 🔴/🟡 only or it drowns in 🟢 housekeeping. (b) durable —— one clause in §6: for any close whose TS precedes 202607232125, the sweeper must ALSO scan un-logged flags, not patterns alone. The clause retires itself once the pre-wiring window is swept.
- Ref: `cp/ccsim/CLAUDE.md` §6; `universal/close.md` § Preamble (commit `8df3237`, 202607232125)

## `universal/coding.md` —— new path-walking tooling still defaults to a single repo root
- Problem: the same defect was found, fixed, and rebuilt three times in five weeks. An SA scan surfaced four single-root offenders on 9 Jul; most were fixed on 19 Jul; then `tlint` —— built a fortnight AFTER that audit —— needed AJAP-side registration and was queued for it. The structural fix (user-level hook registration plus per-script repo-scope guards) landed only on the third repetition and only for the five hooks. Non-hook path-walkers were never brought under any rule: `gscpt/DAMF.py:48` and `gscpt/DXMF.py:60` still carry the dupbus root alone whilst `gscpt/DATS.py:39–40` carries both. `universal/coding.md` contains no root-scope guidance at all. Cataloguing offenders demonstrably did not stop the next one being built.
- Suggestion: (a) `universal/coding.md` § Scripts & pcmd gains one rule —— any script or hook that RESOLVES REPO PATHS must carry a `Root scope:` docstring line naming every root it walks and why the others are excluded, decided at creation when it costs a sentence, not retrofitted by a later audit. (b) extend `cp/ccsim/sandbox/cscpt_header_contract_regression_test.py` to assert that line on path-resolving scripts. (c) close the live instance —— put DAMF/DXMF's second root to the user for a verdict rather than leaving it undecided indefinitely.
- Ref: `close_202607090408.md` §1.9; `close_202607190110.md` §2; `ccsim_close_202607250023.md` §3.4; `gscpt/DAMF.py:48`; `gscpt/DXMF.py:60`

## `universal/close.md` —— no defined shape for a supplementary close with zero comms pairs
- Problem: four of 202607's twelve closes carry ZERO comms pairs because the real record lives in `AJAP_repo`, and each improvises its own formulation for the same situation —— an "N/A, out of scope by design" note, a "supplementary default-repo close" note, and a §4 heading with no pair count at all. `close.md`'s Non-DevPlan template hard-codes `## 4. Comms Files ([no.] pairs)` and says nothing about the case. Consequence: a third of the month's closes point elsewhere in four different ways, so the §4.2 funnel and `#wrap` must parse each afresh and the convention is re-derived every time instead of read.
- Suggestion: three lines in `close.md` § Preamble —— a SUPPLEMENTARY close (all comms in another repo/workspace) marks itself in Line 2's heading, writes §4 as `## 4. Comms Files (0 pairs)` with a single `- 4.1. N/A —— comms live in [repo/path]; full list: [file] §[n]`, and carries the pointer nowhere else. Lowest value of the five —— take it only if the wrap-reading cost is felt.
- Ref: `close_202607090408.md` §4.1; `close_202607130651.md` §4; `close_202607190110.md` §4.1; `close_202607231053.md` §4; `universal/close.md` § Non-DevPlan Template

## Root `CLAUDE.md` §3.1.6 —— TEAs fired whilst SAs were still in flight, four times
- Problem: §3.1.6 gates Turn-End Actions on "ensuring no SAs in-flight" and the gate was breached at least four times across two sessions. Not a comprehension failure and not a not-noticed one: each time the rule was read, understood, and consciously overridden by a judgement that the outstanding SA "would not matter" —— after which it returned with substantive work, forcing a second commit or a corrected claim. Per `cp/ccsim/CLAUDE.md` §8.7 a MISAPPLIED rule is fixed by rewriting words, but this one was applied and then set aside, so no rewording could reach it.
- Suggestion: replace the judgement with a mechanism —— a PreToolUse gate that blocks the commit itself.
- Ref: `ccsim_close_202607291954.md` §13.2–13.5
→ ✅ RESOLVED 202608012034 (Ref: `cscpt/alint.py`, `cscpt/alint_hook.sh`, `cp/ccsim/sandbox/alint_regression_test.py`) —— `alint` blocks `git commit`/`git push` whilst any dispatched agent has not reported back, exempts sub-agents' own commits, releases an agent gone quiet past a threshold so it can never brick the repo, and warns visibly on every path where it cannot read the evidence. Wiring proven through the real harness, not by hand.
- TWO RESIDUAL GAPS, recorded rather than implied closed: (a) a WORKFLOW in flight is not gated —— its launch record exposes a `transcriptDir` instead of a single file, so nothing ages it for the staleness release; (b) the harness honouring `exit 2` for THIS hook is untested, because only the main agent can trigger a block and a sub-agent is exempt by design. Probe for (b): with an SA in flight, run `git commit --dry-run` from the main agent —— it must be blocked with the alint message, and allowed once every SA has returned.
→ ✅ RESOLVED 202608012342 (Ref: `cscpt/alint.py`, `cp/ccsim/sandbox/alint_regression_test.py`) —— gap (a) CLOSED; gap (b) still open and unchanged. Workflows are now gated by the SAME rule as agents rather than a second mechanism: a launch is `taskId` + `taskType` (both required —— a bare `taskId` also marks 110 TodoWrite ticks and the Monitor sleep-loop's own record, so keying on it alone would have blocked every commit forever), it rests via the very same `<task-notification>` carrying its `taskId`, and it ages by the NEWEST mtime across its `transcriptDir` AND that directory's entries. The entries are load-bearing: appending to a file never updates its parent directory's mtime, so a dir-only clock would have called a furiously busy fleet stale and released it. Gating workflows separately is necessary rather than redundant —— 0 of run `wf_9704e270-7d9`'s 14 child agents appear in the main transcript, so nothing else was ever watching them, and a TEA1 fired mid-run passed straight through. Every escape covers workflows (TaskStop by task id, staleness release, `ALINT_OFF=1`); an unreadable `transcriptDir` holds the workflow LIVE and logs the distinct `wf?:` stage so it can never go dark unnoticed. Suite 53 → 71 cases, all green, with the workflow cases verified to FAIL against the pre-change file first; additionally validated against that run's real transcript truncated mid-flight, not fixtures alone.

## Query/response pairing was enforced in one direction only
- Problem: `.githooks/pre-commit` checked that a `response_` has a `query_`, never the reverse, so an orphaned `query_` committed silently. Root `CLAUDE.md` §3.5.3 was skipped live —— a new `ccsim_query_202608012325.md` was answered by appending 83 lines to the PREVIOUS turn's `ccsim_response_202608011950.md`, and the commit carried both into history without a word. Replaying all history, 152 of 472 query-ADD commits would have flagged.
- Suggestion: DONE —— `cscpt/hlint.py` gained a prompt-time pairing reminder (UserPromptSubmit, advisory, already registered) that names the owed `response_[TS]` before the turn's first write; `.githooks/pre-commit` gained a non-blocking reverse arm as the commit-time net; `cp/ccsim/sandbox/pairing_lint_regression_test.py` (37/37) pins both, with the real failing commit as its fixture and a standing liveness probe at `cp/ccsim/sandbox/ccsim_query_209912312359.md`. Residual gap recorded honestly: nothing fires at the moment of the WRITE, and no check can distinguish a sanctioned mid-turn answer-in-place from the breach.
- Ref: `cp/ccsim/hook_guide.md` §3, §7.3, §9.3.10; `ccsim_close_202607291954.md` §6.19

## Push allow-rules —— the short-circuit mechanism is still unproven
- Problem: narrow `git` allow-rules were added so TEA1's push escapes classifier gating, but nothing has ever demonstrated they are the reason a push succeeds. A live reproduction ran 3/3 clean in Auto mode using the exact blob and command form that were denied on 28 Jul —— yet that blob has been public on `origin/main` since the day of the denial, so "the rules short-circuit the classifier" and "this content is no longer sensitive" fit the evidence equally. The `cd`-in-compound hypothesis WAS refuted by the same run. Note also that the rules live at PROJECT scope only, whilst project-scope HOOKS are proven inert in this app —— so whether the Desktop app honours project-scope PERMISSIONS is itself unverified.
- Suggestion: the only test that discriminates would push genuinely non-public third-party personal data to a public repo, which is not worth doing. Leave it as a known unknown, and re-open only if a turn-end push is ever denied again —— that denial, with its stated reason, would be the evidence no safe experiment can produce. If it recurs, capture the verbatim refusal before retrying.
- Ref: `ccsim_close_202607291954.md` §5.3; commits `f8323e0`, `051665b`, `0f58ca3`


## `career_close_202608041700.md` —— SendMessage-continuing an SA with isolation:worktree silently strands edits
Problem: When an MA dispatches an SA with `isolation: "worktree"` and later continues that SAME SA via `SendMessage`, any file edit the SA makes lands only in its isolated git worktree, not the real working tree —— no error surfaces, and `git status`/`git diff` on the main tree show nothing changed. The MA can easily carry `isolation: "worktree"` forward by habit (copying an earlier call's parameters) when continuing a conversation, even though the original reason for isolating (e.g. parallel-write safety) no longer applies to a single continued SA. Discovered live this session: an SA was asked to edit a real AR file in place; the edit "succeeded" per the SA's own report but existed only in `.claude/worktrees/agent-[id]/`, recovered only because the MA happened to check `git log`/`git diff` for a mismatch before trusting the report.
Suggestion: Either (a) a `plint`-style advisory that fires when `SendMessage` targets an SA whose original dispatch had `isolation: "worktree"` set, reminding the MA to check whether isolation is still warranted for the continuation; or (b) document this sharp edge directly in the Agent tool's own isolation guidance, so "continuing an isolated SA" is flagged as a known gotcha rather than left implicit.
Ref: `career_close_202608041700.md` §6.1

## `universal/numbered.md` §Continuity —— reset when composing a fresh multi-topic response, not just on true 1st-of-session
- Problem: mid-session (`response_202607141726.md`), point numbering reset to 1 instead of continuing from the prior response's last point (16), despite replying to a numbered response (none of §Continuity's 3 reset-conditions applied). Root cause, best guess: the response covered several distinct sub-questions (`## 3`, `## 4`, `## 5`, `## Your Pref`) under fresh headings, and composing each section fresh made it easy to lose track of the CARRIED-OVER count from the previous response — a single-topic reply doesn't have this failure mode, a multi-heading one does. Caught by the user, fixed same session (`ec0ee2f`); every response afterward in the same session (5 more, across 2 further sessions of work) maintained continuity correctly once flagged, so the fix held — this is a composition-habit gap, not a comprehension gap.
- Suggestion: a `#numbered`-adjacent lint check (or a `nlint`-style advisory, mirroring `dlint`/`hlint`'s pattern) that reads the immediately-prior `response_` in the same folder, extracts its last top-level point number, and flags if the new `response_` starts anywhere other than n+1 (exempting the 3 documented reset conditions). Cheap, deterministic, catches exactly this class of mistake before the user has to.
- Ref: `response_202607141726.md` (pre-fix, git history); `response_202607141758.md`–`response_202607222100.md` (5 subsequent, all correct)

## `universal/cic.md` mandate lapsed across several delegated turns before being caught
- Problem: across multiple turns in one session, `#cic` was typed explicitly by the user (a hard §7.3 trigger — "MUST read first") but the underlying research was actually done via `WebSearch`/`WebFetch` only (triage-only per `cic.md`'s own text) inside SA dispatches that just said "use CIC" without quoting the file's actual mandate — `universal/cic.md` itself was never read by the MA this session until the user pointed out "no CIC use witnessed". The MA's own SA-briefing habit (name the tool, trust the SA to find/apply its governing doc) was the gap, not the trigger mechanism (§7.3 is unambiguous). Root-caused, fixed, and captured as a standing memory note (`feedback_sa_brief_frontload_mandate.md`) the same session — every SA dispatch afterward (CD/Avast/treadmill research, 6+ instances) quoted the relevant protocol file's operative text directly into the brief and used genuine CIC tool calls, self-reported by each SA as such.
- Suggestion: the pattern likely generalises beyond `cic.md` — ANY skill/pcmd with a "MUST" mandate (writing.md, coding.md, branding.md) is equally at risk of being named-but-not-applied once work is delegated to an SA that has no memory of this repo's conventions. Worth a one-line addition to root `CLAUDE.md` §9.03 (SA-spawning) or `cic.md`/`writing.md`/`coding.md`/`branding.md` themselves: when briefing an SA for work gated by one of these files, QUOTE its operative mandate into the prompt, don't just name the file/tool. (The memory note already carries this for the MA's own habit; a written rule would make it durable/discoverable beyond one session's memory.)
- Ref: this session's `response_202607092059.md` §Preamble (the self-correction); `feedback_sa_brief_frontload_mandate.md`

## `universal/sprint.md`'s slog mandate skipped for a short, single-dispatch `#sprint`
- Problem: `sprint.md` states the Sprint Log is mandatory "BOTH modes" with no size exemption, but a `#sprint` this session (treadmill belt-repair research: 1 SA dispatch, single-topic, ~15min wall-clock) completed with no `slog_[TS].md` ever created — apparently treated as "too small to need it" without that exemption actually existing in the doc. An earlier, larger `#sprint` the same session (`git_move_batch.py` testing, TS 202607080651) DID get a slog correctly. No compaction occurred either time, so the omission caused no actual data loss here, but the mandate exists precisely for the case where compaction DOES hit unexpectedly mid-sprint.
- Suggestion: either (a) `sprint.md` gains an explicit exemption for single-dispatch/short sprints (if the user is fine with that risk), or (b) reinforce that the slog is unconditional regardless of apparent sprint size — a short sprint can still get compacted, and the doc already says "BOTH modes" for a reason. Cheapest fix: a `#sprint` self-check the moment `#sprint` is recognised — before the first task begins, create the slog file, no size judgement call involved.
- Ref: this session's `slog_202607080651.md` (created, correct) vs the treadmill `#sprint` turn (no slog, `response_202607222100.md`)

## Root `CLAUDE.md` §3.1.6.2 chapter-mark never actioned for an entire session
- Problem: §3.1.6.2 requires marking a session chapter ("Turn [N]") as part of EVERY practical turn's TEA sequence, right before the final I/O-declarations batch. Across one entire session (25 comms pairs, `close_202608041735.md`), this was never done ONCE until the closing turn itself, where doing TEA2 for `#close` was the first time the rule was even consciously applied. No self-catch happened at any of the 24 earlier turns —— the rule was silently absent from execution, not misapplied. Consistent with the pattern already logged elsewhere in this backlog (`#cic`'s mandate, `numbered.md`'s continuity rule): a "MUST"-worded rule sitting in root `c.md` §3 alongside many others is easy to lose track of turn-to-turn without something forcing a check.
- Suggestion: mirroring the existing `alint`/`plint`/`nlint` advisory pattern —— a lightweight check that fires once TEA1 (commit) has happened for a turn and no chapter-mark tool call is found in that turn's tool-use log, printing a non-blocking reminder rather than a hard block, since chapter marks are cosmetic/navigational rather than data-integrity-critical.
- Ref: `close_202608041735.md` §3.6 (this session's own discovery, 0 chapter marks across 25 comms pairs until now)
