# RETIRED probe —— NOT a real query, and no longer a working probe

*This was the liveness probe for `cscpt/hlint.py`'s query/response pairing reminder. That check was PURGED from `hlint.py`, so naming this file now produces nothing. It is kept only so the retirement is visible: a probe that silently stops probing is exactly the failure `cp/ccsim/hook_guide.md` §7 exists to prevent, and a deleted one leaves the `hook_guide.md` §7.3 row pointing at a file nobody can find. It is scratch, never comms —— the far-future timestamp (`209912312359`) keeps it from colliding with a real comms file, and `cp/ccsim/sandbox/` keeps it from being mistaken for one.*

## Status

RETIRED. Submitting this filename as a prompt yields NO output, and that is now the correct result, not a dead-wiring symptom.

To test whether `hlint.py` is still wired at all, use the `#trigger` row of `hook_guide.md` §7.3 instead: submit a prompt containing a real bare `#trigger` and confirm a reminder line arrives, or check `cscpt/.hlint.log` grew a line for that prompt.

## Why the Check Went

At prompt-submit time the pairing check had no discriminating power: a brand-new `query_` has no `response_` beside it BY CONSTRUCTION, so it fired identically on a compliant turn and on the breach. Measured over the live log: 5 fires in 29 real invocations, roughly once per turn opened by a query file, at `~`124 tokens each.

Full reasoning, and the argument for where it belongs if it is ever rebuilt, is in `cscpt/hlint.py`'s own retirement note. Do not rebuild it here, and do not restore this probe without a check for it to probe.

## Do Not

- Do not create a sibling `ccsim_response_209912312359.md`. It would be an orphan response in a scratch folder, and `.githooks/pre-commit`'s forward arm would flag it on every commit that touched it.
- Do not rename this file. `cp/ccsim/sandbox/pairing_lint_regression_test.py` reads it by exact name to confirm the retirement is declared.
