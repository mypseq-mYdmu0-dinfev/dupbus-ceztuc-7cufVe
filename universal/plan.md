# Dev Plan (Development/Project Plan)

- Must be strictly #numbered (fetch `numbered.md` if not already) throughout: Nothing can be unnumbered at all except brief basic info before Part A) and constitutes at least 3 parts:
  - `Part A —— Phases`: From current stage all the way to finale (typically QC+delivery), where provisional content outline (numbering system temporarily changes to `S` e.g. `S01` for Section 1 of outline) shall be embedded in one of the phases if our project involves writeup/keynote, etc. (inapplicable to technical projects which would use architecture/other structure); ensure to specify which phase shall perform further research (if applicable) and refine/finalise the outline (if we have one); first phase = `## P01. [concise_title]` and its first item would be `### P01.1. [item_name]`; avoid having 9⁺ items in a phase (e.g. `P01.10` complicating search results), handle by another phase or, when necessary, additional level (e.g. `P01.1.1`)
  - `Part B —— Context`: For all chats in CP to retrieve a coherent context; first section (usually “Organisation & Scope” but NOT always true) = `## C01. [section_name]`
  - `Part C —— Addenda`: Each chat's end will create a `Chat Handoff` (to be sent to next chat only, not persistent) and an Addendum (to be added here in Part C of Dev Plan, persistent for all chats in CP); example of first section (NOT always true) = `## A01. Phase 1 Addendum (Chat 01) —— Planning`; examples of subsections (NOT always true) = `What Was Done` `Key Decisions Made` `Reminders for Later Chats` `Deviation from Dev Plan` `Remarks`