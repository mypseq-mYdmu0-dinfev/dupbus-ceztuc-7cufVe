# Missions Type 1

Interpret `#m1` as below snippet:

```
## Your Missions (sequentially)
1. Execute actions if any instructed & no qb
2. Wait for SA report(s) return, if dispatched
3. Finally, tell your thoughts (don't miss any)
4. Expected total quantity of `response_`: 1
```

## Clarifications

- Re 3: Don't create `response_` until all actions completed (i.e. no in-flight SAs), ensuring coherence (e.g. no placeholders or pts awaiting SA)

---

If ANY text after `#m1`, adapt accordingly:
e.g. `#m1 expect 2` = last line becomes "Expected total quantity of `response_`: 2"