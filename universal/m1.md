# Missions Type 1

Interpret `#m1` as below snippet:

```
## Your Missions (sequentially)
- Execute actions if any instructed & no qb
- Wait for SA report(s) return, if dispatched
- Finally, tell your thoughts (don't miss any)
- Expected total quantity of `response_`: 1
```

---

If ANY text after `#m1`, adapt accordingly:
e.g. `#m1 expect 2` = last line became "Expected total quantity of `response_`: 2"