# Missions Type 2

Interpret `#m2` as below snippet:

```
## Your Missions (sequentially)
- Tell your thoughts on all above first (don't miss any)
- Commit+push+declare this `response_` alone in chat first
  - Don't proceed further until the above two steps are delivered
    - "Delivered" = emitted by you; never stop here awaiting a reply
  - These interim actions are NOT TEAs —— the turn does NOT end here:
    - Root c.md §3.1.7.5's "nothing follows" governs the REAL TEA3 only
    - Root c.md §3.1.6's "no SAs in-flight" gates TEAs only, not these interim actions
  - Emit that declaration in the SAME message as your next tool call
- #sprint to execute actions as instructed above; use SA(s) if apt
- If needed, update THIS QUERY's `response_` —— from MY seat, cut MY reading load:
  - Appending sub-pts under existing pts each finding ANSWERS (default)
  - Crossing out (e.g. `~word~`) whatever the update supersedes or contradicts
    - NEVER a heading —— a struck heading reads as its whole section being gone
  - Creating new pts only if necessary (e.g. sprint report)
    - A parallel new pt that merely ANSWERS an existing one is the defect
    - e.g. 10 pts + 10 new = I read all 10 before learning they NO LONGER MATTER
  - Never park new findings inside the sprint report's pt
  - Avoid editing/deleting existing pts, despite not banned
- Declare again in batch as usual (per root c.md §3.1.6.3)
- Expected total quantity of `response_`: 1
```

---

If ANY text after `#m2`, adapt accordingly:
e.g. `#m2 expect 2` = last line became "Expected total quantity of `response_`: 2"