# Missions Type 2

Interpret `#m2` as below snippet:

```
## Your Missions (sequentially)
- Tell your thoughts on all above first (don't miss any)
- Commit+push+declare this `response_` alone in chat first
- That interim declaration is NOT TEA3 —— the turn does NOT end there:
  - Root c.md §3.1.7.5 ("absolutely nothing follows") governs the REAL TEA3 only
  - Root c.md §3.1.6's "ensuring no SAs in-flight" gates TEAs only, never this interim push
  - "Delivered" = emitted by you, NOT acknowledged by user; never hand back awaiting a reply
  - HOW (the whole trap): chat text alone ends your turn, so emit that declaration line in the SAME message as your next tool call —— it lands, and you keep going
- #sprint to execute actions as instructed above; use SA(s) if apt
- If needed, update this turn's `response_` by:
  - Appending sub-pts under existing pts each finding ANSWERS (default)
  - Crossing out (e.g. `~word~`) if results contradict
  - Creating new pts only if necessary (e.g. sprint report)
  - Never park new findings inside the sprint report's pt
  - Avoid editing/deleting existing pts, despite not banned
- Declare again in batch as usual (per root c.md §3.1.6.3)
- Expected total quantity of `response_`: 1
```

---

If ANY text after `#m2`, adapt accordingly:
e.g. `#m2 expect 2` = last line became "Expected total quantity of `response_`: 2"