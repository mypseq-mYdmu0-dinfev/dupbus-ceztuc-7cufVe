# Missions Type 2

Interpret `#m2` as below snippet:

```
## Your Missions (sequentially —— 1 & 2 both DONE before 3)
1. Tell your thoughts on all above first (don't miss any)
2. Commit+push+declare this `response_` ALONE in chat:
- 2.1. TYPE that declare line in the SAME msg as your next tool call
- 2.2. Move straight on to Step 3 —— never pause, never await my reply
3. #sprint to execute actions as instructed above; use SA(s) if apt
4. Update THIS QUERY's `response_` if needed —— cut MY reading load:
- 4.1. Append sub-pts under the existing pt each finding ANSWERS (default)
- 4.2. Cross out (e.g. `~word~`) whatever the update supersedes/contradicts
- 4.3. Create new pts only if necessary (e.g. sprint report)
- 4.4. Never park new findings inside the sprint report's pt
- 4.5. Avoid editing/deleting existing pts (for record)
5. Declare again in batch as usual (per root c.md §3.1.6.3)
6. Expected total quantity of `response_`: 1
```

## Clarifications

- Re 2: NOT TEA3 —— §3.1.7.5's "nothing follows" & §3.1.6's SA gate bind TEA3 only
- Re 2.1: chat text as a message's LAST content ends your turn —— that is the trap
- Re 2.1: a tool-call-only message is the twin miss —— the line simply went untyped
- Re 4.1: e.g. pt 2 promising 3 answers → 2.1/2.2/2.3, NOT new pts
- Re 4.2: NEVER a heading —— a struck heading reads as its whole section being gone
- ALWAYS ensure #numbered #bite `response_`; actively use `glossary.md` to trim verbose

---

If ANY text after `#m2`, adapt accordingly:
e.g. `#m2 expect 2` = last line becomes "Expected total quantity of `response_`: 2"
