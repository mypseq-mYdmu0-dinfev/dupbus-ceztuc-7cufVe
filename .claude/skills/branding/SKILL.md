---
name: branding
description: Use when creating or editing ANY design or visual-output task — HTML, slides, documents, decks — unless an official template is enforced. Loads the user's personal visual-identity guideline (colour palette, typography, logo, layout conventions).
---

Read `universal/branding.md` in full and follow it for the current task. This skill is a thin alias for that project protocol — the authoritative, always-current rules live there, not here. (The manual `#trigger` / §7.2 conditional-read for this file still applies too; this skill just adds a model-invoked path.)
