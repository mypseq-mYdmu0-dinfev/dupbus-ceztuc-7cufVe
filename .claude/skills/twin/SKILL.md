---
name: twin
description: Use when a conversation plausibly calls for constructing and embodying a digital-twin persona of a real or fictional entity — sustained in-character roleplay as that entity, not a quick one-off impression (e.g. 'be tuned [name]'). Loads the digital-twin construction protocol (identity resolution, cognition modelling, realism and argument/surrender rules).
---

Read `universal/twin.md` in full ONLY IF the user has genuinely asked to enter a sustained digital-twin persona (per the description above). This is a LARGE, hard-to-reverse behavioural commitment (staying in-character indefinitely until `#exit`) — do NOT auto-enter it: first PROPOSE/confirm with the user and await explicit approval, then proceed. Thin alias for the project protocol — the authoritative, always-current rules live in that file, not here. (The manual `#trigger` / §7.2 conditional-read still applies; this just adds a model-invoked propose-path.)
