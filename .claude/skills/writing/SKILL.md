---
name: writing
description: Use when creating or editing ANY deliverable — anything sent to or seen by a third party (email, report, slides, letter, LinkedIn post, cover letter, job application), when the user mentions casual or whatsapp, or before linting finished copy with dlint. Loads the writing-style rules and the mandatory FULL-mode dlint workflow.
---

Read `universal/writing.md` in full and follow it for the current task — including its § Deliverable Lint, which is the authority on the MANDATORY FULL-mode run of `cscpt/dlint.py` before any deliverable is output (`<path>` for a separate-file deliverable, `--text "…"` for copy embedded in a `response_`), and on looping until 🔴 RED = 0. RUN that script, never read it. This skill is a thin alias for that project protocol — the authoritative, always-current rules live there, not here. (The manual `#trigger` / §7.2 conditional-read for this file still applies too; this skill just adds a model-invoked path.)
