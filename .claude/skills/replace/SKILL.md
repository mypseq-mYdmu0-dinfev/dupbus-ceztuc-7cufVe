---
name: replace
description: Use when `#replace` is prompted, or before editing/proposing a change to a file with 50+ lines of code or 500+ words of text. Loads the replace protocol (verbatim quote-snippet Replace/With blocks instead of regenerating full content).
---

Read `universal/replace.md` in full and follow it for the current task. This skill is a thin alias for that project protocol — the authoritative, always-current rules live there, not here. (The manual `#trigger` / §7.2 conditional-read for this file still applies too; this skill just adds a model-invoked path.)
