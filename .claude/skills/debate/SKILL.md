---
name: debate
description: Use when the user says `#debate`, or faces a genuinely critical, high-stakes decision needing multidimensional pros and cons with pre-empted counter-arguments surfaced across debated stances — not routine or low-stakes questions. Loads the multi-agent role-playing debate protocol (debater sub-agents around a shared, live board).
---

Read `universal/debate.md` in full ONLY IF the current situation genuinely warrants a structured debate (per the description above). This is a LARGE, EXPENSIVE multi-agent operation (debater sub-agents around a live board) — do NOT auto-run it: first PROPOSE it to the user (what it entails plus rough cost) and await explicit approval, then execute. Thin alias for the project protocol — the authoritative, always-current rules live in that file, not here. (The manual `#trigger` / §7.2 conditional-read still applies; this just adds a model-invoked propose-path.)
