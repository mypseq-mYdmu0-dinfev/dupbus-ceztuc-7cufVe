# Sessions

- This folder stores comms files & some outputs from CC sessions.
- Its root (outside `[YYYY]/`) has some templates (`*_query_.md`) for user's copy-pasting to create new queries; never touch them.
- `queued_queries/` stores queries being drafted by user (e.g. during sesL); unless asked to, CC should never visit this folder.