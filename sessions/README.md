# Sessions

- This folder stores comms files & temporary/throwaway outputs from CC sessions.
- Its root (outside `[YYYY]/`) has some templates (`*_query_.md`) for user's copy-pasting to create new queries —— never touch them; disregard these blank files.
- `queued_queries/` stores queries drafted by CC or the user (e.g. during sesL); unless asked to, CC should never visit this folder.