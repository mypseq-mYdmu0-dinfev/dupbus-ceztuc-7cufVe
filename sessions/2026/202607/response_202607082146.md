# Response to query_202607082146.md
*No Fix Needed — GUI Settings Suffice*

1. Both SAs' findings converge cleanly —— no code modification is needed or advisable.

2. Code-investigation SA (`/Applications/Avast.app`, read-only):
- 2.1. Popups come from 2 distinct engines inside the app: `Notifications.framework` (Avast's own custom alert/toast UI, e.g. scan results) and `IPMStuff.framework` (in-product marketing/upsell popups, fed every 2h from Avast's own server for its other Gen Digital products)
- 2.2. `Notifications.framework` already contains a built-in `DoNotDisturbManager` tied to macOS Focus status (`Info.plist`'s `NSFocusStatusUsageDescription` literally says "so we can silence our notifications for you") —— i.e. Avast is DESIGNED to self-silence when a Focus mode is active
- 2.3. No raw config-file flag exists to kill popups outright; no "silent/gaming mode" key found in its preferences plist

3. CIC web-research SA (official Avast docs + community):
- 3.1. Confirms 2.2 exactly, from the OTHER direction: `☰ Avast menu ▸ Preferences ▸ Notifications tab` has 2 checkboxes —— "Disable notifications when in Focus mode" and "Disable notifications when in full screen mode"
- 3.2. Separately, `☰ Preferences ▸ Privacy tab` has an "offers/app-usage data sharing" checkbox —— unchecking it specifically kills the upsell/promo popups (the `IPMStuff.framework` category), independent of Focus mode
- 3.3. Officially confirmed by an Avast moderator: Mac has NO global "Silent Mode" (unlike Windows) —— routine "scan complete"/"definitions updated" popups during normal unfocused use are a genuine product gap, not a hidden setting; worth raising as a feature request to Avast directly if you want that specific case gone too
- 3.4. Disabling notifications does NOT disable the actual weekly scan —— your auto-scan keeps running regardless, confirmed by Avast's own docs

4. Recommended plan (nothing executed —— your call to action):
- 4.1. Open Avast ▸ Preferences ▸ Notifications tab → check BOTH boxes (Focus mode + full-screen)
- 4.2. Open Avast ▸ Preferences ▸ Privacy tab → uncheck the app-usage/offers-sharing box (kills the marketing popups specifically)
- 4.3. Set up a macOS Focus mode (System Settings ▸ Focus) and grant Avast permission to read your Focus status when prompted (or via System Settings ▸ Privacy & Security ▸ Focus) —— then leave that Focus mode on as your default state; Avast's own DND hook (2.2) will then suppress its custom popups whenever it's active
- 4.4. This 3-step combo (4.1–4.3) should eliminate ALL popup categories the app can suppress via its own logic —— the one residual gap (3.3) is routine scan-complete alerts appearing during normal, non-Focus, non-full-screen use, which has no known fix on Mac short of Avast shipping one

5. Explicitly NOT done, and NOT recommended, per both SAs: no binary/plist patching, no blocking Avast's servers via `/etc/hosts` or firewall —— the app is code-signed, checks for updates every 10 min, and any file-level tamper risks breaking it or being silently reverted; the GUI path in §4 achieves the same goal with zero risk.
