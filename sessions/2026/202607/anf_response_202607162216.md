# Response to anf_query_202607162216.md
*Pain-Map, Research, and 3-Tier Recommendation*

## Findings & Recommendation

1. Pain-Point Map —— Streamlined & Enumerated
- 1.1. Freemium ads —— minor, tolerable, but real enough to name
- 1.2. Usage shifted home/batch (weekly`~`monthly) —— the broken web portal is now your PRIMARY interface, not a fallback
- 1.3. Web portal broken in Safari since the revamp —— forces Chrome, the only such case besides CIC
- 1.4. Import-history page reloads your ENTIRE historical volume every time —— crashes the tab even in Chrome
- 1.5. Fear of vendor collapse —— losing years of records; a business-continuity worry, NOT security
- 1.6. CSV import only takes Date/Payee/Note/Amount —— Label & Category excluded, forcing manual per-record re-editing after every import
- 1.7. Spreadsheet rejected —— its UI froze at 100⁺ rows; hopeless at your real scale (tens of thousands)
- 1.8. Receipt digitisation gap —— most transactions are still paper-only; your existing `ocr_reads.py` (in `gscpt/`) does raw text extraction only, no structured fields yet
- 1.9. Underlying constraint tying all of the above together —— volume is too high for either of us to handle manually, so ANY solution needs real bulk/programmatic (API/MCP) access, not just a nicer UI

2. Headline Finding —— Changes The Whole Calculus
- 2.1. Wallet (BudgetBakers) just shipped a Premium-only BETA REST API AND a native MCP integration (live-verified today) —— Claude/ChatGPT can already read/write your Wallet data in natural language, opt-in per data type
- 2.2. If it holds up, this could resolve 1.4 & 1.6 directly —— bulk API writes bypass the crash-prone import UI entirely, and its schema includes a proper Labels endpoint, so records could be created with Label + Category already set —— no more manual re-editing
- 2.3. Caveats —— BETA (can break/change), Premium-subscriber-only (cost unconfirmed, see Q1), and a RELAUNCH after BudgetBakers previously removed their old API (1,055-vote "Bring back API" request still open on their own feedback board) —— don't treat it as guaranteed-stable
- 2.4. Does NOT resolve 1.5 (collapse fear) or the ongoing sync/reliability complaints found in reviews

3. Recommendation —— Three Tiers, Try In This Order
- 3.1. Tier 1 (cheapest test): upgrade to Wallet Premium, connect its new MCP directly, see if it actually fixes 1.4 & 1.6 in practice
  - 3.1.1. Zero migration effort; reversible; directly targets your two most time-costly pains
  - 3.1.2. Risk: BETA + relaunched-API history means this could regress later —— don't treat it as your forever-home without a backup plan
- 3.2. Tier 2 (if Tier 1 disappoints, or you want a clean break): migrate to Actual Budget —— free, open-source, self-hosted
  - 3.2.1. Structurally solves 1.5 —— there's no company to collapse, you hold the raw file directly; proper bulk API + several ready-made MCP servers already exist, none need building from scratch
  - 3.2.2. Cost: official mobile apps are deprecated (PWA or community client only, acceptable given OTG is read-mostly for you) plus a one-off historical-data migration effort
- 3.3. Tier 3 (last resort, highest effort): self-build on Supabase or Cloudflare D1
  - 3.3.1. Fully feasible, NOT blocked on any missing building block —— Supabase ships an official MCP server already (zero custom MCP code needed if chosen); a custom one would only run `~`100`~`200 lines regardless
  - 3.3.2. But: no existing template shortens this (confirmed —— no Claude Skill exists for personal finance tracking), and the receipt-OCR-to-structured-data step is still unbuilt even here —— reserve this tier for if Tiers 1 & 2 both genuinely fail your test

4. "Can You One-Shot A Working Prototype?" —— Direct Answer
- 4.1. A MINIMAL prototype (Supabase/D1 + its MCP + a barebones read/search page) —— yes, plausibly one-shot
- 4.2. The FULL vision (migrating years of BudgetBakers history out, building receipt-OCR into structured records, polish) —— no, that's a multi-phase project, not one session; flagging this now so expectations are calibrated upfront

5. Open Questions
- Q1. Wallet Premium pricing wasn't surfaced by research —— want a quick follow-up look-up before you decide on Tier 1, given the whole tier hinges on cost?
- Q2. Pulling your FULL history out of BudgetBakers looks payment-gated regardless of final direction (per your own note) —— OK to treat a short Premium subscription as a likely prerequisite either way, independent of whether you end up staying or migrating?
- Q3. Self-hosting Actual Budget (Tier 2) means something —— you or me —— needs to run a small always-on server —— do you already have somewhere in mind (spare machine, cheap VPS), or is that its own open thread?

6. Scope Note & Next-Step Logistics
- 6.1. Finance/trading (the `trade.md` pcmd, likely an Agent SDK per your own suspicion) is understood as explicitly deferred —— kept entirely out of scope this session per your own scope-creep warning
- 6.2. `cp/anf/` currently holds only a placeholder —— once you've picked a tier from point 3 (or told me otherwise), a proper `CP_index_cc.md` would follow before any actual building starts, per your own "discussion only" instruction this round

---
*`#opt`: Below are optional reading.*
---

## Appendix —— Full Research Detail (Record-Keeping, Not Bite-Size)

7. Appendix —— Full SA Findings
- 7.1. BudgetBakers/Wallet Deep-Dive (SA1)
  - 7.1.1. Company —— founded 2010 in Prague; `~`A$11.2M (US$7.8M) raised across 5 rounds, last `~`2021; 11`~`50 staff, "Active"; no new funding disclosed in 5⁺ years (ambiguous signal —— bootstrapped-fine or fundraising-stalled)
  - 7.1.2. Health signals (live-verified, all positive) —— `~`753K monthly web visits; both apps updated within the last `~`3 weeks; 10M⁺ downloads, 4.7★/382K ratings (Google Play), 4.6★/5.9K (Apple); survived a 2018 near-halving pivot then went cash-flow-positive —— but Trustpilot only 2.8/5 (32 reviews, small/unclaimed profile) and no public status page exists
  - 7.1.3. Safari/web-portal breakage —— not confirmed as Safari-specific; their own help docs name Edge/Opera instead; generic cache/cookie fix helps only 5% of readers (12/240); a 3-year-old acknowledged bug was still reported broken 6`~`8 months later; one Trustpilot review describes a permanent "synchronising" screen across three browsers/PCs
  - 7.1.4. Export —— Premium-only (free tier: none at all); clearing filters + Select All on Records should capture full history as CSV/XLS, but size cap unconfirmed and whether this page shares the Imports page's crash risk is unconfirmed; exported columns confirmed = date/amount/category/account —— Label inclusion UNCONFIRMED
  - 7.1.5. Import —— confirmed Amount+Date mandatory, Note/Payee/Currency optional only, Category/Label simply excluded (exactly confirms pain 1.6); recommended cap `~`1,000 rows/file
  - 7.1.6. Mitigation you may not be using yet —— "Automatic Rules" (auto-Category from Note/Payee keywords) + "Bulk Edit Records" (multi-select, set Category/Label on many at once) —— meaningfully less tedious than one-by-one, though still not import-time mapping
  - 7.1.7. NEW —— Premium BETA REST API + native MCP: bearer-auth endpoints for records/accounts/categories/budgets/goals/standing-orders/labels/record-rules; GET up to 200/request, batched writes (POST`~`20/PATCH`~`10/DELETE`~`10), 300 req/hr; MCP lets Claude/ChatGPT read+write in natural language, opt-in per data type —— but it's a RELAUNCH after BudgetBakers previously removed an API (1,055-vote "Bring back API" request still on their own feedback board)
  - 7.1.8. Other complaints & switcher signal —— general slowness/crashing and `~`1yr-unresolved iOS`~`web sync bugs per reviews; CSV date-format & duplicate-transaction bugs on re-sync also reported; a live r/BudgetBakers thread (`~`7 months old) shows a paying user leaving over bank-sync failures plus a "support has given up" perception, naming Spendee, Toshl, Bilance, MoneyWiz, PocketGuard, Actual Budget, Firefly III, Cashew, and Lunch Money as what people switch to
- 7.2. Perplexity Thread Extraction (SA2, thread `624f0280`)
  - 7.2.1. Reframe —— this thread is titled "Can I run py scripts on GitHub"; it's the DIY/self-build angle (not a comparison of named finance apps), so it corroborates 7.4 more than 7.3 —— don't conflate the two
  - 7.2.2. Tools/approaches named in-thread —— GitHub Actions, Codespaces, Pages (static-only, confirmed no server/DB); a "Run Py Script" Marketplace action; local clone+venv; SQLite as the data format; sql.js/sql.js-httpvfs (client-side SQL over a hosted SQLite file); github-db/github-as-a-database wrapper projects; hosted-DB backends Neon, Supabase, CockroachDB, Aiven, InfinityFree, db4free.net, Turso, a self-hosted VPS (Contabo/Netcup examples), MongoDB Atlas, Upstash, and DataStax Astra DB; Cloudflare Workers as a backend-API layer
  - 7.2.3. Thread's own conclusion —— GitHub alone can't be a queryable DB server; a GitHub-Pages+SQLite+sql.js setup works read-only but is unsafe for real transactional writes (no concurrency control); recommends hosting the frontend on GitHub Pages with the DB on Neon or Supabase behind a proper backend API
  - 7.2.4. Staleness checks run (CIC-validated today) —— Neon's free tier is NOW 0.5GB storage/project (was 3GB when the thread was written, tightened after its 2026-01-15 Databricks-acquisition repricing, though compute allowance doubled and paid storage got cheaper); CockroachDB's free "Serverless" tier was renamed "Basic" (Dec 2024) but still exists; Turso's Dec-2023 data-loss incident (0.07% of free DBs, fixed within `~`1hr, users compensated) is real but its infrastructure has since been rebuilt on AWS S3 with much stronger durability claims; Supabase's 7-day auto-pause-when-idle is confirmed current; InfinityFree's "unlimited databases" claim is misleading —— actually 400 DBs × 50MB cap each, not unlimited storage
  - 7.2.5. Sources the thread itself cites for further reading (unvalidated, triage-level) —— github.com/erickthered/free-db-services; a bmaupin gist of free DB services; merginit.com's free-DB-hosting blog post; hostadvice.com's SQLite-hosting page; codeless.co, guru99.com, tech-recipes.com, and hostingadvice.com "free database hosting" round-ups; ⚠️ one Townscript.com link flagged as spam/SEO-farmed, not a real source —— disregard it
  - 7.2.6. Operational observation —— whilst this SA had a CIC tab open, other tabs in the SAME shared real-Chrome tab-group visibly changed (BudgetBakers help articles, Trustpilot, Google Play, Airtable docs, Cloudflare Pages docs) —— almost certainly the three other parallel SAs' own CIC activity, not anything this SA triggered; see 7.5 for what this means going forward
- 7.3. Ready-Made Alternatives, Ranked (SA3)
  - 7.3.1. Top 5, ranked (all pricing converted at today's `~`US$1:A$1.43 rate):

| Rank | Option | Cost | MCP/API | Biggest Caveat |
|---|---|---|---|---|
| 1 | Actual Budget | Free (self-hosted) | Official bulk API; 3 community MCP servers | Official mobile apps deprecated —— PWA/community client only |
| 2 | Firefly III | Free (self-hosted) | Confirmed bulk endpoint; 3 MCP servers | No official iOS app; "Export" ≠ real backup per its own docs |
| 3 | Sure | Free (self-hosted) | Dedicated MCP server; native Anthropic support | Alpha-stage native mobile; documented perf issues at scale |
| 4 | Lunch Money | `~`A$14/mo (US$10/mo); no permanent free tier | Best AI-agent-oriented API; 6⁺ MCP servers | Fails your "free" requirement outright |
| 5 | YNAB | `~`A$21/mo or `~`A$156/yr (US$14.99 / US$109); no free tier | Mature API; best-in-class apps | Fails "free" requirement —— benchmark only |

  - 7.3.2. Ruled out —— Monarch Money (subscription-only, no official API, only unofficial reverse-engineered libraries warning they can break/violate ToS anytime); Copilot Money (no real API —— its one "MCP server" just reads a local SQLite file on the same Mac, no web/Android either); PocketSmith (has freemium+API but scored 29/100 on a third-party "agent-readiness" audit —— deprioritised, not disqualified, worth a second look only if 1`~`3 don't pan out)
  - 7.3.3. On your rejected-spreadsheet question —— Airtable's free tier hard-caps at 1,000 records/base (Team plan is `~`A$29/seat/month [US$20] for 50,000/base —— still thin at your scale), which rules it out regardless of performance; but the freeze you actually hit looks attributable to the spreadsheet UI's own rendering/formula-recalculation engine, not a hard data ceiling —— pure API access (Sheets/Airtable/Supabase APIs) moves raw JSON without ever rendering a grid, so tens of thousands of rows is fast via API alone on any of them; the catch is if "browsing on the go" ever means reopening the native grid at full scale, which could reproduce the original pain unless you stick to filtered views
  - 7.3.4. Bottom line from this thread —— if free/self-hosted is a firm requirement, Actual Budget first, Firefly III close second, Sure a bolder newer third; if a small subscription becomes acceptable, Lunch Money is the single most purpose-built option found for exactly this "AI agent bulk-edits my transactions" use case
- 7.4. Self-Build Feasibility (SA4)
  - 7.4.1. Backend/DB free-tier options (all CIC-verified today) —— Supabase (500MB, unlimited API requests, official first-party MCP server already exists, BUT auto-pauses after 7 days idle —— relevant given your weekly/monthly batch usage pattern); Cloudflare D1 (5GB storage, 5M row-reads/day & 100K row-writes/day free, never pauses); Neon (0.5GB/project but 100 free projects, scales to zero after 5min idle yet auto-wakes with no manual restore); Turso (5GB, 500M row-reads & 10M row-writes/month); Google Sheets via its API (10M-cell ceiling, 300 read+300 write requests/min —— confirmed API-only access sidesteps the grid-freeze problem, but the per-minute quota means bulk edits must be chunked); Airtable RULED OUT (1,000-record hard cap on free tier); PlanetScale and Xata both ruled out —— their free tiers were discontinued (Apr 2024 and 28 Feb 2026 respectively)
  - 7.4.2. No shortcut exists —— Anthropic's own `knowledge-work-plugins/finance` is corporate accounting only (close-management/SOX/reconciliation —— not applicable); the official `anthropics/skills` repo has no finance/budget skill; the one community template found (`danielrosehill/Claude-Budgeting-Plugin`) stores data as plain markdown files and itself lists DB+MCP+OCR as unbuilt "Future Opportunities" —— confirming the gap you'd be filling isn't solved anywhere else already
  - 7.4.3. Frontend hosting (all CIC-verified today) —— Cloudflare Pages (unlimited free requests/bandwidth) + Cloudflare Workers (100K free requests/day) can pair with D1 as a single-vendor free stack covering DB AND frontend at once; Vercel (1M free requests/month) is a solid alternative; GitHub Pages alone is static-only and cannot talk to a database directly
  - 7.4.4. Building a custom MCP server, if needed —— official Python (`FastMCP`) and TypeScript SDKs exist; a working 2-tool example runs `~`80`~`100 lines; your use case (list/add/bulk-add/bulk-rename/search transactions) would be a similarly small handful of thin SQL-calling tools —— genuinely one-shot-buildable as a component; Anthropic's own `mcp-builder` skill recommends TypeScript + Streamable HTTP for this; moot anyway if Supabase is the chosen backend, since its official MCP server already covers it
  - 7.4.5. Still-open gap either way —— your existing `gscpt/ocr_reads.py` only extracts raw text from a receipt photo; parsing that raw text into structured fields (Date/Payee/Amount/Category) is a separate, still-unbuilt step in EVERY path considered, not just the self-build one —— you currently have `~`50⁺ paper receipts plus some digital ones (Woolworths, NSW Transport) waiting on this regardless of which tier wins
- 7.5. Operational Note For Future Multi-SA Dispatches
  - 7.5.1. Two of the four SAs above independently noticed the SAME real-Chrome tab-group changing underneath them mid-task —— because CIC drives your actual browser, four SAs running CIC in parallel all share ONE browser, so they can see (and in principle collide with) each other's tabs
  - 7.5.2. No actual failure resulted this run —— but worth flagging: if a future multi-SA CIC dispatch ever produces a wrong-page-read or similar glitch, this shared-tab-group behaviour is the first thing to suspect; a mitigation would be serialising CIC-heavy SAs rather than running them fully in parallel, at the cost of wall-clock time
  - 7.5.3. Agent IDs preserved in case any Q above gets a "yes, dig deeper" —— continuing the SAME SA (via SendMessage) reuses its cached context instead of re-researching from scratch: SA1 (BudgetBakers) = `a88f875a8c3699517`; SA2 (Perplexity) = `a2e108e33938ee4fd`; SA3 (Alternatives) = `a744db243887c79e6`; SA4 (Self-Build) = `af3fedad8db9e18de`

---

## Yellow-Flag Justifications

- L39`~`41 (`Q1.`/`Q2.`/`Q3.` not in `N.N.` form): intentional —— `universal/glossary.md` defines Qs as their own labelled convention ("raise all Qs (Q1, Q2…) & Bs (B1, B2…) separately & #numbered... NEVER label as QB1, QB2"), distinct from ordinary `N.N.` sub-points precisely so a question reads as a question at a glance. Kept as `Q1.`/`Q2.`/`Q3.` under numbered point 5 rather than renumbered to `5.1.`/`5.2.`/`5.3.`.
- L99 (this justification list itself un-numbered): intentional —— §3.5.7 asks only that yellow-flag justifications sit as the LAST content of the file, not that they join the body's `N.N.` scheme; a footnote-style aside keeps it visibly separate from points 1`~`7.
