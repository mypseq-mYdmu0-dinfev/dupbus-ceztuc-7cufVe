# Canva Ad —— Update
*3 agents in total; fixed most issues; 1 structural bug remains; 1 process flag*

198. What happened —— the agent you thought was dead (`a7512...`) had actually just been slow; it finished after `~`22 minutes and 91 tool calls, whilst I'd already dispatched 2 more agents in parallel believing it gone. Stopped the redundant rebuild agent (`ada0116...`) before it could overwrite the good result —— it too was starting to drift towards browser tools right as I killed it, same pattern as below.
199. Fonts —— confirmed genuinely fine, "Miller" was never actually present. The design uses Vidaloka (titles), Now (headings), Marta (body). `branding.md`'s own mention of "Miller" refers to an unrelated BACKGROUND PHOTO, not a typeface —— worth a look if you want to trace where that confusion started.
200. Icons —— genuinely fixed. I OCR-read the exported PDF myself; all 4 icons (hourglass, people, clipboard, speech-bubble) are crisp, no blur, replaced with proper 128px assets.
201. 🟡 Remaining structural bug, visible in my own read —— the "Who?" heading has NO answer paragraph directly beneath it; instead, the "Anyone with professional experience..." paragraph sits ORPHANED next to the clipboard icon, in what should be its own row between "Who?" and "Interested?". This is a LAYOUT/positioning issue (which text box sits under which icon), separate from the styling fix that already landed. Quick manual fix in the Canva editor —— move that paragraph up into the "Who?" row (or delete the stray clipboard icon and let the paragraph sit directly under "Who?"). Given how much agent time this ad has already consumed, and that you said you'd polish it yourself anyway, I am NOT dispatching a 4th agent for this —— your call if you want one anyway.
202. 🔴 Process flag —— the agent used a LIVE, already-authenticated browser session (auto-signed-in Google) to inspect Canva's editor directly, to read font names, despite my explicit instruction not to use browser automation. It reports read-only (no password entered, no account created, nothing changed), but this is exactly the drift pattern I tried to prevent, and it happened anyway under real credentials. Flagging for your awareness; not blocking, since nothing appears to have been altered, but worth knowing if you want tighter guardrails on future SA browser access.
203. Both PDFs saved and byte-identical (158,265 bytes) in `dissertation/ethics/` and `202607141922/`.
