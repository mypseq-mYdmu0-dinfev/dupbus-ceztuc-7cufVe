# Response to
> done, try to access/edit that CD project again

1. Progress: `/design-login` itself wasn't available, but your client ran `/design-consent` instead, which succeeded —— "Design agent access granted for your Claude Design projects." So the AUTH layer is now genuinely connected (this is real progress, not another dead end).

2. But `DesignSync.list_projects` still comes back EMPTY —— retried twice (incl. after a short pause for propagation lag), same result both times.

3. My best-supported read, given the tool's own description ("list DESIGN-SYSTEM projects... filtered to writable projects only"): "Tall Poppy Syndrome research recruitment" is very likely a REGULAR Claude Design project, not specifically created as the special `PROJECT_TYPE_DESIGN_SYSTEM` kind —— `DesignSync` is scoped ONLY to that one project type (component-library-style design systems), not to every Claude Design canvas/project you can create. That would fully explain empty-even-after-consent —— not a permissions gap, a project-TYPE gap.

4. I can't self-verify this distinction (no method exists here to browse/create-by-name a NON-design-system project or to confirm your project's actual type) —— QB, your call:
- 4.1. Could you check on claude.ai/design itself (now that you're set up) whether "Tall Poppy Syndrome research recruitment" shows a "Design System" label/badge, or is just a plain project/canvas? That'd confirm/kill this theory in one look.
- 4.2. If it turns out to be a regular (non-design-system) project, `DesignSync` genuinely cannot reach it —— the only path back to the original goal (prove I can add a "TESTING" overlay) would be via CIC/browser after all, this time with you actually logged in (unlike my earlier failed attempt on a fresh, unauthenticated tab) —— tell me if you want that retried now that auth exists in your real browser too.

---
*Yellow justification: L4 period sits inside the closing quote because it's verbatim from the tool's own output message ("...Claude Design projects.") —— original to the quote per Hart's rule.*
