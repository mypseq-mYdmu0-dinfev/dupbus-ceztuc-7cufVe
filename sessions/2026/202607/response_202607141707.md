# Response to query_202607141707.md
*Root Cause Found —— Projects ≠ Design Systems (Structural, Not a Bug)*

1. Read all 3 screenshots directly myself (quick, 3 images —— didn't need an SA for this part; used one where genuine web research was needed, as usual).

2. Diagnosis, fully confirmed by screenshot 3's own on-screen hint text:
- 2.1. Claude Design has 2 SEPARATE galleries: "Projects" (home tab —— where "Tall Poppy" lives) and "Design systems" (a different tab). `create_project` via `DesignSync` can ONLY create the SECOND kind —— its own tool description says so verbatim ("create a new design-system project"). That's why "Random Test Card"/"CC Access Test" never appeared under Projects —— not a failure, a different, correct gallery.
- 2.2. Inside a design-system project, screenshot 3 shows the exact reason nothing rendered: "No cards yet — add `<!-- @dsCard group="..." -->` as the first line of any .html file." My file didn't have that marker, so it existed (confirmed earlier via `list_files`) but showed as nothing in the visual pane —— only reachable via the raw file tab ("No file open" dropdown), which is presumably the "dice thing... seen in here (CC)" you noticed.

3. Fixed the immediate issue: added `<!-- @dsCard group="Random" -->` as line 1 of `Random Card.dc.html` and re-pushed via `write_files` (`{"written":1}`) —— it should now render as a proper card in that project's Design System pane. Reload it to check.

4. But that only gets you "visible in a Design System pane", not "on the Projects home tab like Tall Poppy" —— `DesignSync` structurally cannot create THAT kind of project; there is no `type` option, no alternate method. 3 ways forward, your call:
- 4.1. Accept design-system-pane visibility as good enough for testing CC's write access (already working, per §3)
- 4.2. You create a new regular Project yourself via the CD web UI (the "What should we create?" box on the home/Projects tab), then Share → Claude Code → Send it to me exactly like you did for Tall Poppy —— guaranteed to work, same proven path
- 4.3. I attempt it via CIC instead —— now that you're actually logged into the real browser (unlike my earlier failed unauthenticated attempt), I could type a prompt into that same "What should we create?" box and submit it myself, generating a real Project the CD way. Slower/heavier than 4.2 and still browser-automation-based rather than direct MCP, but possible if you'd rather I drive it.

5. Also —— separate from all this —— please double-check the Tall Poppy "TESTING" overlay by opening THAT project directly (click it under Projects in screenshot 1), not the Design systems tab: it's a regular Project, so it should render normally there, unlike the dsCard issue above which only affects design-system projects.

---
*Yellow justification: L8 period sits inside the closing quote because it's verbatim from the CD app's own on-screen hint text —— original to the quote per Hart's rule.*
