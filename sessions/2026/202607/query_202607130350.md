# Light/Dark Mode Template

## Last Turn's Task
well done, everything as expected; light/dark mode button works well and seems to already auto-detect my OS scheme
objective already achieved so i stopped you (you were visually checking it, which is good, just not needed for this test build)

## Problem
another session recently added a huge chunk to coding.md (SHA `29cc366`) which will be useless for sessions reading coding.md but not working on .html (e.g. working on pcmd only)
this wastes tokens and i wanna detach them

## Suggestion
based on the random page html you created, develop a .html template w/ that light/dark mode button as only visual content (i.e. Fact of the Moment box all gone, since their design/layout may bias future sessions), then save it as `cscpt/dark_mode.html` (simultaneously update its README.md)
except the css code, which i assume will already be in the .html, move that "huge chunk" from coding.md as comment (visually invisible) in the .html
then add a concise line in coding.md to simply pt to that .html whenever building a light/dark mode button (default requirement of .html coding as instr in coding.md right now)

## Expected Outcome
for non-html-coding sessions, CC will read coding.md only, w/o having to read those "huge chunk" which saves tokens/context
for html-coding sessions, CC (per current coding.md content) should know the light/dark mode button is required and will immediately read `cscpt/dark_mode.html` (unless already built or user specifically mentioned it's not needed), which she will simultaneously see the comments + be able to directly use the light/dark mode button already built inside as a template
i.e. directly copy-paste to working .html, or adapt as needed; e.g. turning into text rather than icon, if instructed
an example is .html outputs by git_history.md (you can read it), which in such use case indeed its light/dark mode button design and placement is better than our current one
lesson: adapt as needed, this is default template only

## Your Missions
- Execute actions if any instructed & no qb
- Wait for SA report(s) return, if dispatched
- Finally, tell your thoughts (don't miss any)
- Expected total quantity of `response_`: 1