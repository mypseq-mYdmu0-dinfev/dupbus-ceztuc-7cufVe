# Query (QMM)
*Created by CC on the user's behalf (QMM); verbatim below.*

no this is NOT what you're supposed to do it; CD apparently has backend direct protocol/MCP for CC to directly access/manipulate, not via CIC; dispatch SA to #cic & find out; if official channels don't reveal, consider community content; either way, CD MUST have been officially enabled for CC's direct-use; QMM
