# Response to query_202607130350.md
*dark_mode.html Built; coding.md Chunk Detached*

## 1

1. Done —— `cscpt/dark_mode.html` created: circular ☀/☾ toggle only, no "Fact of the Moment" box or other layout content.
- 1.1. Design rationale (label-destination rule, symbol-font pin reasoning, plain-glyph trap) moved in as an HTML comment at top —— visually invisible, CC-visible on read.
- 1.2. CSS/JS reference build kept as REAL functional code (not re-duplicated as comment), since it must actually run when copy-pasted.
2. `coding.md` § HTML & Visual Output —— the 3 prose/code paragraphs (labelling rule, font rationale, reference build) replaced w/ 1 pointer line to `cscpt/dark_mode.html`.
- 2.1. Kept in `coding.md`: the bullet-glyph rule (unrelated to dark mode) & the SA visual-check requirement (general, not dark-mode-specific).
3. `cscpt/README.md` updated —— new entry for `dark_mode.html`, flagged as the folder's one READ (not run) exception, w/ a caveat line added to the intro so it doesn't contradict the "never read" framing.

## 4 —— Thoughts on git_history.py's Toggle (re Expected Outcome)

4. Re-3.1: agreed its placement suits its own layout better, but not as a universal default.
- 4.1. `git_history.py`'s toggle is a text-labelled button inline in a fixed header —— reads well there because that page is a dense toolbar-style layout w/ other header buttons already ("Copy Only", "HTML") already establishing a text-button row; adding one more matches the existing pattern.
- 4.2. Our circular icon fixed bottom-right suits content-first/reading-focused pages (e.g. the random test page) —— it stays out of the way & doesn't compete w/ a layout that has no existing button row.
- 4.3. So I captured this as a "default template only, adapt by layout" note inside `dark_mode.html`'s comment block, rather than switching the template's default shape —— switching would just relocate the same one-size-fits-all problem to a different design.
5. Net effect —— that section dropped from 978tk to 71tk (`~`907tk saved per non-html session reading it going forward).
