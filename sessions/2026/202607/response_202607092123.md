# Response to query_202607092123.md
*One-Off Slip; Cleanup Confirmed; 2 More Orphans Found*

## 2 —— structural or one-off?

8. One-off slip on my part —— no `c.md` hardening needed. The rule that should've stopped this already existed, unchanged, before your recent edits: §7.3's "`#[trigger]` → `universal/[trigger].md` → MUST read first" is a hard, explicit mechanism, and you literally typed `#cic` several times. I just didn't apply an already-clear rule —— reading it now wouldn't have taught me anything I didn't already have access to.
9. Optional, not proposing to act on it now: worth a personal (memory-system) note that when I delegate research to an SA, I should front-load the relevant protocol file's mandate INTO the SA's brief (which I now do —— see this turn's SA prompts) rather than assuming "tell the SA to use CIC" is enough on its own. Say the word if you want that saved to memory; not doing it unprompted per §8.6.1.

## 3 —— confirmed as expected

10. Yes, exactly as expected —— I independently re-verified live just now: zero Avast processes, zero LaunchAgents/Daemons, zero `AvastHUB` folder, zero `pkgutil` receipt, zero Background-Task-Management entries. Both "Boot-out failed: No such process" lines and the "No receipt found" line are the correct outcome on a 2nd run against already-removed things —— not errors.

## Side Ask —— Mac-wide orphan audit

11. Found 2 more genuine Avast-style orphans (SA verified: process/daemon file exists, parent app confirmed absent from `/Applications/`), plus Avast's own dangling System Extension, plus some inert (harmless) leftover folders.

12. **Malwarebytes** —— the closest match to what just happened with Avast:
- 12.1. `/Applications/Malwarebytes.app` is gone, but 3 plist files still reference its (also-gone) engine binary: `com.malwarebytes.mbam.frontend.agent` (LaunchAgent), `com.malwarebytes.mbam.rtprotection.daemon` + `com.malwarebytes.mbam.settings.daemon` (LaunchDaemons)
- 12.2. Still `enabled` in Background Task Management —— macOS still thinks it's allowed to run
- 12.3. Cleanup (not yet run —— confirm first):
```
sudo launchctl bootout system/com.malwarebytes.mbam.rtprotection.daemon
sudo launchctl bootout system/com.malwarebytes.mbam.settings.daemon
sudo launchctl bootout gui/$(id -u)/com.malwarebytes.mbam.frontend.agent
sudo rm -f /Library/LaunchAgents/com.malwarebytes.mbam.frontend.agent.plist /Library/LaunchDaemons/com.malwarebytes.mbam.rtprotection.daemon.plist /Library/LaunchDaemons/com.malwarebytes.mbam.settings.daemon.plist
sudo pkgutil --forget com.malwarebytes.mbam.installer
rm -f ~/Library/Preferences/com.malwarebytes.mbam.frontend.agent.plist
rm -rf ~/Library/WebKit/com.malwarebytes.mbam.frontend.application ~/Library/HTTPStorages/com.malwarebytes.mbam.frontend.application.binarycookies
```

13. **Reincubate Camo** (webcam app) —— also orphaned:
- 13.1. No `Camo.app` anywhere on disk, but its privileged-helper LaunchDaemon, support folder, AND a virtual audio driver (loads into every app using CoreAudio, even now) are all still present
- 13.2. Cleanup (not yet run —— confirm first):
```
sudo launchctl bootout system/com.reincubate.macos.cam.PrivilegedHelper
sudo rm -f /Library/LaunchDaemons/com.reincubate.macos.cam.PrivilegedHelper.plist /Library/PrivilegedHelperTools/com.reincubate.macos.cam.PrivilegedHelper
sudo rm -rf "/Library/Application Support/Reincubate" "/Library/Audio/Plug-Ins/HAL/ReincubateCamoAudio.driver"
```

14. **Avast's own leftover**: `systemextensionsctl list` still shows its System Extension as "terminated waiting to uninstall on reboot" —— this is normal and self-clears on your next restart; no command needed.

15. Low-priority, inert (no processes, no daemons, just disk space —— your call whenever, no urgency): leftover `Application Support` folders for Notion (588MB) + Notion Calendar (30MB), Mendeley (49MB), Canva (28MB), Tableau Prep Builder (`~`5.6MB), TeamViewer Host + Movavi (a few KB each, essentially empty). None of these run anything or auto-start.

16. Ruled out (checked, legitimate): a Bluetooth thermal-printer driver (`Luckjingle`) —— its printer queue is still active, not orphaned. Dozens of other `pkgutil` receipts (TeamViewer, NordVPN, WeChat, Telegram, 1Password7, etc.) are bare metadata only, with no matching daemons/processes —— those apps' uninstalls were clean; not flagged.

17. Nothing executed for 12–13 —— tell me to proceed (both, either, or neither) and I'll run them.
