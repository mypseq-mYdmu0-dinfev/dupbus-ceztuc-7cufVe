# Response to query_202607092029.md
*"Avast" is 15 Processes, Not 1 —— Please Confirm Before I Go Further*

1. Tested "kill Avast now" as instructed —— important discovery, stopping here to check with you before continuing:
- 1.1. "Avast" isn't one app —— `ps aux` shows **15 running processes**: 1 user-level GUI (`Avast.app/Contents/MacOS/Avast`, PID 44445) PLUS 13 root-owned background services (`fileshield`, `behaviourshield`, `securedns`, `proxy`, `filter`, `hns`, `daemon`, `activity`, `leap`, `service`, `ext`, an `EndpointSecurity` helper, plus a System Extension) PLUS 4 more from the separate AvastHUB product. The root services are the REAL-TIME protection layer —— they've been running continuously since last boot (Tuesday), independent of the GUI.
- 1.2. `osascript quit` on the GUI failed ("User cancelled -128") —— Avast intercepts a plain quit AppleEvent with its own confirmation, so that path doesn't work headless.
- 1.3. `kill` (SIGTERM) on the GUI's PID DID work —— but within `~`2s a brand-new instance auto-respawned (new PID). None of Avast's own LaunchAgents/Daemons have `KeepAlive: true` (checked every one), so the respawn is coming from tamper-resistance logic baked into one of the root daemons themselves, not a simple launchd setting I can just toggle off.

2. Why I'm pausing rather than pushing through to force it to stay dead: doing so would mean fighting an antivirus vendor's deliberate anti-tamper protection, likely by killing/disabling one or more of the ROOT real-time-protection daemons (fileshield/behaviourshield/System Extension) to stop the respawn —— that's no longer "stop a popup", it's "turn off actual malware protection" for however long the "kill window" lasts. Your original ask was about visibility/annoyance (popups), not about disabling protection —— I don't want to assume you want the latter.

3. QB —— your call on how to proceed:
- 3.1. Option A: Abandon the open/kill-on-schedule idea; go back to the Focus-mode approach from `response_202607082210.md` (fully solves the popup problem, zero risk, doesn't touch real-time protection at all, already has a re-enable script + a documented Shortcuts automation to close the gap you found).
- 3.2. Option B: Continue down the kill-on-schedule path ANYWAY, accepting that it means finding + disabling the specific root daemon(s) responsible for the respawn/tamper-resistance —— i.e. accepting reduced real-time protection outside the Sunday scan window. If you pick this, tell me and I'll investigate WHICH daemon(s) specifically own the respawn behaviour before touching any of them.
- 3.3. Option C: Something in between —— e.g. only ever kill the GUI (not the root daemons) right before/after the scan, accepting it'll auto-respawn within seconds (so it wouldn't stay "closed" for the whole week as you pictured, but WOULD still dismiss whatever popup is on-screen at that instant, if that's actually enough for your use case).

4. Login-item question answered (no action taken, per your "check" wording —— not "disable"):
- 4.1. It's not in "Open at Login" because Avast doesn't register via the modern Login Item mechanism (`SMAppService`) at all —— it starts via a LaunchAgent (`com.avast.userinit.plist`, `RunAtLoad: true`) plus root LaunchDaemons (`com.avast.init.plist`, `RunAtLoad: true`). These are a different, older mechanism; modern macOS surfaces LaunchAgents/Daemons under System Settings ▸ General ▸ Login Items & Extensions, but typically under a separate "Allow in the Background" list, not "Open at Login" —— worth checking there specifically.
- 4.2. Same caution as §2 applies here too: disabling these would stop BOTH the GUI's auto-start AND the root protection services' auto-start at boot —— i.e. no real-time protection until you manually launch something. Tell me if you still want this investigated/actioned once you've decided on §3.

5. GUI process currently: back running (respawned, as expected) —— no lasting change made; nothing else was touched.
