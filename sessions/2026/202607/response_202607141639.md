# Response to query_202607141639.md
*Found It —— a One-Time Setup Step, Not Yet Done Here*

1. You were right —— confirmed via CIC live reads of 2 official Anthropic pages (not just search snippets):
- 1.1. [Claude Design launch announcement](https://www.anthropic.com/news/claude-design-anthropic-labs) —— launched 17 Apr 2026, Pro/Max/Team/Enterprise, genuinely public (not a private waitlist). Your "officially enabled weeks ago" belief checks out.
- 1.2. [Official "Get started" support article](https://support.claude.com/en/articles/14604416-get-started-with-claude-design) —— spells out the EXACT Claude Code connection method, quoted verbatim:
  > "If you prefer to work from Claude Code, connect the Claude Design MCP server... 1. Add the server: `claude mcp add --scope user --transport http claude-design https://api.anthropic.com/v1/design/mcp` 2. Run `/design-login` to sign in."

2. This explains the empty result precisely: `/design-login` is a slash command run INSIDE a Claude Code session (not a claude.ai website setting), and it's gated behind a prior `claude mcp add` step that registers the MCP server per machine/user config (`--scope user`). Neither step has been completed for THIS session/installation —— that's the whole gap, nothing broken.

3. Confirms further: THIS session doesn't even have `/design-login` or `/design-sync` as available commands, and a direct `ToolSearch` for `DesignSync`'s schema by name came back "no matching deferred tools" —— fully consistent with the connection never having been established here specifically (possibly done once on a different machine/session, which wouldn't carry over).

4. This is a one-time step only YOU can run (it opens a real sign-in flow to your own claude.ai account —— not something I can complete on your behalf):
```
claude mcp add --scope user --transport http claude-design https://api.anthropic.com/v1/design/mcp
/design-login
```
Run both in a terminal Claude Code session on this Mac (the 1st line registers the server, the 2nd opens the sign-in). Once done, `DesignSync`'s `list_projects` should show "Tall Poppy Syndrome research recruitment" for real, and I can proceed with the "TESTING" overlay proof directly via that tool —— genuinely "direct manipulation", no browser needed, matching what you originally expected.

5. One sourcing caution the SA flagged: several search hits (a few SEO-style blog sites, and a GitHub file literally named `tool-description-designsync.md` in an unrelated repo) look like speculative/scraped content mimicking official docs —— none of those were used; everything above is sourced only from the 2 Anthropic-owned pages, actually read live.

6. Say the word once you've run the 2 commands above and I'll retry `DesignSync.list_projects` immediately.

---
*Yellow justification: L7 period sits inside the closing quote because it's verbatim from the official support article's own sentence-ending punctuation —— original to the quote per Hart's rule.*
