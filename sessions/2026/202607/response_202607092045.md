# Response to query_202607092045.md
*Nothing Promising —— Reverted per Your Instruction*

1. #cic researched thoroughly —— nothing promising. Bottom line, confirmed by an Avast STAFF member directly (2020+ era community thread, current product): there is no menu-bar "turn off"/quit toggle on Mac (unlike Windows, which has one) —— staff explicitly said it's withheld deliberately ("dangerous for users who don't know what they're doing"). The only official Mac-side control is toggling individual shields (Web/Mail/Real Site) inside Preferences, which reduces network impact but leaves every process running. No documented safe "pause all daemons, resume later" path exists —— full uninstall is genuinely the only sanctioned way to fully stop it. Your ⌘Q and Force-Quit-all both failing is expected: Force Quit runs as your user and can't touch the root-owned daemons or the System Extension anyway.

2. Reverted as instructed: `local.avast.weeklyopen.plist` unloaded (`launchctl bootout`, confirmed —— launchd now reports it not found) AND deleted from `~/Library/LaunchAgents/`. Nothing else was touched or left behind.

3. My honest take: given 1, your plan (uninstall via the official `Avast Security Uninstaller.app`) is reasonable —— you're not missing a hidden option, this really is by design, and your reasoning (no true off switch + undismissable non-notification ads, despite 6⁺ months with nothing genuinely threatening surfacing) is a fair basis to drop it. Not something I can act on for you (running the official uninstaller is squarely yours to do), but say the word if you want me to verify afterwards that it fully removed everything (LaunchAgents/Daemons, System Extension, `/Applications/Avast.app`, AvastHUB) with no orphaned pieces.

4. Side ask (.plist, ≤100w):
> A `.plist` is macOS's XML config format. The one I made was a LaunchAgent: a `.plist` in `~/Library/LaunchAgents/` that tells `launchd` (macOS's built-in scheduler) to run a command on a schedule or trigger, no cron needed. Mine said run `open -a Avast` every Sunday 08:45. General use: auto-launch/quit apps, run scripts at login, periodic backups/reminders, anything you want to happen automatically on a time or event, without keeping Terminal open.
