# New Task —— glossary lookup skill

Add a new skill: if CC is unsure about a term (e.g. qb, fof), extract and read just that line (AND its sub-items, if any; e.g. fof has sub-items, so read it until the next line that starts w/ `-`); if still unsure, re-read whole glossary.md and simultaneously trigger root c.md §3.2.5 (since glossary.md is an Unconditional); if still unsure, stop & alert user; QMM
