# Response to query_202607092059.md
*Uninstall Gap Confirmed + Fixed (Partial); No AV Needed*

## Preamble

1. Diffed `CLAUDE.md` + `glossary.md` against my last known-read commit (`a6a5414`, 202607050313) instead of a full re-read —— caught the relevant changes myself, incl. §9.04 (pcmd/script editing rules) and the `root`/`default repo`/`AJAP repo`/`sesL`/`wkL` glossary additions.
2. Read `universal/cic.md` in full just now —— it was never actually read this session despite Conditionals §7.2 mandating it for "ANY CIC ops". That's the exact violation you flagged: my earlier "#cic" turns used `WebSearch`/`WebFetch` only (triage-only per `cic.md`'s own wording) and never actually drove live Chrome via CIC tools —— I was guessing what `#cic` required instead of reading its actual mandate. Corrected for this turn (see Research below —— confirmed genuine CIC use this time, not guessed).

## 3 —— Uninstall Verification

3. Your suspicion was right —— the official uninstaller cleaned the CORE Avast engine but left a separate companion product, **AvastHUB**, completely untouched. That's what you saw under App Background Activity —— confirmed directly via `sfltool dumpbtm` (the same database that list reads): "Avast Coordinator" (`com.avast.hub` + 2 daemons) was still registered.
- 3.1. Core Avast —— fully gone: `/Applications/Avast.app`, all its ~13 real-time daemons, the System Extension shows `[terminated waiting to uninstall on reboot]` (normal —— System Extensions always need one reboot to fully clear; not a leftover bug).
- 3.2. AvastHUB —— found running (4 processes) + registered (1 LaunchAgent, 2 LaunchDaemons, `pkgutil` receipt, 2 cache folders). Cleaned everything I could WITHOUT sudo:
  - 3.2.1. Unloaded + the user-level process stopped (`launchctl bootout gui/.../com.avast.hub`)
  - 3.2.2. Removed `~/Library/Application Support/AvastHUB` (fully deleted, not voided —— this is leftover uninstaller debris, not your data)
  - 3.2.3. Removed `~/Library/Caches/com.avast.av.uninstaller` + `~/Library/Caches/com.avast.hub.alpha`
- 3.3. Still remaining (root-owned, needs YOUR sudo password —— I don't have it): 2 processes still alive, backed by `/Library/LaunchAgents/com.avast.hub.plist` + `/Library/LaunchDaemons/com.avast.hub.{schedule,xpc}.plist` + `/Library/Application Support/AvastHUB`. Run this in Terminal to finish it:

```
sudo launchctl bootout system/com.avast.hub.schedule
sudo launchctl bootout system/com.avast.hub.xpc
sudo rm -f /Library/LaunchAgents/com.avast.hub.plist /Library/LaunchDaemons/com.avast.hub.schedule.plist /Library/LaunchDaemons/com.avast.hub.xpc.plist
sudo rm -rf "/Library/Application Support/AvastHUB"
sudo pkgutil --forget com.avast.pkg.hub
```

- 3.4. After running that, `App Background Activity` should show nothing "Avast" anymore; the System Extension entry clears itself on your next reboot (no action needed for that one).

## Research (genuine CIC this time —— confirmed live-page visits, not WebSearch snippets)

4. Task 1 —— does WSM need a dedicated scanner: **No**, not for how you actually use it. Apple's own security docs (visited live) confirm 3 built-in layers (Gatekeeper/Notarization, XProtect, sandboxing); an independent Jan-2026 deep-dive (also read live) states built-in protection is adequate for users sticking to the App Store/trusted sites —— third-party AV is reserved for high-risk-download habits, businesses, or heavy Windows-file interop. Your actual track record (6⁺ months, zero genuine detections, careful habits, already running CCleaner + CleanMyMac) fits squarely in the "built-in is enough" bucket.
5. Task 2 —— if you still want one anyway: **Bitdefender Virus Scanner** (Mac App Store, free tier) —— confirmed live on its official page AND its App Store listing (not just search snippets): genuinely free (no paywall), ad-free (App Store sandbox model, no upsell popups visible), lightweight (on-demand only —— you run a scan when you want, no persistent daemons like Avast's). Caveat found on the live listing: on-demand only, no real-time protection, last updated Dec 2024 —— i.e. exactly the "XProtect + occasional manual scan" model, not a full suite. Malwarebytes was also checked live and ruled out —— its free tier has shrunk to a 30-day trial reverting to a limited scanner, weaker than Bitdefender's straightforwardly-free listing.
6. Task 3 —— left open in Chrome for your review, per your instruction: Bitdefender Virus Scanner's Mac App Store listing. Nothing downloaded/installed.

7. My honest overall take: given 4, I'd personally lean towards NOT installing anything and just relying on XProtect/Gatekeeper (zero footprint, zero risk of repeating this whole saga) —— but the tab's there if you'd rather have a manual-scan safety net anyway.

---
*Yellow justification: L31 "notarization" = Apple's own fixed proper-noun term for that specific process, not a general -ize/-ise spelling choice —— kept as Apple names it.*
