# Response to query_202607082210.md
*Direct Spoof: Not Viable. Real Fix: Close the Gap.*

1. You're right that my prior plan breaks exactly as you described —— apologies, I missed the pre-emption issue. Corrected below.

2. Re "make Avast alone believe": researched properly this time, and it's a genuine dead end, not just a caution:
- 2.1. Confirmed locally (`codesign -d --entitlements -` on the real Avast binary): Hardened Runtime is ON, and it has ZERO of the 2 specific entitlements (`disable-library-validation`, `allow-dyld-environment-variables`) that would let any code be injected into it. Without those, macOS itself blocks the injection technique this would require —— it's not a policy choice CC is being cautious about, it's an OS-level lock Avast's build already has closed.
- 2.2. The Focus-status API itself has no per-app concept —— it only ever reports ONE system-wide true/false. "Fooling Avast alone" necessarily means intercepting Avast's own in-process calls to that shared API, which is exactly what 2.1 blocks.
- 2.3. The remaining theoretical routes (disable SIP, re-sign Avast's binary to strip its own protections) would break Gatekeeper/notarization, almost certainly trip Avast's tamper-protection, get silently undone by its 10-min auto-update check, and cross into EULA-violating tampering with a security product. Not a "few lines of code" thing —— genuinely not worth pursuing.

3. The actual fix —— closes your exact gap, zero risk, no hacking, confirmed available on your macOS version:
- 3.1. Shortcuts.app → Automation → `+` → Personal Automation → **Focus** trigger has both **"When Turning On"** AND **"When Turning Off"** (confirmed via Apple's own docs) —— this is the missing piece.
- 3.2. So: for EVERY real Focus mode you use (Sleep, and any others), add one automation: *"When [that Focus] Turning Off → Set Focus [your Avast-decoy Focus] On."* The instant Sleep (or any other real Focus) ends, the decoy snaps back on automatically —— no gap, ever, and your real Focus modes keep behaving 100% normally whilst active (Avast's `isFocused` check just sees SOME Focus is on, same as before, regardless of which one).
- 3.3. `nscpt/avast_focus_reenable.sh` (built last turn) still stands as a manual fallback button if you ever want to force the decoy back on outside the automation chain —— no changes needed to it.
- 3.4. One caveat carried over from the SA (couldn't click through your live Shortcuts UI): confirm the exact automation options match this description when you set it up —— minor UI wording can vary by macOS version.

4. Side note answered, confirmed correct via docs: Avast's popups are its own regular app windows (`NSWindow`-level UI, drawn freely by any running app), NOT going through `UNUserNotificationCenter` (Apple's actual notifications API —— the one that needs permission and shows up in System Settings). Apple's review/permission system only gates the LATTER. So this isn't a special exception Apple carved out for Avast —— ANY app can do this; it's a known "roll-your-own-alert-UI" pattern some apps (esp. AV/utility software) use specifically to dodge user notification controls. Not very Apple-like in spirit, agreed, but it's a platform-wide gap, not an Avast-specific favour.

---
*Yellow justifications: L9 "notarization" = Apple's own fixed proper-noun term for that specific process, not a general -ize/-ise spelling choice —— kept as Apple names it. L13 the period sits inside the closing quote because it ends the quoted instruction itself (the composed automation phrase, not an external quote) —— original to the quote per Hart's rule.*
