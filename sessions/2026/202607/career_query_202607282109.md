# Push Problem —— Documentation Request

fully document the push problem (what you're trying to do, what you saw, why struggled, etc.) in a `*_query_` in `queued_queries/` for me to send to another session so it gets fixed; when done, declare just that file then proceed to #sprint (i manually pushed); QMM
