# Session Closing (202607232338–202607250023)
*CCSIM (04): #wrap 202606, CAI/SSD migration, 5 linters, doomsday*

## 1. Coverage
- 1.1. #wrap 202606 (i1) —— synthesised all 24 June `close_` files into `wrap_202607232332.md` via a wave-staged Workflow SA fleet (23 SAs, 0 fail); ran the CCSIM Backlog Sweep.
- 1.2. i2 read + graduated —— revealed it is a HOME-FOLDER SSD-wear migration, NOT the skills files; it does NOT gate i3–i5 (a wrong belief corrected).
- 1.3. SSD-wear migration (internal SSD health 94%) —— relocated to FURY: npm cache, `~/.vscode`, `~/.codex` (guard), VS Code state, Chrome profile + caches, Perplexity cache, TradingView, the whole `~/.claude`, CAI (`~/Library/Application Support/Claude`, ~13G), and `CLAUDE_CODE_TMPDIR`. SA-audited PASS; CAI verified writing to FURY.
- 1.4. CAI migration failure + full recovery —— the first script's `rm -rf SRC && ln -s` gutted the live dir (helpers still running) → user data-loss scare → rescue restored it → hardened v2 via a 3-reviewer `#debate` → then 3 more real false-abort fixes (mount `pipefail`+SIGPIPE, over-broad `chrome_crashpad_handler` match, redundant `lsof` guard) → migration SUCCEEDED.
- 1.5. Five linters (was only dlint) —— `nlint` (numbering-continuity), `clint` (chat-prose, block-once so it reaches the model), `tlint` (TS-collision, own folder + cross-repo mirror), `hlint` (`#trigger` → read-the-pcmd reminder). Pairing-lint hook demoted block→warn.
- 1.6. Docs/scripts —— `cp/ccsim/{ssd_migration_guide,doomsday,README}.md`; `nscpt/{recover_cai_appdata,setup_fury_guard}.sh`; migration + tmpdir setup scripts; AJAP migration + doomsday queued queries; per-turn migration revert-logs.
- 1.7. pcmd edits —— root `CLAUDE.md` §3.1.6/§3.1.7 reordered to marker-BEFORE-declarations (user streamlined in `f49413c`); §3.1.7.3.3 edit reverted by user; `numbered.md` heading rules (user); `ccsim/CLAUDE.md` new-file reminder.

## 2. Decisions
- 2.1. CAI migrated to FURY NOW (not deferred to APFS) —— given the UPS + FURY's 3-year reliability + the airtight v2 + integrity-scanning recovery + mount-guard; the surprise-unmount SQLite risk is inherent (mitigated, not eliminated until APFS).
- 2.2. SAFE MIGRATION PATTERN (mandatory, `ssd_migration_guide.md` §7): force-quit + verify → ditto → verify count+bytes → RENAME ASIDE (`mv`, never `rm` before symlink) → symlink → keep backups. Meta-lesson: OVER-GUARDING (crashpad kill, lsof) caused more failures than it prevented; the simple proven rescue set was correct.
- 2.3. `clint` must BLOCK-ONCE (exit 2) —— a Stop-hook `systemMessage` reaches only the user, never the model; only exit-2 stderr reaches CC. `stop_hook_active`-guarded (one extra turn max).
- 2.4. `tlint` exempts only co-located `{query,response}` or `{close,artefact}` pairs; checks the cross-repo `sessions/`↔`inv/` mirror (same year-month only). Latency ~38ms (≤1s) → always-on, no skip.
- 2.5. TS collisions (35 found) left AS-IS per user (nothing breaks; tlint prevents recurrence). Mandate is at `AJAP_repo/inv/CLAUDE.md:31`.
- 2.6. `#wrap` mandates `#sprint` + SA fleet (user edit); marker-before-declarations fixes the post-marker-prose + chapter-click UX.

## 3. Issues (parked → next CCSIM session, top-down)
- 3.1. 🔴 i3 —— create thin ALIAS-skills for the ~8 situational `universal/` files (writing, profile, pro_profile, coding, cic, shrink, plan, google); each a one-file `SKILL.md` "read+follow `universal/<file>.md`" w/ a `description:` mirroring its §7.2 condition; CC-only, keep `#triggers`. Was to be `#sprint`'d THIS turn but git_history + context deferred it.
- 3.2. 🟡 i4/i5 —— install vetted external skills behind a safety-gate skill built FIRST; CCSIM/#wrap auto-propose skills for surfaced problems.
- 3.3. 🟡 git_history.py `.html` improvements (`ccsim_query_202607250021.md`) —— Show More/Less + Full/Changes toggles + multi-line input; an SA is building it; its own `response_` is still PENDING (not yet delivered this session).
- 3.4. 🟡 AJAP-side `tlint` registration —— cross-repo enforcement needs the hook in `AJAP_repo/.claude/settings.json` (queued: `ajap_migr_query_202607242027.md`).
- 3.5. 🟡 APFS reformat of FURY —— standing task, target ~Dec 2026 (after the Nov dissertation); procedure in `ssd_migration_guide.md` §5 (Safe-Mode backup first).
- 3.6. 🟢 Post-confidence cleanup —— delete the labelled backups (§ Remarks) after CAI runs clean ~a day.
- 3.7. 🟢 Queued queries awaiting send: `citi_query_202607162351`, `ajap_query_202607162356`, `ajap_migr_query_202607242027`, `ajap_doomsday_query_202607242300`.
- 3.8. 🟢 Backlog sweep —— due at the NEXT `#wrap` (per `ccsim/CLAUDE.md` §6), not at this close.

## 4. Comms Files (13 pairs + non-paired)
- 4.1. `202607232338`: @ccsim + #wrap 202606 (i1).
- 4.2. `202607240012`: numbered.md indentation fix.
- 4.3. `202607240227`: pairing-lint block→warn + i2 read + risk-assess.
- 4.4. `202607241459`: SSD-wear migration (npm/vscode/codex/wear-map/projects script).
- 4.5. `202607241642`: Library migrations + numlint/nprose hooks + whole-.claude script.
- 4.6. `202607242009`: CAI failure owned + #debate airtight v2 + recovery + Chrome/Perplexity/TrV migrated.
- 4.7. `202607242145`: clint/hlint/tlint shipped + APFS/recovery answers.
- 4.8. `202607242246`: CAI mount-bug + doomsday.md + tlint cross-repo + marker reorder.
- 4.9. `202607242314`: migrate_cai process-check (crashpad) fix.
- 4.10. `202607242319`: migrate_cai lsof-guard removal.
- 4.11. `202607250012`: migration PASS audit + clint fix + doomsday rework (this turn's response #1).
- 4.12. Non-paired —— `ccsim_query_202607242011.md` (rescue terminal-output FYI, answered within 202607242009's thread); `ccsim_query_202607250021.md` (git_history request —— response PENDING, see §3.3); `ccsim_query_202607060438.md` (i2 input, graduated from queued_queries, no direct response).
- 4.13. NB —— `wrap_202607232332.md` belongs to the 202606 wrap, not this session's comms.

## 5. Non-Comms Files
- 5.1. Created —— `cscpt/{nlint_hook.py,nlint_hook.sh,clint_hook.py,tlint_hook.py,tlint_hook.sh,hlint_hook.py}`; `cp/ccsim/{ssd_migration_guide.md,doomsday.md,README.md}`; `nscpt/{recover_cai_appdata.sh,setup_fury_guard.sh}`; `sessions/2026/202607/{migrate_to_fury,migrate_cai_to_fury,setup_cc_tmpdir}_*.sh` + 3 `ccsim_migration_revertlog_*.md`.
- 5.2. Modified —— root `CLAUDE.md` (§3.1.6/§3.1.7); `.claude/settings.json` (nlint/tlint/hlint/clint registered); `.githooks/pre-commit` (block→warn); `cscpt/README.md`; `.gitignore`; `ccsim/{CLAUDE.md,backlog.md,last_seen.md}`; `gscpt/git_history.py` (SA, in progress).
- 5.3. Moved/Voided —— `ccsim_query_202607060438.md` graduated to 202607/; `❌_migrate_cc_projects_202607241459.sh`, `❌_migrate_apps_to_fury_202607242011.sh`; numlint→nlint, nprose→clint (renamed).

## 6. Remarks
- 6.1. sesL BENCHMARK (per your §46) —— this session's Opus/Ultracode MA spent ~346.4k tk = ~20% sesL → an Opus session ≈ ≥1.7Mn tk (rough). Future scientific test: create a ~1k-tk file; have Sonnet/Opus/Fable read it with NO response; record CAI-shown token use + sesL jump (ideally at ses%=0); run it AFTER Opus 5 ships so the benchmark lasts.
- 6.2. ACTIVE SESSIONS (per your Side Note, CAI Settings ▸ Account) —— Claude Desktop (macOS), Sydney NSW, 24/07 19:42 = WSM CAI (current); Safari (macOS), Ballarat VIC = Claude Web (the `usage_pct.py` login); Safari (macOS), Gold Coast QLD = true Safari (WSM-CAI authoriser only); Claude (iOS), Sydney NSW, since 19/03 = OTGC.
- 6.3. DELETABLE backups (after CAI clean ~a day): `Claude.premigrate-backup-20260724232502` (12G internal) + `Claude.stale-20260724232502` (12G FURY); deletable NOW: `Claude.broken-backup-202607241939` (341M); after Chrome/TrV verified: `Default.migbak-202607242021`, `TradingView.migbak-202607242021`.
- 6.4. §64 marker-reorder —— WORKS per Turn 10/11 (chapter-click lands on declarations); user to report cross-session observations next session, harden if needed.
- 6.5. Next CCSIM session —— run the every-turn change-hunt (`last_seen.md` anchor); Spotify was NOT migrated (daemon respawns —— left local, a ~136B FURY orphan is deletable); this close belongs to the NEXT `#wrap`.
- 6.6. DATS at this close —— left 1 file ≥24h off UNCHANGED for user review: `ccsim_query_202607060438.md` (Date-Added 24 Jul when graduated vs its 6-Jul filename TS; 429.9h) —— decide whether its Date-Added should be the 6-Jul original or the graduate date. Also, DATS skipped an AJAP-side offender (`AJAP_repo/inv/.../close_202607231053.md`: filename TS ≠ content range-end 202607231242) —— an AJAP fix, flagged for AJAP CC.
