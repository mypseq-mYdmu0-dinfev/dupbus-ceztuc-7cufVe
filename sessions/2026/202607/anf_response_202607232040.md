# Response to anf_query_202607232040.md
*Sprint Report —— Nano Prototype Built, Reviewed, and Verified*

## Sprint Report (202607232040–202607232105) · ran ~25m

8. Done —— a working, branded, local nano tracker (all built by SAs; my context stayed lean for our discussion)
- 8.1. Location `cp/anf/nano/` (the CP's own home) —— 3 files: `server.py` (483L), `index.html` (558L), `README.md` (66L); runtime `anf_nano.db` is gitignored
- 8.2. Stack: pure Python stdlib (`http.server` + `sqlite3`), ZERO pip installs —— runs on bare system python3
- 8.3. Backend: full JSON REST API —— list, text-search, add, edit, delete, batch-rename, summary, CSV-export; auto-seeds 25 realistic AUD records on first run
- 8.4. Frontend: single-file SPA, all CSS/JS inlined (no external/CDN requests), C.YU-branded, with a persisted dark-mode toggle
- 8.5. Every feature live-verified in-browser (search, add, edit, delete, batch-rename, CSV, summary, dark mode + reload-persistence) —— all PASS

9. How to run it (30 seconds)
- 9.1. Paste this into a fresh terminal:

```bash
cd "/Volumes/FURY 2TB/Fury Documents/GitHub/dupbus-ceztuc-7cufVe/cp/anf/nano" && python3 server.py
```

- 9.2. Then open `http://localhost:8731` in Safari (it works —— this is local, not BudgetBakers)
- 9.3. It seeds itself on first launch; `⌘C` in the terminal to stop. Delete `anf_nano.db` anytime to reset to fresh seed.

10. Why this nano build is a faithful miniature —— it lets you judge all 3 tiers against something concrete
- 10.1. The local SQLite `.db` = you OWN the raw file, copy/export it anytime —— this directly embodies the data-safety property that answers your #1 fear (vendor collapse); it's the Tier 2/3 "own your data" feel, in your hands right now
- 10.2. The JSON REST API = already the exact shape an MCP server wraps —— so the "AI bulk-edits my transactions" capability (Tier 1's new Wallet-MCP, or a Tier 3 custom MCP) is previewed concretely
- 10.3. The batch-rename control = the ONE thing BudgetBakers makes painful (re-editing Label/Category per imported record) —— rename a whole Category in one action and feel the difference
- 10.4. CSV export = the "leave anytime" escape hatch, one click
- 10.5. What it deliberately is NOT —— scalable: load-all (no pagination), no auth, no receipt-OCR wiring; at your real tens-of-thousands scale you'd swap SQLite-local for a hosted DB (Tier 3) or adopt Actual/Wallet (Tiers 1`~`2), but the SHAPE you're looking at stays the same

11. Assumed —— working calls made to push through (audit & overturn any; none invalidate downstream work)
- 11.1. Location = `cp/anf/nano/`, not `sessions/` (comms-only) or `temp/` (throwaway) —— it's the CP's first real artefact
- 11.2. Stack = stdlib/SQLite over any framework —— chosen for max one-shot reliability + faithful miniature; overrule if you'd rather I prototype directly on a hosted DB (Supabase/D1)
- 11.3. Added 4 features beyond bare CRUD (search, batch-rename, CSV export, summary) —— highest-signal for tier evaluation: search + batch-rename map to your two biggest pains, export to your biggest fear
- 11.4. Amount stored SIGNED (expense negative, income positive), not a separate type field
- 11.5. Seed rows use your real merchants (Woolworths, NSW Transport Opal, Ampol, Chemist Warehouse, AGL/Optus, etc.) so search & batch-rename demo on realistic data

12. Deferred polish —— 2 optional items (not bugs; left as-is under sprint, your call)
- 12.1. Delete & batch-rename confirm via native browser `confirm()`/`alert()` dialogs —— fully functional but slightly off-brand vs the app's own styled edit-modal; a toast/modal swap is cheap future polish
- 12.2. The summary strip stays GLOBAL during a live search (not the filtered subset) —— a design choice, not a defect; say if you'd prefer it to reflect the filtered view

13. Interruptions —— none
- 13.1. Zero compactions, zero sesL/usage-limit hits, zero SA/Workflow deaths: 6/6 workflow agents + 1 browser-verify SA all completed clean

14. Quality trail —— how confidence was built (not merely "it exists")
- 14.1. Adversarial review fan-out (3 independent lenses: correctness/security, branding/coding.md, UX/API-contract) surfaced 8 issues incl. 2 HIGH —— a non-finite-amount crash (`NaN`→500, `inf`→invalid JSON) and a CSV formula-injection hole in the export
- 14.2. 7 fixes applied (both HIGHs closed); every SQL statement parameterised; batch-rename column allow-listed to `category`/`label` only
- 14.3. Automated API smoke test —— 10/10 pass (auto-seed from scratch, 400-not-500 on bad input, injection rejected + table survives)
- 14.4. Live in-browser SA verification —— all 10 UI features PASS, branding matched computed styles (`#005166`/`#468EA1`/`#20898A` teals + `#E64A37` Arancio), light + dark both legible, zero console errors → verdict SHIP

15. Open —— needs you (priority-tagged)
- 15.1. 🟡 Run it and click around —— does this concrete shape shift your lean between the 3 tiers? (That was the whole purpose of building it.)
- 15.2. 🟢 The 3 open Qs from the previous response still stand and are unaffected by this build —— Wallet Premium pricing lookup; whether a short Premium sub is a likely prerequisite either way; where a Tier-2 self-host server would live
- 15.3. 🟢 Optional polish 12.1 / 12.2 —— fix or leave?
