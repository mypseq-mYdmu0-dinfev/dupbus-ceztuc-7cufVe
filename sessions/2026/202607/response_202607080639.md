# Response to query_202607080639.md
*git_move_batch.py built & logic-tested*

1. Built `nscpt/git_move_batch.py` —— 2-stage as spec'd, top docstring styled per `gscpt/quote_fix.py` (USAGE + numbered steps + example), `.sh`-generation pattern styled per `nscpt/git_move_rename.sh` (clean-tree guard, `git mv`, commit)

2. Thoughts on the spec, incl. calls I made where it was ambiguous or silent:
- 2.1. Your two "## To" error bullets ("not a single folder path" / "not folder i.e. file") read as overlapping —— merged into ONE clean rule: destination must be exactly 1 line, must exist, must be a directory; anything else errors with a specific message
- 2.2. "Stage 2 final actions" only said "check if the git mv succeeded" —— I added a `git commit` at the end of the generated `.sh` anyway, matching `git_move_rename.sh`'s convention and keeping the tree clean for the NEXT run's guard-check; flag if you'd rather it stop at `git mv` (uncommitted) instead
- 2.3. `git_move_rename.sh` also does `git push origin main` —— I deliberately did NOT include a push (your spec never mentions one); tell me if batches should push too
- 2.4. Flattening a folder source never deletes the now-empty subfolders it leaves behind (e.g. `cd_teset/` stays, empty) —— consistent w/ Void Rule (never auto-delete); you'd clean those manually if wanted
- 2.5. Unhandled edge case: 2 source files sharing the same basename (from different subfolders) would collide at the flat destination —— the 2nd `git mv` would fail, `set -e` aborts the `.sh`, exit code ≠0, so it surfaces as a FAILED run (not silent data loss), but the error message would just be git's own, not a friendly pre-check; didn't add a pre-check since not spec'd —— say if you want one

3. Verified: `validate()` + `collect_files()` (the flatten logic) tested standalone against an isolated scratch folder matching your own example shape (folder w/ a direct file + a nested subfolder file) —— both passed, flattening confirmed correct. Did NOT run a live end-to-end `git mv` (stage 2 fully, incl. the actual move+commit) since that needs real files you intend to move —— first real run will exercise that path live, reviewable before you type `yes`.

4. Stage 1 smoke-tested (template creation) then voided the test artifact (`❌_gmb_202607080641.md` in `nscpt/`) —— yours to delete.
