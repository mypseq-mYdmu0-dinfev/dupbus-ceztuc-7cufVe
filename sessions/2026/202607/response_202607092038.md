# Response to query_202607092038.md
*Confirmed True; Auto-Open Set & Loaded*

1. Your read is correct —— confirmed true. That respawn behaviour is textbook AV self-defence: the whole point is to stop malware (or anyone/anything else) from silently killing real-time protection. It working against MY kill attempt too is just it doing its job correctly, not a bug.

2. Your suggestion is the right call —— sidesteps the tamper-resistance fight entirely, touches zero real-time-protection daemons, and manual GUI quit is the "intended human way" so it won't hit that same resistance.

3. Done —— `local.avast.weeklyopen.plist` created at `~/Library/LaunchAgents/`, loaded via `launchctl bootstrap`, and verified live:
- 3.1. Runs `open -a Avast` every Sunday at 08:45 (15min before your 9am scan start)
- 3.2. Confirmed registered + correctly scheduled —— `launchctl print` shows the exact trigger: `Weekday=0 (Sun), Hour=8, Minute=45`, state "not running" (i.e. armed, waiting for next Sunday —— no `RunAtLoad`, so it did NOT open Avast just now)
- 3.3. Nothing else touched —— no kill/quit logic, no daemon changes

4. Not something `cscpt/`/`nscpt/` conventions apply to (it's a system LaunchAgent config, not a runnable script —— it only works living at `~/Library/LaunchAgents/`), so it stays there per macOS's own requirement.

5. Reminder as you said you'll self-handle: manually quit Avast `~`5pm Sundays. If you'd ever rather I build a "5pm reminder" (e.g. a notification/alarm, NOT an auto-kill) instead of relying on memory, say so and I'll add one —— separate from anything Avast-internal, so no risk of hitting its tamper-resistance.

6. To remove this later if ever wanted: `launchctl bootout gui/$(id -u)/local.avast.weeklyopen` then delete the plist —— noting for future reference, not needed now.
