# Response to query_202607141758.md
*Agree —— CD Adds No Value to How I Work; Your "Deal Breaker" Is Real*

## 10 —— reconciled

17. You were right to push back —— my earlier "always read-only" claim was overgeneralised from testing only ONE surface. Best-supported explanation (medium confidence —— rests on a leaked, unofficial but internally-consistent CD system-prompt, since official docs are silent on this internal mechanism):
- 17.1. The variable isn't "Project vs Design System" per se —— it's LIVE EDITOR CANVAS vs STATIC PREVIEW PANE. A Design System project's card gallery is confirmed (your own screenshot) to render raw HTML as a static, non-interactive preview —— it never runs CD's template compiler, so nothing there EVER gets the runtime click/edit tracking (`data-cc-id`), regardless of who authored it.
- 17.2. Opening a regular Project in the actual Edit-mode canvas DOES compile/mount whatever's in the file as a live tree and stamps those tracking ids on interaction —— so if my `write_files` call happened to land as valid, parseable template markup (not inside a JS-rendered subtree, properly closed tags, inline styles), it became indistinguishable from CD's own generated nodes. That's why my Tall Poppy "TESTING" div worked but the Random Card one, viewed only via the Design System pane, never got the chance to.
- 17.3. So: no reliable "trick" to guarantee it beyond what already happened —— write clean, canonical inline-styled HTML (which I do anyway) AND make sure you're viewing it in a Project's live Edit canvas, not a Design System's preview pane.

## Critical —— pixel-move, confirmed deal-breaker, not a misunderstanding

18. Verdict, HIGH confidence (leaked system prompt + 2 independent field reviews spanning before AND after a June 2026 update, both consistent): Claude Design is fundamentally an auto-layout (flex/grid) system, NOT a freeform canvas. Dragging a sibling element is explicitly "drag-REORDER" by design, not free-position —— exactly the swap-with-neighbour behaviour you hit, on 2 separate CD-native (not CC-authored) designs. Not a bug, not something you're missing.
19. One genuine free-positioning mechanism DOES exist in CD's spec (`position:absolute` + a special "canvas mode") —— but it's meant for laying out multiple side-by-side design OPTIONS, is only reachable by hand-authoring the source (i.e. me, via `DesignSync`), and is NOT a discoverable drag gesture in the visual editor for repositioning elements within one design. No hidden modifier-key/Pro-mode toggle found anywhere, official or community.
20. Real tension worth flagging: even if I hand-authored absolute-position elements for you, per §17's mechanism that's more likely to land as an "opaque"/non-template subtree —— i.e. the SAME read-only problem from §10 could resurface. CD doesn't currently offer "free-position AND user-clickable" together. Your "deal breaker" read is accurate, not overstated.

## Your questions, answered directly

21. Does CD hold value to ME (CC)? No, not for authoring. Every successful edit I made this session (Tall Poppy overlay, Random Card, the `@dsCard` fix) was via `DesignSync`'s plain read/write file API —— functionally identical to editing a `.html` file in this repo. I never touched CD's visual editor.
22. Does it make me create better/easier? No —— if anything, slightly WORSE: `.dc.html` comes with its own conventions I have to track (canonical-markup rules, `@dsCard` markers, `dc_write` semantics per the leaked spec, project-type quirks) that a plain `.html` file in CC doesn't demand. Same underlying skill (writing HTML/CSS), extra rules layered on top, zero authoring upside.
23. The only thing CD gives ME specifically: a hosted, shareable, live-rendering surface + the account-level share/consent mechanism —— useful for HANDING OFF a result to you, not for the actual design work.

## Agree with your read + framing

24. "Simple stuff → CC designs directly in `.html`; complex fine-tuned work → escalate to Canva" —— agree, and today's evidence supports it: CD earns its keep only as a lightweight shareable-preview layer, not as a real editing surface for either camp.
25. Your BCIC/CCIC analogy is apt and I'll validate rather than just nod along: CD's own in-app agent operates like BCIC —— thin, chat-box context, no access to your heavy project/session state, same category of constraint that makes BCIC "very rarely used" per this repo's own `cic.md`. What you're describing (a CC session, WITH full heavy context already loaded, directly manipulating a rich design canvas the way I drive Chrome via CIC —— arbitrary clicks/drags/pixel placement, not a file API) doesn't exist for design tools today, CD included.
26. Conclusion: until CD ships either (a) true freeform positioning in its OWN editor, or (b) a CIC-equivalent that lets a fully-loaded CC session drive its canvas directly (computer-use style, not `DesignSync`'s file API), it doesn't add meaningful value beyond "shareable preview" to this workflow. Your read stands.
