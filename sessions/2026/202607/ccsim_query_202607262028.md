# Reply to ccsim_response_202607252223.md

## Numbering Issue
note numbered.md line 54 (read only that line if needed); e.g. your §37.9 should have been §37.09
no need to fix, just dont mistake again (either make it 2-digit or split as 2 separate sections)
suggestion: edit nlint to also scan for any line starting w/ `- [n].10` ([n] = any integer w/o space) in ANY FILES (NOT JUST `response_`; so yes, this makes `nlint` having a new function, not just scanning for numbering reset); when detected, yellow-flag remind to EITHER stop at or before [n].9 (+create a separate pt for remaining content) OR make 2nd level ([n].1–9) as 2-digit ([n].01–09) —— #SA may read `plan.md` which already mandated something likewise; this new function of `nlint` further strengthens it and possibly help close the gap
rationale: when i search [n].1, i want only that single sub-pt (and its sub-items), but now [n].10 onwards will also surface (not preferred); 2-digit solves the problem despite making them look clumsy, so i softly prefer (not mandate) splitting as separate pts as said, which keep it cleaner

## 37
37.2: agree to keep root c.md §7.2; suggestion for `profile.md` & `pro_profile.md`: "profile" is a rather generic term, and doesnt quite distinguish the 2 files, so i suggest to rename them as `personal_bg.md` & `career_bg.md` so the filenames themselves are more self-explanatory + the latter fits where it is (Career CP); i also notice you made the current `pro_profile.md`'s skill as `pro-profile` —— does it mean a skill can't use underscore? if yes, user hyphen instead of underscore for both pcmd; if no, rename both skills to match pcmd filenames; when both done, #SA rename both pcmd's headings (directly do it) then read through and suggest if content should need some changes (propose only; don't act)
37.3.3: #SA #cic to check if true; if yes, since all models i use have 1Mn tk context, this means the cap for the skills name+descr = 1Mn*1%=10k tk; if agreed, concisely note in `skill_guide.md` (see §37.5 below for the rename)
37.5: For consistency, cleanly rename `skiller.md` to `skill_guide.md` so it matches `hook_guide.md`; also this is more self-explanatory (it guides skill creation, just like `hook_guide.md` guides hook creation); if agreed & executed, note where apt that future similar notes in `ccsim/` also use `*_guide.md` filename for consistency to be more systematic
37.6: your `ccsim_response_202607251750`'s §14.5 told me to "Removable any time from the same Device Management pane." so i thought you advised me to remove something; clarify if it still matters, otherwise just leave as is; i agree to keep the `.mobileconfig` for retrospective purpose, just in case something goes wrong we have the trace
37.7.7: you did great, just ensure the files in `ccsim/` are only #r when truly needed but not default (just except `last_seen.md` that was #r every turn; CIIW); harden if needed; rationale: some grew quite large (e.g. `hook_guide.md` at 4842tk)
37.9: how about FURY unmounting? Will the recovery workflow (see 38.4 below) reconnect these hooks? i saw you updated `doomsday.md` but seems it merely tells the arrangement and what could go wrong, but didn't actually provide any resilience; i just duplicated `Volumes/FURY 2TB/.claude/settings.json` as `backup/backup_Claude/backup_Claude_FURY/backup_settings.json.md` (no need to read it; it's identical); read the README.md beside it and tell if agree & adjust if needed; then help me duplicate more files from `Volumes/FURY 2TB/.claude/` into this backup folder as needed, because i dont know which ones should be backed up; finally note where apt (if this only or mainly concerns hooks, put in `hook_guide.md` as i expect it shall be #r whenever working on hooks) to manually mirror changes from `Volumes/FURY 2TB/.claude/` to `backup/backup_Claude/backup_Claude_FURY/` for files that exist in the backup folder (add more as needed right now and in future) so they can be identical; NET: if FURY is lost, we simply have to dup these configured files from backup to the new `~/.claude/` after syncing from GH again
37.11.2: `yours` is too generic, make it `yours sincerely ` & `yours faithfully` (the only 2 possible "yours")
37.12: i notice this new file is not in `ccsim/CLAUDE.md` hence it will never be #r, just like `hook_guide.md` and everything else; btw i no longer see the value of `ccsim/README.md` —— since it's so lightweight, let's just fold it into c.md; NET: create a §7 in `ccsim/CLAUDE.md` that concisely tells what each file in `ccsim/` is and when to read (suggest ~10w each; but not strictly), except those already mentioned above (e.g. `backlog.md` & `sandbox/`); this is supposed to be an `index_cc.md`, but since this CP is CC only, there won't be an `_otg` variant, which makes `index.md` unnecessary, that's why we just need the new §7 (1 less file for me to ensure being #r, which is good); if agreed & done, void `ccsim/README.md`

## 38
38.1: i edited `ccsim/CLAUDE.md` to better define this CP; tell if agree; adjust if needed
38.4.1: as I raised above, what happens if FURY unmounted during CAI open or vice versa? Will the recovery workflow cover that? as in `setup_cc_tmpdir_202607241642.sh` —— im not very sure, but seems created by last session for me to run just in case `migrate_cai_to_fury_202607242011.sh` failed, or when FURY unmounted whilst CAI open (vice versa); tell if true; if yes, don't move it but develop a new one (suggest naming; e.g. `fury_unmounted.sh`) that supports rescuing the hooks, since a new `~/.claude/` would possibly be created if CAI opens whilst FURY unmounted (vice versa); only do so if you agree and ensure to put it in `nscpt/` (niche scripts) since it's user-facing; also tell if my §37.9 above helps; make use of the backup if agreed & executed
38.4.2: doomsday doesn't seem to carry that (CIIW) but I suspect it should now, pointing to `fury_unmounted.sh` so CCSIM CC may also ref it or work based on it when FURY unmounted; so really, anything happens to FURY/WSM = CC read doomsday.md then act/advise; if CC N/A then, i still know what to do (run `fury_unmounted.sh` myself; CIIW)
38.5.2: i remember it now and it must fire automatically; fix its hook if haven't so when CC edits a file, only mod time change but not "Date Added" (DA), i.e. won't jump to top
38.6: can we make clint to work also in the reader session (working dir: `GitHub/` alone, not anything else); it's on Haiku (very simple, well-defined task) and should produce absolutely zero chat text —— not even declaration; it only #ww for me, mostly used when CC is on super long missions + not yet pushed `response_` + im OTG + i wanna read latest state (quite rare); if not possible, leave it
38.6.3: when i prompt `#[name]`, hlint scans if `[name].md` exists throughout entire repo right? if true, limit it to only scan for `universal/` + `cp/` in `dupbus-ceztuc-7cufVe/` (so `sessions/` large quantity files won't strain the script or introduce meaningful latency) AND simultaneously expand to also scan for `AJAP_repo/inv/inveng.md` (only scan for this 1 file only in `inv/`, otherwise too large) + `AJAP_repo/protocols/` (everything inside, as usual); rationale: the 100⁺hr lost was due to CC not reading `AJAP_repo/protocols/eng.md` when i prompted `#eng`
38.8.1: actually i dont mind 1 turn becomes multiple; i mind 1 turn becomes multiple marked chapter (in my view), but seems this block will make CC immediately stop that "turn" (not the full practical turn) before being able to mark a chapter (CIIW), which is fine to me and i suspect it should be "loud" enough to discourage further chat prose; if true and you agree, i propose we can strengthen clint, making it red-flag block (unlimited; no longer once per turn) rather than yellow-flag warn (no longer needed; red only), BUT exempt when ` yn` (inc. the space) is in user msg (not file; i never put that in file), which is what false-blocked you just now, AND exempt when chat prose starts w/ `DATS` (MUST be 1 line only AND ≤10w) —— i just updated close.md (see `a8f2341`) to support this mechanism (now always starts w/ `DATS`)

## 39
39.2: i saw you added `=== NON-CCSIM —— all you need to RUN it ===` which is good, but im not sure (CIIW) if CC can extract and read just this section cleanly because it's just the 1st line of this section; if CC struggles to find its end, she might accidentally read everything after this line (i.e. whole script; wastes token/context); if you agree there is this concern (don't do it if you're certain there is not such concern), for each § NON-CCSIM, make the heading `=== NON-CCSIM —— start of all you need to RUN it ===` then add an end line like `=== NON-CCSIM —— end of all you need to RUN it ===`; then note this in `cscpt/README.md` so CC can accurately extract exactly the § NON-CCSIM and nothing more; btw i forgot to define the word-limit of § NON-CCSIM which some are quite long (e.g. nlint's is 213w); i suggest to make it ≤100w ONLY if you agree; e.g. nlint is run by harness, so no CC would have to read it, hence that "Run by the harness ..." block might not be needed (CIIW), and the remainder can also benefit from a trim, basically CC might only read it when unsure what this is or what to do when receiving its yellow/red flags; rule of thumb: stand from the perspective of non-CCSIM CC working on real tasks, which might not concern much on the script itself but the task she's on
analogy again: you're arsenal master so you care the make/material/durability of a weapon, but generals/soldiers merely need to know its feature/limitation/etc. that can support their battles

## 40
this doesnt just apply to `cscpt/` but the whole harness/environment of CC so i suggest to create a concise section in `ccsim/CLAUDE.md` so it stays and guides future CCSIM ops
e.g. the 5 lints (CC's harness/environment) are basically like a smart literal harness of a horse, autonomously blocking/warning behaviours that will harm the horse and guiding generals/soldiers on how to properly ride the horse; in reality: since the 5 lints run by themselves, CC MA/SA dont need to know how to run it but just know (when the fired blocks/warnings dont suffice in letting them know what's happening or they're confused) basically what it is and what to do when receiving red/yellow flags

## Alert
alert me if i missed out replying anything critical
also alert me if `#m1` below didn't fire `hlint`

#m1
fan-out SA fleet (ses% = 0)