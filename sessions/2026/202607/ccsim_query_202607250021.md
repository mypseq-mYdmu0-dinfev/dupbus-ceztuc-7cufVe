# New Request

## Preamble
this requires a separate `response_`, don't blend w/ the 2 being created (if still on Turn 12 when receiving this)

## Task
i want some minor improvements for git_history.py's .html outputs
on the header bar, add a new button: `Show More`/`Show Less`
Default is latter (`Show Less`), showing only commits since (NOT after) last rename
Click `Show More` = current state (show all identified commits)
then add another button: `Full Content`/`Changes Only`
Default latter (`Changes Only`), showing only changes of that commit + 3 lines above/below each changed line, rather than full content (useful when reading through historic changes of a file)
Click `Full Content` = current state (show full content of file)
These also apply to `HTML` button's view:
if `HTML` + `Full Content` (either order) both clicked (i.e. current is `Code` + `Changes Only` view), render .md preview (the "HTML" view in current script) on only changes of that commit + 3 lines above/below each changed line (as descr above)
also ensure git_history.py can accept multi-line input; 1 line = 1 file path = 1 output html

## Your Missions
- Execute actions if any instructed & no qb
- Wait for SA report(s) return, if dispatched
- Finally, tell your thoughts (don't miss any)
- Expected total quantity of `response_`: 1