# Reply to ccsim_response_202607262028.md

## Preamble
my last msg (`continue`) was misfired (supposed to be another session)
i.e. your changes on last turn (10) was totally unexpected, since you seemed to have already fully addressed ccsim_query_202607262028.md in Turn 9
explain why Turn 10 made changes (did you not fully complete by Turn 9?)
then assess whether any damage was done; revert if needed

## Minor Problem
another CC reported a problem whilst i set as "Auto" mode
she generated `queued_queries/push_classifier_query_202607282109.md`
then i asked `i've changed from "Auto" to "Bypass" mode; tell if the push would have succeeded just now in this mode yn; if unsure, say "Unsure"` and she said "Yes"
#SA to read that file and independently investigate whether "Auto" mode was the sole blocker and whether it means i must now always use "Bypass" mode for all CC sessions, otherwise root c.md §9.05 won't work at all
i don't mind using "Bypass" mode at all, but OTGC msgs will always make the session "Auto" mode (no "Bypass" mode option there), though i saw CC seemed to have still been able to push it regardless, so im rather puzzled why that CC struggled
on this problem alone, limit your response to ≤50w (still #numbered)

## 44
44.2: it's ok as long as all skills name+descr were injected to your context every turn and accumulate, otherwise it would exhaust all sessions much sooner than current (reading root c.md §7 just once)
44.3: if you're sure of the final conclusion (30k chars limit for 1Mn context window), note it in `skill_guide.md`; as of right now, all models (exc. haiku; almost never used just) has 1Mn context window so such conclusion would hold for quite some time
44.4: in that case, don't mention the ~10k as it's imprecise & misleading
44.5: don't mention working target; you and i both would never know the total amount of skills we'll end up w/, so just state the total (30k chars) and let future CC do the math

## 45
45.2: why don't we rename the pcmds as `personal-bg.md` `career-bg.md` to match the skills name then? if agree fully (no drawbacks at all), do it cleanly then redo your §45.2.4 actions
45.6.8: CIIW `backup_Claude_FURY/` seems just a snapshot of the files inside; if true, i suggest to add to `backup_Claude_FURY/README.md` then pt to it in `ccsim/CLAUDE.md` that every CCSIM ses start #SA checks if those files changed (diff them against the real files i guess?) then mirror again (overwrite is fine; this folder is tracked) if needed to keep the snapshot up to date
accepted risk: if FURY unmounted long after a CCSIM ses start, those snapshots in backup might not fully recover and something could be lost; of course we can do it every CCSIM turn but i suspect that would cost a huge deal of token/context (CIIW)

## 46
46.2.1: "scope" = `sessions/`? if yes, expand to cover `AJAP_repo/inv/` as AJAP's comms live there; then tell me why not remove the scope (i.e. any file changes by CC under `GitHub/` will do this, so DA always unchanged since adding to folder, only mtime changes)
46.5: you still emitted some chat prose despite very concise; this doesnt piss me off as much but im curious: were the 2 in Turn 9 and the 3 in Turn 10 triggered by and classified as root c.md §3.2.4 (blocker; e.g. `⚠️ Fleet running; awaiting reports.` seems not a blocker at all despite i just happen to appreciate such quick notification one-off)? if no, ensure the 5 declarations owned those 5 emojis (i.e. no chat prose can use these 5 emojis unless they're of these 5 types; doesn't apply to file contents), so no chat prose other than these 5 are possible
clint further reinforcement (no matter your answer to my last line was yes or no): if chat text contains `🚨`, it MUST (red-flag block if not) match exact content as root c.md §3.2.5; if it contains `⚠️`, it must (red-flag block if not) have ≤5w AND ≤3 hyphens AND ≤68chars, whichever fires first blocks it; rationale: w/o the hyphen limit, a CC can skewer long prose by numerous hyphens and cheat as a few very long words; the char-limit is the final safety net (68w = ~1 line in my view, since i always split-view as chat on left side and file preview on right side) but this number (68) must NEVER be seen by CC (unless reading `nlint` in full) so CC won't strategically use up such limit, because 5w (in a practical sense) should never reach 68w at all (just a contingency)
clint new exemption: i believe you already saw my update to glossary.md; just like `yn` (which allows 1w only; either `Yes`/`No`), if user msg (not query file) contains `sic` (whole word; unlike `yn`, it doesn't always follow a space; e.g. i could say "sic 8w as i saw you ..."), exempt the block if the chat prose is ≤5w OR the mandated word count in the user msg directly following `sic` (e.g. `sic 8w` = limit ≤8w)
another clint new exemption: if user msg (not query file) contains `override`/`overriding`, one-off disarm clint ONLY for that turn, so CC can override the chat prose prohibition (rare though)

## 49
49.1: you're right; don't remove those prose but comment them out so the .sh can be directly run and i can still see what it means if i choose to; do the same (comment out) for other .sh in this `nscpt/` as i suspect there're more, since they were originally from my Apple Notes
49.2: fix it for me; #sync as needed

## 50
50.2: learn from your mistakes; concisely note in your c.md only if apt; you're one of the most important CP because any errors you made = suffered by ALL CC

## 55
55.4: do it; my suspicion: this seems to also allow CC to only read the relevant section/lines; e.g. if only needs info about my home, only that section was retrieved & #r

#m2
FOF