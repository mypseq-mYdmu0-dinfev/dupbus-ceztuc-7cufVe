# Session Closing (202607130337–202607130414)
*Dark-Mode Toggle Detached to Template*

## 1. Coverage
- 1.1. Built a random test .html w/ the coding.md-standard light/dark toggle (circular ☀/☾, fixed bottom-right, OS-scheme-aware, localStorage-persisted).
- 1.2. Detached `coding.md`'s toggle rationale + reference build (978tk) into a new READ-only template, `cscpt/dark_mode.html`, leaving a 71tk pointer line in `coding.md`.
- 1.3. Read `gscpt/git_history.py` in full to verify its own toggle (text-labelled "Dark Mode"/"Light Mode" button inline in a fixed header, session-only, no persistence) —— confirmed as a valid alt-design, captured as an "adapt by layout" note inside `dark_mode.html`, not a template-default change.
- 1.4. Updated `cscpt/README.md` w/ a `dark_mode.html` entry + a caveat exempting it from the folder's "never read" framing.
- 1.5. Corrected an earlier response's word-count figure to token-count, then added a permanent rule to `coding.md` § Scripts & pcmd: measure/report pcmd size or trim savings in tokens, never words.

## 2. Decisions
- 2.1. Split "what's REQUIRED" (short pointer, stays in `coding.md`) from "HOW to build it" (full rationale + code, moved to `cscpt/dark_mode.html`) —— saves `~`907tk per non-html session that reads `coding.md` going forward.
- 2.2. Kept the CSS/JS reference build as REAL functional code in the template (not re-duplicated as an inert comment) —— it must actually run when copy-pasted, whereas the design rationale is comment-only (invisible on render, CC-visible on read).
- 2.3. `dark_mode.html`'s circular-icon-bottom-right stays the DEFAULT shape/position, not a fixed rule —— alt layouts (e.g. `git_history.py`'s header-button, better suited to its own toolbar row) are a documented adaptation, not a competing standard.

## 3. Issues
N/A

## 4. Comms Files (3 pairs)
- 4.1. `202607130337`: random test .html w/ toggle
- 4.2. `202607130350`: dark_mode.html template + coding.md detach
- 4.3. `202607130409`: word→token correction + pcmd rule

## 5. Non-Comms Files (4)
- 5.1. Created:
  - 5.1.1. `sessions/2026/202607/random_page_202607130337.html`
  - 5.1.2. `cscpt/dark_mode.html`
- 5.2. Modified:
  - 5.2.1. `universal/coding.md`
  - 5.2.2. `cscpt/README.md`
- 5.3. Moved/Voided/Deleted:
  N/A

## 6. Remarks
N/A
