# Migrate Hidden Home Folders to the External SSD (SSD-Wear Reduction)
*Drafted by an AJAP `#inv` session on the user's behalf (202607060641), from an SA-generated audit (Fable) —— the CC receiving this prompt MUST re-verify every claim/size/verdict herself; do not blindly trust or act.*

## 1. Problem
- 1.1. WSM's internal SSD health is at 94% (in use since 20230202); frequently-written hidden folders in `/Users/culous/` age it further.
- 1.2. Goal: relocate what is SAFE to '/Volumes/FURY 2TB' (external, replaceable), keep everything risky local.
- 1.3. ⚠️ Filesystem caveat discovered: FURY 2TB is HFS+ mounted `noowners` —— ownership is ignored and POSIX permissions are not reliably enforced there; NEVER move credential/key material onto it. Consider whether reformatting to APFS (owners enabled) is worth it BEFORE migrating live app state (e.g. SQLite).

## 2. Already Done (by the AJAP session; do not redo)
- 2.1. `~/.ajap` (AJAP programme home: Chrome profile + `.env`) —— moved to '/Volumes/FURY 2TB/.ajap' and symlinked back; verified working (preflight + live browser runs).
- 2.2. Method used: quit consumers → `mv` → `ln -s '/Volumes/FURY 2TB/.ajap' ~/.ajap`.

## 3. Inventory & Verdicts (audited 202607060630; re-verify sizes before acting)
- 3.1. `~/.claude` (Claude Code; 1.7GB, 1.7GB of it = `projects/` transcripts; HIGH write freq) —— BEST candidate.
  - 3.1.1. Recommended surgical form: move ONLY `~/.claude/projects` → symlink; settings/credentials stay local (`~/.claude.json` stays in HOME either way).
  - 3.1.2. Alternative: `CLAUDE_CONFIG_DIR` env var for the whole dir; env-var beats symlink for launch-before-mount safety.
  - 3.1.3. Risk: if the symlink dangles at app launch (volume unmounted), CC recreates a REAL local dir → split-brain; close ALL CC sessions before moving (`lsof +D ~/.claude` to verify).
- 3.2. `~/.npm` (npm cache; 67MB but constant churn) —— SAFE via env, no symlink: `npm config set cache '/Volumes/FURY 2TB/npm-cache'`; worst case npm re-downloads.
- 3.3. `~/.vscode` (extensions; 879MB, LOW writes) —— SAFE-SYMLINK but a SPACE win only, near-zero wear win; quit VS Code first.
- 3.4. `~/.codex` (OpenAI Codex CLI; 85MB, live SQLite WAL churn) —— RISKY: active SQLite on a removable HFS+ volume risks corruption on surprise unmount; only migrate (via `CODEX_HOME`) if the drive is treated as permanent.
- 3.5. `~/.config` (mostly Raycast, 354MB, LOW writes) —— RISKY: Raycast is a login item that starts BEFORE external volumes mount → dangling-symlink data reset; if at all, move only `~/.config/raycast`; keep `~/.config/git` local.
- 3.6. `~/.anydesk` (AnyDesk; 476KB) —— SKIP: holds the client ID (licensing/identity), a root daemon also writes here, and the size is negligible —— breakage risk for zero benefit.
- 3.7. `~/.ssh`, `~/.android` (keys) —— NEVER: key material must stay on the internal encrypted disk; HFS+ `noowners` cannot hold the 0600 perms ssh demands.
- 3.8. `~/.Trash`, `~/.zsh_sessions` —— NEVER: Finder/zsh-managed, written pre-mount, no benefit.
- 3.9. Dormant 4KB-class dirs (`.adobe` `.cups` `.idlerc` `.knime` `.matplotlib` `.mono` `.swiftpm` `.equo` `.fontconfig` etc.) —— SKIP (fragility for zero savings); `.fontconfig`/`.equo` are regenerable caches, deletable instead.

## 4. What You (the Default-Repo CC) Should Do
- 4.1. Re-run the inventory quickly (sizes/mtimes may have drifted) and confirm each verdict above against the CURRENT state; check each folder's parent app individually —— do not blanket-migrate.
- 4.2. Execute the SAFE migrations the user approves, one at a time: quit the owning app → verify no open files (`lsof +D <dir>`) → `mv` → symlink (or env var per 3.1.2/3.2) → launch the app → verify.
- 4.3. Prefer env-var mechanisms over symlinks wherever the app offers one (survives the login-before-mount race).
- 4.4. Remind the user of the standing caveats: volume must stay mounted (esp. before any CC/npm/VS Code launch); Time Machine excludes external volumes by default —— migrated data leaves the backup set (add FURY paths to the backup tool or accept the risk); always quote the path ('/Volumes/FURY 2TB/...'); eject cleanly.
- 4.5. Do NOT touch: `~/.ajap` (done), 3.6–3.9's folders.
- 4.6. When done, report per folder: moved/skipped, method, verification result.

## 5. Addendum (202607071751): CC Harness Temp Writes Also Hit the Internal SSD
- 5.1. The Claude Code harness writes sub-agent transcripts/outputs and session scratchpads to the INTERNAL SSD under `/private/tmp/claude-501/<project-slug>/<session-id>/tasks/*.output` (observed live 20260707: `/private/tmp/claude-501/-Volumes-FURY-2TB-Fury-Documents-GitHub-AJAP-repo/22075f91-604e-4376-81b7-90fa9e406405/tasks/a2b5a478f53a656ea.output`). This affects EVERY CC session that uses agents —— all repos, incl. the default repo —— not AJAP specifically; the intent is that heavy write traffic lives on '/Volumes/FURY 2TB', not the internal SSD.
- 5.2. Investigate in this session: can the harness temp/scratchpad root be relocated (env var/setting, or symlink `/private/tmp/claude-501` → a FURY path)? Mind that `/tmp` is wiped on reboot and per-user, so a symlink needs recreating (e.g. via a LaunchAgent) and the volume mounted before any CC launch (same race as 3.1.3). If not relocatable, at minimum document the exposure and set up a periodic clean-up.
