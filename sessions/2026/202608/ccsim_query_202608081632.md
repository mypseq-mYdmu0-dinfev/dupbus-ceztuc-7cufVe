# Reply to ccsim_response_202608081357.md

## 198.5.6
first problem: just reverse the scan; if there are multiple `🦈` lines, each MUST contain the repo shorthand as label, else warn at next turn start, but stress that labelling ONLY applies when the turn involves multiple repo changes, preventing this warning to make CC panic (e.g. Turn 1 touched 2 repos & emitted 2 `🦈` lines w/o label; at Turn 2 start the harness fired and CC took note of that, however Turn 2 only involved single repo edit but CC emitted the label anyway —— this is the "panic" im trying to prevent)
second problem: no idea what that means; #rephrase

## 199
199.4.2: i don't need (although not banning) any user-visible alert; again, i just need a CC-visible alert whenever chat prose emitted to stop that
199.4.3: this is great, implement it; when done, tell if any drawbacks
199.4.7: as said, build them except the MessageDisplay flagger —— reaching me = additional chat interface contamination; as said, ideally speaking i expect the chat interface to have: my msg (ideally a `query_`) → single-line encompassing all actions (e.g. "Ran N commands, ...") → TEAs → my next msg → loop; i.e. your last (Turn 14) was perfect just except the 2 lines of `.`, which split the actions into 3 lines (before, between, and after)
199.4.8: not quite sure what you meant; unrelated reminder: chat prose should remain exempted when user msg (not file) contained ` yn` (can only output `Yes`/`No`) OR user msg/file contained `override` (maybe some more; i forgot)
199.4.11: i only saw phase 1 and am not sure what phase 2 is; anyway if Fable SA is confident, build it first then report any drawbacks/caveats
the existence of §199.4.10/11 proved the nlint (or maybe another linter) is not working: we previously set up that a section (e.g. §199) can only have 9 sub-pts (same for 3rd, 4th, etc. level), UNLESS it's in double digit; i.e. your §199 should either be divided into multiple sections (preferred) OR become §199.01/02/03/etc. (only if necessary); once again, the rationale is that when i search `199.1` i expect only 1 result but now it shows 3 (199.1/10/11); #SA (Fable) review → fix → review to ensure gap-free

## QQ Sweep
i ordered a QQ sweep before but i still see `ccsim_`-prefixed queries in `queued_queries/`
#SA hunt for our last comms pairs on it; if all resolved, handle them per the README.md; if any not yet resolved, resolve them then handle them
goal: all `ccsim_`-prefixed QQs are confirmed resolved + voided, which i'm assured of them w/ concise justification (≤30w each) that i can proceed to delete them

#m2
FOF; reminder: use Fable SA whenever needed