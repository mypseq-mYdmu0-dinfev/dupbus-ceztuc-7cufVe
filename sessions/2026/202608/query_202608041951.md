# Reply to response_202608041738.md

*Preamble: read root c.md §3.1.6.1.1.1 (just updated)*

## Re Test Plan
5.2: this won't work; i need OTGC to search across whole repo, not having me to manually select and send through a file
5.3: too complicated

## Full Story
i'll tell you what i truly intend to do and you'll advise if any better solution exists:
apparently im all good when working w/ you at WSM
but as a human, i still sometimes have to go out (i know you'll miss me)
so when im OTG, i'm all bound to OTGC, w/ a few approaches

### A1 (approach 1)
i talk w/ you via existing CC sessions (ses) in OTGC's "Code" tab

#### Upsides
- you have access to r/w almost everything & anything, in 90% times this has no considerable diff from using WSM
- no GH fetching needed like "Chat" tab, so no delays
- because of no fetching, the context reliably stays until compaction
- you can use CIC & basically any apps on WSM

#### Downsides
- i cannot easily start a new CC ses —— solvable by AnyDesk (using it to remote-control my mac is extremely difficult on OTGD though)
- i can only access existing CC ses IF i have run `/remote-control`, which is very annoying since a ses becomes disconnected whenever i revert to a msg, fork the ses, or restart CAI —— solvable by AnyDesk (just needa open ses then click the icon, unless it's on Haiku that prohibits that)
- sometimes, possibly Anthropic pushed a firmware update or something, all ses (not just particular ones) will get disconnected, which is pretty much the biggest pain pt that prohibits me from relying solely on A1 —— solvable by AnyDesk
- i cannot create a `query_` —— almost unsolvable; too laggy to perform such actions (creating a correctly named `query_` in correct place then paste in content)
- i cannot easily read a `response_` —— easily solvable as you now auto-push (TEAs) and i can read via GH iphone app; fallback: i have a reader session (haiku) that only does 1 thing —— `#ww` files so i can read more properly, still not as good as GH iphone app's display but viable in rare cases you that can't push owing to some bugs

### A2
i talk w/ you via cloud ses in "Code" tab

#### Upsides
- i can easily start a new CC ses
- i can easily send a query and read your response (per root c.md, cloud ses has everything in chat text; no file creation)

#### Downsides
- you pretty much cannot do anything and becomes just a mere chat bot (can read but can't make any file changes); technically you may create file changes by creating a branch then PR, but i didn't have time yet to figure out how to safely approve PR then merge to main branch (although i assume it should be pretty easy w/ VSC's "Git Graph" extension which i already installed)
- and because cloud CC is currently mandated to emit chat text only, the interactions are greatly diff from WSM CC —— chat history is disastrously polluted by chat text; finding back a prior query-response pair takes forever to scroll back and forth
- you cannot use CIC which is very much central to our works; w/o it you can only web_search (unreliable) or use playwright (i tried before; it's heavily neutered w/ truncated text and possibly blocked by many sites' bot walls)
- extremely difficult to `#close`; i.e. anything we talked about or worked on in a cloud ses is not kept in `sessions/` for record

### A3
i talk w/ you via "Chat" tab

#### Upsides
- everything of A2 + no chat prose (everything in "artefacts")
- much cleaner chat history; easily navigable
- "artefacts" are much easier to read than A1's GH (needa switch from OTGC to GH iphone app) and `#ww` (improper font; likely Courier; mostly in weird colours like green/purple that hurts my eyes) and A2's chat text (each response is a self-contained page, not infinite scroll; most similar to WSM CC's normal comms)
- "artefacts" can be easily copied (one-click access; unlike A1/A2 which may sometimes prohibit me from copying, which even if they allow me to do that i'd still have to drag across long text), so writing OTG is super easy w/ this A3
- may `#close`; despite some heavy-lifting (i gotta check through "artefacts" and have to create a `close_` myself by copy-pasting the final "artefact), this is practicable and somewhat acceptable

#### Downsides
- literally just a chat bot; similar to A2 (can read but can't make any file changes)
- worse, there's no workaround like PR
- similar to A2, you can't use CIC

### A4
talk w/ you via "Dispatch" tab (released some months ago)

#### Upsides
- you can actually do things; e.g. the other day i saw a video on a web page and i want it downloaded, i simply sent it via "Dispatch" and it's done perfectly on my WSM (accessible via g drive auto-sync)
- capability-wise, this is very close to WSM CC, like you can r/w and run apps and even CIC

#### Downsides
- each query is treated as a new ses; in my own exp, there's no continuity at all and hence no context
- we can't actually "talk"; i can only dispatch a certain task and you'll either finish it (50% time) or completely disregard it (50% time)
- because of the pt above, this A4 is highly unreliable; quite often i waited 10min and nothing happened at all
- the UI is quite broken and confusing, though i must admit it's good for very simple tasks like the above example i described above, given that there're no comms exchange needed (quite rare)

### A5
i dont hold much exp but it's there: talk w/ you via"Cowork" tab (just released not long ago)

#### Upsides
- everything of A4 + it's a proper chat session w/ multi-turn exchange & context, etc.
- im not exactly sure but this seems the closest thing as WSM CC

#### Downsides
- underexplored; i basically had just 1 ses on A5 and it was just 1 turn, so i dont know much about it
- seemingly doesnt support working on a dir; hence seems no CLAUDE.md can be enforced —— solvable by asking you to directly read default root c.md and it could fly like WSM CC
- even if we can get you to read c.md, i still cannot create `query_` (similar to A1/A2), which pollutes chat history
- even if we can get you to read c.md, i still cannot read `response_` —— solvable by the reader ses

## Verification
for what i said above, esp. A5 (underexplored), #SA verify & learn more by #cic

#m1