# GH Config Problems

## Problem 1
our default repo is currently set as public, so anyone knowing my GH username and the repo name can access anything inside
it wasn't a risk before but now it could be a risk as i might have to collaborate w/ others and expose my username and even our default repo name

## Problem 2
the reason why default repo being set as public is solely because OTGC's "Chat" (not CC) cannot access private repo files (even w/ a password or something generated in GH, which i forgot how) before, but not sure if that problem was fixed by Anthropic and i intend to find out so we can CONSIDER to set default repo as private AFTER verifying it's fixed

## Problem 3
this is more like a question: if i create a new repo using the same GH account, is it absolutely unavoidable that users w/ access to this new repo will simultaneously see my GH username? or is there some way i can hide my GH username whilst granting access to the new repo? i ask this because i can do similar in google drive: i can create a folder, make it public, share it to others, and nobody will see my username at all

## Suggestion
actively use a dynamics workflow to solve the problems which the SAs shall actively #cic so latest info (e.g. whether Anthropic fixed the problem)
when confident, guide me on how to share a specific file of a private repo (is that possible? like i can share a single file w/o sharing the whole repo?)
FYI: i'll then try w/ an adjacent, private repo (`AJAP_repo/`) and ideally just a single file for testing w/ OTGC

#m1