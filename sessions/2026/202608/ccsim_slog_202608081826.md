Sprint Log 202608081826

GOAL: fix the §3.1.6.1.4 violation airtight, repair the numbering fault, and defer the rest to next session before context runs out.
TASKS: T01 investigate the glossary commit violation (MA) · T02 airtight `git add -A` gate + §202 TEA-line count + §208.6 SHA gate + §209.7 standfirst check (Fable) · T03 numbering resequence + citation sweep (Fable) · T04 close_ deferral list for next session · T05 response_ + TEAs

[202608081826] Sprint opened. `#m1` this time, not `#m2` —— so NO interim commit or declaration; one `response_` at the very end, after every SA has returned.
T01 CONFIRMED by git: commit `05b938d9` swept the user's IN-PROGRESS `universal/glossary.md` edit (his Preamble restructure) into a CCSIM commit. Cause: TEA1 ran `git add -A`. Root §3.1.6.1.4 says commit ONLY files touched by me this turn; §3.1.6.1.5 says if his edits touch MY files, do not commit at all and raise a blocker.
⚠️ THIRD TIME `git add -A` has done damage: 88 voided files published on 07/08, and now his live edit. Prose has failed twice, so the fix is a GATE —— alint already parses every Bash git command as a registered PreToolUse hook, so it is the natural home.
Checked and cleared: the 7 CLAUDE.md lines in that same commit were ALL mine (§5.5.5), not his.
T03 is the entangled one: the 1632 file has TWO §204s and an out-of-order §208, but 1721 already occupies §209–§220, so a naive renumber collides. Briefed the SA to choose the sequencing itself on numbered.md's grounds and to sweep every inbound citation —— backlog.md, close_ §4.22 and 1721 §218.5 are known.
STATUS: T01 done · T02/T03 in flight (Fable) · T04/T05 pending
NEXT: merge both returns, write the close_ deferral list, then TEAs
ASSUME: the user's "if heavy, defer to next ses" applies to the numbering only if the SA cannot land it cleanly —— he explicitly said heavy "for you, not SA", so it goes to an SA rather than to next session.
ASSUME: m1.md #r this turn (first time this session); sprint.md and m2.md NOT re-#r (both #r earlier this session) —— declared per hlint's deferral rule.
