Sprint Log 202608071745

GOAL: execute ccsim_query_202608071742.md —— mint `re-#r`, broaden the read linter, harden the 🦈 single-repo rule + the both-glyphs rule, build the UserPromptSubmit chat-prose tally, action the two queued queries, memory, close_ update.
TASKS: T01 mint `re-#r` + linter on ANY read (whole or partial) · T02 🦈 no-colon-single-repo hardening · T03 §3.2.1/3.2.2 both-glyphs wording · T04 142.4 SA-reads-under-#opt rule · T05 UserPromptSubmit chat-prose tally (181.3) · T06 action both queued queries per README + reply to AJAP · T07 memory: never gitignore voided files · T08 #rephrase 156.3/4 + any still-open sections · T09 close_ update (LAST, just before TEA1) · T10 response_ update + TEAs

[202608071745] Sprint opened. m2 step 2 done: response_ committed + pushed ALONE at 4adcb4d8, declared in the same msg as this tool call.
STATUS: T01–T10 pending
NEXT: dispatch Fable FOF for the coding tasks (T01, T02, T05), MA takes the protocol/comms tasks
ASSUME: user reverted the `❌_*` gitignore stanza himself (ac4af63e) and added root §3.1.6.1.6.3; I agree with both, so nothing to undo —— only the memory to write.
ASSUME: "FOF; anything coding = consider Fable SA" → T01/T02/T05 to Fable SAs in one wave; T05 is NEW wiring on a live event so it gets the strictest brief.

[202608071752] MA tasks landed whilst the Fable FOF runs.
T06 DONE: both queued queries actioned per queued_queries/README.md —— copied to 202608/, originals voided in place. Reply drafted as `ajap_evidence_query_202608071749.md` (short, per the user's "not a chat room"); it closes the dead-pointer loop and states the hazard as jointly closed.
T07 DONE: memory `feedback_never_gitignore_voided.md` written, indexed, MAP'd into mirror.sh, mirror sync exit 0, mirror_test 13/13.
T02/T03/T04 DONE (protocol half): root §3.2.1.4–5 (SA reads under #opt, not TEA3), §3.2.3.4 (both glyphs when #r AND edited), §3.2.4.6 + .6.1 (single repo → no colon, binds every repo's CC). All ≤90 chars; dlint clean.
T02 mechanical half DEFERRED with reasons at response_ §189.3.2–3: clint judges line-by-line but the colon rule needs whole-batch context, AND clint's output currently reaches nobody —— build it when the tally makes its classes visible.
T08 DONE: 156.3/4 rephrased in plain five-field form at §192.3.1–2.
STATUS: T01/T05 in flight (Fable) · T02–T04, T06–T08 done · T09 close_ + T10 TEAs pending
NEXT: on both SA returns —— merge, update close_ (LAST), TEAs
ASSUME: a further AJAP qq IS warranted despite "don't extend indefinitely" —— her §2 explicitly asked for "one line back", and leaving a dead pointer unanswered is the exact defect class we were both fixing.
