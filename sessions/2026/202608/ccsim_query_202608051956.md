# Reply to ccsim_response_202608042135.md

## 59
LIVE TESTING: another CC is using the new m2.md (written by you; untouched by me as per 202608050217) right at this moment; i'll see if she behaves well (she's the one who wrote ccsim_query_202608042035.md; so she knows to NOT stop after emitting thoughts but dont know the rest)
if not, we should detach some current lines (so the snippet is cleaner and less likely to confuse) into a new section called "Clarifications:" then enrich w/ apt directives so CC can act more ideally
UPDATE: she did not declare the `response_` at all this time despite having created it and being able to resume (not stopping after telling thoughts, possibly because she didn't declare, or maybe because of your Stop-hook at your §59.6); for safety, i suggest creating that "Clarifications:" section below the snippet in m2.md; be super concise; ≤90chars each line; simultaneously i propose to make the snippet #numbered, so the clarifications can just refer to the pt no. instead of repeating
59.7: i have a bit of concern you might have over-engineered (not an accusation) —— m2 has worked well before, as in CC didn't stop after creating & declaring the `response_`, despite most CC just append but not edit the created `response_` upon SAs return; i want a sanity check on why this problem we're facing is emerging, and tell if we should revert something rather than adding more, because i feel like we might be working against ourselves (e.g. instead of `3+3-2-1+2=5`, we could have just tell `5`)

## 60
the violated CC drafted `ccsim_query_202608050233.md`
#SA read it then fix again/close gaps if needed
goal: don't let that happen again; this is seriously damaging

## Query-Response Pair
i believe i have asked in a previous query that i want you to add in root c.md where apt that 1 `query_` leads to 1 `response_` (nothing more & nothing less), unless the user or a pcmd instructs otherwise
this is important or `#close` (which lists comms as pairs, not full filenames) won't work
i dont quite see that addressed; feel free to defend
if indeed not addressed, concisely explain why it was missed out by you

## m2 & PDF Handling
#SA read `ccsim_query_202608050402.md` & `ccsim_query_202608050403.md` then judge by herself: EITHER independently fix + report result to you OR suggest escalating to #MA read & fix
as always, keep pcmds concise per #coding
finally, #rephrase for me

## dlint update
a CC emitted a `response_` w/ the word `rigor`, not `rigour`, despite having run quick dlint
so dlint failed to catch that Americanism instance; #SA harden it accordingly

## 61
so is this resolved? if yes, did you append as a subsequent response down below in this response? if yes, why didn't you append at this pt as sub-pts instead? seems even you don't get #m2

## 65
safe to delete elint.py & elint_hook.sh now?

## 66
speaking of that, help me restructure numbered.md to have the bite-size section as a modifier triggered by #bite, similar to #coding #rephrase so i can use them tgt or separately
by default, everything should be already #numbered #bite (since numbered.md is an Unconditional); so i should never have to prompt #bite; i.e. everything should be like current state, except i can now REMIND on either
like if i ever want something to be just either, i can prompt #bite only, or in opposite #numbered only
e.g. coding.md is basically telling #bite only but not #numbered; though don't edit coding.md, as it's even more precise than #bite, specifically mentioning ≤90chars

## coding.md
you unnecessary removed the #rephrase headings and i reverted then manually touched it; review my changes; tell if agree

## 69
69.1: i thought it was solved (since it was voided)? if not, keep fixing it; if yes, confirm if that voided file can be deleted now

## Queued Queries Sweep
confirm these queries were addressed (if yes, void them; if no, fix them):
deliverable_lint_gap_query_202607302336.mdccsim_query_202608042035.mdccsim_query_202608050233.mdccsim_query_202608050402.mdccsim_query_202608050403.md

## 73
73.7: i didn't expect it to be noisy; it should only flag `read`/`Read` (whole word), not `reading`/`#r`/etc.; so if a CC writes correctly, this shouldn't fire at all; and after this fired, that CC shouldn't repeat such mistake in the session; that's why i DON'T expect it to be noisy; ensure of it for me

## 77
so is #cic and the hlint now both fixed and working? if not, why is this in `#opt`?

## 79
so what must i do? i dont get it; seems you already built the script? is it working now? if not, why is this in `#opt`?

---
below are my owed replies to `ccsim_response_202608012325.md`:
---

## 34.2
this filename `pending.py` is highly generic and should be avoided
im not very sure what it does but seems it scans for any ≥7d-old voided files AND anything in `queued_queries/`, then check if they are addressed and can be deleted
if true, rename as `ccsim_housekeeping.py ` and this should only be run by CCSIM at ses start, else it would be too noisy and other ses shouldn't be on this (out of scope)

## 34.4
i haven't used these for long, but i think they expect an input .md file right?
if yes, they only needa scan for that input .md right beside them inside `gscpt/` then just follow the path
and yes that means i cannot just put in a filename, but a full path of either a file OR a whole folder
update them for me

## 35
add to `ccsim_housekeeping.py` so CCSIM runs at ses start and flag for any stray-space-filename

## 36
36.5: should NOT scope to this repo only, but ONLY exclude `AJAP_repo/`; rationale: im building 2 new repos soon and they are for work so needa be maintained as well
36.6: CIIW this script shouldn't delete anything but just scans, checks, and suggests deletion; if true, no need to exclude `backup/` as it shouldn't contain any voided files either

## cscpt sweep
for easy understanding, i want each `*lint.py`'s 1st line to become a title w/ its full name, then the rest starting next line
e.g. flint.py should become `"""Filename Linter (PreToolUse hook)`; skip a line; `Guards root...`
basically this explains what the "f" in "flint" is, same for other linters; if unsure, figure out why it's named that way and edit it then alert me it's inferred; though i seem to see most linters already got their full names inside

## 37
i already answered §34.4 above; confirm this is now fixed & closed

## 38
seems this is closed

## 39
in short, you're telling me what i requested in §13 was implemented, and so far the hooks (max. that can be fired in parallel or sequentially) would not exceed 1s, right?
if true, restructure `hook_guide.md`'s italic top section before its §1 (btw tell what you call it?) as a `## 0. Preamble` so it also has to be #numbered #bite; within it, make the 1st pt showing directly (just 1 single no. on it) at worst (based on largest .md in `sessions/`) how much delay current hooks can impose on WSM (to be checked and changed every time hooks edited/created; OTGM duration to be estimated & added later); net: i open `hook_guide.md` and i immediately see how taxing all currently established hooks are
39.7: as said, better estimate the WORST CASE possible, ensuring these hooks won't possibly drag our work beyond 1s; if reaching 1s, it's not the end of the day —— just alert me and we'll look into them, like feasibly what duration it can be in real use
39.8: if you meant SA FOF, they don't count because i won't be sitting there to wait in live; basically this only measures my wait for #m1 and the initial response of #m2, as well as quick exchange like `yn`, etc.

## 40.1
you directed me to read this but i dont quite see any actions owed by me, basically you're telling me we have a sum of 7 lints, not 6; confirm if true

## 41
41.2: update: `,"` is treated the same as `."` —— red-flag block; no exemption; only demote to yellow-flag upon 5 instances in a single file
41.4: i dont get it; the lint should only apply to the currently being created/edited file, not historic ones
41.5: this is concerning; if ≤3 red, tell me; if more, edit it for me and remind me to see git changes

## 42
i see it diff; i reckon flint absorb tlint (TS is part of filename, so it's logical) and become PostToolUse warning only
CC won't typically create stray space so this is just a safety net anyway
PreToolUse doesnt seem logical to me because the file wasn't even named (CIIW)

## 47
47.2: confirm this is done
47.3: #rephrase if not yet fixed
47.4: do you have to experience it yourself or you can see its log like experienced by another CC? anyway keep that in backlog.md so it remains watched; it's genuinely my fortune to not having experienced that yet since migrated to CC from Claude Chat

## 49
49.1/2: should have both been resolved
49.3: i renamed it myself; keeping it
49.4: already answered above
49.5: neglect them; SAVE IN YOUR MEMORY: any filename w/ stray space = likely user's own files, not expecting CC's direct use (e.g. `.numbers`); any filename w/ ` _ ` = same as stray space, plus it's likely an exported file showing its export time (typical workflow: work at `X.md` → layout at `X _ [TS].pages` → export as `X _ [TS].pdf` for stakeholders)

## 51–58
still reading; will reply in next turn; flag if anything no longer stand / i shouldn't waste time reading

#m2
FOF (ses%=0)