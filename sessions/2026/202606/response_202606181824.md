# Response to query_202606181824.md
*Interviews folder —— created; thoughts*

10. Done —— I agreed on all points and executed (no blocker). Actions:
- 10.1. Created `seek/interviews/` with `g/` and `c/` sub-folders.
- 10.2. Created `seek/interviews/README.txt` (3 short lines: purpose + out-of-scope note + `g/` vs `c/`).
- 10.3. Added one bullet to `seek/CLAUDE.md § Absolute Rules` (after the `/ccl/` line) pointing future CC to the folder and marking it OUTSIDE AJAP scope.

11. Re preamble —— noted: the "on DATS.py…" msg and my reply to it are leftover; I will NOT include them when we `#close` later. (Not acting on it now.)

12. Re §3 —— agreed, and your refinements are sound:
- 12.1. `interviews/` (plural) is better than my "interviewed" —— it neutrally covers both "invite scheduled" and "already interviewed".
- 12.2. `g/` + `c/` instead of `gcl/` + `ccl/` is a deliberately good call —— it sidesteps ANY name collision or prefix-glob confusion with the real `/gcl/` and `/ccl/` scan trees. Clean.
- 12.3. `README.txt` is the correct convention —— root `CLAUDE.md` §8.5 already tells any CC to read a folder's `README.txt` to learn what it is, so this self-documents for free.

13. Re ## Other —— you are correct (no CIIW needed): because every AJAP scan and the heartbeat file-watch are rooted at the literal `/gcl/` path, a sibling `seek/interviews/` is structurally unreachable. So NO existing-logic edits are required for safety —— the folder is safe by construction, not by rule.
- 13.1. The one-liner you proposed is still worth adding, but its value is INFORMATIONAL (telling future CC where to `find` a job's record mid-interview), not defensive. I folded both purposes into the single bullet (10.3) —— it points the way AND restates "never touched", so your original "MUST NEVER be touched" mandate is honoured in one line rather than the heavier multi-file mandate I first proposed.
- 13.2. The existing `seek/CLAUDE.md` rule "All other paths —— read only" already blocked AJAP from WRITING there; my bullet adds the explicit never-READ/scan guarantee on top.

14. Silent-skip unaffected (carried over from §5, just so it's on record) —— originals stay in `gcl/applied/` (or `ccl/`), so K6 (`` `<`30d ``) and K4 (SEEK green tick, any age) still prevent re-application. The `interviews/` copies are purely your reference and feed nothing back into AJAP.

15. One thing to keep in mind (not a blocker) —— `g/` and `c/` are currently empty; git does not track empty directories, so they will not appear in a commit until you copy your first AR into each (or until a placeholder exists). The `README.txt` at the `interviews/` root IS tracked. No action needed unless you want the empty sub-folders committed now.

16. Ready for your first copies whenever —— just drag the interviewed-job ARs into `g/` or `c/` as appropriate.
