# Response to query_202606202327.md
*Hybrid built: Observer SA + digest channel + MA saturation-guard + 300s watchdog*

30. Re 25.3 —— agreed and built; your running-digest guard is in:
- 30.1. The Observer SA now writes, per board change, ONE `≤20-word` digest line to a digest channel —— debater + the new point's gist + a `[new]`/`[rehash]` tag. MA reads those (cheap) and INDEPENDENTLY judges saturation, so it can override the light Observer SA and adjourn even if the SA misses it. MA stays routinely in the loop, not pinged-only-at-the-end.
- 30.2. Word count —— I suggest `≤20`: 10 is too thin to tell a genuinely new point from a rehash; 20 carries gist + a novelty signal whilst staying cheap. Easy to tune later.

31. Your clarifications:
- 31.1. Does MA still read the whole `debate_`? NOT per change —— during the run MA reads only the digest channel. At adjournment MA reads the board ONCE to write the verdict, and even then SELECTIVELY (guided by the Observer SA's final list of headings worth reading), or in full only if the verdict warrants. So no compounding re-reads —— your CIIW is right, and the digest channel is precisely what avoids that stacking duplication.
- 31.2. How the Observer SA pings MA, and whether it can ping multiple times: via a small append-only file —— the digest channel `debate_[start_TS]_obs.md`. It appends as many lines as it likes (one per change, plus any `SATURATION?` flag and the final report); MA reads them on its 15s/60s wake. Multiple pings are just multiple appended lines —— no limit.
- 31.3. Another file, or backend? A file —— the digest channel. There's no reliable always-on backend SA→MA stream here (an SA's output reaches MA only when it comes to rest, which we don't want it doing). So a tiny dedicated file is the clean channel. It's the one extra file hybrid mode costs (board + digest + `response_` = 3); MA-as-observer mode stays at two. Flagging so you can veto.
- 31.4. The MA→SA direction you flagged: you're right —— MA can't push a message INTO a running SA. But we don't need to. MA's only instruction to the SAs is "stop", and that is `THE_END` appended to the BOARD, which every SA (debaters AND Observer) already watches. So: SAs→MA via the digest channel, MA→all via `THE_END` on the board. No MA→SA push required.

32. Re 27 —— noted.

33. Status: `debate.md` now carries the hybrid (Observer SA + digest channel + MA saturation-guard + `~`300 s watchdog over the Observer SA) ALONGSIDE the MA-as-observer fallback, with MA choosing the mode on the spot (by session-context size and whether you want to keep working). Two flagged choices for your veto —— the digest channel as a 3rd file (31.3), and the `≤20-word` digest (30.2). Holding on a test until you call it.
