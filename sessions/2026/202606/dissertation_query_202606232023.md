# Post-Close Issues

## Stubbing
you stubbed P06–P11 WITHOUT backing them up, leading to them forever gone
fortunately i immediately thought of that and rescued them from git, now saved in `archive/MGTK751_A1R_v1_DevPlan.md`, so future sessions can revisit if necessary
then updated the working DevPlan top section to note the history of this file

## Changes
identify my changes (if anything else); infer why; tell if agree

## re dissertation_close_202606231947.md
you literally told next session (which has zero context) to re-read "MOST"
you gotta tell which comms files to re-read
by "MOST", that means you should filter what's not needed (which you're sure they're no longer relevant/helpful), hence the remaining ("MOST") need to be fed into this `close_` for next session to re-read (refer as AD11.4.[no.]–AD11.4.[no.] to save tokens)
also this will be supplied to next session so "Next" should be "This"
and "You" is highly ambiguous; if you meant me, use "user"; if you meant CC, use "CC"
note: there should be nothing to be done on my side (CIIW), everything needed was supplied
next session should know exactly what to be done next
my suggestion:
1st turn (which i supply this `close_`) — re-read then tell if fully understood & agree
2nd turn — if no qb, start P12.7

### 3
not sure what you meant there
i thought we agreed the .svg is good and would be directly pasted into my .pages by myself
so next session simply reviews it like A1R.md and confirm i should do that
if you think this is inappropriate, that we should instead recreate it in .pages, discuss w/ me now

### 4
all closed; keep as is; no need to mention in `close_` (avoid confusion)

### 5
not sure what you meant there but we should indeed place in the word count lines in my .pages

## re dissertation_response_202606231947.md

you should have subtly remarked this is the 1st dissertation session since we migrated from CWI to CC
AD11.2 header no need `(permanent)` (incorrect format)
AD11.2.1 should directly quote the previous RA that misled Lu so we can effectively avoid it, right now it sounds like we shouldn't mention the term "mgt challenge" at all throughout, which is wrong; again, future sessions won't have your context so they will get confused, so you gotta consider that
AD11.2.3: you should have told it was relabelled as requested by Lu, not us
on Fig 1, i just noticed the header was not title case so i fixed it, but the `(held open)` was confusing to me; if it was already addressed in text, remove that (weird on header)

## Your Missions
- Execute actions (update the 2 files) if no qb
- Tell your thoughts on all above (don't miss any) normally in `response_`