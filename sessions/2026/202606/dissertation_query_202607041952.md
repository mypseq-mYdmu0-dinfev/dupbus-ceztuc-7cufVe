# Reply to response_query_202607040354.md & dissertation_response_202607040448.md

---
re response_query_202607040354.md
---

## 27
i saw you edited DevPlan but you changed P13 title, which now seems spanning beyond P14 (confusing; phases should be sequential unless edge case)
i've restored its title and previous pts; made adaptations; done some housekeeping throughout
this shouldn't impact us as we've past that phase anyway
still, identify my changes to DevPlan (don't re-read whole file); infer why; tell if agree

## 28
saw your edits on plan.md which many DON'T make sense
e.g. you ask to "Re-read this plan.md..." —— if CC is reading, this is useless; if CC is not reading, this line isn't seen at all
so instead, i largely shrank your addition then added `> Standing Rules: ...` in `PART B` myself , so future DevPlan creations will include that line and can actually be effective
PART C rules largely dated; pointed to close.md instead; also some housekeeping throughout
again, identify my changes to plan.md; infer why; tell if agree
on DevPlan again, i also added that `> Standing Rules: ...` at `PART B` top as you should have detected via git changes
tell if mine supersedes your added M5 and hence tell if we can remove your M5

# 30
Investigate why you referred the original A1 dates
I suspect you didn't actually read A1 (which is good) but got misled by some DevPlan lines that previous seasons baked in from original A1 but weren't updated to A1R v2, which may silently impact subsequent works
Identify them and update accordingly since A1R v2 is highly unlikely to change again (approved by Lu & first reviewer)

## 37
re-read root c.md §7.3.2; i edited it because you did NOT read debate.md (CIIW; DON'T read it until next #debate prompted), but don't rectify it (damage is done; im back and you didn't follow #debate protocols), just don't make same mistake

---
re dissertation_response_202607040448.md (before reading any other files)
---

## 5
all makes sense to me, below are brief notes:
5.1: as you noted, not exactly an option; so you did correctly
5.2: first of all, you did well and we should indeed not amend it; i actually think this is very weird to me, because Lu is just my DA; naming her "Data Processor" is like she'll be actively doing/processing something for us, which is likely very much false; however, since i, as a human, look at the actual .docx and find that very much unlikely a part meant to be edited, i agree to keep it untouched
5.3: i actually want it (as i truly love this topic, since i've been TP for decade, so not just "an assignment" to me) and i'd try my best (if feasible) to maximise its possibility; so you did well
5.4: excellent decision; i hate it they "coerced" me to fake a startup (as you might knew original A1 didn't have that)
5.5/6: right and safe call
5.7: you did well; i manually edited just a bit

## 6
you did well, just one note:
6.2: since we're on semi-structured and inherently we won't (and weren't meant to) stick to the script, there's room to flex; my question is: should we pull back something (e.g. intentionally delete some questions and keep it for internal use only) to further magnify the flexibility? so the submitted interview questions are just enough for passing ethics approval, but not exactly all that we'll use during interviews
e.g. we have 10 Qs, maybe keep 5 generic/safe Qs for submission, then remove the remainder and save for internal use that we re-apply to the true question set for actual interviews
tell your thoughts on this but don't edit files; if we are to do it, ensure we do that in new copies (inherit current filename), so current ones (suffix `_internal`) can be served as internal use (possibly enrich it with notes not intended for submission, if needed)

## 7
both: keep as is, see above

## 8
both: got it
 
## Remaining Files from Lu
i notice there are 5 files in `From Lu/Ethics Forms/`, 3 were already processed by you (aptly simplified filenames; great job)
but these 2 remain: `Key Ethics Information Required.docx` `Research ethics application form - example.docx`
i know Lu didn't mention them in the email but have you #r them? if not, concisely scan it
then very concisely tell what they are, and whether we're expected to read/complete them
if yes, should we do right now (before emailing Lu w/ the 6 deliverables in `dissertation/ethics/`) or later (i guess P15)?
again, as Lu didn't mention them, they're likely not very important (at least for now) so i suggest SA to save context

---
re Participant_Information_Sheet.md:
---

## Format Issues
before i read the content, first thing i notice is the format was kinda wiped
e.g. `**Title of Study: xxx**` should have been `**1. Title of Study: xxx**`, even better, make it `**01. Title of Study: xxx**` so i can more easily locate them (e.g. ⌘F `1` will show both `1`, `10`, `11`, etc.; using double-digit avoided that)
so it seems you don't "see" how it actually looks like in .docx
but you added `> ` to indent content below each numbered line, which means you can see it to an extent
however, those `> ` actually made me very difficult to copy-paste content as you broke them into lines
i suggest to still keep `> ` but make them into 1 line so i only have to remove `> ` once for each block (e.g. those under "Invitation to ...study" were broken into so many lines)
"How will my data be collected ... be shredded." are actually in a table
but it seems a good idea to do it in your current way so it's easier for me to grab the text
however it's quite difficult to read so i suggest to make the left column content as headings and the right column as text below it so i can locate easily

## PDF
to ease your viewing, i've converted all 5 files from `From Lu/Ethics Forms/` into PDF at `From Lu/Ethics Forms/202607041906/`
but for now you only need the 3 concerned files (PIS, risk, consent)

## Reformat
reformat the 3 concerned files (both filled & pristine) according to the PDFs
highly suggest using SA to do it as it seems purely mechanical
suggested approach: diff pristine vs filled .md → cross-match pristine .md with .pdf → reformat pristine .md → dup reformatted pristine .md (suffix `_filled2`) → apply diff (retrieved in 1st step) onto the `_filled2` .md → cross-match `_filled` with `_filled2` → if identical despite formatting discrepancy, void `_filled` → user review then delete the 3 voided files and rename `_filled2` to `_filled` for clean storage

## Filled Content
none of the contents of the 3 files (both pristine & filled) were #r by me yet
i'll await these technical issues fixed then review them
simultaneously, estimate how many copy-paste actions do i need to perform across the 3 concerned files
if ≥10, suggest if it's possible to dispatch SA (so you preserve context) to do it for me
suggested approach (after all above done): diff pristine vs filled .md → open each .docx in `From Lu/Ethics Forms/202607041946/` (dup of the 3 concerned files as working copies) → apply them with appropriate means (fallback: `computer-use`)
note: suggest only, don't act yet

## Side Task
owing to the "Format Issues" above, i've updated root c.md §8.8.5
read only that then briefly tell if apt now, that future CC can all perform as expected and this issue could be avoided (i.e. you would have directly converted to PDF via Quick Mode, so no reformatting needed)

---
re Interview Guides:
---

## Status
also haven't #r them yet
will do after you responded and during your reformatting

## Your Missions
- Tell your thoughts on all above first (don't miss any) so i can read whilst you act
- Execute actions if any explicitly instructed & no qb