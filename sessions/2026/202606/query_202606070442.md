# Reply to response_202606070422.md

## 25
i actually wanna build a new file in `universal/` called br.md
here's my draft BEFORE learning about the term "word wrapping" in your response:
```
When prompted `#br`, dup last turn’s output files → filename: prefix `temp_` → force break line every 41 chars. If possible, restructure content to ≤41char bullets. One-off only, inapplicable to subsequent turns.
```
i want you to review (efficient & understandable enough? any new session can get it and act accordingly?) then build it for me in `universal/`; ensure it is lean as right now or leaner
if easier to understand, you may use the term "word wrapping" if more intuitive, etc.
the usage of it is that when im reading CC sessions via `/remote-control`, the `response_` (and other files you generate) can still be opened in the app, but unlike the mac app's Files panel, it reads like VS or other IDE —— each line is extending infinitely, leading to me having to scroll left/right for each and every line
so if you're on a long task and i went away (hence can't push to GH then read OTG as i usually do), i can prompt `#br` and i expect that session's CC to do above so i can read properly
why 41 chars? your response's "25. Re 16 ... is called" is precisely the max. width i can see on my iphone 13 mini and i counted it's 41 chars —— might be diff if i ever change my phone (not very likely)

## 26
26.1: great news, that makes it super useful but i'll likely forget that it can
create a README.txt (no MD formatting; inside that folder) to tell what it does, what're supported, etc. —— keep it concise and human-reading friendly
then update it to exclude the README.txt you're about to create to ensure it won't get converted to PDF as well
26.2: i have 2 burning questions:
26.2.1: can you run that .scpt? can you run .app (Automator-created)? can you do that w/o my permission if i turn on "Bypass permissions"?
26.2.2: you seem able to open and read MS Office files almost identically as i read it, but can you do that for iWork? if not, and if 26.2.1=yes, seems this is the way you can help —— when asked to work on iWork file, you can dup a copy to that folder and run that .scpt then read the generated PDF and then delete them (clean; no traces)