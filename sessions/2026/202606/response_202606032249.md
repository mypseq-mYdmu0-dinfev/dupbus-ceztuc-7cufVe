# Response to query_202606032249.md
*Root-cause fix in sync.md + index guards; branches cleaned; CLAUDE.md untouched*

8. Re your note —— CLAUDE.md left alone
- 8.1. I added nothing to root CLAUDE.md; all of today's fixes live in `sync.md` and the index files. So the 99% local case reads no extra cloud detail beyond the single `uname -s` line you already refined.
- 8.2. On trimming §1.1 further: I left it as-is —— you already leaned it (pointing to §3.3), and a deeper trim would drop the precise behaviour we negotiated (read-as-usual, override only §3.1–§3.2, keep `✅`/`➡️`, allow `#sync` edits/deliverables). Say the word if you still want it slimmer and I'll show exactly what'd be lost.

9. Re 4.1 —— fixed at the root, plus the Line-1 guard you asked for
- 9.1. Root cause: `#sync career`/`#sync dissertation` made the cloud session treat itself as that CP's chat → §6.3 → it read `CP_index_cc.md` → which chains into the CP's gigantic unconditional files. That's the token blow-up.
- 9.2. `universal/sync.md` now opens with a **Discipline** section: `#sync` is a mechanical task, NOT a CP chat; do NOT read `CP_index_cc.md`, any CP unconditional files, or follow the Line-1 fetch directives; the script reads the control files, the model ingests no file contents.
- 9.3. Added the Line-1 guard to all three index files (`universal/index_otg.md`, `career/CP_index_otg.md`, `dissertation/CP_index_otg.md`): `` `CC running #sync:` IGNORE every fetch directive in this file… ``. It's scoped to CC-during-`#sync`, so the OTG fetcher (CWI/OTGC) ignores it and behaves normally. sync.md also notes new CP indexes should carry this line.

10. Re 5.3 —— done
- 10.1. Deleted both stray remote branches (`otg-sync` and `claude/sync-career-dissertation-xxzoF`); the remote is now main-only. Any auto-PR from that branch is closed by its branch deletion.

11. Re 7.2 —— apply-then-commit list (none committed yet; do these before the fresh cloud test)
- 11.1. `.sync/sync.py` —— cloud `git fetch --unshallow` fix (from last turn).
- 11.2. `universal/sync.md` —— Discipline section + cloud strict rule + unshallow note.
- 11.3. `universal/index_otg.md`, `career/CP_index_otg.md`, `dissertation/CP_index_otg.md` —— Line-1 guards.
- 11.4. CLAUDE.md —— NOT touched.
- 11.5. Order matters: commit + push all of the above, THEN start a FRESH cloud session (the current one is likely contaminated) and run `#sync`. NB —— the index edits make those 3 files dirty, so commit them BEFORE any `#sync` (a dirty index makes `#sync` abort by design).

12. Honest residual
- 12.1. The unshallow fix (11.1) is architectural —— it removes the non-idempotency trigger. The Discipline + Line-1 guards are prompt-level steering; a determined cloud model could still over-read, but these directly target what it did wrong. If over-reading or over-acting recurs, the next step is a thin cloud entrypoint (a wrapper that does only the sync, nothing else).
