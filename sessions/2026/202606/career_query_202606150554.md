# Reply to career_response_202606150530.md

## 2
2.6: good pt, trim it for me in `Rennly_20260612_dup/` which i'll then copy all its contents to my g drive package
the `build/` is mainly to showcase this is done by AI, that Canva wasn't used
ensure it doesn't leak privacy of ours, inc. those you flagged
note: they 99% won't understand; to be frank i dont understand the `build/` contents either; so if anything unsure, just delete them (override: delete is fine here since it's a dup)

## 3
DXMF.py should remain my own use only so the script should only receive same input as DAMF.py (a .txt in `gscpt/`), so remove the command line usage for this script (for neatness)
to let you can also use it and more conveniently, dup it to `cscpt/` (CC Scripts; for your use only) as `set_dates.py` so it's self-explanatory
edit to make it convenient for your future use (don't hunt for the .txt file but receive your terminal input instead), which i would advise to make 5 modes: 1 for each Date type, 5th mode being changing all 4 Dates
add top comments if helpful to future CC's use
then update `cscpt/README.md` accordingly so you know how to use
finally suggest where to note you can use it (don't repeat how to use it; pt to README.md)
just like current scripts inside, it should be run w/o reading its content so it won't waste tokens (only read README.md; keep it concise)