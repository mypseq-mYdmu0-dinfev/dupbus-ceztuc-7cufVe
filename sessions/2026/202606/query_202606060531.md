# Reply to response_202606060521.md

## Re 6

6.3: let's have a look on them
the temporary codes (as name suggests) are no longer needed
the workspace file is also not needed as we cleanly migrated to GH (CIIW)
what i dont know is `.vscode/` and `requirements.txt`
should we delete them or should we migrate them to `gscpt/` as well? useful? or create a new one as we no longer need so many libs?

## New Request

read `gscpt/` files again if cleared (otherwise proceed)
confirm if typographic_processor.py is the only script that generates files outside the directory it's placed (in Fury Downloads)
if no, name which else, then proceed
if yes, help me edit this script to make the output path the same as input path (`gscpt/`) w/ the output files = `[original_filename]_processed`
file format follows input (.txt/.md)
when done, help me test each script one by one (since all or most of them just read any files in the folder, so parallel seem won't work), ensure they work
if anything, don't fix yet, tell me first