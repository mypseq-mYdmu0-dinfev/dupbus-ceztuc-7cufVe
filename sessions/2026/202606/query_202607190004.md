# Reply to response_202607182321.md

## 27
27.4: #SA do it

## 28
no need, we'll edit `git_history.py` later anyway

## 29
i randomly checked couple files, history intact; well done

## 30
appreciate your adaptive approach

## 31
31.4: all alias files handled by myself
31.5: already deleted them

## 32
already deleted except `c_int/` which needs it until its first true content is born

## 33
33.1–2: do it
33.3: no it should be understood because the file itself is inside that CP so no need to include `cp/` in the path, EXCEPT for the `_otg` variant's URLs or they'll break
e.g. `cp/career/CP_index_cc.md`'s 1st entry `career/CP_notes.md` is already correct now, NO need to make it `cp/career/CP_notes.md`
if you insist, discuss first
33.4/5: do it

## 34
34.1: just a remark: you might have to define what's "too big", which i propose to be more reserved because putting it in `sandbox/` doesnt create any drawback at all and it's safest; another remark: before deleting stuffs in `sandbox/`, commit+push first then delete and commit+push again, so in the very rare case we needa rescue it, its commit is still rescuable
proceed to operationalise it by creating the full `ccsim/CLAUDE.md` (keep terse); FYI: i already created `sandbox/`

## 35
minor: ensure it's TS first then SHA or it's weird; anyway only you'll read it so up to you

## 35–37
proceed to create/operationalise them

## 38
38.1: do it this turn
38.2: i believe all cleared now; raise qb if any

## CP Question
when above is all done, answer me:
if i wanna add a new CP, do i simply create a new folder under `cp/` and start populating it w/ files, or i'd needa update some pcmds/scripts so everything work effectively?

## Your Missions
- Execute actions if any instructed & no qb
- Wait for SA report(s) return, if dispatched
- Finally, tell your thoughts (don't miss any)
- Expected total quantity of `response_`: 1