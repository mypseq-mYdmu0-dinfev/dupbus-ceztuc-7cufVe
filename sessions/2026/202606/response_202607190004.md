# Response to query_202607190004.md
*Ref-updates + #sync generalised + CCSIM built + pairing lint*

---

## Ref-Updates (33)

40. Shipped in reviewable, separate commits.
- 40.1. Root `c.md` §6.1 redefined: any folder under `cp/` is a CP EXCEPT `cp/archive/` (its children are retired CPs); no marker file needed. §6.3.3/§6.4 conditionalised to "if present".
- 40.2. `sync.py` + `sync.md` generalised: no-arg `#sync` auto-discovers `universal` + every `cp/` CP that has an index (excludes `cp/archive/`); the per-scope arg still works. No CP names hard-coded anywhere.
- 40.3. Tested before any push: compiles, and discovery returns exactly `universal, cp/career, cp/dissertation`.

41. Re 33.3 —— followed you: internal `_cc` index refs LEFT as `career/…` (the enclosing-folder clickability form); only the `_otg` URLs were repathed to `cp/…`.

42. HONEST FLAG —— I ran `#sync` one step too early, caught it via its OWN output, fixed it same-turn.
- 42.1. My `git mv` moved dissertation's files but not the `dissertation/…` URL PATHS inside `cp/dissertation/CP_index_otg.md` (career's were already fixed by the AJAP session, so I overlooked dissertation's).
- 42.2. The first `#sync` pinned those stale paths (would 404) and WARNED that both `CP_instr.md` prefs didn't reference the new index paths.
- 42.3. Fix: repathed 20 dissertation URLs + both `CP_instr.md` index refs → `cp/…`, committed, re-ran `#sync`. Now clean (universal NO CHANGE; career + dissertation re-pinned; prefs pins resolved).
- 42.4. Nothing bad reached you OTG —— the wrong URLs lived only in the interim commit; the current ones below are correct.

## #sync userPref URLs (33.4/5)

43. Three scopes synced —— paste each into its userPref (verified as the current permalinks).

```
https://raw.githubusercontent.com/mypseq-mYdmu0-dinfev/dupbus-ceztuc-7cufVe/8de06a7bac630251c2891d68a0ea4c1360f0834a/universal/index_otg.md
```

```
https://raw.githubusercontent.com/mypseq-mYdmu0-dinfev/dupbus-ceztuc-7cufVe/8a662516b1ed9102f10e0a2d7e30e125c4f0c441/cp/career/CP_index_otg.md
```

```
https://raw.githubusercontent.com/mypseq-mYdmu0-dinfev/dupbus-ceztuc-7cufVe/307c22a0bdf8093ed65be1247445998edb9ab0bd/cp/dissertation/CP_index_otg.md
```

## CCSIM Built (34, 35, 35–37, 38)

44. `cp/ccsim/` operationalised —— 4 terse files.
- 44.1. `CLAUDE.md` —— the protocol: §1 every-turn change-hunt, §2 recent-5 index, §3 backlog, §4 blind A/B QA, §5 sandbox, §6 #wrap.
- 44.2. `last_seen.md` —— hunt anchor `[TS] [SHA]` (TS first, per 35).
- 44.3. `backlog.md` —— append-only, format documented; empty for now.
- 44.4. `CP_notes.md` —— purpose + standing decisions.

45. Re 34.1 —— your sandbox remarks baked in.
- 45.1. "Too big" left deliberately loose: §5.2 says default anything uncertain into `sandbox/` (no drawback, safest) —— your reserved stance.
- 45.2. Delete discipline (§5.3): commit + push → delete → commit + push, so a rescue stays git-recoverable.
- 45.3. Note: `#wrap`'s 50% gate (§6.2) is stored as-is; it's the first real-wrap calibration target.

## Pairing Lint (27.4)

46. Built by an SA, tested in an isolated repo, applied —— the §9.05.2 slip can't recur.
- 46.1. `.githooks/pre-commit` now, on a normal commit, blocks any staged `response_[TS].md` whose sibling `query_[TS].md` is neither staged nor already tracked.
- 46.2. Prefix-aware (`career_response_` ↔ `career_query_`); the `#sync` marker path is byte-identical to before (pairing check never fires during #sync); all 6 test cases passed.

## CP Question

47. To add a new CP —— just create the folder under `cp/` and populate it. Nothing else is mandatory.
- 47.1. It IS a CP the moment it exists under `cp/` (§6.1) —— no `c.md` edit.
- 47.2. `#sync` auto-includes it the moment it has a `CP_index_otg.md` —— no `sync.py`/`sync.md` edit.
- 47.3. Add optional files only for the features you want:
  - 47.3.1. `CP_index_cc.md` —— CC's local file index (for a non-trivial CP).
  - 47.3.2. `CP_index_otg.md` + `CP_instr.md` —— OTG fetch; run `#sync` once after, then it self-maintains.
  - 47.3.3. `CLAUDE.md` —— auto-reads root + index whenever you ADD the folder to a session.
- 47.4. A simple CP (like `ccsim/`) needs none of those.
- 47.5. Only special case: a RETIRED CP goes in `cp/archive/` (excluded from CP-detection AND #sync).

## Status

48. Everything on the plate is done; nothing blocking outstanding.
- 48.1. 27.4, 33, 34, 35, 38 —— all executed above; 28/29/30/31/32 needed nothing from me (you handled the aliases + placeholders).
- 48.2. `cp/ccsim/` is live —— from here, CCSIM sessions can carry the `ccsim_` prefix and run the every-turn hunt.
