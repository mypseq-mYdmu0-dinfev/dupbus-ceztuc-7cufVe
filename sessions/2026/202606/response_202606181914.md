# Response to query_202606181914.md
*README changes reviewed; a file-name discrepancy to settle*

27. Re ## Review —— your edits to `seek/README.md`, inferred reasons, and my verdict (all AGREE):
- 27.1. `Claude-in-Chrome` → `CIC`; `cover letters` → `cover letters (CLs)`. Why: use the glossary abbreviations and introduce `CL` once. Agree —— terser, consistent with house terms.
- 27.2. `#seek` → `#seek`/`#psl`/`#ccl`. Why: the consulting cover-letter triggers also enter the `seek/` workflow, so the entry condition was incomplete. Agree —— correct and important.
- 27.3. Guard paragraph: enumerated `context/` files collapsed to "`seek/CLAUDE.md` or `context/` files", PLUS the escape hatch "UNLESS necessary (request approval first) or explicitly instructed (e.g. by `career/CP_index_cc.md`)"; "mandatory reads" → "unnecessary reads". Why: a blanket ban was too absolute —— the career CP legitimately needs `context/pro_profile.md` (it is in root's conditional read table). Agree strongly —— this was the one real gap in my draft; the exception prevents the guard from blocking a valid cross-CP read.
- 27.4. Contents list trimmed: `ccl/` → "consulting ARs"; `interviews/` → "(read its README.md if accessing)"; and `context/`, `runtime/`, `.claude/tmp/`, `CLAUDE.md`, `queue.md` all collapsed into one "Other —— AJAP-only files" bullet; single `—` restored to `——`. Why: a non-AJAP reader needs only the folders they might legitimately touch; the rest is noise, and the guard text already says "don't go there". Agree —— this is the right altitude for a file now read before every `CLAUDE.md` (your new §8.5).

28. ⚠️ One discrepancy I must flag (CIIW) —— you wrote that you "trimmed `seek/CLAUDE.md` bullets", but on disk `seek/CLAUDE.md` is UNCHANGED: all permission bullets are still there, including the one I added at line 58: `` `/interviews/` —— in AJAP mode, NEVER access (out of scope; user-managed) ``. The only file actually modified is `seek/README.md`. So either:
- 28.1. You meant the `seek/README.md` Contents trim (28 above) and the rationale "the last bullet + text above already suffice" refers to its `Other —— AJAP-only files` bullet + the guard paragraph —— in which case: yes, fully agree, nothing more to do; or
- 28.2. You intend to remove the `/interviews/` bullet from `seek/CLAUDE.md` (the edit did not save / was on a different copy). I AGREE it is now redundant: "All other paths —— read only" + the new `seek/README.md` cover it. One tiny caveat: "read only" still PERMITS a read, whereas the dedicated bullet forbade access outright —— but since no AJAP scan path ever reaches `interviews/` (all scans are `/gcl/`-scoped), this is moot in practice.

29. Q1 —— is it 28.1 (README only, done) or 28.2 (you also want me to delete the `/interviews/` bullet from `seek/CLAUDE.md`)? I did not touch `seek/CLAUDE.md` since it governs the live automation; say the word and I will remove that bullet.
