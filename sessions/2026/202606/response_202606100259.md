# Response to "#sync / #sync career / then fix the concerned placeholder URL"
*Placeholder fixed; #sync needs you to commit+push the index first*

1. Done —— **fixed the placeholder** in `career/CP_index_otg.md`: replaced it with the real SHA-permalink for `job.md`, pinned to commit `524c4fce` (confirmed present on `origin/main`). You'll see this as the modified `CP_index_otg.md` in GH Desktop.

2. `#sync career` **aborted —— by design, not an error.** The script refuses to run whilst any control/listed file has uncommitted changes (so your edits stay in their own commits, separate from sync's pin commits). It also **cannot** fill a text placeholder itself —— it only re-pins SHAs inside URLs that are already valid, which is why the fix in pt 1 had to come first.

3. Your working tree currently has these uncommitted (HEAD already = origin/main = `8aee4c15`):
- 3.1. `career/CP_index_otg.md` (M) —— my placeholder fix; **must be committed+pushed for the sync to run.**
- 3.2. `career/CP_index_cc.md` (M) —— your 12.1 fix; not yet pushed (push it if you want it live; not required for this sync).
- 3.3. `career/CCL_Archive/CL_BCG_202603031831.txt` + `CL_Bain_202602282235.txt` (D) —— 2 deletions.
- 3.4. `career/CCL_Samples/` (untracked) —— new folder.
- 3.5. I have **not** touched 3.3/3.4 —— flagging in case unintended (Move/Void rules untriggered); yours to keep or discard.

4. To proceed (GH Desktop): commit + push **`career/CP_index_otg.md`** (minimum). The other listed files (`job.md`, `CP_notes.md`, `pro_profile.md`, `gcl`/`ccl`, resumes…) are already clean + pushed, so only the index blocks the run.

5. Then tell me and I'll run `#sync career` —— it will re-pin `CP_notes.md` (changed) + confirm `job.md`, pin the index permalink in `CP_instr.md`, push only those 2 control files, and report the `=== index URL for userPref ===` for you to paste.
