# Reply to response_202606181815.md

## Housekeeping

edit '/Volumes/FURY 2TB/Fury Documents/PDF Conversion/README.txt' to remark what needs to be installed if this whole folder inc. the script is copied to and used on another mac
keep it super concise, just enough for future CC to do that for me

## Issue 2 (not yet closed)

### Aspect Ratio

the scrip ran successfully but `Company - Background.pdf` became landscape which its original .docx was portrait like the others
i've re-added the 3 .docx to the folder again for you to analyse & compare
identify other discrepancies between the original .docx vs converted .pdf
for the aspect ratio problem, i can understand this might be an expected error, but is there some way to at least get the aspect ratio right? maybe add a new function to probe the doc's aspect ratio and even the doc file format (e.g. A4)?
also concisely tell me why it happened in the first place

### Font & Layout

one of the other problems i notice is the fonts were changed and so the layout also changed
i think you seemed to have mentioned that earlier
how about this: for MS files only, if a file has an identical-filename pdf in one of the subfolders under this folder (`PDF Conversion/`), use the old method (the one i flagged at beginning as Issue 2, that requires me to click; let's call it "Full Mode") to process it again, which should be in a new subfolder of the TS at that moment (unless it's in exact same minute which could be rare; in that case, stop & alert to avoid overwrite)
for example, if i put X.docx into the folder (`PDF Conversion/`) and run the .app
it will convert to pdf w/o opening MS Word, just like right now (let's call it "Quick Mode")
when done, X.pdf will be in a `[TS]/` subfolder and simultaneously the original X.docx will be deleted
i check and found X.pdf is unsatisfactory to me, so i put X.docx to the folder again
then i run the .app again
this time, it should be able to recognise that X.pdf already exists in `[TS]/` 
it MUST IMMEDIATELY pop up to alert me "⚠️ Full Mode: Manual intervention needed." (reword if necessary)
so i know i can't just drift away and expect it's done when im back
THEN it runs the thorough mode (our "old method"; aka "Full Mode") via opening MS Word (or Excel if .xlsx, PowerPoint if .pptx, etc.)
when done, it deletes the original files and presents the pop up confirmation, just like its completion right now
if you agree, proceed to execute if no qb

## Other Issues

we shall revisit when Issue 2 fully closed

## Your Missions
- Execute actions if any explicitly instructed & no qb
- Tell your thoughts on all above (don't miss any)