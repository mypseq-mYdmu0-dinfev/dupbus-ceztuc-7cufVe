# close.md & dissertation DevPlan fix

## Problem
read close.md but DON'T #close this session yet
the original design of `## 4. Session Files` & `- AD[XX].4. Session Files` was to ensure future sessions can read particular session file when needed
but looks like we don't have to enumerate full path, not even full filename, or it can get quite bulky

## Good Example
read `dissertation/MGTK751_DevPlan.md` ONLY the line `- AD11.4. Session Files` ONWARDS
DON'T READ any other files (e.g. dissertation CP files)
AD11.4 actually drifted and did NOT fully comply to close.md
AD12.4 almost fully complied (full path of query only)
despite AD11.4 technically violated the rule, but turns out it's more efficient, as you can see the token savings of AD11.4 vs AD12.4, let alone if BOTH query & response file paths were fully enumerated

## Suggested Fix
my suggestion (feel free to stop & argue):
edit close.md `## 4. Session Files` & `- AD[XX].4. Session Files` to operationalise future #close to enumerate like AD11.4
also, use `:` instead of ` —— ` so it saves horizontal space (benefits my occasional reading)
the `([file_count])` would also need to be updated accordingly as now it's comms pairs
also check if anything needa be updated alongside this change

## Later
DON'T do it now, after you fixed close.md and i confirmed it's good
use it to reformat ONLY AD12.4 as that DevPlan.md is an Unconditional (#r every session start of that CP)

## Your Missions
- Execute actions if any explicitly instructed & no qb
- Tell your thoughts on all above (don't miss any)