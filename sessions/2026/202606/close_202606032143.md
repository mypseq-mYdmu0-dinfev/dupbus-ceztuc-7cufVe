# Session Closing (202606030103–202606032233)
*Maintenance (02): built the #sync OTG mechanism*

## 1. Coverage
- 1.1. Diagnosed the CWI/OTGC stale-fetch ("v01") bug —— two URL-keyed cache layers (Claude web-fetch ~15 min + GitHub raw CDN).
- 1.2. Designed & built the `#sync` mechanism: pins every OTG file URL to its last-commit SHA permalink (immutable → never stale), and pins each index's own permalink inside its prefs file.
- 1.3. Built the toolset, wired the harness (permission rule + Date-Added hooks + git guard), and stress-tested across `universal`, `career`, `dissertation`.
- 1.4. Fixed a Date-Added side-effect on `google.md` and built a permanent Date-Added preservation hook.
- 1.5. Extended `#sync` for cloud CC sessions (offline / local-CC-fail fallback): OS-based mode switch + `otg-sync` branch push; verified a feature-branch-only SHA resolves on raw GitHub.

## 2. Decisions
- 2.1. SHA permalink (per-file last-commit SHA) chosen over query-param cache-bust (CWI ignores query strings) and over the old versioned-duplicate-file scheme (repo clutter).
- 2.2. Workflow: the USER commits content + control-file edits in their own commits; `#sync` commits ONLY the 2 control files; it ABORTS untouched if the index, prefs, or any listed file has uncommitted changes (so user edits stay in separate commits).
- 2.3. Push restricted to those 2 files by 3 model-independent layers: permission rule allowing only `python3 .sync/sync.py`, explicit staging (never `git add -A`), and a marker-gated `.githooks/pre-commit` guard.
- 2.4. Scopes not hard-coded: no-arg → `universal/index_otg.md` + `universal/preferences.md`; `#sync <cp>` → `<cp>/CP_index_otg.md` + `<cp>/CP_instr.md`; file list read from the index, so cross-folder entries (e.g. `seek/context/*`) are handled and `seek/gcl/*` is never touched.
- 2.5. "Date Added" (Finder column) preserved on edits to `sessions/` + `seek/investigation/` via Pre/PostToolUse capture→restore hooks (`.sync/date_added.py`).
- 2.6. `#sync` is safe on Sonnet ("Auto" mode, not Bypass) and even Haiku for the action itself —— safety is architectural; comms-protocol drift is the only downgrade risk.
- 2.7. §8.6 forbids the CC memory store; a one-off `override` was used to add the NO-CHAT-TEXT feedback memory.
- 2.8. Cloud sessions detected via `uname -s` = `Linux`; they can't push main, so `#sync` force-pushes index/prefs to one fixed `otg-sync` branch (SHA permalinks resolve from any branch → no merge needed; next LOCAL `#sync` supersedes on main). Cloud mode overrides ONLY §3.1–§3.2 (reply in ultra-concise chat) but still emits `✅`/`➡️` and writes no comms files.

## 3. Issues
- 3.1. ✅ RESOLVED —— `.sync/sync.py` committed.
- 3.2. ✅ RESOLVED —— updated userPref URLs pasted (universal + dissertation; career unchanged).
- 3.3. ✅ RESOLVED —— harness switched "Bypass" → "Auto" (done in a dedicated session).
- 3.4. ✅ RESOLVED —— May (`202605`) `#wrap` done.
- 3.5. ✅ RESOLVED —— voided `❌_…_moved_202606.md` files deleted.
- 3.6. 🟡 OPEN —— cloud `#sync` (Linux detection + `otg-sync` push + CLAUDE.md §1.1 cloud mode) is built & committed but UNVALIDATED on a real cloud run; one unknown is whether the cloud harness lets the script push to `otg-sync` vs forcing its own PR. Validate, then mark done.

## 4. Session Files (30)
- 4.1. `202606/query_+response_202606030103.md` —— OTG stale-fetch diagnosis & options.
- 4.2. `202606/query_+response_202606030141.md` —— trial URL; push-blocked correction.
- 4.3. `202606/query_+response_202606030221.md` —— per-file SHA; mtime concern.
- 4.4. `202606/query_+response_202606030243.md` —— Date Added; push-restriction design.
- 4.5. `202606/query_+response_202606030315.md` —— toolset built for review.
- 4.6. `202606/query_+response_202606031831.md` —— memory; Date-Added hook; CP scopes.
- 4.7. `202606/query_+response_202606032001.md` —— re-audit; settings + hooks wired.
- 4.8. `202606/response_202606032040.md` —— first `#sync` run audit (direct command, no query).
- 4.9. `202606/query_+response_202606032043.md` —— stress test (career+dissertation); 2 bugs fixed.
- 4.10. `202606/response_202606032145.md` —— combined `#sync` all-three (direct command, no query).
- 4.11. `202606/query_+response_202606032100.md` —— guard aborts; Last Question held.
- 4.12. `202606/query_+response_202606032124.md` —— new guard; all 3 scopes clean.
- 4.13. `202606/query_+response_202606032146.md` —— cloud `#sync` viability + design.
- 4.14. `202606/query_+response_202606032155.md` —— cloud applied; merge Q; close updated.
- 4.15. `202606/query_+response_202606032211.md` —— cloud clause made precise; pycache.
- 4.16. `202606/query_+response_202606032229.md` —— pointed comms-def to §3.3; this close update.
- 4.17. `202606/close_202606032143.md` —— this closing (filename TS to be bumped by user).

## 5. Other Files
- 5.1. Created:
  - 5.1.1. `.sync/sync.py` —— the `#sync` engine (incl. cloud `otg-sync` branch push).
  - 5.1.2. `.sync/date_added.py` —— Date-Added get/set + capture/restore hooks.
  - 5.1.3. `.githooks/pre-commit` —— marker-gated allowlist guard (activate via `git config core.hooksPath .githooks`).
  - 5.1.4. `universal/sync.md` —— `#sync` ops doc (incl. Cloud section); read on the `#sync` trigger.
  - 5.1.5. `memory/feedback_no_chat_text.md` —— NO-CHAT-TEXT discipline (added under override).
- 5.2. Modified:
  - 5.2.1. `CLAUDE.md` —— added §1.1 (Linux⇒cloud chat-mode initiation).
  - 5.2.2. `.claude/settings.json` —— `Bash(python3 .sync/sync.py:*)` allow rule + Pre/PostToolUse Date-Added hooks.
  - 5.2.3. `universal/google.md` —— trial sentinel added then reverted; mtime + Date Added restored (net content unchanged).
  - 5.2.4. `memory/MEMORY.md` —— indexed the new feedback memory.
  - 5.2.5. `.gitignore` —— added `__pycache__/`, `*.pyc`.
  - 5.2.6. `universal/index_otg.md`, `universal/preferences.md` —— SHA-permalink pins (via `#sync`).
  - 5.2.7. `career/CP_index_otg.md`, `career/CP_instr.md` —— SHA-permalink pins (via `#sync`).
  - 5.2.8. `dissertation/CP_index_otg.md`, `dissertation/CP_instr.md` —— SHA-permalink pins (via `#sync`).
  - 5.2.9. git config `core.hooksPath` set to `…/.githooks` (local config, not a file).
- 5.3. Moved/Voided/Deleted:
  - 5.3.1. Moved `query_202606030103.md`, `response_202606030103.md`, `query_202606030141.md` from `202605/` → `202606/`; originals voided then deleted by user.

## 6. Remarks
- 6.1. This session is named **"Maintenance (02)"**.
- 6.2. The next session, **"Maintenance (03)"**, must read THIS close file AND `close_202605300023.md` ("Maintenance (01)") at start. (This file's name keeps its `202606032143` TS for now; user will bump it to the final close TS.)
- 6.3. The `#sync` mechanism is complete; operating doc at `universal/sync.md`, triggered by `#sync [scope]`. Only open thread: cloud-run validation (3.6).
