# Session Closing (202606030103–202606040216)
*Maintenance (02): built & validated the #sync OTG mechanism*

## 1. Coverage
- 1.1. Diagnosed the CWI/OTGC stale-fetch ("v01") bug —— two URL-keyed cache layers (Claude web-fetch ~15 min + GitHub raw CDN).
- 1.2. Designed & built `#sync`: pins every OTG file URL to its last-commit SHA permalink (immutable → never stale), and pins each index's own permalink inside its prefs file.
- 1.3. Built the toolset, wired the harness (permission rule + Date-Added hooks + git guard), and stress-tested across `universal`, `career`, `dissertation`.
- 1.4. Fixed a Date-Added side-effect on `google.md` and built a permanent Date-Added preservation hook.
- 1.5. Extended `#sync` for cloud CC sessions (offline / local-CC-fail fallback): OS-based mode switch + `otg-sync` branch push.
- 1.6. VALIDATED cloud `#sync` end-to-end —— idempotent (no-change → nothing pushed), single-pass, no CP/gigantic reads; refined its reporting (snippets; silent on no-change).

## 2. Decisions
- 2.1. SHA permalink (per-file last-commit SHA) chosen over query-param cache-bust (CWI ignores query strings) and over the old versioned-duplicate-file scheme (clutter).
- 2.2. Workflow: USER commits content + control-file edits in their own commits; `#sync` commits ONLY the 2 control files; it ABORTS untouched if the index, prefs, or any listed file is uncommitted.
- 2.3. Push restricted to those 2 files by 3 model-independent layers: permission rule (only `python3 .sync/sync.py`), explicit staging (never `git add -A`), marker-gated `.githooks/pre-commit` guard.
- 2.4. Scopes not hard-coded: no-arg → `universal/index_otg.md`+`universal/preferences.md`; `#sync <cp>` → `<cp>/CP_index_otg.md`+`<cp>/CP_instr.md`; file list read from the index (cross-folder entries like `seek/context/*` handled; `seek/gcl/*` never touched).
- 2.5. "Date Added" preserved on edits to `sessions/`+`seek/investigation/` via Pre/PostToolUse capture→restore hooks (`.sync/date_added.py`).
- 2.6. `#sync` safe on Sonnet ("Auto" mode, not Bypass) & even Haiku —— safety is architectural; comms drift is the only downgrade risk.
- 2.7. §8.6 forbids the CC memory store; a one-off `override` added the NO-CHAT-TEXT feedback memory.
- 2.8. Cloud detected via `uname -s`=`Linux` (CLAUDE.md §1.1): can't push main → `#sync` force-pushes to fixed branch `otg-sync` (SHA permalinks resolve from any branch → no merge; next LOCAL `#sync` supersedes on main). Cloud mode overrides ONLY §3.1–§3.2 (ultra-concise chat) but keeps `✅`/`➡️` and writes no comms files.
- 2.9. `#sync` Discipline (in `sync.md` ONLY —— index files deliberately left untouched after a paradoxical Line-1-guard experiment was reverted): it's a mechanical task, NOT a CP chat; never read `CP_index_cc.md`/CP unconditionals or act on any index Line-1 "Unconditionally fetch…" directive.
- 2.10. Cloud robustness: script `git fetch --unshallow`es first (cloud only) for true per-file SHAs + idempotency; reports a URL ONLY when a scope changed, each in its own snippet; on no-change it reports nothing and makes no edits.

## 3. Issues
- 3.1. ✅ RESOLVED —— `.sync/sync.py` committed (note: re-commit the latest version, see §5/§6.4).
- 3.2. ✅ RESOLVED —— userPref URLs pasted (universal + dissertation; career unchanged).
- 3.3. ✅ RESOLVED —— harness "Bypass" → "Auto".
- 3.4. ✅ RESOLVED —— May (`202605`) `#wrap` done.
- 3.5. ✅ RESOLVED —— voided `❌_…` files deleted.
- 3.6. ✅ RESOLVED —— cloud `#sync` validated on a fresh cloud session (idempotent, single-pass, no gigantic reads, correct no-change behaviour).

## 4. Session Files (41)
- 4.1. Pairs `query_+response_` for: `…030103` (diagnosis), `…030141` (trial URL; push block), `…030221` (per-file SHA), `…030243` (Date Added; push design), `…030315` (toolset), `…031831` (memory; CP scopes), `…032001` (settings wired), `…032043` (stress test; 2 bugs), `…032100` (guard aborts), `…032124` (3 scopes clean), `…032146` (cloud viability), `…032155` (cloud applied), `…032211` (cloud clause precise), `…032229` (close update), `…032241` (cloud-run analysis), `…032249` (Discipline + index guards), `…032305` (guards reverted), `…040208` (reporting tweaks), `…040215` (cloud validated; this close).
- 4.2. Unpaired responses (direct `#sync` commands, no query): `response_202606032040.md`, `response_202606032145.md`.
- 4.3. `202606/close_202606032143.md` —— this closing (filename TS kept; user will bump to final).

## 5. Other Files
- 5.1. Created:
  - 5.1.1. `.sync/sync.py` —— the `#sync` engine (per-file SHA, guard, cloud `otg-sync` + unshallow, no-change reporting).
  - 5.1.2. `.sync/date_added.py` —— Date-Added get/set + capture/restore hooks.
  - 5.1.3. `.githooks/pre-commit` —— marker-gated allowlist guard (activate via `git config core.hooksPath .githooks`).
  - 5.1.4. `universal/sync.md` —— `#sync` ops doc (Discipline, Run, Reporting, Guarantees, Scopes, Cloud).
  - 5.1.5. `memory/feedback_no_chat_text.md` —— NO-CHAT-TEXT discipline (under override).
- 5.2. Modified:
  - 5.2.1. `CLAUDE.md` —— §1.1 Linux⇒cloud chat-mode initiation.
  - 5.2.2. `.claude/settings.json` —— `Bash(python3 .sync/sync.py:*)` allow rule + Pre/PostToolUse Date-Added hooks.
  - 5.2.3. `universal/google.md` —— trial sentinel added then reverted; mtime + Date Added restored (net unchanged).
  - 5.2.4. `memory/MEMORY.md` —— indexed the feedback memory.
  - 5.2.5. `.gitignore` —— added `__pycache__/`, `*.pyc`.
  - 5.2.6. `universal/index_otg.md`+`universal/preferences.md`, `career/CP_index_otg.md`+`career/CP_instr.md`, `dissertation/CP_index_otg.md`+`dissertation/CP_instr.md` —— SHA-permalink pins (via `#sync`). (Indices also got a Line-1 guard that was REVERTED —— net no change there.)
  - 5.2.7. git config `core.hooksPath` = `…/.githooks` (local config).
- 5.3. Moved/Voided/Deleted:
  - 5.3.1. Moved `query_202606030103.md`, `response_202606030103.md`, `query_202606030141.md` from `202605/`→`202606/`; originals voided then deleted by user.
  - 5.3.2. Deleted stray remote branches `otg-sync` + `claude/sync-career-dissertation-xxzoF` (cloud-test cleanup); remote is main-only.

## 6. Remarks
- 6.1. This session is named **"Maintenance (02)"**.
- 6.2. Next session **"Maintenance (03)"** must read THIS close file AND `close_202605300023.md` ("Maintenance (01)") at start. (Filename keeps `202606032143` for diff-tracing; user will bump it to the final close TS.)
- 6.3. `#sync` is complete & validated (local + cloud). Ops doc: `universal/sync.md`, triggered by `#sync [scope]`.
- 6.4. Pending only: commit + push the latest `.sync/sync.py` + `universal/sync.md` (the no-change/snippet reporting tweaks) so a future cloud session inherits them. No other open threads.
