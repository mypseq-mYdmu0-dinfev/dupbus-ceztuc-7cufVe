# Response to query_202606262227.md
*computer-use on Chrome —— tested, doesn't work (by design); numbering continues from pt 28*

## computer-use on Chrome (re 27)
29. Result —— it does NOT work, and now I can show exactly why.
- 29.1. I requested computer-use access to Chrome; the harness granted it at tier "READ" only —— verbatim: "visible in screenshots only; no clicks or typing... cannot navigate, click, or type into it. For browser interaction, use the Claude-in-Chrome MCP." So I can't open a tab or load google.com via computer-use —— the access layer blocks the click/keystroke itself (and explicitly says not to work around it).
- 29.2. I therefore couldn't even reach your underline test —— computer-use can't issue ANY click/keypress to Chrome to begin with.
- 29.3. Not illogical —— it's by DESIGN. The harness tiers apps: browsers (Chrome/Safari) = "read" (deliberately forced through CIC), IDEs (VS Code) = "click", most other apps = "full". That's precisely why VS Code worked but Chrome won't —— confirmed by the two access results (VS Code → "click", Chrome → "read").
30. So the "nuclear approach" is NOT viable for websites —— I did NOT add it to cic_bot.md (per your "if you succeeded"); step 4 there already records this. CIC stays the only browser controller. Note: human-like CIC clicks already avoided CAPTCHA on Temu/Taobao —— so CAPTCHA isn't "because CIC", it's the site's bot-detection reacting to SCRIPTED (not human-like) access; computer-use wouldn't change that even if it could reach the browser.
31. Taobao daily-logout —— noted. Per your cic_bot.md, when it's logged out I'll flag "needs login" and surface the stored credential in chat for you to paste; you sign in —— I never type it into the field myself.

*🟡 (dlint L6 justified): the full stop sits inside the closing quote because it's part of the verbatim harness quote —— Hart's logical-quotation rule satisfied.*
