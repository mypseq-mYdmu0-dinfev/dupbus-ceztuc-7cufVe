# Response to query_202606032305.md
*Index guards reverted; rule lives in sync.md alone*

13. Re 9 —— you're right, reverted
- 13.1. The Line-1 guard was self-defeating (a Line-1 instruction telling readers to ignore Line-1) and it disturbed files the OTG fetcher + many sessions read constantly. I reverted all three index files (`universal/index_otg.md`, `career/CP_index_otg.md`, `dissertation/CP_index_otg.md`) —— `git status` confirms they're back to their committed state, so nothing to commit there.

14. Why sync.md alone suffices (the persuasion you asked for)
- 14.1. In a correct `#sync`, the MODEL never reads the index files at all —— the Python SCRIPT reads them, and a script doesn't "follow" prose directives, it just parses URLs. So Line 1's "Unconditionally fetch…" is never acted on in the normal path, with or without a guard.
- 14.2. The only failure mode is the model going OFF-script (exactly what the cloud session did when it wandered into `CP_index_cc.md`). That's a model-behaviour problem, so the fix belongs where the model looks on a `#sync`: `sync.md` (read via the §7.3 trigger), governing cloud AND local.
- 14.3. I tightened `sync.md`'s **Discipline** section to state it plainly: the script reads only the scope's 2 control files; the model opens NO files itself (so 3 folders = 6 reads, full stop); never act on any Line-1/fetch directive (it's for the OTG fetcher, not `#sync`); never enter CP-mode. Removed the guard-line bullet.
- 14.4. Net: single source of truth, zero disturbance to the constantly-read indices, no paradox.

15. Re 11 —— revised commit list (shorter now)
- 15.1. Commit + push just these two: `.sync/sync.py` (cloud unshallow) and `universal/sync.md` (Discipline + cloud rules). The 3 index files are reverted/clean; CLAUDE.md is untouched.
- 15.2. Then start a FRESH cloud session and `#sync` —— and watch that it (a) does a single pass, (b) doesn't read `CP_index_cc.md`/CP unconditionals, (c) reports one URL set.
