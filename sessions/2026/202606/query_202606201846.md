i heard you can now create artefact, which is basically a file synced to anthropic server so others can access its live state via a web page under claude.ai domain, where our edits can be seen live
if true, create a concise random file then make it artefact
print URL of it if not already shown in it by system default
if all succeeded, concisely tell how i should ask future CC to make a file artefact (e.g. do i just say "make it artefact"?)
