# Query (QMM)
*Created by CC on the user's behalf (QMM); verbatim below.*

br.md is currently token-expensive as it requires a session to re-parse the file, then determine whether to simply wrap text or rearrange (FYI: all sessions so far simply wrap text), then re-read same content
make the warp text deterministic and token-free by creating `cscpt/br.py` (doing what you did above when i prompted #br)
then instead of re-reading the temp file, CC should use bash to display so i can read
find below 2 images in `Fury Downloads/` (per c.md)
`IMG_2267.PNG` shows how it current looks by the re-reading above, which is clean on the top (no code or anything) but the first column (1–31) are contamination, also i suspect this will consume double tokens in a normal session (producing the file, then re-reading it due to #br)
`IMG_2263.PNG` shows a bash where a session was QMMing my msg then writing a `response_` which i dont know why it appears this way (typically just shows "created xxx...") but it accidentally appears apt for #br; i dont know what `cat` means but it shows the content text-wrapped despite some code on top; if you agree, make this the new approach (w/o the QMM part) to replace br.md's re-read of the targeted files (per br.md, should be files in immediate last turn, or anything i instr), as it seems to consume less tokens than re-reading (tell if true)
QMM
