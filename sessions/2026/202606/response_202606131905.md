# Response to query_202606131905.md
*SortSS.app verified identical; #replace dup-group edit*

1. Verification —— `automator/SortSS.app`
- 1.1. The script lives in `SortSS.app/Contents/document.wflow` as the `COMMAND_STRING` of the Run Shell Script action.
- 1.2. Stored value (newlines shown as real `\n`): `src="/Volumes/FURY 2TB/Fury Pictures/Screenshots/Sort"\ndst="$src/Delete"\n\nmkdir -p -- "$dst"\n\nfor f in "$src"/*; do\n  [[ -f "$f" ]] || continue\n  name=${f:t}\n  [[ "$name" == *" (4).png" ]] || mv -- "$f" "$dst/"\ndone`
- 1.3. Verdict —— COMPLETELY IDENTICAL to your quoted script, line-for-line. It is stored WITH real line breaks (the `\n` are genuine newlines); the GH app view merely renders them inline as a single line, hence what you saw. No trailing newline after `done`, matching your block.

2. #replace —— new dup-group-only logic
- 2.1. Logic: only act when a COMPLETE set of 4 exists for a base (`BASE.png`, `BASE (2).png`, `BASE (3).png`, `BASE (4).png`; ` (1)` never exists). Then move `BASE.png`, `BASE (2).png`, `BASE (3).png` to `Delete/` and KEEP `BASE (4).png`. Any screenshot without a `(4)` sibling (e.g. a lone `...10.00.09.png`) is never iterated, so it stays untouched.
- 2.2. Driven by globbing the `*(4).png` files (zsh `(N)` nullglob so it no-ops on an empty/dup-free folder), then stripping the suffix to test the siblings —— matching your example exactly (only `...10.00.09.png` & `...10.13.14 (4).png` remain in `Sort/`).

## Change 01
**Replace:**
```
mkdir -p -- "$dst"

for f in "$src"/*; do
  [[ -f "$f" ]] || continue
  name=${f:t}
  [[ "$name" == *" (4).png" ]] || mv -- "$f" "$dst/"
done
```
**With:**
```
mkdir -p -- "$dst"

for f in "$src"/*" (4).png"(N); do
  [[ -f "$f" ]] || continue
  base=${f%" (4).png"}
  [[ -f "$base.png" && -f "$base (2).png" && -f "$base (3).png" ]] || continue
  mv -- "$base.png" "$base (2).png" "$base (3).png" "$dst/"
done
```
