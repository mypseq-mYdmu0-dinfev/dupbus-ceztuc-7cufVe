# Response to "for all flagged emails in this session: ensure they're either Label_2 or Label_3 to prevent repeated #jop"
*All §2 threads now labelled —— but flag: the scan must also EXCLUDE these labels*

1. Done —— every § 2 flagged thread now carries `Label_2` or `Label_3`. Applied this turn:
- 1.1. **`Label_3`** (concluded —— dead, so they never resurface as actionable) to the 8 pre-session-dead items incl. their sibling reminder threads: **Meshki** (2 threads), **My Guardian**, **Ecom Republic** (3), **Lifeline** (2), **Dental Lounge**, **DyFlex**, **APCO**, **KFC**.
- 1.2. **`Label_2`** (still needs your action) to **Civilmart** (2.1).
- 1.3. Already labelled (no change): from the run —— `Label_3`: 2.5 Endotherapeutics, 2.8 Reload (pre-filled); `Label_2`: 2.2, 2.3, 2.7, 2.13, 2.18 (needs-you). And you'd already set **2.4 Global Tech = `Label_2`**, **2.6 Compnow = `Label_3`**.

2. Net state of the 18 actioned items: `Label_2` (open/needs action) = 2.1, 2.2, 2.3, 2.4, 2.7, 2.13, 2.18; `Label_3` (concluded/ready/dead) = 2.5, 2.6, 2.8, 2.9, 2.10, 2.11, 2.12, 2.14, 2.15, 2.16, 2.17. None bare.

3. ⚠️ **Important —— labelling alone does NOT prevent repeated `#jop` yet.** The current `#job` scan (job.md § Scope / google.md) only excludes `-label:Label_1`. So a future `#job` would **still re-surface** these `Label_2`/`Label_3` threads as pending actions. To actually stop re-processing, the scan must also exclude them:
- 3.1. Change the scan query rule to **`-label:Label_1 -label:Label_2 -label:Label_3`** (skip Unimportant + in-progress + done).
- 3.2. This touches **both** `career/job.md` and `universal/google.md` (the skip rule lives in both), and `google.md` would need a re-`#sync universal` for OTG. Say the word and I'll make the edit + re-sync —— I've not done it unprompted since it changes scan behaviour.

4. Not labelled (tell me if you want them covered):
- 4.1. **2.19 RFI Global** —— optional anonymous diversity survey (not a pending action; unlikely to re-flag). 
- 4.2. **§ 1** —— 1.1 Nature's Energy (interview cancelled/closed, you said ignore) and 1.2 Saul Recruitment (boilerplate auto-ack). Neither is a `#jop` pending action.

5. Reminder: the 9 `❌_⏳_…` voided duplicate ARs in `seek/gcl/applied/` are still there for you to delete.
