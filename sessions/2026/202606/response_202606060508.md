# Response to query_202606060508.md
*Moved to gscpt/GH.code-workspace; still opens the whole repo; Desktop dropped*

1. Inside `gscpt/`, yet still opens the WHOLE repo —— done
- 1.1. A workspace file's `folders` paths are relative to the FILE's location, NOT a limit on what it can open. So the file can sit in `gscpt/` and still open everything by pointing UP: `"path": ".."` (= `gscpt/..` = the repo root). It is NOT restricted to `gscpt/`.
- 1.2. Created `gscpt/GH.code-workspace` (folders → `..` = full repo; interpreter still pinned to `gscpt/.venv`). Voided the old root file as `❌_dupbus.code-workspace_moved_gscpt` —— delete it whenever.

2. Renamed —— it's `GH`, not `dupbus` (`gscpt/GH.code-workspace`).

3. Desktop —— removed
- 3.1. It was only there because your OLD `Default.code-workspace` listed it; I copied that habit. Since you never use Desktop, I dropped it. The new workspace contains just the repo.

4. Double-click —— yes
- 4.1. Double-clicking a `.code-workspace` opens VS Code in that workspace (VS Code registers as the default app for `.code-workspace` on install). It'll show the repo as the folder, with `gscpt/.venv` preselected as the interpreter.
- 4.2. Tip: it'll be at `gscpt/GH.code-workspace` in Finder. If you'd rather not see it in the editor's file tree either, that's unavoidable for a file living in the repo —— but it's tucked inside `gscpt/`, not at the root.
