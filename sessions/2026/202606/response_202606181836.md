# Response to query_202606181836.md
*Trimmed c.md bullet; README → .md*

17. Done —— agreed on both, no blocker. Actions:
- 17.1. `seek/CLAUDE.md` bullet shortened to: `` `/interviews/` —— in AJAP mode, NEVER access (out of scope; user-managed) `` —— one short line, in the same style as the `/ccl/` bullet.
- 17.2. Created `seek/interviews/README.md` (terse; no record-keeping mention).
- 17.3. Voided the old `README.txt` per Void Rule → renamed `❌_README.txt` (awaiting your manual delete; I never delete files myself).

18. Re seek/CLAUDE.md —— you are right on both counts:
- 18.1. It is re-read every session (and on every compaction), so terseness there has real token value —— my first version was bloated for a frequently-loaded file.
- 18.2. The "where to find" pointer was genuinely redundant: since each copy keeps a filename whose TS differs from nothing AJAP touches, and you will hand me the full filename anyway, a plain `find` locates it. AJAP needs zero knowledge of the folder —— "never access" is the whole requirement.

19. Re README —— converted to `.md`, kept it to four short lines (title, one-line purpose, `g/`, `c/`). Dropped "record-keeping" as you said —— it is self-evident. The `g/` vs `c/` split is the only thing worth stating, so that is what remains.

20. One housekeeping note —— there is now `❌_README.txt` sitting in `seek/interviews/`. Please delete it manually when convenient; until you do, it is treated as non-existent and ignored.

21. Folder is final and ready —— drop interviewed-job ARs into `g/` or `c/` whenever.
