# Response to query_202606132345.md
*Fenced-block masking removed; embedded deliverables now linted*

41. Agreed —— you are right, and this supersedes pt 38 of response_202606132319
- 41.1. Removed the FENCED-code-block masking. Embedded deliverables sitting in fenced blocks inside a `response_` are now LINTED by `--quick` (my earlier design wrongly skipped them, which is exactly your objection).
- 41.2. Kept ONLY inline `code`-span masking. Rationale: a deliverable is never written inline, so this hides nothing real; it merely spares a back-ticked REFERENCE to a banned term in prose (e.g. when we discuss `color` vs `colour`, or `while` vs `whilst`) from false-flagging our own meta-talk. CIIW —— if you would rather have zero masking for full simplicity, say so and I will drop the inline masking too.
- 41.3. `--quick` still fires ONLY on a `response_` write/edit (the PostToolUse hook, unchanged); separately created deliverable FILES remain covered by `writing.md`'s FULL lint, as you noted.
- 41.4. Verified: an inline `color` reference stays silent, whilst a fenced deliverable containing `favorite`/`color` now flags RED.
- 41.5. Net effect for the embedded-deliverable case —— `--quick` catches its British-spelling / Hart / #numbered breaches; the full em dash / colon / GenAI checks still come from running the FULL `--text` lint on that deliverable per `writing.md`.
