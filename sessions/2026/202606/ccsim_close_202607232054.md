# Session Closing (202606181505–202607232249)
*CCSIM (03): converter Option C, cp/ migration, CCSIM CP*

## 1. Coverage
- 1.1. Issue 1 (DATS) —— per-file skip: one bad `close_` no longer blocks the rest of its type (done early; verified it survived the 08 Jul edit).
- 1.2. Issue 2 (PDF Conversion.app) —— Option C: MS files convert via LibreOffice headless (Quick Mode, no "Grant File Access" prompt), iWork untouched; added dual Quick/Full mode (re-drop a file → native-Office re-convert); honest end pop-up (success only on real conversion + clears an empty folder); aspect ratio diagnosed as FAITHFUL (the source declares landscape); README + new-Mac setup documented.
- 1.3. cp/ migration —— test-move win-signal validated, then full batch: career→`cp/career/`, dissertation→`cp/dissertation/`, mip→`cp/archive/mip/` (114 files; move-only R100 commits; `--follow` history verified). 3 macOS alias files excluded.
- 1.4. CP definition (c.md §6.1) —— any folder under `cp/` is a CP EXCEPT `cp/archive/` (retired children); no marker file needed; §6.3.3/§6.4 conditionalised.
- 1.5. #sync generalised —— no-arg auto-discovers `universal` + every `cp/` CP with a `CP_index_otg.md` (excludes `cp/archive/`); per-scope arg kept; all OTG permalinks re-pinned.
- 1.6. CCSIM CP built (`cp/ccsim/`) —— `CLAUDE.md` (change-hunt, recent-5 index, backlog, simulation QA, sandbox, #wrap), `backlog.md`, `last_seen.md`, `sandbox/`. `CP_notes.md` merged into `CLAUDE.md` §7 then voided.
- 1.7. Pairing lint —— `.githooks/pre-commit` now blocks a staged `response_[TS]` without its sibling `query_[TS]` (SA-built, isolation-tested).
- 1.8. Issue 7 (skills) —— discussed; full package in §6.1.
- 1.9. Backlog wiring (post-close) —— `close.md` records CCSIM items in BOTH the `close_` body AND `backlog.md` (whose `Ref` points back to the close_); a CCSIM `#wrap` runs the monthly Backlog Sweep (`ccsim/CLAUDE.md` §6). A blind §4 SA-simulation drove several fixes (stale refs, unnumbered `## Issues`, a typo, no-DATS-at-wrap); design in §2.8.

## 2. Decisions
- 2.1. Option C over grant-access/FDA —— external-volume Office bookmarks are fragile; LibreOffice headless is the only truly hands-off fix. Full Mode fixes font/layout fidelity, NOT orientation (Word honours the same landscape source).
- 2.2. Migration via move-only R100 commits (`git mv`, zero content edits); dissertation done IN-REPO (not `filter-repo`) since `git_history.py` already recovers full history; career was PARTIALLY pre-migrated by the AJAP #inv session (7 files + `int/`) —— never re-move those.
- 2.3. CP membership = location-based (under `cp/`), no `CP_index_cc.md` required; `cp/archive/` is the sole non-CP.
- 2.4. #sync no-arg = ALL scopes (user always syncs all); a `cp/` folder without `CP_index_otg.md` is CC-local only (not OTG-fetchable).
- 2.5. Simulation QA (ccsim §4) —— CCSIM CC ACTIVELY proposes SA simulation for ANY non-trivial change; blind A/B (current vs `git show`/`sandbox` baseline) for before/after questions.
- 2.6. #wrap = a CCSIM job; gate on user-reported context (>50% full → fresh session); `wrap_` stays unprefixed.
- 2.7. `sandbox/` = CC's sole directly-deletable folder; delete discipline = commit+push → delete → commit+push (rescue-safe).
- 2.8. Backlog wiring (finalised): each `close_` records its CCSIM items in BOTH the `close_` body (§ Issues) AND `backlog.md` (whose `Ref: close_[TS] §x.x` points back for context —— the backlog can't intelligibly cite raw comms). The monthly sweep is a CCSIM-session job (a non-CCSIM `#wrap`, e.g. AJAP's, doesn't run it), hunts cross-session PATTERNS + proposes against open items, never fixing at wrap time; runs AFTER the wrap (an allowed 2-commit override of §9.05.5); no DATS at `#wrap` (the session's `#close` DATS's the `wrap_`).

## 3. Issues (parked TODOs → next CCSIM session, SEQUENTIAL, top-down)
- 3.1. 🔴 i1 —— #wrap `202606/` FIRST (23 days behind).
- 3.2. 🔴 i2 —— temp-file migration per `ccsim_query_202607060438.md` (unread; in `queued_queries/`); GATES the rest (believed to hold the skills files).
- 3.3. 🟡 i3 —— create "skills" (thin alias-skills; see §6.1).
- 3.4. 🟡 i4 —— install considerable external skills (e.g. `agentskills.io`); the FIRST skill = one that vets the SAFETY of installing skills.
- 3.5. 🟡 i5 —— have the CCSIM CP and/or #wrap auto-search + PROPOSE installing skills that address surfaced problems.
- 3.6. Caveat (on i4/i5, not a task) —— external skills are untrusted code + instructions; the safety-gate skill is the right first move; never enable an external skill unreviewed.

## 4. Comms Files (12 pairs)
- 4.1. `202606181505`: Maintenance (03) start —— Issues 1–7 (DATS, PDF, cp/, CCSIM, skills).
- 4.2. `202606181815`: replies —— history-rewrite analysis; skills alias-hybrid.
- 4.3. `202606182003`: Issue 2 aspect-ratio + dual Quick/Full mode.
- 4.4. `202606182051`: Issue 2 closed.
- 4.5. `202607162105`: status recap; migration test file picked.
- 4.6. `202607182249`: note followed; test move done (R100).
- 4.7. `202607182321`: full cp/ migration; 3 aliases flagged.
- 4.8. `202607190004`: ref-updates + #sync generalised + CCSIM built + pairing lint.
- 4.9. `202607232032`: assessed `ccsim/CLAUDE.md` edit; parked skills TODOs.
- 4.10. `202607232122`: wired ccsim/backlog into close.md + wrap.md.
- 4.11. `202607232200`: assessed your pcmd edits; ran §4 sim; fixed defects.
- 4.12. `202607232246`: backlog-wiring refinements —— unbar close body, #wrap sweep after, no DATS.
- 4.13. Non-paired —— `query_202607232053.md`: the #close trigger (produced this file).
- 4.14. NB —— `202606/` ALSO holds PARALLEL non-CCSIM sessions' unprefixed comms (e.g. the "Interview Folder" chain `181528/181824/181836/181846/181914`; "Final Request" `162126`) —— NOT part of CCSIM (03).

## 5. Non-Comms Files
- 5.1. Created:
  - 5.1.1. `ccsim/CLAUDE.md`, `ccsim/backlog.md`, `ccsim/last_seen.md` (+ user-made `ccsim/sandbox/`).
- 5.2. Modified:
  - 5.2.1. `gscpt/DATS.py` —— Issue 1 per-file skip (later user `SCAN_DIRS` edit).
  - 5.2.2. `dupbus-ceztuc-7cufVe/CLAUDE.md` §6.1/§6.3.3/§6.4; `.sync/sync.py` (auto-discover all scopes); `universal/sync.md` (generalised + no-index remark).
  - 5.2.3. `.githooks/pre-commit` (pairing lint); `ccsim/CLAUDE.md` (§4 broadened, §7 added; user tightened title/preamble).
  - 5.2.4. OTG control files repathed + re-pinned by #sync: `dissertation/CP_index_otg.md`, `career/CP_instr.md`, `dissertation/CP_instr.md`, `universal/index_otg.md`, `universal/preferences_otg.md`, `career/CP_index_otg.md`.
  - 5.2.5. [outside repo] `PDF Conversion.app/Contents/document.wflow` (Option C + dual-mode + honest pop-up; re-signed adhoc); `PDF Conversion/README.txt` (user).
  - 5.2.6. (post-close) `universal/close.md` + `universal/wrap.md` + `cp/ccsim/CLAUDE.md` —— backlog wiring, sweep, sim-driven fixes (mostly user-streamlined; CC added the wiring + fixes).
- 5.3. Moved/Voided/Deleted:
  - 5.3.1. Migrated (git mv, R100): `career/*`→`cp/career/`, `dissertation/*`→`cp/dissertation/`, `mip/`→`cp/archive/mip/` (114 files); test-move `dissertation/archive/MGTK751_A1_formative.md`→`cp/dissertation/archive/` first.
  - 5.3.2. Voided: `ccsim/CP_notes.md`→`ccsim/❌_CP_notes.md`.
  - 5.3.3. Renamed THIS close (prefix-only, move-only): `close_202605300023.md`→`ccsim_close_202605300023.md` (CCSIM 01); `close_202606070527.md`→`ccsim_close_202606070527.md` (CCSIM 02).
  - 5.3.4. 3 alias files (`career/:seek:context:`, `career/Resumes`, `dissertation/Non-MD Files`) excluded from migration; deleted by user.

## 6. Remarks
- 6.1. **Skills package (i3; per your 53) —— everything about "skills":**
  - 6.1.1. Discussed as Issue 7 in `response_202606181505.md` §15–18 (verdict: your pcmd/context files ARE a bespoke skills system —— don't fully migrate) then `response_202606181815.md` §37–41 (revised conclusion).
  - 6.1.2. CONCLUSION —— create thin ALIAS-skills for the `~`8 SITUATIONAL files ONLY: `writing.md`, `profile.md`, `pro_profile.md`, `coding.md`, `cic.md`, `shrink.md`, `plan.md`, `google.md`. Each = a one-file `SKILL.md` whose body is "read and follow `universal/<file>.md`", with a `description:` mirroring its §7.2 condition. CC-only (model-invoked); KEEP `#triggers` (no `/name` in prompt files); OTG untouched. Explicit `#trigger` files (close/wrap/sync/br) stay plain —— no skill.
  - 6.1.3. Your EXACT extensions (from `query_202607232032.md` + `query_202607190004.md`): (a) create these skills; (b) install considerable external skills (e.g. `agentskills.io`); (c) the FIRST skill = one evaluating the SAFETY of installing skills; (d) CCSIM CP and/or #wrap auto-search + propose installing skills addressing surfaced problems.
  - 6.1.4. Gate —— i2 (temp-file migration) is believed to hold the skills files; do i2 before i3.
- 6.2. FIRST `ccsim_` file. Sessions 01 & 02 closes now `ccsim_`-prefixed for retrospection; their CONTENT still reads "Maintenance (01)/(02)" (prefix-only, move-only rename per your ask) —— earlier comms of those sessions were NOT retro-prefixed (avoids a large audit).
- 6.3. Next CCSIM session —— run the every-turn change-hunt (`ccsim/CLAUDE.md` §1) + print the recent-5 `ccsim_close_` index (§2). `last_seen.md` anchor = `68c6d40` (will be superseded).
- 6.4. `202606` not yet `#wrap`'d (i1).
