# Query on ajap_logs.py

read ajap_logs.py
add a new function
if the input file has any `.png` in text
identify & convert all into the expected TS before starting current functions
this new function ignore anything between minute (`mm`) and `.png` (i.e. `.29 (4)` in below example) to keep 12-digit TS; and only process anything before `.png`; then remove the `.png` itself
e.g. 
```
202606131710 X%X% comment
Screenshot 2026-06-13 at 17.01.29 (4).png X%X% comment
```
would become
```
202606131710 X%X% comment
202606131701 X%X% comment
```
because 1st line does not involve `.png` so it will just be passed on to current functions
2nd line involves `.png` so everything before it will be processed into TS before current functions
after that, they will be processed by current functions as usual
also update the script's top descr as needed