# Response to query_202606110316
*Thoughts on MBA setup follow-ups*

10. **Re 1 —— CC session history on MBA:** the session content (query_/response_ files) syncs via GH and will be fully accessible on MBA. What WON'T carry over is CC's own UI session list —— the sidebar showing past CC conversations is stored locally at `~/.claude/` on the Mac Mini. On MBA that list starts empty. In practice this is negligible because you (and I) navigate via the GH session files anyway, not CC's UI history.

11. **Re 2 —— machine detection:** confirmed viable. `sysctl -n hw.model` returns a unique identifier (e.g. current machine just returned `Mac14,12` = Mac Mini M2 Pro); MBA M1 would return something like `MacBookAir10,1`. I can run this at session start and branch on the result. Proposed CLAUDE.md edit:
- 11.1. §8.8.1 & §8.8.2 become two-branch entries: `Mac14,12` → FURY paths (current), any `MacBookAir*` → MBA local paths (to be filled in once you know them)
- 11.2. Your fallback logic (item 2's last para) is also sound —— if path not found, `find` across storage. Slightly slower but zero-config. Given these paths are rarely invoked, the fallback alone may be sufficient and saves a CLAUDE.md edit.
- 11.3. Recommend: add the two-branch table to CLAUDE.md now (MBA paths TBD), and rely on `find` fallback until MBA paths are confirmed.

12. **Re 3 —— memory files:** you're right, and §8.6 is explicit: I must never create/edit them. The 3 existing files were likely auto-written before that rule existed or before the system was properly enforced. Your diagnosis is also correct on WHY they fail: by the time any memory about "no chat text" could influence me, the system prompt has already injected CLAUDE.md which covers it more authoritatively anyway. The memory layer adds nothing here. Agreed: treat those 3 files as inert legacy; no action needed beyond ignoring them.

13. **Re 4 —— MCP connectors likely account-bound:** this is a good catch and almost certainly correct. The OTGC evidence is solid —— if Gmail appeared there without config, the OAuth tokens are tied to the Anthropic account, not machine. Expect most CAI connectors (Gmail, Calendar, etc.) to just work on MBA after sign-in. Chrome MCP extension is the one exception: that's browser-local and needs the extension installed/enabled on MBA's Chrome separately.

14. **Re 6 —— commit/push before switching:** yes, that fully resolves it. Push on Mac Mini before leaving → pull on MBA before starting → no divergence possible. Clean and simple.

15. **Net revised picture for MBA setup:**
- 15.1. Install GH Desktop → clone repo
- 15.2. Install CAI → sign in (same account) → MCP connectors auto-restore; Chrome extension needs manual install
- 15.3. `~/.claude/` global settings: just re-login, nothing else needed
- 15.4. Memory files: ignore entirely; don't copy
- 15.5. §8.8.1/8.8.2 paths: rely on `find` fallback OR add MBA paths to CLAUDE.md once known
- 15.6. Switching discipline: always push before switching machines —— sufficient to prevent conflicts
- 15.7. No meaningful perceivable differences beyond the first-session absence of CC's UI history sidebar
