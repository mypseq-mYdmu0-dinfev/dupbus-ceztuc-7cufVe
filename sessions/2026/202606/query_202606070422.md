# Reply to response_202606062336.md

## 16

i noticed you successfully added top comments to `gscpt/` files
and instead of a single line, you seem to break them into lines so they don't extend overly horizontally
where you also didn't force breaking words (like many e-book readers do)
e.g. 
```
...with your
account id...
```
instead of 
```
...with y-
our account id...
```
this is great
im just interested to know what this approach is called?

## 18
fixed

## 20
20.1: can't remember what it's for; deleted
20.2: seems used to batch-convert .pages to .pdf? if yes, leave as is; indeed useful and should be standalone there; though im curious if it only converts .pages? any other file formats supported?

## 21
ok i edited it again and tested #sync on cloud and local again, both worked as expected; fixed

## 22
22.1: all confirmed except trade_records.py (very rarely used; no time to test now)
22.2: whole `VS Code/` deleted as i expect all coding now happens in GH; FYI `gscpt/` = General Scripts (as name tells, they're for general usage); in future we might create `pscpt/` (Project Scripts) if we have some large scale projects (codebase w/ many scripts; e.g. rebuilding my website; though unlikely short-term), though small scale projects (single/few scripts) might live in its CP (e.g. MBA dissertation interview transcript processing)
22.4: fixed

## Missions
- address my question in 1st section
- briefly tell your thoughts on others