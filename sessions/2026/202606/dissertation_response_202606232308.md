# Response to dissertation_query_202606232308.md
*Root-placement rule encoded; the offending temp script relocated*

5. Done —— recorded in the root `CLAUDE.md` as a new **§8.9 Creation Placement** (the governing doc, so it binds every future session, not just this CP):
- 5.1. §8.9.1 —— NEVER create anything in root unless explicitly told.
- 5.2. §8.9.2 —— temp/throwaway script (to be voided after use): create beside the `response_` (`/sessions/[YYYY]/[YYYYMM]/`) so you see and delete it.
- 5.3. §8.9.3 —— reusable script: create in `cscpt/` then update `cscpt/README.md`.
6. The offending file is relocated: `❌_temp_build_p127.py` moved out of root to `/sessions/2026/202606/`, beside this response, still `❌_`-voided for your deletion. Root is now clean.
7. My fault on the original root placement —— acknowledged; §8.9 prevents a recurrence. No `cscpt/README.md` update was due (the script was throwaway, not reusable).
8. P12.7 hold unchanged —— awaiting your `.pages` application of the 31 changes, then mirror back and I run P12.7.4 (re-QC).
