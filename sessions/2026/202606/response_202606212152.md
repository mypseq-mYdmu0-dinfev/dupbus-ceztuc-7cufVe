# Response to test 6 —— correction + root-cause fixes
*Correcting response_202606212145: the debate ran to turn 9, not 6; my close sequence was flawed; fixed*

77. Correction to my last (`response_202606212145`) —— I closed test 6 on stale info; the honest picture:
- 77.1. The debate did NOT end at turn 6 —— it reached turn 9 (A027 / B024, `~`7,533 tokens). My oversight READ was at turn 6; whilst I composed the verdict the fast debaters posted turns 7–8, so my `THE_END` actually landed after turn 8, and my verdict —— written from the turn-6 read —— missed turns 7–9.
- 77.2. Worse: I wrote the verdict IMMEDIATELY after `THE_END` rather than waiting for the debaters to stop, so Debater A's turn 9 (A025–A027) landed in the post-`THE_END` compose-race and appears on the board BELOW my verdict —— a block stranded after the closing block.
- 77.3. Net on the verdict: the CONCLUSION holds (A narrow; graduated-release convergence —— the Observer SA's independent full-debate report agrees), but it MISSED turns 7–9, which SHARPENED the crux from "who is the gatekeeper" to the DEFAULT DIRECTION (A: open unless an independent verifier blocks; B: closed until the lab clears). I appended an honest addendum to the board capturing this.

78. Root-cause fix (applied to `debate.md`) —— MA's CLOSE SEQUENCE was wrong:
- 78.1. New rule: after `THE_END`, MA must (a) WAIT until every debater has actually stopped (their acks —— a debater can append in its compose-window after `THE_END`), THEN (b) RE-READ the board's final state afresh, and only THEN (c) write the `## Observer` verdict. Never verdict from an earlier oversight read; never before the board settles. This kills both the stale-verdict and the stranded-block problems.
- 78.2. It's the same "re-read right before you write" discipline the debaters already follow —— I'd simply failed to apply it to MA's own verdict step. Now fixed.

79. Two more fixes applied (the test-6 Observer-degradation finding, 75.1):
- 79.1. Observer digest robustness —— each digest line must be ONE `printf` / one line (never fragmented), and SUGGEST-idle only after SEVERAL consecutive empty deltas, not one quiet poll. Those were exactly this run's two degradation modes.
- 79.2. (The Opus-Observer option for large/dense debates and the Observer-compaction check were already added the prior turn.)

80. The deeper lesson, doubly confirmed this run: MA oversight is essential —— it caught the failing digest —— AND MA must execute the CLOSE carefully (let it settle, then re-read), or oversight-from-a-stale-read introduces its own error. Both halves are now in `debate.md`.

81. Status: test 6 fully closed; Debater B and the Observer SA stopped on `THE_END` (Debater A likewise, after its post-race turn 9). Verdict conclusion stands; board addendum + the three `debate.md` fixes applied. This was the most valuable run yet —— it stress-tested the close path and surfaced two real bugs, both now fixed.
