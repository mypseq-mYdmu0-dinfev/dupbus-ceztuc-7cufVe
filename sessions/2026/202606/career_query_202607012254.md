# Query (QMM)

haven't read it yet, but it might risk being too long
DON'T edit the speech but create a new one (`mini_[current_filename]`) and shrink it by half, which will be used whenever she prompted me faster or appears impatient, etc.
then in `response_`, tell what were compromised/shortened, etc.
QMM
