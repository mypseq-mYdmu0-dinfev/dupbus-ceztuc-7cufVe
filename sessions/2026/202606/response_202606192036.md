# Response to query_202606192036.md

1. Edit Msg 2, not Msg 1 — the stop instruction is loop behaviour, so it belongs in the loop command alongside the other on-wake instructions.

2. Msg 1 stays unchanged. Updated Msg 2 (your version + the fix):

```
/loop 60m On wake: run `TZ='Australia/Sydney' date +"%H:%M"`; if ≥ `[HH:MM]`, address `sessions/2026/[YYYYMM]/query_[TS].md` then stop the loop; otherwise, do nothing.
```
