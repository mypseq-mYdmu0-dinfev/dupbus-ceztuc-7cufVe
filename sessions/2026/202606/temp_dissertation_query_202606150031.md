# Query (QMM —— created on user's
  behalf, §3.6.2)

i manually edited Lu.md Log 17
spot out all changes; infer why;
  concisely tell if agree; confirm
  if ready to send now
QMM (re-read glossary.md if
  unrecognised, only the line
  starting `- QMM`)

