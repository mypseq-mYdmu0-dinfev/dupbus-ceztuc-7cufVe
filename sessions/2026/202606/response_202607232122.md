# Response to query_202607232122.md
*Backlog wiring: decisions extracted + implemented in close.md/wrap.md*

## What We Decided (found + extracted)

1. It was Issue 5/6 —— decided in `response_202606181815.md` §33–36, built on `response_202606181505.md` §13–14.
- 1.1. `close.md` —— after the normal close, scan the session for CCSIM-worthy items; if any, APPEND to `cp/ccsim/backlog.md`; do NOT repeat them in the `close_` body (§36.1). Entry format was fixed in §34 (now lives in `ccsim/CLAUDE.md` §3).
- 1.2. `wrap.md` context gate —— `#wrap` is heavy; if the session is context-loaded, advise a fresh session (§36.2; later refined to the >50%-full gate, already in `ccsim/CLAUDE.md` §6.2).
- 1.3. `wrap.md`, BEFORE writing the wrap —— (a) surface `backlog.md` open items; (b) scan the month's `close_` for EMERGENT cross-session patterns → append any to `backlog.md` (§36.3). Per-session items self-register via each close (§36.4), so the wrap hunts PATTERNS, not un-logged flags.
- 1.4. Wrap reminder —— review the backlog → drive items towards resolution via a dedicated CCSIM session; don't fix at wrap time (§36.5).

2. Your added framing —— `#wrap` as an active MONTHLY SWEEP (propose solutions + aim to clear), reconciled.
- 2.1. §36.5 said "never fix at wrap time" (context cost). Your "aim at clearing" is honoured WITHOUT contradicting it: the sweep PROPOSES a solution per open item and TEES UP a CCSIM session to action them —— it drives clearance, it doesn't do the fix at wrap time.
- 2.2. Net: the backlog can't grow unbounded —— every month it's reviewed, proposed-against, and pushed towards zero.

## Implemented

3. `close.md` —— added a Preamble step (after Sprint Log).
- 3.1. Whilst drafting the close, scan the session for anything CCSIM-worthy → if any, append to `cp/ccsim/backlog.md` (format `ccsim/CLAUDE.md` §3), and don't repeat it in the `close_` body (single source). None → skip.

4. `wrap.md` —— added the context gate + a "CCSIM Backlog Sweep" section.
- 4.1. Preamble gate: >50% context-full → advise a fresh session (refs `ccsim/CLAUDE.md` §6.2); `#wrap` is a CCSIM job, `wrap_` unprefixed.
- 4.2. New section (runs BEFORE writing the wrap): surface open backlog items; scan the month's closes for emergent patterns → append new ones; PROPOSE a solution + rough effort per open item; don't fix at wrap time —— tee up a CCSIM session; record the sweep in `wrap_` § Issues, referencing `backlog.md`.

5. Both are pointers, not duplication —— the entry format + backlog rules stay solely in `ccsim/CLAUDE.md` §3 (one source of truth, per coding.md).

## §4 Simulation —— proposed (your call)

6. Per `ccsim/CLAUDE.md` §4 (actively propose a sim for any non-trivial pcmd change), I recommend a quick blind check before we rely on this.
- 6.1. Dispatch 1–2 SAs given ONLY the edited `close.md`/`wrap.md` + a mock session, and see whether they correctly (a) append a CCSIM item to `backlog.md` at close, and (b) run the sweep + propose at wrap —— catching any ambiguity a fresh reader would hit.
- 6.2. Low urgency (the edits are additive + pointer-based); say the word and I'll run it, else we leave it for the first real close/wrap to exercise.
