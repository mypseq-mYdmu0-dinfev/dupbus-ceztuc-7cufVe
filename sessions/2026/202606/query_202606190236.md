# Creating `sprint.md`

create `universal/sprint.md`
when prompted `#sprint`, it means i'll be away from WS for an extended period of time, and you should utilise such time to accomplish as many as possible
since time is not a limiting factor, you may consider to:
- dispatch SAs, which might be slower because of thinking/briefing, but saves context
- do additional CIC research (if meaningful) for validation, etc.
- practice deeper thinking for wiser decision making, as if effort = Max (if not already)
---
if prompted `#sprint #quick`, it means i'm in a hurry; so in opposite to above, time is a limiting factor, but you would still not compromise quality, just working as if not prompted `#sprint` BUT push through (see below), yet you should actively consider dispatching SAs for parallel workstreams if that can meaningfully speed up the process (not slowing, unlike described above)
---
for both `#sprint` & `#sprint #quick`, you would push through given tasks (and derived sub-tasks, if any)
for each, raise qb in `<thinking>` then predict how i'd answer based on contextual understanding (& previous `close_` files if applicable, which more likely applies in CPs)
make assumptions to clear blockers & proceed continuously, whilst retaining fidelity to the goals behind tasks
i.e. don't stop until all tasks are completed thoroughly
.
e.g. we're drafting a pitch deck, in last `query_` i delegated 10 tasks
where task 5 depends on OR can't start before task 4 (as i instructed OR as you evaluated), same for task 6 & 7
so normally the dependency is task 4→5→6→7
the rest of tasks are not dependent on any
in such scenario, if prompted `#sprint #quick`, you'd do as `#sprint` (see below) but utilise the time to do what i instr above (e.g. additional CIC research)
in such scenario, if prompted `#sprint`, you'd do task 1–4 first, as you usually would do
THEN task 8–10 since they're not dependent to other tasks
THEN, normally (if not prompted `#sprint`) you would have to pause for my confirmation on task 4, but in this situation you would push through to start task 5–7 sequentially, predicting how i'll respond to task 4, recite if needed, then assume acceptance on task 4 and start task 5; same treatment for 5 to 6 and 6 to 7
for all, since it's a pitch deck task, the goal is of course to deliver the best quality deck possible and to achieve other objectives (e.g. client buying it)
.
caveat: for highly critical tasks (judge by case), esp. task is not on GH, i.e. not tracked (e.g. CIC execution; not CIC research), you should instead #sprint by planning ahead then telling what you'd do in `response_` (or a separate file if output ≥ 1,000 words) so then when im back there's still progress made even for critical tasks
---
## Your Missions
- Execute actions if any explicitly instructed & no qb
- Tell your thoughts on all above (don't miss any)