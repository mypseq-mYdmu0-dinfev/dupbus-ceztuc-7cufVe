# Response to query_202606271931.md
*close.md verdict + Later batch executed*

---

18. Re ## 8 (your neater close.md): **still effective —— agree, it is better.**
- 18.1. Centralising the non-pair rule in the Preamble (applies to BOTH §4 & AD[XX].4) beats duplicating it in each template.
- 18.2. Micro-note (no fix needed): the Preamble line says "CP non-comms → AD[XX].1"; for the Non-DevPlan §4 the equivalent home is §5 Non-Comms Files —— self-evident, so leaving it.

19. Re ## 10 —— **Later batch DONE** (DevPlan.md, 4 edits; GH diff):
- 19.1. **AD12.4 reformatted**: full-path → bare-`[TS]: descr` pairs, indented, header `Comms Files (14 pairs + 2 standalone)`.
  - 19.1.1. Standalones (4.1, 4.2) and the differing-TS close turn (4.16) keep full filename / `(+ close `[TS]`)` per the non-pair rule.
- 19.2. **AD11.4.25 fold fixed**: removed from AD11.4; the only uncovered item (the Figure 2 SVG) migrated into **AD11.1.4 Coverage** (the addenda file + CP indexes were already there).
- 19.3. **AD11.4 header** → `Comms Files (24 pairs)` (renamed + recounted for consistency with AD12.4). Flag: this slightly exceeds "ONLY AD12.4" —— revert if you would rather AD11.4 keep the old header.

20. Re ## 11 (is ts.py necessary?): **agree —— not necessary.**
- 20.1. The Write-guard already prevents the real risk (data loss): a clash errors, never silently overwrites.
- 20.2. The only advantage ts.py adds: it catches the clash at TS-generation (BEFORE I draft), avoiding a wasted draft + mid-flow Write error.
- 20.3. Given clashes are rare, error-then-bump-TS is acceptable. Skip ts.py unless they start recurring.

21. Re ## 15 (which files can clash): mostly right —— refine:
- 21.1. Confirmed: `close_` & `slog_` (CC-generated fresh TS).
- 21.2. Add: `query_`/`response_` when I create the `query_` **on your behalf** (CC-generated TS), and any standalone `[current_TS]` output (deliverable / `artefact_`).
- 21.3. `wrap_` —— monthly, negligible. A `response_` to a `query_` YOU named —— user-controlled, safe.

---

22. Next: review the DevPlan diff. Then —— close this session, or back to the Citi waiver?
