# Career — new session (continuing CC-Actioning/Actioned label work)

After Unconditionals, read `202606/career_close_202606100039.md` (the handoff for the prior session), then the files I list below by their `close_` item numbers.

## Context
I have now **reconnected the Gmail connector** (the prior session's 🔴 blocker 3.1/3.2 — write scope was denied; refreshing alone didn't grant it). Goal this session: verify the label apply/remove tools actually work, then continue unresolved issues in last session.

## Mission
1. First confirm the write scope is live: call `update_label` (no-op on `Label_1` → "Unimportant"). If it still errors "requires additional permissions", STOP and tell me — I'll reconnect again.
2. If live, do close §3.3 (§22): create `CC-actioning` + `CC-actioned`, then move ONE `Label_1` email into each (add the new label AND remove `Label_1`; never archive), verify, and report the 2 thread subjects so I can revert them.
3. Then resume close §3.4 (the §24/25 reorg): build `career/job.md` + `career/jop.md`, update both `career/` index variants, trim `google.md`, and write the `CC-actioning`/`CC-actioned` protocol —— drafting **directly into the files** (not a response_).

## Lists (by `close_` item no.)
### A. Read now (test labels + continue)
- 4.9 (latest state: reconnect req, connector verdicts, §22 plan, §24/25 hold)
- 4.7 (which tools to enable + approval settings; connector audit)
- 4.5 (actuator facts + the reorg wiring for §24/25)
- 4.3 (label-workflow feasibility + two-state semantics)
- 5.2.1 (`google.md` —— now holds the labelId map + skip rule)

### B. Remind me to approve re-reading after all tasks resolved
- 4.1 (the #job 30-day scan results —— pending #jop)

### C. Do NOT read
- 4.2, 4.4, 4.6, 4.8 (my prior query messages —— already distilled into the responses + close handoff)