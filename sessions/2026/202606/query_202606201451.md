# slog relocation + Debate edit

identify my edits since your last edit; infer why; tell if agree
specifically, i added "debate" w/ SA, tell if agree; improve as needed (i'll track diff)
then update slog file org:
it should NO LONGER be a temp file as it holds retrospective value (no need to state why)
make it in same folder as `response_`
filename: the same as right now, but prefix CP folder name if CP (e.g. career_slog_[current_TS].md)
and because of this, close.md no longer needs to clean up slog so update that as well
also c.md no longer pt to the `tmp/` but where slog now sits
to deal w/ situation where the folder has multiple slogs, scan if most recent 5 (by mod time) has `slog_`; if single found, that's it; if still multiple found, use context window (although compacted) + selective content read to identify the right one to continue on after compaction
QMM
