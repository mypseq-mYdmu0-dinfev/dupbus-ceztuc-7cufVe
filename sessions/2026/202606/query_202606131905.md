i just migrated my automator .app files to `dupbus-ceztuc-7cufVe/automator/`
read `automator/SortSS.app` and tell if below script (the input i see in Automator UI) is found completely identical (in lines like below, not a single line; as i seem to see them embedded as a single line in 1 of the files in GH app view):
---
```
src="/Volumes/FURY 2TB/Fury Pictures/Screenshots/Sort"
dst="$src/Delete"

mkdir -p -- "$dst"

for f in "$src"/*; do
  [[ -f "$f" ]] || continue
  name=${f:t}
  [[ "$name" == *" (4).png" ]] || mv -- "$f" "$dst/"
done
```
---
concisely report your findings in `response_`
when done, address below:
---
using #replace, edit the script (NOT the file or anything inside `automator/`; put it in `response_`) to only sort images w/ 4 identical filename except suffix ` ([no.])`
Note: ` (1)` never exists
e.g. if the folder (`Fury Pictures/Screenshots/Sort/`) has:
```
Screenshot 2026-06-12 at 10.13.14 (2).pngScreenshot 2026-06-12 at 10.13.14 (3).pngScreenshot 2026-06-12 at 10.13.14 (4).pngScreenshot 2026-06-12 at 10.13.14.pngScreenshot 2026-06-12 at 10.00.09.png
```
THEN: `Screenshot 2026-06-12 at 10.00.09.png` should be left untouched (no dup names)
only `Screenshot 2026-06-12 at 10.13.14.png` should be handled like right now
RESULT: only `Screenshot 2026-06-12 at 10.00.09.png` & `Screenshot 2026-06-12 at 10.13.14 (4).png` remain in `Sort/` w/o the filtered files in `Sort/Delete/`
proceed if no qb