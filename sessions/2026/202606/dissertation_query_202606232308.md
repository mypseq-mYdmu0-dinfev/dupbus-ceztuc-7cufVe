I saw you built `dupbus-ceztuc-7cufVe/❌_temp_build_p127.py`
Remark where appropriate:
- NEVER create anything in root, unless explicitly told
- if temp script needed and to be voided, create beside `response_` so user sees & deletes
- if temp script can be reusable, create in `cscpt/` then update its README.md
---
QMM
