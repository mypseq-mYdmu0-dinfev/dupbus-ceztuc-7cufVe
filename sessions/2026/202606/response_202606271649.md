# Response to query_202606271649.md
*close.md Session Files —— compact to bare-TS pairs*

---

1. Verdict: agree —— AD11.4's bare-TS-per-pair format is the better design; I will operationalise it in close.md. One refinement to argue (pt 5). Not #closing this session. DevPlan/AD12.4 deferred to "Later" per your instruction.

2. Why AD11.4 wins (the token case):
- 2.1. AD11.4 line = `` `202606040416`: descr `` —— bare 12-digit TS only.
- 2.2. AD12.4 line = `` `202606/dissertation_query_…232204.md` + `…response`: descr `` —— full query path + response shorthand.
- 2.3. The strict close.md spec is bulkier still —— TWO full-path lines per pair (query AND response separately).
- 2.4. Ratio ~ 1 : 3 : 5. In an Unconditional read every CP session-start, that compounds.

3. Why bare TS still works (the original "locate the file" goal survives):
- 3.1. A pair shares ONE TS (response TS = query TS, root §3.5.3).
- 3.2. Folder = the close file's own folder; filename = `[CP_]query_/response_[TS].md` —— both fixed conventions.
- 3.3. So `[TS]` alone reconstructs both filenames unambiguously.

4. Format decisions adopted:
- 4.1. ONE line per PAIR, bare `` `[TS]` `` (backticks kept for scannability).
- 4.2. `:` as the TS-to-descr separator (saves the 4-char ` —— `).
- 4.3. Count label `([file_count])` → `([no.] pairs)`.
- 4.4. Edge cases (differing-TS pair, extra standalone file) appended in `( )`, exactly as AD11.4 already does.

5. The one refinement I would argue (your "feel free to argue"):
- 5.1. AD11.4 also folded **non-comms** into its last item (AD11.4.25). I would NOT propagate that.
- 5.2. Reason: we just fixed (this session) the rule that non-CP files live in close §3 / §5, and CP non-comms in AD Coverage. Folding them back into §4 re-introduces that drift.
- 5.3. So the new §4/AD[XX].4 stays **comms-pairs-only**; non-comms keep their own section. AD12.4 is already comms-only, so it reformats cleanly.

6. Alongside checks (nothing else needs editing):
- 6.1. §5 Other Files / §3 Non-CP Files —— unchanged; non-comms have varied names/paths, NOT reconstructable from a TS, so they keep full paths.
- 6.2. Non-CC line (close.md L21) —— still overrides to "artefact no." for Non-CC; unaffected.
- 6.3. No other section references the Session Files format.

---

## close.md #replace (apply, then confirm —— AD12.4 reformat follows)

### Change 01 (Non-DevPlan §4)
**Replace:**
```
## 4. Session Files ([file_count])
- 4.1. `[YYYYMM]/query_[TS].md` —— [max. 8w descr]
- 4.2. `[YYYYMM]/response_[TS].md` —— [ditto]
- 4.3. ... [all `query_`/`response_` pairs (comms files) created this session, in chronological order]
```
**With:**
```
## 4. Session Files ([no.] pairs)
- 4.1. `[TS]`: [max. 8w descr of that turn]
- 4.2. ... [ONE line per `query_`/`response_` PAIR —— bare shared `[TS]` only; folder + `[CP_]query_/response_[TS].md` filenames are implicit & reconstructable; chronological]
[Differing-TS pair or an extra standalone file: append in `( )`, e.g. `(+ `[TS2]`)`. Non-comms go to § Other Files, NOT here.]
```

### Change 02 (Addendum AD[XX].4)
**Replace:**
```
- AD[XX].4. Session Files ([file_count])
  - AD[XX].4.1. `[YYYYMM]/query_[TS].md` —— [max. 8w descr]
  - AD[XX].4.2. `[YYYYMM]/response_[TS].md` —— [ditto]
[... all `query_`/`response_` pairs created this session, in chronological order]
```
**With:**
```
- AD[XX].4. Session Files ([no.] pairs)
  - AD[XX].4.1. `[TS]`: [max. 8w descr of that turn]
  - AD[XX].4.2. ... [ONE line per `query_`/`response_` PAIR —— bare shared `[TS]` only; folder + `[CP_]query_/response_[TS].md` implicit & reconstructable; chronological]
[Differing-TS pair or extra standalone: append in `( )`, e.g. `(+ `[TS2]`)`. CP non-comms go to AD[XX].1 Coverage, NOT here.]
```

---

7. Next: apply Changes 01–02, confirm close.md reads well, then I reformat **only AD12.4** in the DevPlan to this format (AD11.4 left as-is —— already lean).

---
*🟡 L32 `."`: the dot is part of the quoted abbreviation "no." (number), original to it —— Hart satisfied. Accepted.*
