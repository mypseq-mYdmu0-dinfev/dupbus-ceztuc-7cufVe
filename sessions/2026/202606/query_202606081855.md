i found a folder named `__pycache__/` appeared in `gscpt/` which was created today at 1840
i only ran this script today
investigate why this folder appeared and if we should keep it (never saw it for earlier runs)
respond normally in `response_`
