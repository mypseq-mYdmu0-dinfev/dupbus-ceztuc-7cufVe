# Response to query_202606142233.md
*Spotted your writing_otg.md edits; wired index_otg.md for #sync*

13. Your edits to `writing_otg.md` —— spotted, inferred, all agreed
- 13.1. Title `(OTG / non-CC)` → `(non-CC)`. Inference: simpler, and the file IS the non-CC variant. Agree.
- 13.2. Removed my intro italic note. Inference: OTGC fetches this under the alias `writing.md`, so a "this `_otg` file, CC disregards it" note reads oddly from her side; the CC-ignore behaviour is already handled by the `_otg` suffix convention. Agree.
- 13.3. Removed the whole "Language & Units" section. Inference: those base rules (British English, AUD, metric, Hart) live in `rules_otg.md` —— the OTG analogue of `c.md`, fetched unconditionally per `index_otg.md` line 1. I VERIFIED `rules_otg.md` lines 11–14 carry all four, so the removal is gap-free, not a loss. Agree.
- 13.4. Added "Strictly comply with `rules_otg.md`" and renamed "General" → "Shared Rules". Inference: defers the base layer to `rules_otg.md`; "Shared" = applies across the writing styles below. Agree.
- 13.5. Split the dash-restructure into two bullets; abbreviated "abbreviations" → "abbrev.". Inference: readability and brevity. Agree (minor).
- 13.6. Overall —— the core move (delegating language/units/Hart to `rules_otg.md` so `writing_otg.md` is purely style) is sound and verified gap-free. No objection to any edit.

14. Fetchability —— `index_otg.md` updated
- 14.1. Done exactly as you described: the `writing.md` ALIAS is unchanged; its URL had the SHA removed and the filename suffixed to `_otg`, so it now reads `…/dupbus-ceztuc-7cufVe/universal/writing_otg.md` —— the same no-SHA "pending-sync" shape as the `replace.md` entry. OTGC keeps referring to it as `writing.md` but now fetches `writing_otg.md`.
- 14.2. This only STAGES it for `#sync`; the URL is intentionally non-functional until pinned (raw GitHub needs a ref). Per `sync.md`, the SHA pin happens when you (a) commit + push `writing_otg.md` (and any `rules_otg.md`/`index_otg.md` changes) via GH Desktop, then (b) run `#sync universal`, which re-pins `writing.md`'s URL to `writing_otg.md`'s last-commit SHA and prints the new index URL for userPref. I did NOT run `#sync` —— content must be committed first, and `#sync` is a separate trigger.
- 14.3. One reminder —— until that `#sync`, OTGC fetching `writing.md` would fail (no ref in the URL), so run `#sync universal` once `writing_otg.md` is pushed.

---
*Accepted 🟡 YELLOW (dlint --quick): L13.5 `."` in "abbrev." —— the period is original to the abbreviation itself, so under Hart it correctly sits inside the quote.*
