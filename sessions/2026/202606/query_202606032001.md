# Reply to response_202606031831.md

Note: we needa mandate no file contents shall be read except the 4 files (`index_otg.md` `CP_index_otg.md` `preferences.md` `CP_instr.md`); critical as the files can be very large and #sync doesn't require reading them at all; if this is already mandated, just reassure me

## 34
34.2.3: future question: if i later have more CPs that separately store its own comms files just like `seek/investigation/`, do i simply add it in `date_added.py` e.g. `SCOPED = ("/sessions/", "/seek/investigation/", "/new_path/")`?
34.3: i don't fully understand `.sync/date_added.py` so if you relied me on reviewing it, better re-audit & re-test it to ensure it leads to what i expect; no need if you already assumed i don't understand or won't read the .py file

## 37
37.2: this is good and is important; just FYI (not necessary to edit any files, just validate if our setup is good for it): `seek/gcl/` often have hundreds of yet-to-be committed files pending my review so i just wanna ensure they won't impact #sync (we'll never #sync `seek`; even if we #sync `career`, it should only concern files in `seek/context/`, NOT `seek/gcl/`)

## 38
38.1: i suspect that was due to "Auto" mode which i just set to "Bypass permissions" so you can override and proceed w/ that, ensuring it's edited as yous see fit
38.2: i ran it in terminal but it shows:
```
culous@Culous-Mac-Mini ~ % git config core.hooksPath .githooks
fatal: not in a git directory
```

## 39
39.1: all committed and pushed
39.2: see 38.1
39.3: see 38.2
39.4: check and confirm with me: if no folder is mentioned (just `#sync` alone), it should process `universal/`, correct?
39.5: see 38.1