re "the linter must ignore code blocks in comms (else it false-flags)":
this doesn't work
we often have deliverables in code blocks inside `response_`
just ensure the `--quick` only fires when created a `response_` file
writing.md would catch any other separately created deliverable files
QMM
