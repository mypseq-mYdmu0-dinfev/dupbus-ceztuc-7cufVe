# Response to query_202606032001.md
*Mandate reassured; date_added re-audited & passed; settings + hook wired; ready for first #sync*

40. Preamble (no content reads) —— already true; now mandated in writing
- 40.1. Reassured: `#sync` reads the *content* of only the 2 control files (the index + its prefs/instr). For every file the index lists, it uses git metadata alone (`git log -1`, `git status --porcelain` on that path) and **never opens the file**. So however large those listed files are, they cost nothing and are never read.
- 40.2. I've added this as an explicit MANDATE bullet in `universal/sync.md` so future instances are bound by it, not just the current code.

41. Re 34.2.3 —— yes, exactly
- 41.1. For a future CP that stores its own comms separately, you just extend the tuple: `SCOPED = ("/sessions/", "/seek/investigation/", "/new_path/")` in `.sync/date_added.py`. Nothing else.

42. Re 34.3 —— re-audited and re-tested (since you won't read the .py)
- 42.1. I ran 5 checks against `date_added.py`; all passed:
  - 42.1.1. Editing an **existing** comms file → Date Added preserved (`2026-03-21 03:51:00` survived a simulated atomic replace).
  - 42.1.2. A **new** comms file → Date Added stays "now" (correctly newest; capture skips because the file doesn't exist yet).
  - 42.1.3. An **out-of-scope** file (e.g. in `universal/`) → no capture, left alone.
  - 42.1.4. Malformed/empty hook input → exits cleanly (never blocks a tool call).
  - 42.1.5. `get`/`set` sanity → correct.
- 42.2. Mechanism in plain terms: just before any edit to a file under `sessions/`/`seek/investigation/`, it records that file's Date Added; just after, it writes it back. New files keep "now". Nothing else is touched.

43. Re 37.2 —— validated: your `seek/gcl/` pending files are safe
- 43.1. `#sync` only ever looks at files **listed in the scope's index**. We never `#sync seek`; and `#sync career` lists only `seek/context/*` (whatever its `CP_index_otg.md` names) —— `seek/gcl/*` is never listed, so it's never checked, staged, committed, or blocked.
- 43.2. Triple-safe: (a) not in any index we sync; (b) the script stages only the 2 explicit control-file paths (never `-A`); (c) the guard hook would reject anything else. Your hundreds of uncommitted `gcl/` files are untouched.

44. Re 38.1 —— done (bypass on, proceeded as you authorised)
- 44.1. Edited `.claude/settings.json`: added the permission rule `Bash(python3 .sync/sync.py:*)` and the two Date-Added hooks (Pre/Post on Edit|Write|MultiEdit|NotebookEdit). Validated it still parses as JSON, with the existing `PostCompact` hook kept.
- 44.2. Note: this is a local harness config; it doesn't need to be on GH for `#sync` to work. It now shows as a local change in your repo —— commit it via GH Desktop only if you like.

45. Re 38.2 —— that error was just the wrong folder; I've done it for you
- 45.1. `fatal: not in a git directory` happened because the terminal was in `~`, not the repo. No need to retry —— I ran it for you with an absolute path: `core.hooksPath` = `…/dupbus-ceztuc-7cufVe/.githooks` (absolute avoids any cwd ambiguity, incl. GH Desktop). Confirmed set; `pre-commit` is executable. The guard is now live (and fail-safe: zero effect on your normal commits).

46. Re 39.4 —— confirmed
- 46.1. Yes: `#sync` with no folder processes `universal/` (→ `universal/index_otg.md` + `universal/preferences.md`). `#sync career` → `career/CP_index_otg.md` + `career/CP_instr.md`.

47. Status —— everything is now live; one step to validate
- 47.1. Done: toolset committed (your 39.1) · permission rule + Date-Added hooks wired (44) · guard activated (45).
- 47.2. The only thing left is the live test. On your word I'll run `#sync` (universal); it will rewrite the 2 control files to SHA-permalinks, push only those 2, and print the index URL —— you paste it into userPref and confirm OTGC reads latest.
- 47.3. Q1 —— shall I run the first `#sync` now?
