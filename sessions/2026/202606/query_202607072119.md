# Query (QMM)
*Created by CC on the user's behalf (QMM); verbatim below.*

since #br now no longer technically break lines (my original naming rationale from `<br>` in HTML code)
i think it only makes sense to rename both files
br.md → ww.md (easier for me to type)
br.py → word_wrap.py (self-explanatory; i never needa type it)
do it then update content/header, etc. as needed
QMM
then, directly #ww this `response_` so i can test it
