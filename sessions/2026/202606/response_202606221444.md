# Response to query_202606221444.md
*Observer-SA likely better as the exception (MA-as-observer default); fixed-vs-open ledger; usability verdict*

82. Re 70 (is the Observer SA still needed?) —— sharp; my honest take: it should become the EXCEPTION, not the default.
- 82.1. You're right that delta-reads dissolve its ORIGINAL rationale (freeing MA from compounding board reads —— now linear either way).
- 82.2. Its remaining edge is narrow: it keeps MA's CONTEXT lean DURING the debate (digest `~`20w vs board `~`200w per entry —— your 10× thinking point), so MA can do OTHER work concurrently. But (a) MA re-reads the full board at the verdict anyway (per the close fix), so MA's TOTAL read becomes board+digest (35KB) > board-only (31KB); (b) the Observer costs its own `~`47k run; (c) it ADDS a failure mode (the test-6 digest degradation) that MA-as-observer simply lacks.
- 82.3. Recommendation: DEFAULT to MA-as-observer (simpler, cheaper, more reliable —— and it sidesteps every Observer-specific bug); reserve the Observer SA for when MA must run heavy CONCURRENT work alongside a debate (e.g. `#sprint` parallel workstreams), where lean MA-context-during is worth the extra SA + the risk. Worth deciding next session —— it would also let us trim `debate.md`.

83. Re 73.1 (root cause + removal) —— yes, removing the Observer SA solves it at the root:
- 83.1. Root cause: under fast churn the Observer rushed its digest COMPOSE —— fragmented `printf` lines + a misfired idle call. A weakness of the digesting STEP, not of reading.
- 83.2. MA-as-observer has NO digest → the bug cannot occur. So it's another incentive for that default. (I also patched the Observer's robustness for when it IS used —— one-line `printf`, stricter idle bar.)

84. Re 75.1 (the "churn → delta unreliable → debaters silently compromised" worry) —— largely reassuring, with one caveat:
- 84.1. Delta reads are COMPLETE under churn IF the agent sets its marker to the true line count after each read: the board is append-only, so each read covers `[last-EOF+1 .. current-EOF]` with no gap, and nothing earlier ever mutates. The debaters' rebuttals stayed coherent and on-target to the freshest points throughout (A025–A027 answered B022–B024 precisely) —— strong evidence they missed nothing. The debaters were NOT silently compromised.
- 84.2. The Observer's failure was in its OUTPUT (digest compose), not its INPUT (the delta): it tracked A004–A027 but rendered them badly. "Churn" hit digest QUALITY, not delta COMPLETENESS.
- 84.3. Caveat: completeness depends on correct marker tracking; a mis-tracking agent could skip. The guards are the debaters' re-read-right-before-append + append-only immutability. MA-as-observer (reading the board directly) removes even this theoretical risk on the observation side —— one more point for that default.

85. Re 71 (re-stimulation / idle) —— agreed; spawning a debater purely to re-stimulate is wasteful. The cheaper handling (to add next session): on a genuine idle-not-saturated, MA either (a) adds a debater ONLY if a new stance is independently apt (re-stimulation then a free side-benefit), or (b) NUDGES the existing debaters with a cheap board append ("continue", "consider X", "elaborate Y") —— which wakes them via their watch-wait at a fraction of the cost. Not yet in `debate.md`.

86. Re Status + 78/79 (fixed vs open) —— the ledger:
- 86.1. FIXED (in `debate.md` over the last two turns): delta-reads; dynamic token cap (debaters×4k, 50/100%); compaction hedge incl. the Observer; Director/Manager oversight (MA not asleep, both can close); MA close-sequence (settle → re-read → verdict); Observer digest robustness; header-safe close (`^THE_END$`); 3-digit bulleted labels; real-append TS; MA-creates-and-declares digest; `sprint.md` ↔ `debate.md` cross-fit.
- 86.2. OPEN (next session): (i) DECIDE the default —— MA-as-observer vs hybrid (82–83); (ii) the idle NUDGE option (85); (iii) post-`THE_END` race —— IMPACT fixed (close-sequence catches a straggler before the verdict), race itself residual + benign (77); (iv) bloat —— `debate.md` is `~`17KB, a trim pass is due; (v) optionally a one-line delta-completeness note (84) if deltas stay central.

87. Usability verdict (your closing question): `debate.md` IS practically usable NOW.
- 87.1. In MA-as-observer mode it's solid —— tests 3–5 validated the mechanics and that mode carries NONE of the test-6 Observer bugs. No vital fix blocks it.
- 87.2. Hybrid (Observer SA) mode is usable but carries the digest-degradation risk —— mitigated by MA oversight + the patches, not bulletproof; fine for heavy-concurrent-work runs, otherwise prefer MA-as-observer.
- 87.3. Treat it as a LIVING protocol, not a perfect one: each test sharpened it, and real use will surface more. Don't gate usage on perfection. The one decision worth making early is the default-mode (82); the rest is incremental. So: usable now (prefer MA-as-observer); no vital fix is a prerequisite.

88. Proceeding to `#close` —— the `close_` will carry the OPEN list (86.2) forward for next session.
