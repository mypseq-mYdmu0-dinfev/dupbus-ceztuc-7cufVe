# Response to query_202606030243.md
*Date Added: cause found, fixed, future-proofed; push can be hard-scoped*

18. First thing first —— understood, NO CHAT TEXT
- 18.1. Strictly complying from here: chat shows only the §3.2 declarations (reads / comms / generated), nothing else, unless you say `override`. Everything substantive lives in this file.

19. Re 14 —— "Date Added" change: why it happened, and it's fixed
- 19.1. Cause: my Edit/Write tools save **atomically** —— they write a temporary file then rename it over the original. On APFS, that rename is a *new directory entry*, so Finder stamps a fresh "Date Added" (= the moment of the edit, `02:02`). That is a different attribute from "Date Modified": I'd restored mod time with `touch`, but `touch` doesn't touch "Date Added", so it slipped through.
- 19.2. **Fixed.** I found a reliable way to read/write the live "Date Added" attribute (the exact one Finder's column uses) and have set `google.md`'s back to `2026-05-30 07:35:28` (its creation/commit time —— the most defensible value, since the original was destroyed by my edit). Mod time left untouched. Please verify in Finder.
  - 19.2.1. Note: `mdls`/Spotlight may still display the old value briefly due to index lag, but Finder's column reads the live attribute, which is now correct.
- 19.3. The bigger reassurance —— two layers:
  - 19.3.1. `#sync` will **never touch comms files** (`query_`/`response_`). It only edits `index_otg.md` + `preferences.md`. So your Date-Added ordering of comms files is structurally safe from the mechanism.
  - 19.3.2. For the rarer case where you ask me to **edit an existing comms file**: I'll now capture its "Date Added" *before* editing and restore it *after* —— so my edits won't shift it to the top of your sort. (Creating a *new* comms file is fine as-is: Date Added = now = correctly newest.)

20. Re 15 —— locked: trigger is `#sync` (reads `universal/sync.md`)

21. Re 16.1 —— OTG limitation accepted, plus an architectural fix
- 21.1. Correct: with push blocked, I can't update `index_otg.md`/`preferences.md` while you're OTG —— so we run `#sync` when you're **home**, as you said.
- 21.2. And yes, I can make the allowance **architectural, not prompt-based** (see 22) so that even a glitch can't push the wrong thing.

22. Re 16.2 —— yes, edit+push can be hard-restricted by file, enforced by git itself
- 22.1. Two independent guards, neither relying on my prompt-compliance:
  - 22.1.1. The `#sync` **script stages only explicit paths** —— never `git add -A`. It builds the list from `index_otg.md` and adds only `index_otg.md` + `preferences.md`. Anything else dirty in the tree is simply never staged, so it can't be pushed.
  - 22.1.2. A **pre-commit guard hook** that aborts the commit if any staged file falls outside the allowlist (the paths listed in `index_otg.md`, plus those 2 control files). This is enforced by git, so a glitch in the script can't get past it.
- 22.2. One precision on "only these 2 files": I will only ever *edit* `index_otg.md` + `preferences.md`. `#sync` also has to *commit* the content files **you** edited (e.g. `google.md`) to mint their new SHA —— but it commits them exactly as you left them; I never alter their content. The guard hook's allowlist covers exactly those (index-listed files + the 2 control files) and nothing more.
- 22.3. The hook is scoped to `#sync` only —— it activates via a marker the script sets and removes, so your **manual GH-desktop commits are completely unaffected**.

23. Re 16.3 —— nothing can slip past your "Changes" tab
- 23.1. Because only allowlisted files are ever staged (22.1.1) and the hook rejects anything else (22.1.2), a `#sync` push can contain *only* `index_otg.md`, `preferences.md`, and files you actually edited. So the GH "Changes since last push" view you do check will always show the full, expected set —— there's no path by which a stray edit reaches GH unseen.

24. Re 16.4.3 —— agreed: interactive approval (a) for now
- 24.1. Combined with 22, you get defence in depth: a push happens only when *you* approve it, and even then git will only let allowlisted files through. We can revisit mode (b) later if approving ever feels heavy.

25. Next —— on your go
- 25.1. Both decisions are now settled (`#sync`; mode (a) + the two guards). On your word I'll build: `universal/sync.md` (doc), the `#sync` script, the guard hook, and persist the Date-Added helper —— then run the first real `#sync` on `universal/` for you to validate in OTGC.
- 25.2. Q1 —— shall I proceed to build now, or anything to adjust first?
