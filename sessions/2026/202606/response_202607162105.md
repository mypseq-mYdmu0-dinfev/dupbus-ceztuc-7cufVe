# Response to query_202607162105.md
*Status recap; test file picked; git_move_batch rejected*

---

## Status

1. Issues 1 & 2 —— confirmed done.
- 1.1. Issue 1 (DATS per-file skip): fixed 18 Jun; I verified it survived the 08 Jul edit to `DATS.py`.
- 1.2. Its parked offender `career_close_202606162126.md` no longer exists —— you reconciled it during the month.
- 1.3. A fresh `DATS.py --dry-run` now flags zero offenders, so that reminder is dropped.
- 1.4. Issue 2 (PDF Conversion): closed —— you confirmed Quick Mode "ran perfectly" and Full Mode "yes".
- 1.5. One parked item survives: remark at `#close` that `automator/Archive/PDF Conversion.scpt` is the revert backup, not to be touched.

2. Issues 3–7 —— all still open; your read is right.
- 2.1. Issue 3: unblocked by your `git_history.py`; this turn = pick a test file.
- 2.2. Issue 4: `cp/ccsim/` exists but is empty —— nothing built yet.
- 2.3. Issue 5: you now lean Option 1, which contradicts our June agreement (see 11).
- 2.4. Issue 6: superseded by your new #wrap-is-CCSIM proposal (see 12).
- 2.5. Issue 7: parked —— agreed.

3. Re-read everything that changed.
- 3.1. Re-read `CLAUDE.md`, `glossary.md`, `numbered.md`, `sync.md`.
- 3.2. `numbered.md` changed most —— § Bite-size and `#opt` are new; this response follows them.
- 3.3. §6.1 still defines a CP the old way (root folder holding `CP_index_cc.md`) —— your migration rewrites it.

## Issue 3 —— Test File

4. Recommend `dissertation/archive/MGTK751_A1_formative.md`.
- 4.1. It carries the repo's deepest rename chain —— 4 hops across 11 commits.
- 4.2. It sits in `archive/`, so moving it alone disturbs no live work or parallel session.
- 4.3. It is nested in a subfolder, so it also proves subfolder paths move cleanly.
- 4.4. Target: `cp/dissertation/archive/MGTK751_A1_formative.md`, mirroring the real migration.
- 4.5. Rejected `MGTK751_Official_Files_full.md` —— same 4 hops, but live and listed in `CP_index_cc.md`.
- 4.6. Rejected `MGTK751_DevPlan.md` —— 41 commits but only 1 hop, and heavily in use.

5. Do NOT use `git_move_batch.py` here.
- 5.1. It FLATTENS folder contents by design —— its own docstring states subfolder structure is NOT preserved.
- 5.2. Every CP has subfolders: `career/CCL_Samples`; `mip/Archive`; and dissertation's `addenda`, `archive`, `ethics`, `Lectures`, `Meetings`, `Seminars`, `archive/WCIC_Research`.
- 5.3. At scale it would therefore destroy that structure —— wrong tool for this migration.
- 5.4. Proving it on one file would validate nothing anyway —— single-file and folder-walk are different branches.
- 5.5. Use a plain `git mv`: a pure rename (R100), exactly what `git_history.py`'s `--follow -M20%` follows trivially.
- 5.6. Keep `git_move_batch.py` for its real purpose —— consolidating scattered files INTO one folder, where flattening is intended.

6. Calibrate the pass criterion —— "identical except 1 line" won't hold.
- 6.1. Expect 4 legitimate differences, not 1.
- 6.2. The header count `Git History (n)` becomes n+1.
- 6.3. One new top card appears (the move commit), badged "renamed", with an empty word-diff.
- 6.4. The header sub-line path changes to `cp/dissertation/archive/…`.
- 6.5. The `Generated [TS]` line changes.
- 6.6. The real criterion: all 11 existing cards —— SHAs, dates, subjects, diffs —— stay byte-identical.
- 6.7. Watch one failure signal: a `⚠️ Structural Bridge` divider appearing after the move that wasn't there before.

7. Operational note for the full migration later.
- 7.1. The `cp/*` subfolders exist on disk but are untracked —— git doesn't track empty directories.
- 7.2. So `git mv dissertation cp/dissertation` would nest into it → `cp/dissertation/dissertation/`.
- 7.3. Fix: `rmdir cp/dissertation` first, then `git mv dissertation cp/dissertation` moves the whole tree, structure intact, in one commit.

8. CP list —— you missed none, but one contradiction.
- 8.1. Live CPs are `career/` and `dissertation/` (both hold `CP_index_cc.md`); `mip/` holds none.
- 8.2. So your three (career, mip, dissertation) is complete; `ccsim/` is a new build with no source to move.
- 8.3. Contradiction: in `query_202606181815` you proposed `cp/archive/` for retired CPs, with `mip/` as its first inhabitant.
- 8.4. You have now created `cp/mip/` and list mip beside the live CPs instead.
- 8.5. Q1: still want `cp/archive/`? If yes, mip → `cp/archive/mip/`, and §6.1 needs one carve-out —— `cp/archive/` itself is not a CP, its children are retired CPs.

## Issue 4 —— CCSIM

9. Endorse the SA simulation QA step.
- 9.1. It is strongest exactly where you said —— validating a shrunk pcmd.
- 9.2. Refinement: a lone post-shrink simulation cannot prove "as effective as before", having no baseline.
- 9.3. Better: A/B —— two blind SAs run the SAME scenario, one on the pre-shrink version, one on post; compare outputs.
- 9.4. The SA must not learn what changed or why, else the fresh-eyes premise is contaminated.
- 9.5. §9.03.1 applies —— every SA prompt must state "You're a sub-agent".

10. Endorse the every-turn git hunt, with 3 refinements.
- 10.1. It is cheap and high-value —— `git log` plus `git diff --name-only` against an anchor.
- 10.2. The anchor must survive compaction: store the last-seen SHA in `cp/ccsim/`, or derive it from the previous `ccsim_close_`'s TS.
- 10.3. It must also run `git status` —— a hasty uncommitted edit leaves no commit to find.
- 10.4. Excluding `cp/` is correct —— after migration all CP content lives there, and that is work, not system change.

## Issue 5 —— Backlog

11. Recommend keeping `backlog.md`; the hunt doesn't replace it.
- 11.1. The hunt detects file CHANGES; it cannot detect an idea that never became a diff.
- 11.2. Example: "this pcmd reads confusingly" produces no commit for any hunt to find.
- 11.3. A backlog is durable across sessions; the hunt only ever sees since-last-turn.
- 11.4. Without it, every unresolved item must be re-derived by rescanning `close_` files forever.
- 11.5. They are complementary —— hunt = facts, backlog = intent and open items.
- 11.6. Contradiction: in June you agreed Option 2 and specified `backlog.md`'s entry format in detail.
- 11.7. Q2: keep both (my pick), or genuinely drop the backlog for Option 1 plus the hunt?

## Issue 6 —— #wrap

12. Agree —— #wrap becomes a CCSIM job.
- 12.1. A wrap is a maintenance task, so it belongs to CCSIM by definition.
- 12.2. SA fan-out over the month's `close_` files solves the context blowup cleanly.
- 12.3. This supersedes June's "run #wrap in a fresh session" gate —— drop that.
- 12.4. Q3 (naming clash): §3.3.6 prefixes ALL 4 comms types in a CP chat, giving `ccsim_wrap_[TS].md`.
- 12.5. But a monthly wrap is repo-wide, not CCSIM-scoped —— that prefix would misdescribe it.
- 12.6. Suggest an explicit §3.3.6 exception: `wrap_` stays unprefixed even when produced in a CCSIM session.

## Issue 7 —— Parked

13. Agreed —— park until Issues 3–6 and the `seek/` migration land.
- 13.1. Overlap to watch: §7.2 points at `seek/context/pro_profile.md`, which you say moves to `cp/career/`.
- 13.2. So that migration and Issue 3's ref-updates edit the same table —— sequence them, never run both blind.

## Next

14. Awaiting your go on 4, plus Q1–Q3.
- 14.1. You run `git_history.py` on `dissertation/archive/MGTK751_A1_formative.md` and confirm it's ready.
- 14.2. Next turn I `git mv` it into `cp/dissertation/archive/`, then you re-run and compare against 6.
- 14.3. No SAs dispatched this turn —— nothing warranted one.
