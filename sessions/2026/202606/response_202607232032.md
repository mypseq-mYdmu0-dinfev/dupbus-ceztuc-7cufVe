# Response to query_202607232032.md
*ccsim/CLAUDE.md merge + §4 broadened; skills discussion confirmed; TODOs parked*

---

## Your ccsim/CLAUDE.md Edit

49. Identified + assessed —— all good.
- 49.1. Only lines 1–3 changed: title → "CC System Improvement & Maintenance (CCSIM)"; preamble tightened ("pcmds/scripts/etc."; dropped "user" and "governs —— read it first").
- 49.2. §1–6 untouched; purely cosmetic/terseness —— still fully effective.

## 40.2

50. Correct —— and now remarked in `sync.md`.
- 50.1. Any `cp/` folder = a CP; but only those with a `CP_index_otg.md` are synced → OTGC-available.
- 50.2. Added exactly that line to `sync.md` §Scopes.

## 44

51. `CP_notes.md` —— agreed: merged, then voided.
- 51.1. Right —— it serves OTGC CPs (which can't see `CLAUDE.md`); CCSIM is CAI-only, so it's redundant here.
- 51.2. Imported the only non-redundant bit —— the `ccsim_` prefix rule —— into `CLAUDE.md` §7; Purpose/Key-Files were already implicit.
- 51.3. Voided → `❌_CP_notes.md` (delete at leisure).

52. §4 simulation —— broadened per your correction.
- 52.1. I'd wrongly scoped it to shrink/edit; §4 now covers ANY non-trivial change (create, edit, shrink, delete a helper, restructure).
- 52.2. CCSIM CC ACTIVELY PROPOSES an SA simulation of the change's real-world effect, then improves/debugs from findings —— not a wait-to-be-asked step.
- 52.3. The blind A/B (2 SAs, current vs baseline) is kept as the sub-case for before/after questions; baseline via `git show` or `sandbox/`.

## 48 —— Skills

53. Yes, we discussed "skills" —— your suspicion is right, so NO SA hunt.
- 53.1. It was Issue 7: `response_202606181505.md` §15–18 (your pcmd/context files ARE a bespoke skills system; don't fully migrate) → `response_202606181815.md` §37–41 (the revised conclusion).
- 53.2. Conclusion: thin ALIAS-skills for the `~`8 SITUATIONAL files only (`writing.md`, `profile.md`, `pro_profile.md`, `coding.md`, `cic.md`, `shrink.md`, `plan.md`, `google.md`) —— a one-file `SKILL.md` pointing to the canonical `universal/` file; CC-only; keep `#triggers`; OTG untouched.
- 53.3. Your new parked items EXTEND that (external skills, a safety-gate skill, auto-search) —— captured below.

## Parked —— for `close_` (your sequence)

54. Logged; next session executes these TOP-DOWN. (They also live in this committed `response_`, so they survive even if `#close` slips.)
- 54.1. `#wrap 202606/` FIRST —— 23 days behind.
- 54.2. Temp-file migration per `ccsim_query_202607060438.md` (unread) —— GATES the rest (believed to hold the skills files).
- 54.3. Create "skills" —— builds on Issue 7's alias-skill conclusion (53.2).
- 54.4. Install external skills (e.g. `agentskills.io`); the FIRST skill = one that vets the SAFETY of installing skills.
- 54.5. Have the CCSIM CP and/or `#wrap` auto-search + PROPOSE installing skills that address surfaced problems.
- 54.6. Security caveat on 54.4/54.5 —— external skills are untrusted code + instructions; the safety-gate skill is the right first move, and no external skill should ever be enabled unreviewed.

## Status

55. This turn done; nothing blocking.
- 55.1. I did NOT run the CCSIM every-turn change-hunt (§1) —— this session predates the protocol and its comms are unprefixed (not a fresh `ccsim_` session); your two working-tree edits (`ccsim/CLAUDE.md`, `README.txt`) are already known + intentional. The hunt applies from the next proper `ccsim_` session.
- 55.2. At `#close` I'll transfer the §54 list into the `close_` verbatim.

---

*dlint 🟡 accepted (L9): the period in `"pcmds/scripts/etc."` is original to the quoted phrase (the `etc.` abbreviation in your actual new preamble), so per Hart's rule (§2.1.4) it correctly sits inside the quotes.*
