# Maintenance (03)

*read close_202606070527.md*

## Issue 1 (act if confident)

on DATS.py, does it currently only fix `close_` if ALL unfixed `close_` files have filename TS matching content 1st line TS?
like if there're 5 unfixed, one of them has that unmatched problem, will the script just stop and skip all `close_` fix?
if yes, this makes the other 4 collateral
find out why we decided to code it that way
only if no specific rationale given, proceed to fix the script so it only skip & alert me (like right now) on the unmatched file, fixing the other 4 w/o problem

## Issue 2 (act if confident)

read '/Volumes/FURY 2TB/Fury Documents/PDF Conversion/PDF Conversion.app' and the README.txt beside it
this script works fine for iWork (completing whole task w/o manual intervention) but whenever input = MS files, alerts like `Screenshot 2026-06-18 at 14.33.00.png` will be shown which breaks the automation, requiring me to click
evaluate how to bypass or grant all for MS apps so i don't receive these alerts again where the automation can truly run fully by itself

## Issue 3 (discuss only; same for below)

the root currently already has many folders
i wanna add new CPs but that will lead to even more folders in root
analyse CLAUDE.md & related files on CP
im considering to create `./cp/` and cleanly migrate existing CPs (`career/` & `dissertation/` & `mip/`) into it, inc. all CP paths involved in existing files so they don't break (e.g. sync.md would also need to be updated; possibly more files)
more importantly, i needa make sure all edit history of all files remain readable in GH UI
side question: i realise that in your sense the histories are "never gone", because you read them via git/CLI; but to me, when they're moved/renamed more than twice, they're gone and i cannot read them at GH web UI (opening file on github.com>"History")
i want you to tell me what this is called, so i can conveniently refer to that
e.g. "move [filename] whilst preserving [X]" → find correct naming of [X]

## Issue 4

after issue 3 is fully addressed and `./cp/` created
i wanna create `cp/ccsim/` (stands for "CC System Improvement & Maintenance") which would be our 4th CP
after that we would have to cleanly rename maintenance (01), (02), and this session (03) comms files to prefix `ccsim_`, as well as future CCSIM sessions, which are mainly for our better comms/work, similar to OS/apps firmware update
and would also have to create CLAUDE.md & CP_notes.md for `ccsim/`

## Issue 5

after issue 4 is fully addressed and `./cp/ccsim/` created
Option 1: update close.md & wrap.md to add new section: actively suggest CCSIM issues (optional for each run); e.g. any file changes/additions could have improved this session/this month's sessions?
Option 2: instead of inside `close_`/`wrap_`, instr to append (never edit/delete) issues like Option 1 to a growing file (suggest naming) inside `ccsim/` whilst actively referring to the created `close_`/`wrap_` file to save listing comms files (use item no. instead; e.g. [problem]...; [suggestion]...; ref `close_[TS]` §4.1–4.7 → instead of listing all filenames)

## Issue 6

after issue 5 is fully addressed and either Option 1/2 (or other suggested options) developed
edit wrap.md: when `#wrap` (don't do it now) → also remind to review that file (from issue 5 option 2; or all `close_`/wrap_` files for unresolved) → start a CCSIM session if needed (don't fix at a session end; context window concerns)

## Issue 7

i realised anthropic has created `/skill-creator` and related commands
despite the trigger diff (skills use `/[name]` vs my `#[name]`; skills must be put directly in chat vs mine can be in `query_` and other prompt files), evaluate whether my current prompt/context files (e.g. files in `universal/`; for instance close.md is prompt, profile.md is context) are practically the same or similar to "skills"
then tell if migrating from my current prompt/context files to "skills" provide any meaningful advantages (e.g. more easily/accurately triggered their reads?) and evaluate whether i should consider such migration
if yes, tell how difficult/time-consuming it will be as i imagine my files (currently mostly self-contained single .md) would have to be broken down into numerous files (each "skill" seems a handful of .md) and could be disastrously chaotic for my work
e.g. each "skill" seems to have a SKILL.md, leading to numerous identical filename in my repo

## Your Missions
- Execute actions if any explicitly instructed & no qb
- Tell your thoughts on all above (don't miss any)