# Response to query_202606192022.md

1. **Msg 1** — append as-is to the bottom of any `query_` (fill `[HH:MM]` only):

```
---
⏰ Timed execution — do NOT act on this query_ now. Execute at [HH:MM] SYD; await the `/loop` trigger.
```

2. **Msg 2** — send as a separate message after Msg 1 (fill `[HHMM]` and `[TS]` to match the `query_` filename):

```
/loop 1m On wake: run `TZ='Australia/Sydney' date +"%H%M"` and compare to [HHMM]. If result >= [HHMM], read and execute `sessions/2026/[YYYYMM]/query_[TS].md` (the one marked ⏰), write the `response_` as normal, then stop the loop. If before [HHMM], do nothing.
```

3. Notes:
- 3.1. `[HHMM]` is zero-padded 24 h (e.g. `0015` for 00:15) — string comparison is safe within the same day
- 3.2. Pinning the exact `query_[TS].md` filename in Msg 2 prevents me from confusing it with a later query_ created before the loop fires
- 3.3. `>=` catches the case where I wake a few seconds past the exact minute
