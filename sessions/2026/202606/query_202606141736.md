# On dlint.py

## Diff

Can you briefly tell the diff between all the modes?
Esp `--quick`, how's it diff? How can it be quicker? Does it also burn less tokens/context?

## Coverage

i notice you linted the `close_`
was that by system or by your own intention?
im thinking maybe we should expand the coverage of `--quick` dlint.py (maybe by dlint_hook.py? not sure) from currently only `response_` to all comms files (aka + `close_` + `wrap_`)
and btw does the current hook only work for a file w/ filename starting from `response_`? because CP sessions have diff prefix
if my concern is true, i propose to make the `--quick` hook to take effect on ANY .md w/ filename containing `response_` `close_` `wrap_` (if you agree)

## Deliverables Hook

and then i just thought of a new idea to mechanically force run the full dlint.py on deliverables
you mentioned that deliverables might not have patterns so the hook won't work
but actually it does and it's simple:
basically any .md w/ filenames NOT containing `response_` `close_` `wrap_` are 99% deliverables
the 1% left are our own system files e.g. c.md, and worst case there'll only be yellow flags like for not being #numbered
which i can grant approval to accept those yellow flags on the spot (don't rule that in; might lead to accidental yellow acceptance)
in the absolute worst case, i just specifically instruct CC to draft system files (which are supposed to be .md) in .txt then i manually convert them to .md for the nuclear override

## OTGC Compatibility

Also i notice a little gap: OTGC can't run dlint.py (at least not w/ current setup)
address below:
- since it's on GH, if we add its URL to `universal/index_otg.md` (no need to read it; it's injected by userPref every turn), can OTGC run it?
- if no (can't run), should we develop an additional .md file, pointed by writing.md only for CWI/OTGC (non-CC)?
- if yes (can run), can she run it w/o reading it?
- if yes (can run w/o reading), suggest how but don't act yet
- if no (can't run w/o reading), should we make variant of it (e.g. `dlint_otg.py`) that is a bit more lightweight (so it won't hurt as much for OTGC to read+run)? suggest how but don't act yet

## Your Missions
- Don't act on anything yet
- Tell your thoughts on all above (don't miss any)
- Don't edit the created `close_` until i ask to