# Response to query_202606060500.md
*Don't copy the old workspace; use a repo-level one + the gscpt venv*

1. Q1 —— neither "copy & rename into gscpt"; make a fresh repo-level one (I created it)
- 1.1. The old `Default.code-workspace` is bare: it just opens `/VS Code` (`.`) + a *relative* `../../../../Desktop`, with empty settings (no interpreter pinned). Copying it into `gscpt/` would make `.` = gscpt but break that Desktop relative path, and it still wouldn't see the rest of the repo.
- 1.2. I created `dupbus-ceztuc-7cufVe/dupbus.code-workspace` —— opens the whole repo (`.`) + your Desktop, and pins the interpreter to `gscpt/.venv/bin/python`. Just double-click it / File → Open Workspace.
- 1.3. Two equally fine alternatives: (a) skip the workspace file entirely —— File → Open Folder → the repo, then `Cmd+Shift+P` → "Python: Select Interpreter" → `gscpt/.venv/bin/python` (VS Code remembers per-folder); (b) if you'd rather open ONLY the scripts, tell me and I'll point the workspace at `gscpt/` instead of the repo root.

2. Q2 —— to run as it usually did
- 2.1. **Select the interpreter `gscpt/.venv/bin/python`** (the workspace in 1.2 already sets it). This matters for `trade_records_processor` + `transport_records_processor`, which need **pandas**; the other 5 are stdlib-only and run on any Python 3, but use the venv for consistency.
- 2.2. **Put each script's INPUT files in `gscpt/`** —— they now read from their own folder: battery `.txt`s, shopping receipts, trade `U11885223*.csv`, transport `Contactless Payment*.csv`, and ajap's timeframe instruction `.txt`/`.md`. (This is open confirm 12.5a —— say the word if you'd prefer a `gscpt/data/` subfolder and I'll repoint them.)
- 2.3. No CWD worries: the migrated scripts are `__file__`-relative now, so VS Code's "Run Python File" works regardless of the active folder.
- 2.4. Outputs unchanged in spirit: most land in `gscpt/`; `shopping` + `typographic` still write to `/Volumes/FURY 2TB/Fury Downloads` (open confirm 12.5b —— keep or repoint?).
- 2.5. The old `/VS Code/.venv` (602MB) is no longer needed by these scripts once `gscpt/.venv` is selected —— safe to delete later (your call), or keep it for any other scripts that still live under `/VS Code`.

3. If you don't want `dupbus.code-workspace` in the repo, just delete it (or I'll move it into `gscpt/`).
