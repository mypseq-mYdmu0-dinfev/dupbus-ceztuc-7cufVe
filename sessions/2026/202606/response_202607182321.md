# Response to query_202607182321.md
*Migration done (114 files, R100, verified); 3 aliases flagged*

---

## Commit Slip

27. Re preamble —— a slip, not structural; hardened.
- 27.1. What: I committed `response_202607182249.md` alone and left the paired `query_202607182249.md` (it landed in your `cf0089f "done"`).
- 27.2. Cause: I staged the response by name and forgot §9.05.2's "± this turn's query_" clause —— a discipline lapse, not a tooling limit.
- 27.3. Fix now: from here I stage query_ + response_ together in the one turn-end commit; done this turn.
- 27.4. Optional harden: a pre-commit lint that refuses a `response_[TS]` whose sibling `query_[TS]` is still untracked —— cheap, guarantees the pairing. Say the word and it's a CCSIM item.

## Issue 17

28. Re 17 —— the `git_history.py` caveat, in coding.md format.
- 28.1. what: it can draw a "Structural Bridge" —— a content-unrelated, GitHub-style GUESS at lineage —— when `--follow` dead-ends on a severed rename.
- 28.2. if-unfixed: a bridged card-chain could be misread as verified history (it IS labelled, but still a guess).
- 28.3. pre-fix-question: none for us —— our moves are R100, so `--follow` never dead-ends and no bridge is ever drawn.
- 28.4. risk-if-pushed: n/a —— no code change proposed.
- 28.5. outcome: DEFERRED —— a display caveat, not a migration issue; log to backlog if you want it to flag guessed-vs-verified more loudly.

## Migration —— Done

29. Executed the full batch —— 114 files, 3 move-only commits, pushed.
- 29.1. career → `cp/career/` (11 files, `d3047c8`); dissertation → `cp/dissertation/` (83, `f42d2d2`); mip → `cp/archive/mip/` (20, `3d25333`).
- 29.2. Every staged change was R100; the script aborts on any non-R100 —— none occurred.
- 29.3. Spot-verified the crown jewels: `Official_Files_full` (8 commits, all 4 prior hops + the move), `DevPlan` (42 commits) —— full history follows to the new paths.
- 29.4. Source folders now hold ONLY the 3 excluded aliases (mip is empty).

30. Re `##SA` —— honoured, but constrained for a no-margin task.
- 30.1. I dispatched one SA, but the logic sat in a deterministic, self-verifying script (R100-gate + `--follow` check) —— near-zero SA judgment.
- 30.2. The script COMMITS but never pushes; I reviewed, spot-verified, and pushed myself —— the irreversible step stayed with me.
- 30.3. Net: your context stayed lean (the SA absorbed the run) whilst "no margin for mistakes" ran under my direct eyes.

31. Flag —— 3 macOS ALIAS files I deliberately did NOT migrate (Finder shortcuts, not CP content).
- 31.1. `career/:seek:context:` —— DEAD (its target `seek/context/` was deleted) —— recommend void.
- 31.2. `career/Resumes` —— live alias to external `/Volumes/FURY 2TB/IYM/…/Resumes`.
- 31.3. `dissertation/Non-MD Files` —— alias.
- 31.4. Q1: for each —— void, keep-in-place, or copy into its `cp/` CP? My default: void the dead one; your call on the other two.
- 31.5. Once decided, the near-empty `career/`, `dissertation/`, `mip/` (+ their `.DS_Store`) can go.

32. Vestigial placeholders to sweep later —— harmless.
- 32.1. `cp/mip/placeholder.md` (real mip now lives in `cp/archive/mip/`), plus `placeholder.md` in `cp/`, `cp/dissertation/`, `cp/career/int/c_int/`.
- 32.2. I'll void them during the ref-updates unless you want any kept.

## Next —— Ref-Updates

33. Queued for a SEPARATE commit (per 3.3) —— the migration moved files, not the references to them.
- 33.1. Root `c.md` §6.1 —— every folder in `cp/` is a CP, EXCEPT `cp/archive/` (its children are retired CPs).
- 33.2. `sync.md` —— generalise: `#sync` covers `universal/` + ALL `cp/` folders, no hard-coded names.
- 33.3. Each moved `CP_index_cc.md` / `CP_index_otg.md` / `CLAUDE.md` —— repoint internal `career/…`, `dissertation/…` paths to `cp/…`.
- 33.4. Then `#sync` re-pins OTG permalinks at the new paths (old SHAs still resolve until then).
- 33.5. Edits, so they must NOT ride with any move (coding.md git discipline) —— next turn, on your go.

## Discussion

34. Re 21 —— agreed; `cp/ccsim/sandbox/` it is.
- 34.1. `git show <SHA>:<path>` stays the default baseline; `sandbox/` holds the exceptions —— too-big-to-inline baselines AND non-historic A/B versions (new pcmds with no git history).
- 34.2. Confirmed as my sole directly-deletable folder —— I keep it clean myself; only scratch/`prev_` files ever live there.

35. Re 22 —— agreed; `last_seen.md` stores SHA + TS, so a CCSIM session sees how long since the last audit.

36. Re 23 —— both (backlog = intent, hunt = facts); 11.6 was recapped last turn.

37. Re 24.2 —— correction taken; my inversion fixed.
- 37.1. Rule: on `#wrap`, if you report context >50% full, I suggest a fresh session; ≤50% full, I proceed.
- 37.2. You always state "% full", so I read your number —— no self-measurement needed.
- 37.3. Re 24.3/24.4 —— agreed; we calibrate the exact number on June's real `#wrap` once the plate clears.

## CCSIM Setup —— Held

38. Re 26.3 —— I did NOT scaffold `cp/ccsim/` this turn, by your own 10.1.
- 38.1. You said operationalise the hunt "LATER, when we're writing `ccsim/CLAUDE.md` in full".
- 38.2. That file must bake in the agreed pieces —— recent-5 close index, git-hunt, backlog + sandbox rules, SA-simulation QA —— a dedicated writing pass, not a cleared quick task.
- 38.3. Propose it as the sub-task right after the ref-updates.

39. No blocker outstanding —— only Q1 (alias handling) awaits, and it doesn't gate the ref-updates.
