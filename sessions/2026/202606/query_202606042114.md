## Intention
i wanna build a new function called #order
and hence we should create `universal/order.md`
when prompted `#order [file_type] in [folder_name]`, make these files' "Date Added" identical to the TS in each of its filename
if any doesn't have TS in filename, stop all (not just that) and alert me as it's a serious error
i.e. scan target files for any missing TS before action

## Rationale
i found many files' Date Added shift by unknown reason which affect my browsing
e.g. dissertation_query_202606041852.md's Date Added is 202606041955, 2 hours from its TS
Update: i seem to find the reason —— this very file, added to `202606/` at 202606042114, shifted both its Date Modified and Date Added as i edit it, which the latter is illogical but i still dont know why it happened

## Rules
- if [file_type] undefined, assume `query`/`query_` (and `[CP]_query_` if any; same for other file_type —— incl. CP sessions), which is most prone to this unknown error
- if [file_type] defined as `response`/`response_`, stop and confirm if mistaken, because `response_` files' TS match the `query_` files they each respond to and are inherently non-identical to Date Added
- if [file_type] defined as `close`/`close_` or `wrap`/`wrap_`: for each, check if filename TS is identical to the 2nd TS (end of range) on its 1st line of content; if yes, proceed; if no, stop & alert (needa edit either the filename TS or the content TS to ensure match)
- if [folder_name] undefined, assume current month (e.g. `202606/`) in `sessions/`
- if [folder_name] defined but no month (e.g. `investigation/`), assume current month (e.g. `investigation/2026/202606/`)
- if [folder_name] defined only month but no folder (e.g. `202606/`), assume `sessions/` (e.g. `sessions/2026/202606/`)

## Missions
- tell your thoughts first; simultaneously scan filenames and Date Added of all files in `sessions/` and `seek/investigation/` to see if any clues/patterns/alerts noteworthy; also note any Date Added are even longer after or even before its TS for isolated fix (edge case)
- when done, only if agree and no qb, proceed to create the file