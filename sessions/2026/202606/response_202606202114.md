# Response to query_202606202114.md
*Wait-mechanism bug fixed (foreground, not background); starting test 3*

33. You're right on both counts —— and the wrong inference was mine, not the harness's:
- 33.1. I blamed an unavailable resume path (`SendMessage`). The real bug was that I ran the SA's idle wait with `run_in_background` —— a backgrounded wait makes the SA come to rest and hand control up to MA, which then genuinely can't resume it. That is a mistake in MY wait design, not a limit of the model.
- 33.2. Precisely as you said: a FOREGROUND blocking wait keeps the SA inside one continuous execution, so it loops and is woken by board changes —— the same "run, pause, run again on routine re-check" that AJAP relies on. No extra control file; the SA watches `debate_` itself.

34. The fix (now in `debate.md`):
- 34.1. The idle wait is a FOREGROUND board-watch —— poll `debate_`'s mtime and exit the instant it changes (or after `~`4.5 min), run as a normal Bash call with `timeout: 300000`, NEVER `run_in_background`. Woken by any change (new Debater block, `## User` block, or `THE_END`).
- 34.2. I verified the primitives before writing: foreground `sleep` actually WORKS here (both shell and Python) —— the tool's "blocked" note doesn't bite in this session; `fswatch` isn't installed, so a `stat -f %m` poll loop is the portable watcher. (Your "5–10 min" option is supported too —— just lengthen the loop.)
- 34.3. Updated three places to match: the Wait mechanism, the SA sustain-loop's "nothing new" branch, and the SA briefing template.

35. Starting test 3 now —— 2 Sonnet SAs on the fixed sustaining loop. Same arrangement: I observe and stay out of the way; you ask me (chat) to add a debater mid-way, then post your `## User` `STOP` when done; I adjourn gracefully (`THE_END` → confirm SAs stop → Observer → this verdict).
