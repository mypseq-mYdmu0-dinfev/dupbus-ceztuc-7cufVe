# Response to query_202607182249.md
*Note followed; test move done (R100); baseline via git, not prev_ files*

---

## Note

15. The note validates my method —— I follow it fully.
- 15.1. Its §2 (move-only commit, zero edits, R100) = exactly my plan; the move below proves it.
- 15.2. Its §4 (never tidy whilst moving) = my "edits in a separate commit" from last turn.
- 15.3. Its §5 (verify with `--follow`) = what I just ran.
- 15.4. Biggest new intel —— §6: `career/` is PARTIALLY migrated already.
  - 15.4.1. The AJAP #inv session moved 7 live-context files + `seek/interviews/` → `cp/career/int/` (`956ad90`), histories intact.
  - 15.4.2. So the full career move must EXCLUDE those —— re-moving moved files is where history severs.
  - 15.4.3. Revises my last-turn "career has minimal files": part is done, the rest is yours to build.

## Test Move

16. Done and pushed —— `47faeea`.
- 16.1. `dissertation/archive/MGTK751_A1_formative.md` → `cp/dissertation/archive/`.
- 16.2. Committed as **R100** (100% similarity) —— the pure-rename shape.
- 16.3. `--follow` shows 12 commits on the new path (your 11 + the move); all history intact.
- 16.4. Over to you —— run `git_history.py` on the new path and compare.

## Win Signal

17. Re 6, correct —— the win is that no commit CARD is lost.
- 17.1. Expect all 11 original cards byte-identical.
- 17.2. Plus one new top card (the move, `47faeea`), badged "renamed", empty diff.
- 17.3. Header count 11→12 and the path/`Generated` lines change —— expected, not losses.
- 17.4. One red flag to check: NO `⚠️ Structural Bridge` divider that wasn't there before.

## Issue 3

18. Re 3.1, sync.md re-read —— one trivial change.
- 18.1. `06197fc` only dropped the stale `seek/context/*` example; no logic change.
- 18.2. The generalisation (every `cp/` folder = a CP scope, no hard-coding) stays the post-migration edit.
19. Re 3.3, agreed —— all ref-file updates land in their own reviewable commit.

## Issue 8

20. Re 8.5, locked in —— `cp/archive/` is the SOLE non-CP in `cp/`.
- 20.1. §6.1 rewrite (later): every folder directly under `cp/` is a CP, EXCEPT `cp/archive/`.
- 20.2. `cp/archive/`'s children ARE CPs, but retired/legacy.
- 20.3. So `mip/` still needs moving to `cp/archive/mip/` —— you currently have `cp/mip/`.

## Issue 9

21. Re 9.3, I'll push back —— no `prev_` file needed; git already holds the baseline.
- 21.1. An SA reads the pre-shrink state directly: `git show <pre-shrink-SHA>:<path>`.
- 21.2. So dispatch 2 blind SAs —— one fed the current file, one fed the `git show` baseline; neither sees the other.
- 21.3. Cleaner than materialising `cp/ccsim/name/prev_X.md` —— nothing to create, delete, or override.
- 21.4. Fewer moving parts = the fresh-eyes premise cannot be contaminated by a stray file.
- 21.5. Fallback only if a file is too big to pass inline —— then a `prev_` in `cp/ccsim/baseline/`, deletable as you said; unlikely needed.

## Issue 10

22. Re 10.2, one tiny single file —— not multiple, no subfolder.
- 22.1. Store the last-audited SHA in `cp/ccsim/last_seen.md` (one line: SHA + TS).
- 22.2. Each turn: diff `HEAD` against it, surface changes, then rewrite it.
- 22.3. Compaction-safe —— it lives on disk, not in context.

## Issue 11

23. Re 11.6, the June recap, briefly.
- 23.1. In `query_202606181815` §13.3 you set `backlog.md`'s entry format (`## close_[TS] — title` / Problem / Suggestion / Ref).
- 23.2. And §13.4 you agreed append-only resolution (never delete; append `✅ RESOLVED`).
- 23.3. So "keep both" (11.7) simply honours that —— nothing to reconcile.

## Issue 12

24. Re 12.2, agree —— gate `#wrap` on context; still discussing the number.
- 24.1. Caveat: I can't reliably self-measure remaining context —— the gate must rely on YOU stating it (or the harness indicator).
- 24.2. Mechanism: on `#wrap`, if you haven't given the remaining %, I ask; below threshold → I suggest a fresh session.
- 24.3. Your 50%-remaining (`~`500k tk) default is reasonable but tunable —— an SA-fan-out wrap spends most tokens in the SAs, so the main-session cost may be well under 50%.
- 24.4. Suggest we fix the number empirically after the first real CCSIM `#wrap`.

## Agreed

25. Confirmed as you stated, no action —— 4 (file), 5, 6, 7, 9.4, 10.1, 10.3/4, 11.7, 12.6.

## Next

26. Your compare, then we scale.
- 26.1. Run `git_history.py` on `cp/dissertation/archive/MGTK751_A1_formative.md`; if before/after match except the move card → win.
- 26.2. Then batch-migrate the rest into `cp/` as move-only commits (note §2), EXCLUDING the already-placed `cp/career/{7 files, int/}` (note §6).
- 26.3. No SAs dispatched this turn —— nothing warranted one.
