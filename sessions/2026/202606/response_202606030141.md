# Response to query_202606030141.md
*Trial URL ready; one push-permission blocker*

7. Re 2.1.1 —— yes, I retrieve the SHA for you; no GH desktop, ever
- 7.1. The SHA you "randomly" tried —— `62dc81bce69aa81b074ca6a280ff07954ef301cf` —— is actually the **current `HEAD`** and is already on `origin/main`. So your manual find happened to land on the live commit; you'll never need to do that by hand.
- 7.2. I get it programmatically with `git rev-parse HEAD` (one command, instant) after committing, then string-build every URL. So the full chain is fully automatable on my side.

8. Re 2.1.2 —— query-param is dead, confirmed
- 8.1. You saw `?v=...` work in your browser but CWI still served v01. That matches the known trap: many caches **ignore the query string** and key only on the path —— so `?v=` changes nothing for CWI.
- 8.2. A SHA permalink changes the **path itself** (`/main/` → `/<SHA>/`), which is a genuinely different resource → guaranteed cache miss. That's why 2.1.1 is the only reliable route. We drop 2.1.2.

9. Re 4 —— your understanding is correct, with one important caveat
- 9.1. Correct, exactly as you wrote: I (a) get the right SHA, (b) rewrite every file URL in `index_otg.md` to that SHA, (c) rewrite `index_otg.md`'s own SHA inside its URL in `preferences.md`, (d) hand you that one `preferences.md`/userPref string to paste —— usable in OTGC too.
- 9.2. And yes —— I can `git add`/`commit`/`push` to GH for you from this local CC session with no GH desktop click. Your mental model is right; this is what makes `#otg` worth building.
- 9.3. **B1 —— push to `main` is currently blocked by the harness.** When I tried to commit+push the trial sentinel, the Claude Code permission classifier denied a direct push to the default branch (`main`) without your explicit authorisation, and rolled the commit back. So right now I *cannot* push, which is the engine the whole `#otg` mechanism needs.
  - 9.3.1. To unblock: add a Bash permission rule allowing `git push` (and `git commit`) in your settings —— I can set this up for you via the config skill if you say go, or you can approve the push interactively each time.
  - 9.3.2. Without this, `#otg` can still regenerate `index_otg.md`/`preferences.md` locally, but *you'd* have to push via GH desktop —— defeating the main convenience. So I'd resolve B1 before building.

10. Trial run —— testable by you right now (no push needed)
- 10.1. `62dc81b` is already on GH and holds the **latest** `google.md`, so you can test SHA-pinned fetching immediately. Paste this into a **fresh** CWI/OTGC chat (fresh = avoids any same-conversation cache):
  - 10.1.1. `https://raw.githubusercontent.com/mypseq-mYdmu0-dinfev/dupbus-ceztuc-7cufVe/62dc81bce69aa81b074ca6a280ff07954ef301cf/universal/google.md`
- 10.2. What to check: CWI should show the **current** `google.md` —— it ends with a "Calendars" section listing `Deadlines`, `Notices`, `General Events` and the line *"prioritise those with locations"*. If you instead still see v01, tell me —— that would mean CWI caches even across differing paths and we'd rethink.
- 10.3. I had prepared a crisper version (a unique sentinel line so there's zero ambiguity), but that needs the push in B1. Once B1 is cleared I'll push a sentinel and give you a second URL where you simply look for one unmistakable trial line.

11. Re 6.1 —— done
- 11.1. Created `sessions/2026/202606/` and moved all three June comms files there (`query_...0103`, `response_...0103`, `query_...0141`) per Move Rule —— originals voided in `202605/` as `❌_..._moved_202606.md` for your manual deletion.
- 11.2. This response sits correctly in `202606/`. No `#wrap` performed —— you'll do May's wrap in another session.

12. Re 6.2–5 —— parked until the trial confirms, as you asked
- 12.1. Once you confirm 10.1.1 reads latest in CWI, and you decide on B1 (9.3), I'll lock the keyword (`#otg` default `universal/`), the scope syntax (`#otg career` ⇒ `career/` + its `seek/context/` deps), then build the script.
- 12.2. Your call on two things: (a) test result of 10.1.1; (b) B1 —— shall I add the `git push`/`commit` permission rule via the config skill, or will you approve pushes interactively?
