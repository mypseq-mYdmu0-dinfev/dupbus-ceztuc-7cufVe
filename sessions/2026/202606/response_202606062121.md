# Response to query_202606062121.md
*V/A & VF/A reformatted; analytics intention checks out*

5. Done —— reformatted the two columns
- 5.1. `V/A` = `V ÷ Applied`, plain number, 2 dp, NO `%` (e.g. `3.75`) —— avg violations per Applied job.
- 5.2. `VF/A` = `VF ÷ Applied × 100`, 0 dp, WITH `%` (e.g. `25%`) —— error rate (share of Applied jobs with ≥1 violation).
- 5.3. Both blank when a timeframe has 0 Applied. Compiles; verified: `A=8,V=30,VF=2 → 3.75 / 25%`; `A=3,V=1,VF=1 → 0.33 / 33%`.

6. Re your analytics intention —— coherent, no gaps
- 6.1. `VF/A` is a clean per-session quality KPI (what fraction of applied jobs were flagged). `V/A` is density (avg symbols per applied job); at 2 dp it reads naturally as "≈ per 100 applied" when you mentally ×100. Together they separate "how many jobs had issues" from "how heavy the issues were" —— good pairing.
- 6.2. Optional extra (NOT a gap, only if ever useful): `V ÷ VF` would give avg violations among the flagged files only (severity of the bad ones). Not added unless you ask.
- 6.3. Reminder of the one definitional point (already by design): `V` counts the symbols `—`/`–`/`+` AFTER the "## N. Cover Letter" marker —— so one symbol-heavy file lifts `V/A` a lot but adds only `+1` to `VF`. That's precisely why both metrics earn their place.

7. Re 3 —— noted, nothing more to do: 3.1 confirmed (per-timeframe Applied), 3.3 archive behaviour is intentional (kept as-is), 3.4 you'll test.

8. Nothing committed/pushed.
