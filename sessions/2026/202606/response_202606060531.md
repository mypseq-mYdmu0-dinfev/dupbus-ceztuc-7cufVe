# Response to query_202606060531.md
*All 4 leftovers can go; typographic edited; 2 scripts output outside; tests pass*

8. Re 6.3 —— verdict on the `/VS Code` leftovers
- 8.1. `Temporary Codes/` → delete (you confirmed not needed).
- 8.2. `Default.code-workspace` → delete; CIIW confirmed —— it's superseded by `gscpt/GH.code-workspace`.
- 8.3. `.vscode/` → delete; `settings.json` is empty `{}` and `tasks.json` is just a "Run Processing Sketch" task using `processing-java` (the Processing creative-coding language) —— nothing to do with your Python scripts. Not worth migrating.
- 8.4. `requirements.txt` → don't migrate; it's the bloated 112-package list. I created a LEAN `gscpt/requirements.txt` (`pandas==3.0.3` only; numpy/dateutil/six arrive as its deps) so you can rebuild the venv anytime. So: a fresh, minimal one already exists —— the old one isn't needed.
- 8.5. Net: from `/VS Code` you can delete `.vscode/`, `Default.code-workspace`, `Temporary Codes/`, and the 602MB `.venv`. Keep `Culous Codes/` as the backup until you're satisfied everything runs from `gscpt/`. (These are outside the repo —— delete in Finder; I didn't touch them.)

9. New Request —— output-outside scripts: TWO, not one
- 9.1. `typographic_processor.py` AND `shopping_records_processor.py` both write to `/Fury Downloads`.
- 9.2. Edited `typographic_processor.py` per your spec: output now lands beside the input (`gscpt/`) as `[name]_processed[ext]` (extension follows the input), and it skips its own `_processed` files on re-run. Tested: `He said "hello" and it's John's.` → a `_processed` file with curly quotes; original untouched.
- 9.3. I did NOT touch `shopping_records_processor.py` —— your `_processed` / "format follows input" convention doesn't fit it (it converts receipts into a standardised `Note,Amount` CSV, a different format + name). Q1: do you want shopping's output moved into `gscpt/` too? If so, what naming —— keep its CSV name, or a convention you prefer? Tell me and I'll edit.

10. Testing —— results (no fixes made, per your instruction)
- 10.1. ✅ `typographic`: functional test passed (9.2).
- 10.2. ✅ `DATS`: `--dry-run` clean —— close type skipped (the legacy investigation closes), 39 query+wrap files to fix.
- 10.3. ✅ All 8 scripts compile under `gscpt/.venv`, and `pandas` imports (3.0.3) —— so the migration broke no imports or paths.
- 10.4. The other 6 (`battery`, `shopping`, `trade`, `transport`, `ajap`, `DAMF`) each need their SPECIFIC input present to truly run, and they have real side effects (write CSVs; `ajap` scans your live `seek/gcl`; `DAMF` sets a real file's Date Added). I did NOT blind-run them —— running against the current stray `gscpt/` files would process the wrong files or touch real data.
- 10.5. Q2 —— to test those properly, one-by-one as you suggested: drop that ONE script's input into `gscpt/`, I run it, we verify, clear it, next. Want to do that now (you supply each input), or is the compile+import-clean enough for now?

11. Nothing committed/pushed by me. Awaiting Q1 (shopping output) + Q2 (test the data scripts now?).
