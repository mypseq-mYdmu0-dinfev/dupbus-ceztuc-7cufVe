# Response to "explain why you reset the numbers in response_ ... compaction? one-off slip? ... if structural problem, directly edit numbered.md ... QMM"
*Why the reset happened*

11. Cause —— a one-off slip, not compaction
- 11.1. No compaction occurred this session, so that's not it.
- 11.2. The slip: I defaulted to starting every `response_` at pt 1, instead of running the numbered.md continuity check (§44 —— continue at n+1 when the `query_` replies to a prior `response_`). `query_202606101933`'s reply (`query_202606102009`) cited "re 2 / re 3 / re 4 / re 5", so it WAS replying to `response_...1933` (which ended at pt 5) and should have continued from pt 6 —— which is now the corrected state.
- 11.3. My earlier resets this session were actually valid: the inputs behind `response_...1821`, `...1852`, and `...1933` did NOT cite the word `response_` nor any prior pt number, so §40 ("input is NOT replying a response") permitted a reset to pt 1. Only `...2009` breached the rule.

12. Structural? No —— so I did NOT edit numbered.md
- 12.1. §38 ("no number repetition throughout an issue"), §40 (when a reset is allowed), and §44 (continue at n+1) already cover this unambiguously. The fault was my execution, not the spec, so editing numbered.md would be unwarranted.
- 12.2. If you nonetheless want belt-and-braces, I'd add ONE line under § Reply, e.g. "CC: before numbering any `response_`, check §40 —— if the `query_` cites `response_` or any prior pt no., DON'T reset; continue at n+1." Say the word and I'll add it; otherwise I leave numbered.md as is.

13. QMM
- 13.1. ⚠️ Unrecognised —— not in glossary.md, not a known trigger. Possible typo for `QB`? Tell me what QMM means (or confirm `QB`) and I'll action it.
