# Response to query_202606132319.md
*README added, MODES migrated, Hart & hyphen/#numbered refined; one open question*

25. Avoid Reading .py —— agree, README created
- 25.1. Created `cscpt/README.md`: states every script here is RUN-not-READ (read only when polishing it), names `dlint.py`, and points to `writing.md` for usage. This generalises cleanly as you add more scripts —— no per-file mandate needed.
- 25.2. Strength of the existing mandate —— root §8.5 ("if a folder has README, read it") covers a CC that BROWSES `cscpt/`. But the real safeguard is `writing.md` itself, which already says "RUN it, never read it" and is read at deliverable time. So it is double-covered; the token cost of an `ls`/small-README read is trivial versus reading a 3.4k-token script, so the README approach helps rather than hurts.
- 25.3. Optional reinforcement (your call, not done) —— if you want it airtight, add to root §8.5 a clause like "scripts under `cscpt/` are run-not-read (see its README)". I judged it unnecessary given the two existing hooks; say the word if you'd like it.

26. Re "Run AFTER … all-output use" / MODES —— migrated
- 26.1. Agreed. I removed the operational intro + the whole § MODES from `dlint.py` and moved the three modes (FULL file / `--text` / `--quick`) into `writing.md` § Deliverable Lint, since that file IS read at deliverable time and the script is not.
- 26.2. What stayed in `dlint.py` —— only polishing-facing material (WHAT IT DOES, the rule-tier summary that mirrors the code, EXIT CODE, the POLISH NOTE). Nothing a caller needs at deliverable time remains trapped in the script.

27. Comments —— restored. Added back the explanatory inline comments in `convert_quotes` (the `# next " …` toggle note and the open/close-single rationale) plus a few more, since `dlint.py` IS read when polishing.

28. Hart's Quotation —— reworked exactly to your rule. Flags ONLY the char immediately before a closing quote: a comma → RED; a LONE period → YELLOW; an ellipsis (`..`/`...`) is exempt. Verified against your five examples: `test."`→yellow, `test..."`→none, `test. still..."`→none, `test,"`→red, `test, still..."`→none. All pass.

29. Manual Edits —— spotted & inferred (I agree with all)
- 29.1. `dlint.py` L3 "Claude-only" → "CC-only" —— glossary consistency (CC = Claude Code). Agree.
- 29.2. `writing.md` —— you split "NEVER lint the whole `response_`/comms files" onto its own bullet (it was nested inside my `--text` line). Inference: it applies to BOTH modes, so it deserves top-level visibility. Agree (I kept it, now qualified "in FULL mode" since `--quick` IS safe on comms).
- 29.3. `writing.md` closing bullet "rules it cannot lint" → "rules HEREIN, especially those it cannot lint". Inference: stress that dlint is a safety-net, NOT a substitute for complying with every `writing.md` rule. Agree.
- 29.4. (earlier turn) root §3.7.3 → "ANY deliverable: MUST follow `writing.md` & run `dlint.py` before output to user"; §7.2 row → "Right before writing ANY deliverables". Inference: leanness + shifting emphasis to reading `writing.md` BEFORE drafting (so rules guide the draft, not just a post-check). Agree —— both stronger.

30. Re 19.5 —— noted: you never write fiction, so a comma inside a closing quote stays RED unconditionally. No other edge case found; left as red.

31. Re 19.6 —— implemented and broadened to enforce #numbered. The hyphen check now YELLOW-flags `-` + space + NON-number, catching both a dash substitute (`word - word`) and a non-#numbered bullet (`- text`, `  - text`); it stays silent when a number follows (`- 1.`, `- 1.2.`) or the hyphen is intra-word (`re-use`, `cutting-edge`). Verified: `  - testing`→yellow, `  - 1. testing`→none, `- another`→yellow, `5 - 3`→none.

32. Re 20.2 —— done. Trimmed `writing.md`'s GenAI list to a short illustrative set (kept a handful of words + three phrase examples) pointing to `dlint.py` for the full enforced list; KEPT the "web_search … could be dated" line; and added a POLISH NOTE in `dlint.py` so the next CC polishing it will web_search and extend the list.

33. Re 21.2 —— confirmed & one open decision
- 33.1. Yes —— no separate lightweight script is needed; the in-built `--quick` mode covers it. It now ALSO catches `- testing` and Hart violations (your top daily-comms pains), and stays silent on the rare authorised non-#numbered list since those are YELLOW (a future CC judges them acceptable, as you note).
- 33.2. WHERE to enforce —— this is the one thing I did NOT auto-edit, because the choice is consequential and you hedged ("possibly root CLAUDE.md?"). I am asking you via a separate question (hook vs root mandate vs both). My recommendation: a Stop hook, because the whole point is that a manual mandate relies on the very memory that fails. Once you pick, I will wire it next turn.

34. Re 21.3 —— latency: negligible. One extra shell call per output; `python3 … --quick` is `~`100–200 ms (interpreter start + a tiny file). It will not noticeably slow responses. The only real time cost is the fix-rerun loop when flags actually appear —— which is the intended work, not overhead.

35. Re 21.4 —— noted you enabled Bypass permissions so I can edit `settings.json`; I have held the hook wiring pending your answer in 33.2 so I build the mechanism you actually want.

36. Execution summary
- 36.1. `cscpt/dlint.py` —— docstring trimmed to polishing-only + POLISH web_search note; comments restored; `_hart` reworked (comma-RED / lone-period-YELLOW / ellipsis-exempt); `_spaced_hyphen` replaced by `_hyphen_bullet` (dash + #numbered, now in the `--quick` set); tested across FULL & `--quick`.
- 36.2. `universal/writing.md` —— MODES (incl. `--quick`) added; GenAI list trimmed (web_search line kept).
- 36.3. `cscpt/README.md` —— created (run-not-read + script index).
- 36.4. Untouched: `quote_fix.py`, `cl_check.py`. Test files voided (`cscpt/❌_t2.md`, `❌_t3.md`, `❌_t_dlint.md` —— please delete at leisure).
- 36.5. Open: enforcement mechanism for `--quick` (q. in 33.2); optional §8.5 reinforcement (25.3).
