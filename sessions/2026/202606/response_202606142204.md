# Response to query_202606142204.md
*Agree: reuse mini_writing.md for OTGC; one gap to add later*

9. Reminder —— understood and my fault. QB belongs in the `response_`, never in chat (chat is restricted to the five declarations). I will keep all questions/blockers inside `response_` going forward.

10. Re 8.1 —— agree, reusing `seek/context/mini_writing.md` is the cleanest path
- 10.1. Why it fits —— it already exists, is SELF-CONTAINED (it inlines the `c.md` essentials such as British English, Hart's quotation and metric units, because the AJAP automation never reads `c.md`), is entirely `dlint.py`-free (an autonomous run cannot execute scripts), and is purpose-built for non-CC autonomous writing. So it is already exactly the shape OTGC needs —— no new `writing_otg.md`, no rename of `writing.md`, no broken `#writing` trigger.
- 10.2. Caveat (awareness, not a blocker) —— it is AJAP/CL-flavoured: titled "AJAP Writing Rules", and a few lines are cover-letter-specific (`except the 6 in gcl.md §Template`, `more than once in a CL`, the "confident/proactive, highlight skills/experience" and "never use I believe/feel/think" guidance). For OTGC's general writing those are mostly harmless defaults, but the `gcl.md`/CL references are slightly out of place for a non-CL deliverable. OTGC can simply generalise them.
- 10.3. Coupling —— once `index_otg.md` points here, this file serves TWO consumers (the AJAP automation and OTGC). A future AJAP-driven edit will affect OTGC and vice versa. Minor, but worth remembering before either side changes it.
- 10.4. Gap check (this session's NON-`dlint` additions vs `mini_writing.md`) —— only ONE is absent: the dash-RESTRUCTURE principle we added to `writing.md` ("when removing a banned dash, RESTRUCTURE the whole sentence rather than swapping in a comma/colon, since the dash carries emphasis the rest must absorb"). `mini_writing.md` line 22 bans em dash + colon but lacks this how-to-remove guidance. I recommend adding that one line (in `dlint`-free phrasing) when you ask me to act. Everything else new this session was `dlint`-related, so correctly excluded.
- 10.5. GenAI list —— `mini_writing.md` already carries the FULL list (correct, since it cannot run `dlint.py`), so nothing needs inlining and nothing should be removed. Note: as `dlint.py`'s list grows, this one can drift; that is inherent to keeping a readable list for non-script contexts, and acceptable.
- 10.6. Scope —— I will NOT touch `index_otg.md` (OTG-only, injected by userPref); that wiring is yours. Holding all edits to `mini_writing.md` until you ask, per "don't act yet".
