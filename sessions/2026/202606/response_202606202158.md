# Response to query_202606202158.md
*Test-3 issues addressed; debate.md tightened (labels, TS, 30s, single THE_END)*

6. Re Issue 1 (A/B slow to turn 2) —— the startup baseline-race again: both opened within the same few seconds, so each other's opening was ALREADY in the other's watch baseline; the watch saw no change and each sat out its full wait before re-reading. Once posts were spaced (turns 2+), each landed after the baseline and was caught in `~`3 s. Capped now (see 12).

7. Re Issue 2 (MA slow to react) —— honest answer: it was a TIME-BOUND poll, not an instant file-change wake, AND partly my own fault.
- 7.1. My MA watcher polls every 5 s (`sleep 5`), so best case is `~`5 s, not instant. True event-driven instant wake isn't available here (`fswatch` not installed) —— a tighter poll (`~`2 s) on a clean watcher is the practical ceiling.
- 7.2. The "couple of mins" was mostly a self-inflicted detour: my STOP-watcher FALSE-fired on the word "STOP" in my own board header, so I burned turns investigating and re-arming whilst also writing the reply —— the real STOP (21:38) wasn't caught until I'd re-armed the correct watcher (`~`21:41). So: time-bound poll + my processing, not a clean wake.
- 7.3. You said you'll handle the watcher next test —— so I kept the watcher mechanism OUT of `debate.md` (it stays a clean protocol file).

8. Re Issue 3 (TS drift) —— fixed: the header `[HHmm]` must now be computed from `TZ='Australia/Sydney' date +%H%M` immediately before each append, never hardcoded or reused. In the Standing Rules, the briefing, and the Example Outputs.

9. Re Issue 4 (numbering —— critical) —— fixed on all three counts:
- 9.1. numbered.md: the SAs did NOT read it (I never told them to), so the missing sub-levels were just my prompt. I'm keeping it that way deliberately —— the debate now has its OWN labelling scheme in the Standing Rules, distinct from numbered.md's nested `1.1.`, so SAs needn't read numbered.md at all.
- 9.2. No more per-append reset: each debater numbers points with its letter + a counter that NEVER resets across blocks —— A1, A2, A3… (A's running points), B1, B2…, C1, C2…. Per-debater "pt 1" is fine; per-append reset is gone.
- 9.3. Labels follow the debater name exactly as you wanted: A's first point is `A1` (searching `A7` → one hit; searching `1` → dozens), and references shrink to `Re B3:` instead of "re B turn 1 pt 3".

10. Re Issue 5 (double `THE_END`) —— fixed: MA now appends `THE_END` ONCE, then the `## Observer` block, with NO second `THE_END`. Stated explicitly in MA steps 5–6.

11. Re your reply to 2136 §5 (the STOP false-positive) —— agreed, no exception logic added: the cause was a one-off (my board header literally contained "STOP"); I simply won't put that word in a board header again. `debate.md` carries the plain rule ("a `## User` block containing `STOP` → adjourn gracefully") and nothing about watchers —— that stays your call next test, and the protocol stays simple.

12. Re your reply to 2136 §2 / §3 (wait timing + 30 s):
- 12.1. Window shortened 60 s → 30 s (`seq 1 10`, `timeout 45000`). Only marginally costlier (idle re-reads roughly double, but trivial against the actual debating) —— worth it for snappiness.
- 12.2. Clarification on your "woken by file changes vs the wait": SAs ARE woken by changes —— the foreground watch polls mtime every `~`3 s and returns within `~`3 s of any change; the 30 s is only the fallback cap for the baseline-race gap. And the "re-read right before appending" you suggested was already mandated (kept).

13. Re the 2141 bloat —— trimmed: cut the "fatal mistake" sentence to one concise clause, the long race bullet to a single line, and condensed the Board/Architecture prose. `debate.md` is now meaningfully leaner whilst keeping every load-bearing rule.

14. Other issues I spotted (you invited more):
- 14.1. Block length: despite "concise", blocks ran to 3 dense points each. The short labels help references, but if you want a hard cap (e.g. ≤2 points or ≤60 words per block) I'll bake it in —— flagging, not assuming.
- 14.2. Cost shape: `~`199k tokens for a 14-min, 3-SA run is fine, but it scales with debaters × duration; the on-the-spot model/role judgement plus your usage watch are the only governors —— no hard budget in the file (by your earlier call).
- 14.3. Late saturation: by the end A/B/C were recycling the T-shape semantics —— that is MA's saturation-judgement (or your STOP) to catch, not a defect.

15. Good News noted —— add-debater and STOP both proven end-to-end. Not starting test 4 (per your reminder); `debate.md` is updated and ready when you are.
