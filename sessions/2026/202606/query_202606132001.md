# Re writing.md

read `gscpt/quote_fix.py`
you always fail to type typographic quotation marks so we created this script to batch-convert them all
.
read `seek/context/cl_check.py`
it was created to mandate a certain format per ajap.md (don't read it; too long) AND to ensure cc_writing.md (a shrunk ver of writing.md; DON'T mention `cc_writing.md` in the script to be created as `cc_writing.md` is being retired/replaced son) is fully complied
.
restriction: leave above 2 scripts 100% untouched, don't edit/delete/move them
.
problem 1:
i always forget to run quote_fix.py and only found out later after pasting to .pages or other interfaces and mid-way through editing, which i can't run it because the text are already in rich text format or my efforts will be in vain
.
problem 2:
many deliverables by you still sometimes have dash or other banned words
.
proposed solution (open to other options):
assuming deliverables (which need proper quotation marks) are always created after reading writing.md (briefly tell if current mandate is strong enough to mandate its reading)
i propose we create a new script then add one/a few concise lines in writing.md to ensure you always run that AFTER generating the deliverables; if results flag anything, rectify the deliverables then run again; loop until no flags
i propose we may create a new folder `dupbus-ceztuc-7cufVe/cscript/` (meaning Claude scripts; +only used by you; i only use `gscpt/`) to store this new script (temp. name: deliverables.py; feel free to propose other options)
deliverables.py should inherit most content of quote_fix.py just except the input/output path (CIIW) as the paths should be determined by you (in terminal output) when you run it, or you can set it as anywhere convenient to you (e.g. in `cscript/`) though that would involve moving files (and leaving voided copies that pile up if i don't remember to clear it) —— so it's your call to make
deliverables.py would also inherit some elements of cl_check.py that are relevant to writing.md mandates (i.e. filter out everything unrelated like those for AJAP)
if anything not compliant, flag in terminal result so you will rectify and loop as said
.
.
.
tell your thoughts only
don't act on anything