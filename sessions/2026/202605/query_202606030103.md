# Query_202606030103

## Problem
CWI/OTG failed to fetch latest content of GH files via index_otg.md (hereby permit you to read in this session)
e.g. she fetched google.md but retrieved its initial state, prior to all edits followed, aka ver 01
i tried opening the URL myself, it correctly shows latest state
so possibly not a GH problem but CWI's own caching problem
use web_search as needed to see if other viable solutions apart from below

## Proposed Solution
this might not be best solution as it clutters the repo
but this has been proved working:
before our overhaul (see close_202605300023.md & its comms files if needed)
each file has a duplicated version (e.g. google.md & google_04.md)
each time i edit a file, i have to delete the existing duplicate (e.g. google_04.md), duplicate the new file (e.g. google.md → google_05.md), update index_otg.md (previously called directory.md) to replace that (or multiple) URL(s) (e.g. URL of google.md changing from `_04` to `_05`), which it self also has a duplicate (e.g. directory_50.md) and would need the same treatment (e.g. directory_51.md)
then the index has the correct URL to the latest file
and i would have to update preferences.md (mirror of CWI userPref)
finally i have to copy its content and paste to userPref for it to take effect
as you might notice these are many manual works and heavily contaminate the repo (e.g. `universal/` has 23 files but would appear as 45, which is preferences.md + 22 files + their versioned duplicates)
which is one of the many reasons i switched to CC
but from time to time i would still needa your help OTG
that's why i needa ensure the OTG (iphone app) also works

## Invalid Workarounds
i have tried multiple workarounds
most straight forward one is to use `/remote-control` for one of the sessions with you (CC)
and whilst OTG, all our convos will be there
it works, and that session still has local access of all latest files, which is all perfect EXCEPT the `response_` files generated can't be read properly —— each line only shows 1st 22-ish chars, anything further is truncated
i thought of resorting to chat text, but that also doesn't suit my comms style as it will be vertically very long and takes me numerous scrolls just to locate a previous query/response so chat text is also invalid
.
another solution is to start a CC on the OTG (app), which will be on the cloud so some latest files might not be available owing to not having pushed to GH (though acceptable)
real problem (at that moment): the `response_` files will be URL, it takes me so many clicks and very long loading time just to see it in the GH app (no truncation/display issue though), so the comms is disastrous
real problem (rear to that moment): that cloud session could not push to main branch and would create a feature branch, and i still struggle to merge them, let alone it costs me extra time and friction
so cloud CC also doesn't work

## Valid Workaround
final solution (haven't created yet but discussing now) i could think of now is that we create a mechanism for you to execute per § Proposed Solution
`universal/` would need a subfolder (probably named `universal/otg/`)
in initial run, we shall create duplicates for all files in `universal/` into that subfolder, each renamed w/ ver no. (filename_001.md; needa use 3-digits because we used 2-digit before and would clash)
after its est, each time activated by a prompt like #otg (or something else if better), you will check mod time (NOT content, as this saves tokens) of each file and its duplicate (e.g. `universal/google.md` vs `universal/otg_google_001.md`)
if not identical, void its duplicate, then create duplicate of latest file w/ ver no. +1 (e.g. 001→002)
as you might notice, this introduces a bug
e.g. if i edited google.md at 202606030123, even if i run #otg immediately (which is unlikely, i'd just do it from time to time), the duplicate would be very likely rear to the original file (e.g. 202606030124)
so each time when we run #otg, when you spot mod time not identical, you needa simultaneously create do 4 things:
1 - edit original file by adding a dot `.` at its end, then immediately remove it, causing mod time to be current time
2 - void the outdated duplicate
3 - create new duplicate w/ ver no. +1
4 - check if original file mod time is identical to new duplicate mod time; if not, run 1–3 again until they are identical
and as i said in § Proposed Solution, we don't just work on the target file
each time discrepancy is found on any files in this folder and above is done for the affected file
you would also have to do the same for index_otg.md & its duplicate
finally you needa update preferences.md which i'll manually copy its content (w/ latest index_otg.md URL) to userPref
and apparently we don't just have `universal/` but other CPs
so if i say `#otg [folder_name]`, do above for that folder
1 thing to note is that `career/` draws on files in `seek/context/`
so instead of creating an `otg/` folder in `seek/context/` (which will risk contaminating/confusing AJAP), you do that in `career/otg/` (only for this CP) for the duplicates of both files in `career/` and `seek/context/`
clarification: if i just say `#otg`, default = `universal/` (most frequently used and changed)

## Missions Now
- tell your thoughts
- dont execute anything yet