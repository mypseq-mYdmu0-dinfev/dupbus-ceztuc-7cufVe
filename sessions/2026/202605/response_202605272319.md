# Response to query_202605272319.md
*Comms System —— Thoughts, Gaps & Open Questions*

---

## CLAUDE.md —— Critical Answer

**No** —— CLAUDE.md is NOT injected per response. It is loaded once at session start and persists in context for the session. After compaction, CC auto-re-reads CLAUDE.md files in the project directory hierarchy as part of context restoration. **We can use it.**

Recommended location: `/sessions/CLAUDE.md` (scoped to sessions, mirrors how `/seek/CLAUDE.md` is scoped to AJAP). However —— if you sometimes run CC from the repo root, we'd need `/CLAUDE.md` at root level instead. See **Open Question A** below.

---

## Problem 4 —— File-Based Comms

**1.** `query_` naming: Agreed. The collision reasoning is sound, and no better alternative comes to mind.

**2.** Removing `changes_` and `audit_`: Agreed —— those are AJAP-specific. Sessions only needs `query_`, `response_`, `close_`.

**3.** `chat_handoff` → `close`: Agreed —— more concise, session-agnostic.

**4. Reminder:** Update `/seek/investigation/README.md` to use `query_` once finalised.

---

## Problem 5 —— Organisation & File Handling

**5. Path inconsistency (flag):** §5.3 reads `/sessions/202606/` but the established structure is `/sessions/[year]/[yearmonth]/` (e.g. `/sessions/2026/202605/`). Assuming the year-subfolder structure is always intended —— confirm via **Open Question E**.

**6. `respond_` vs `response_` (flag):** §5.6 uses `response_` but §5.7 uses `respond_`. One must be settled. I'd suggest `response_` throughout for consistency with the investigation system convention. See **Open Question D**.

**7.** 5.2 timestamp matching: Agreed, already applied here (`response_202605272319.md` matches the query).

**8.** 5.3.2 timestamp note: When you send a long message directly (not via a query file), I create `query_` and `response_` files with matching timestamps —— both stamped at response time, not when you sent the message. That means the query timestamp won't reflect when you actually wrote it. Acceptable? Or would you prefer the query_ timestamp be set slightly earlier (e.g. one minute prior)?

**9.** 5.8 "early chats of next month" —— suggest making this deterministic: **in the first response of the new month**, if the prior month's subfolder lacks a `summary_` file, I remind you. No ambiguity about "early" vs "mid".

**10. Session cold-start gap:** The protocol specifies mid-session behaviour but not what I should do at the **start of a new CC session**. Suggest: on cold start, I read the current month's `summary_` if it exists, otherwise the most recent `close_` file. This replaces `#pp`'s orientation function without the lock/fetch overhead.

---

## Problem 6 —— Chat Interface

**11.** The 3-element format is clean. One gap: what about **blocking interruptions** from me —— e.g. a file path doesn't exist, a filename collision detected, ambiguity that prevents writing a response file? Suggest a 4th element, e.g. `⚠️` for such cases, kept rare:

```
⚠️ `query_202605272319.md` not found in expected subfolder —— did you mean `2026/202605/`?
```

**12.** §6.2 path display ambiguity: "direct parent folder only" —— does this mean `202605/response_...md` (relative to `/sessions/2026/`) or `2026/202605/response_...md` (relative to `/sessions/`)? The latter is unambiguous across year boundaries. See **Open Question B**.

**13.** Current query file is already in the correct subfolder (`/sessions/2026/202605/`) —— no move needed, 5.4 doesn't apply.

---

## Problem 7 —— Universal Files

**14. `directory.md` rename:** `index.md` is cleaner. `fetch_index.md` is explicit but feels redundant given the `/universal/` location. Lean towards `index.md`. See **Open Question C**.

**15.** For CC, a fetching index isn't needed —— we read by direct path. The CC equivalent is just knowing what files exist and their purpose, which can be absorbed into `/sessions/CLAUDE.md` or a lightweight `/universal/cc_index.md`.

**16. Files to review for CWI-only content (at minimum):**
  - `cc_22.md` —— entire `#pp` section is CWI-only; needs a CC section or separate file
  - `directory.md` / future `index.md` —— fetching mechanism entirely CWI-specific
  - `context_08.md` —— likely references CWI context window management

---

## Additional Considerations

**17. CC Memory System (you may not be aware):** CC has a file-based memory system (separate from sessions) persisting across sessions at `~/.claude/projects/[hash]/memory/`. It stores your profile, preferences, project context, feedback on my behaviour. This partially compensates for "no CWI-style memory" —— I can accumulate knowledge of you across CC sessions without needing to re-read `close_` files every time. Worth populating as we go. I've already saved a few things from this conversation.

**18. `close_` structure (define now, full template later):** Minimum agreed fields:
  - Session context summary (what we covered)
  - Unresolved items / todos
  - Key decisions made
  - Files created/modified this session
  - Pointers to relevant `query_`/`response_` files

**19. Multi-query sessions:** If a session has many `query_`/`response_` pairs, future sessions reading them all could be expensive. Not urgent now —— flag for revisit when it becomes a pain point.

**20. `universal/` vs `/sessions/` CLAUDE.md scope:** If the comms rules live in `/sessions/CLAUDE.md`, they only apply when CC is run from `/sessions/`. Rules in `/universal/` files won't auto-load in CC (unlike CWI). We should decide which conventions live where to avoid them only loading half the time.

**21. iPhone / on-the-go:** Your CWI fallback for mobile is pragmatic. One option if you ever want CC on the go: GitHub Codespaces gives a cloud CC environment that reads/writes your actual `main` branch (no branch fork needed if you open it correctly), but the latency is real. Not urgent —— just noting it exists.

---

## Open Questions (need your answers before drafting CLAUDE.md)

| # | Question |
|---|----------|
| A | Working directory when running CC —— always `/sessions/` or sometimes repo root? |
| B | Path display in ⇠/➡️ declarations —— `2026/202605/filename` (relative to `/sessions/`) or just `202605/filename`? |
| C | Rename `directory.md` → `index.md`, or keep as-is? |
| D | Settle on `response_` (not `respond_`) throughout? |
| E | Confirm year-subfolder path: always `/sessions/[year]/[yearmonth]/`? |
