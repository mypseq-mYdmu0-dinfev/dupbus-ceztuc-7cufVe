i wanna create a comms system for you and me
read below for standing rules of this chat:
---
## Language & Units

ALWAYS use:
- British English (e.g. `learnt` `amidst` `towards` `amongst` `whilst`, BUT DON'T CONVERT TO GBP)
- Metric units (°C, metre, gram, litre, etc.)
- AUD (original currency in bracket)
- Hart's logical quotation rule: punctuation inside quotes if original to the quote, outside otherwise (e.g. ✅ `He said "I'm leaving", then left.` ❌ `He said "I'm leaving," then left.`)
- If a certain term must be in Chinese, put it in HK Traditional Chinese

## Conventions

- Use (or convert if not) SYD timezone; get by `TZ='Australia/Sydney' date +"%Y%m%d%H%M"`
- Date as:
  - Internal: YYYYMMDDHHmm
  - Deliverable: DD/MM/YYYY
- For plus `+` implying "more than", use superscript (e.g. ✅ "10⁺ yr"; ❌ "10+ yr"); regular `+` acceptable in other implications like addition (e.g. "me+you") and name (e.g. "iCloud+")
- For dash `—`, double it w/ space before/after: ` —— `
- When using emojis that support skin tone modifiers
  - ALWAYS apply the light skin tone modifier 🏻
  - NEVER use default ver (e.g. 👍, 👆, 👉, 🤵‍♂️)
  - EXCEPT those don't support skin tone modifiers (e.g. ⭐, 😊)
- Actively use web_search whenever needed for validation

## Special Commands

- `yn`= yes/no; strictly respond w/ just 1 word in chat (override), either Yes or No (e.g. "Should we...? yn"); only usable by ME
- `QB`/`qb` = questions/blockers; raise any before proceeding; e.g. "[task], qb"; MUST separate as Q1, B1, etc.; NEVER: QB1, QB2, etc.
- `CIIW` = Correct (me) if I'm wrong; don't take my msg as direct command but a suggestion which you should validate; e.g. "since 1+1=3, we should... CIIW" → correct me
---
HERE'S MY MSG:
lemme now tell you the full story of what im trying to do (alert me whenever what i said is incorrect/unviable; use web_search as needed)
basically i enjoyed using Claude Chat (claude.ai; aka CWI/Claude Web Interface) but it cannot read/write my local files, thought that's still fine despite being a bit inconvenient
the real pain surfaces when i introduced the GH system for prompts
long time ago (before i became a power user) i had very little prompts so i just put them in userPref (Claude Settings>General>Instructions for Claude)
but as i achieve more with you and became more demanding, the prompts grew into hundreds of lines (~89k chars)
userPref became very heavy and i later found userPref is actually injected EVERY response despite already in context window of a chat
so you and i worked on reconstructing the prompts into diff prompt files which you can see a complex directory to access them in `/universal/directory.md` (alert me if you can't read that file; don't fetch any thing, we're discussing on them)
you can also see the current userPref (`/universal/directory.md`) where every chat in CWI fetches unconditional files then conditionally fetch other files when needed
the pain im suffering now (problem 1) is that CWI cannot retain them in context
because these fetching are technically tool calls (same as web_search) which Anthropic lately made them cleared (tool results/file content became `[Older tool result cleared to save context]`) in ambiguous situations (i thought they only get cleared when certain % of context window consumed but later found they can be cleared anytime the system "feels" like it)
this has led to me creating `#pp` (only read that section if possible; in `/universal/cc.md`)
we've spent countless hours optimising it but CWI still cannot reliably "lock" them in context window, unless i prompted `#lock` (PP2; DON'T do that here) which is extremely inefficient (consumes at least double tokens because the file is fully read then fully copied into artefact)
---
problem 2 is the time consumption
fetching from GH, `#pp` (esp. PP2), and the recovery protocols all take a lot of time
apparently these all won't be an issue if i just copy the needed prompt file's content into my msg but that would become very inconvenient for me so it's not practical
yet the current setup is also impractical due to problem 1 and 2
---
problem 3 is the invisibility of context window consumption
as a programmer and an interaction designer, i understand the need for simplicity and minimalist approach, which makes CWI (as well as all other interfaces except you) not showing the exact consumption in %
---
i've lately found the solution, which is you (Claude Code; aka "CC")
but im still trying to get used to your diff from CWI and trying to optimise the way we communicate so you can fully supersede CWI (not just coding but general chatting)
as i understood it, you can do 100% what CWI do except generating artefacts and maybe some new "fancy" features like interactive dashboard, etc. as a response (although i know you absolutely can do it if i send the right prompt, but as a substantial script file like .html/.py)
however the "artefacts" are precisely one of the top reasons i use Claude
i hate very much the interface cluttered by super long chats, leading to "endless" scrolling just to find back a previous msg by you or me
as you might notice by reading cc.md, in the way i designed it, CWI and i interact w/ a very clean interface
a msg by me (input) is truncated into an expandable chat bubble
a response by CWI (output) is just a tab/button expandable to a right-side panel for reading
i.e. vertical space consumption is minimal
so even the chat had 100k tokens consumed, it is possible that there were only a few artefacts and the whole chat can still be navigated by scrolling ~2-3 times
versus other chatbots or you (before the comms system we're now establishing) that even just 1k tokens consumed, the interface could have been an "endless" scrolling due to the vertical space the msgs take up
as said, your architectural design naturally solved the 3 problems above
1: CIIW, the files you read won't be silently cleared (hence leading to silent misbehaviour) until compaction which i'd see it occurs and can predict by peeking the context pop-up module
2: most of the time i'm working with you locally, which you directly read my local files instead of fetching GH links (as i felt it, seems at least 2x faster); and because they won't get silently cleared, you don't need `#pp` which saves an enormous amount of time; only drawback is i can't quite do it on the go since the claude iphone app doesn't quite support the "Code" interface very well; e.g. i must have prompted `/remote-control` before i left my workstation OR i would have to use a cloud workspace which anything you create/edit becomes a branch (not "main" on my GH), but i kinda accepted this flaw and decide to fall back to Claude Chat on the claude iphone app when im on the go (yes, needing `#pp` over there)
3: as said, i can clearly see it; only problem is you can't see it but there're some workarounds (see below)
---
as said, if i am to switch to CC for general chats (not just coding)
it inherently means there're some new problems we gotta solve (at least i see it as solvable)
---
problem 4 (1st actual problem we gotta solve now):
you can't do artefacts, but you can create files
we've tried a similar approach before in `/seek/investigation/` (don't read any individual files, just have a scan on filenames)
read `/seek/investigation/README.md` which could be the groundwork to the comms system we're constructing, possibly in a CLAUDE.md (critically answer me: does this get injected to context window EVERY response like CWI? if no, we can use this; if yes, we needa put it in another file and mandate reading it at chat start/after compaction) in `/sessions/` which i just created, intended for storing my inputs and your outputs similar to `/seek/investigation/` except it's for general chats (not on a specific project)
as you see in § Files Description & § Critical Notes, i shall write my msg (input) in a .md
you'll read it then you shall write your response (output) also in a .md file, with minimal to no chat text at all except declaring file read
and yes, we shall absorb this prompt in the comms system:
```
Each time a non-`/sessions/` file is (re-)read, you MUST declare in chat exactly which files were successfully read, e.g. `✅ `file1.md`, `file2.md` ... `
```
as for right now, what i can think of that we gotta change from the mentioned README.md are:
- to avoid clashing with prompt files in `/universal/`, we should name my msgs (input) as `query_`
  - we can't name it `input_` because our chats likely involve this word
  - we can't name it `msg_` because both yours and mine are msgs
  - we can't name it `command_` because they might not be commands but chats/questions
  - feel free to suggest other wordings if you can think of anything else
  - if we finalise it should be `query_`, remind me to change that in `seek/investigation/README.md` for consistency
- we would not have `changes` and `audit` files since this is for general chats
- we should change `chat_handoff` to just `close` (maintaining single word) for closing a chat
- properly define what is a `close` (i'll supply my prompt template later; basically summarising a chat for next chat/protecting content against compaction)
> as a result, the folder would only have 3 types of files (much cleaner): `query_timestamp.md` `response_timestamp.md` `close_timestamp.md`, plus 1 more (see below)
---
problem 5 (actually more like a todo):
since we'll be having general chats via local files, we needa organise them 
5.1: each month will be a new folder in its year; e.g. we should create `/sessions/2026/202605/` and everything in there
5.2: if my msg refers to a `query_` file, the response file's timestamp match it, NOT getting current time
5.3: if my msg doesn't refer to a `query_` file, respond in the right monthly subfolder by getting current timestamp via my local terminal: `TZ='Australia/Sydney' date +"%Y%m%d%H%M"`; if the subfolder doesn't exist, create it (e.g. `/sessions/202606/`); before responding:
5.3.1: if my msg ≤ 30 words, include it at file start
5.3.2: if my msg > 30 words, create a `query_` file on my behalf in the right monthly folder, matching timestamp of yours
5.4: if my `query_` file sits in `/sessions/` (not in a monthly subfolder), move (per Move Rule) to the right folder then respond in it
5.5: if my `query_` file sits in a wrong subfolder, confirm whether it's intentional:
5.5.1: if no, follow 5.4
5.5.2: if yes (e.g. continuing a chat started last month), respond in the same subfolder as mine
5.6: each `response_` file (must be .md) must start w/ a header noting which file you're responding to; right below it (line 2), you may optionally add a title (max. 8 words) if you see fit, for example:
```
# Response to query_[timestamp].md
*Title title title title title title title title*
```
see below established rules that should be absorbed:
```
## Move Rule

If moving a file, copy to target folder → void original file (per Void Rule) → add suffix `moved_[directory]` to filename. NEVER leave identical-filename copies across folders.

## Void Rule

If voiding a file, add `❌_` prefix to original filename, signalling user to manually delete after reviewing. NEVER delete a file by yourself.
```
5.7: if generating non-md content, include in `respond_` file if short; create as a separate file if long, with a filename determined by you on the spot but it must end with timestamp (via my local terminal as above); e.g. `name_timestamp.html` `name_timestamp.py`
5.8: you don't have "memory" system like CWI so we needa instruct future sessions to actively read previous `/sessions/` files whenever needed; however that could be an enormous read even just reading `close_` files (could be hundreds per month) so each month we shall summarise what we've discussed into a `summary_` file by reading files in that monthly subfolder (whether only `close_` files or also individual files depend on complexity of issues we were on); each monthly subfolder can only have 1 single `summary_` file, generated by month end (voluntarily by me) or early chats of next month (reminded by you if last month's subfolder lacks one)
---
problem 6:
it's about the chat interface maintenance
as said, it must be kept clean so it can only has 3 elements (no chat text for anything else):
6.1: declaration for files outside `/sessions/` as said above (`✅ `file1.md`, `file2.md` ... `)
6.2: `/sessions/` files (one line each following `⇠`) read with path (direct parent folder only), this may include current `query_` file (at least) and previous files incl. `response_` and others 
6.3: any files (one line each following `➡️`) generated by you with path (direct parent folder only)
example response:
```
✅ `file1.md`, `file2.md`
⇠ `202605/query_timestamp.md`, `202605/close_timestamp.md`
➡️ `202605/response_timestamp.md`, `202605/name_timestamp.html`
```
---
problem 7:
as you see current `/universal/` files are for CWI so we need to:
7.1: edit existing files that involve content only applicable to CWI (e.g. artefacts); side note: is `/universal/directory.md` better renamed as `index.md` or something else considering its content inside? though that's for CWI and you (CC) would certainly need a separate version (you don't fetch via GH link)
7.2: add CC-version of content in it if additional content is small; create a separate file if large
---
Your Missions:
- tell your thoughts on all, don't miss any (don't start drafting prompt files) and don't take any as direct instruction/command/mandate —— everything i said are soft suggestions (open to discussion)
- respond in my suggested protocol (although it's still open to discussion)
- suggest if anything else we should nail down now
- note anything else considerable (considering my migration/adaptation from CWI to CC) but not urgent that we may add / optimise later