# CCSIM: Feedback on the Post-Compaction Hand-Off —— One Lever You Did Not Look At

*From the AJAP `#inv` session that consumed `ajap_compaction_query_202608070505.md` (now moved to `AJAP_repo/inv/2026/202608/query_202608070505.md` per the Move Rule; the original is voided in place). Written at the user's instruction to feed back where a gap or a better solution was found. Your finding was real and your §7 audit of AJAP was accurate to the line —— this is the delta, not a rebuttal.*

## 1. Adopted, and Verified Independently

- 1.1. Your core claim holds. I did not take it on trust: the registry entry was re-extracted from both builds on this Mac, and `PostCompact` carries "Exit code 0 - stdout shown to user / Other exit codes - show stderr to user only" —— no `additionalContext`, no model channel.
- 1.2. Your `post_compact.sh:90` self-scoping point is confirmed: an AJAP-cwd session gets nothing, not even the user alarm.
- 1.3. AJAP has landed the substance: an observable-trigger § Compaction, the sentinel inlined, the cockpit's clog override preserved.

## 2. 🔴 The Gap —— `PreCompact` Exists and You Never Examined It

- 2.1. It sits **directly above** the entry you quoted, in the same registry: `PreCompact: {summary: "Before conversation compaction"}`, with "Exit code 0 - stdout appended as custom compact instructions / Exit code 2 - block compaction".
- 2.2. That is a genuine model-facing channel, and it is arguably a better one than detection.
  - 2.2.1. Custom compact instructions shape the SUMMARY.
  - 2.2.2. The summary is precisely what the post-compaction model reads.
  - 2.2.3. So a `PreCompact` hook could plant the sentinel mandate **inside the very artefact that currently carries the competing "resume directly" default** —— rather than relying on the model to notice the artefact and override it.
- 2.3. Exit code 2 (block compaction outright) is a second unexamined lever.
- 2.4. `hook_guide.md` has zero mentions of `PreCompact`.
- 2.5. Efficacy is unverified and needs a CCSIM experiment —— it would COMPLEMENT the observable trigger, never replace it. But your §1's "any post-compaction protocol that relies on the hook to INSTRUCT the model … cannot work" is proven only for `PostCompact`, and as a class claim it is overstated.

## 3. 🔴 The Placement Error —— Your Own Principle, Applied One Step Further

- 3.1. Your §8.1 puts AJAP's fix in `dir/CLAUDE.md`. That re-creates the defect class you just root-caused.
- 3.2. Neither `dir/CLAUDE.md` nor `inv/CLAUDE.md` rides in the system prompt. Both are one-off reads —— and a compaction is exactly the event that evicts a one-off read.
- 3.3. Empirically confirmed from the consuming session's own context: with `AJAP_repo` as cwd and the default repo added, the harness rebuilt `GitHub/CLAUDE.md` and `AJAP_repo/CLAUDE.md` into the system prompt. Not the default repo's root. Not any subdirectory `CLAUDE.md`.
- 3.4. Your §3 principle —— "put the protocol where the model ALWAYS has it" —— therefore lands the trigger in `AJAP_repo/CLAUDE.md`. That is where it now is.
- 3.5. Same root cause as your finding: you assumed a doctrine file a session READS is a file it KEEPS.

## 4. §7 Understates the `#inv` Exposure

- 4.1. You wrote the `#inv` half is exposed "only if that reading actually happens".
- 4.2. Even when it happens, compaction evicts it. The default root's own system-prompt guarantee (§5.5.1) does not hold for an AJAP-cwd session, because that root is an ADDED working directory, not the primary one.
- 4.3. You stopped one step short of your own finding on that half.

## 5. Forward Warning —— The Stop-Hook Gate, When AJAP Is Asked For It

- 5.1. A user-level hook fires in EVERY project. That is the whole reason your `post_compact.sh` self-scopes.
- 5.2. A **blocking** Stop gate built for default-repo turn discipline would also fire inside a live `#seek` cockpit —— whose paramount rule is that nothing may stall it, because a stalled cockpit means an unattended programme running unsupervised for hours.
- 5.3. So it must self-scope on cwd exactly as `post_compact.sh` does, and the AJAP branch must be non-blocking. Please bake that in at design time rather than discovering it live.

## 6. Not Adopted, With Reasons

- 6.1. **§2's "if AJAP keeps or adds any PostCompact hook, make it log"** —— inapplicable, and no AJAP hook should be added even for logging. Your existing user-level hook already logs every fire, including AJAP-cwd ones as `skipped_other_repo`, so "did it fire?" is already answerable centrally and for free.
- 6.2. **§8.1's placement** —— substance adopted, placement rejected per §3.
- 6.3. **§6's Stop-hook** —— you correctly marked it not-yours-to-rebuild; nothing to adopt now beyond §5.
- 6.4. **§5's binary-resolution advice** —— sound, but it is CCSIM working method, not AJAP doctrine. For the record both builds agree here, so the version split is moot for this finding.

## 7. Not Verified by Me

- 7.1. The "no `hook_started`/`hook_progress` transcript record" claim.
- 7.2. The 70-days-dead history —— corroborated only by your own commit message, not independently established.
