# AJAP Queue & Targeting Strategy (Overqualification Paradox)

## Context (pointer only, NOT truth —— re-derive/verify everything yourself)
- I am a 16⁺yr ex-GM/management-consultant on a 485 visa, no local (Australian) work experience, running AJAP (an autonomous SEEK job-application bot; see `seek/main_ajap.md`/`ajap.md`) targeting via `seek/queue.md`.
- I keep hitting an "overqualification paradox": mid/low (≤$100k) roles reject me as overqualified/flight-risk; higher-value roles largely ignore me for lacking local experience.
- A prior CC session (this repo, career CP) dug into this at length. For quick orientation ONLY (do NOT treat as settled fact —— re-verify/re-derive everything yourself), you may skim these comms pairs in `sessions/2026/202606/`:
  - `career_query_202606302138.md` + `career_response_202606302138.md` —— initial diagnosis (debated whether `queue.md`'s salary cap/keywords, or the CL/resume "altitude", was the real lever)
  - `career_query_202606302233.md` + `career_response_202606302233.md` —— landed conclusion: the lever is CL/resume "altitude" (`seek/context/gcl.md`), NOT `queue.md`; reframed the paradox as sequential, not permanent (land a below-level local role first to unlock the higher tier later)
  - `career_query_202607010037.md` + `career_response_202607010037.md` —— #cic-validated AU statistics on the "Australian experience" barrier (discounted, not disregarded; `~`25% of skilled migrants work below their skill level; `~`15yr wage-gap catch-up; overseas qualifications ARE recognised `~`90% of the time —— it is experience/networks that are discounted)
- Also worth reading (if not already, for scope): `seek/queue.md`, `seek/context/gcl.md`, `seek/context/pro_profile.md`.
- That prior session's take may be incomplete or wrong in hindsight —— treat it as a starting pointer, not a settled conclusion. Actively welcome pushback wherever you see fit; I am not bound to its framing.

## Task
1. Critically re-examine whether `seek/queue.md`'s current Qi set (keywords, salary cap, `tags=new` banding/order) is genuinely optimal for the stated goal: maximise (a) interview-invite rate AND (b) speed-to-offer, given the overqualification-vs-no-local-experience paradox.
2. Consider whether to ADD new Qi (e.g. a probe band, an alternate keyword) or EDIT existing Qi (keyword swap, salary range, classification codes) —— weigh each against AJAP's weekly throughput budget (`~`36–48hr/wk, must diversify across the queue, must not stall on one Qi for an entire run).
3. Revisit whether the `gcl.md` CL/resume "altitude" fix discussed last time (extending the overqualification pre-empt + junior-style opener to ALL sub-$100k roles, plus the resume-choice question) was ever actually implemented —— confirm current `gcl.md` state and finish it if not.
4. Practise #cic research whenever apt (live-validate, not just `web_search` triage) to inform recommendations —— e.g. revisit the labour-market read, or find any newer AU migrant-employment data since the figures already gathered.
5. Land on a clear, actionable recommendation —— and implement it if I confirm. Don't just re-litigate the diagnosis; produce concrete `queue.md`/`gcl.md` edits (or a reasoned decision not to edit).

## Reminders
- #cic is mandatory (per `universal/cic.md`) for anything asserted as fact/statistic —— `web_search`/`WebFetch` are triage only.
- This is a strategic, whole-pipeline change —— confirm with me before editing `queue.md`/`gcl.md`, as the prior session did.
- I am not bound to the prior session's conclusions —— if your independent analysis lands somewhere else, say so and argue it.
